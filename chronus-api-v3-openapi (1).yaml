openapi: 3.1.0
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
info:
  title: Chronus API v3
  version: 3.0.0
  description: |
    Chronus public API, version 3.

    The API is organised into five namespaces, each scoped to a single consumer
    profile:

      * `auth`       — token issuance and exchange (pre-login, OAuth 2.0)
      * `resources`  — standard CRUD on a single resource type
      * `compose`    — bundle multiple resource queries into one round-trip
      * `views`      — page-shaped data with UI metadata for the SPA
      * `manage`     — elevated admin operations with platform-wide impact

    Requests are validated against this contract at the edge: a request that does
    not match its documented path and schema is rejected with HTTP 400 before it
    reaches the application.
servers:
- url: "/api/v3"
  description: |
    The v3 API base, relative to your Chronus host (tenant subdomain or custom
    domain). Path keys are resource-relative to this base.
security:
- cookieAuth: []
- bearerAuth: []
tags:
- name: auth
  description: Token issuance and exchange.
- name: resources
  description: Standard CRUD on a single resource type.
- name: compose
  description: Bundle multiple resource queries into one round-trip.
- name: views
  description: Pre-composed page data with UI metadata (Chronus SPA only).
- name: manage
  description: Elevated operations with platform-wide impact.
paths:
  "/a2a":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - a2a
      operationId: a2a.send
      summary: Send a message to the A2A agent endpoint.
  "/a2a/agent-card":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - a2a
      operationId: a2a.agent_card.show
      summary: Retrieve the A2A agent card.
  "/account_settings":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - account_settings
      operationId: account_settings.show
      summary: The caller's own account settings, including linked SSO configurations.
      description: |
        Always the CALLER's settings — the path carries no id, so there is no way to address another
        member. `lastDeletionRequestAt` is only computed when the organization allows deletion
        requests; when it does not, the field is null regardless of history rather than reflecting it.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AccountSettings"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
    put:
      tags:
      - account_settings
      operationId: account_settings.update_put
      summary: Update the caller's timezone (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — source's singular `resource ... only: [:update]` draws PATCH and PUT
        at the same path, and both dispatch to the same action. Documented rather than left implicit
        because a routed verb with no operation publishes an empty contract for a live endpoint.
        Carries the same dest-only CSRF `403` as the PATCH form.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AccountSettingsUpdateRequest"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AccountSettingsUpdateResult"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: Missing or unrecognised timezone identifier. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    patch:
      tags:
      - account_settings
      operationId: account_settings.update
      summary: Update the caller's timezone.
      description: |
        ⚠️ `timezone` is the ONLY settable field — the rest of the `show` payload is derived or
        org-owned. The identical operation is also served on PUT (see below); both are drawn by
        source's singular `resource ... only: [:update]`.
        The `403` is dest-only with no source equivalent: a SESSION-cookie caller must send
        `X-Requested-With` (`application_controller.rb:29`), and without it the request is refused with
        the `authorization_denied` envelope before the action runs. Bearer callers are exempt, and
        `show` is exempt because the gate skips GET.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AccountSettingsUpdateRequest"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AccountSettingsUpdateResult"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: |
            Missing or unrecognised timezone identifier, checked against
            `TimezoneConstants::VALID_TIMEZONE_IDENTIFIERS` (352 values). Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/account_settings/change_password":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - account_settings
      operationId: account_settings.change_password
      summary: Change the caller's password, verifying the current one first.
      description: |
        ⚠️ An EXPIRED current password still authorizes the change — that is the only route by which a
        member whose password has expired can set a new one, so it is deliberate, not a gap.
        The 403 and 422 both carry a bare `error` string and the distinction is meaningful: 403 means
        the attempt was refused outright (password changes disabled for this member, or the account is
        locked/suspended), 422 means the attempt was understood and rejected (wrong current password,
        password reused within the org's history limit, or model validation).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AccountSettingsChangePasswordRequest"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AccountSettingsSuccess"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: "\U0001F534 TWO DIFFERENT BODY SHAPES share this status, and
            a client must branch on the payload\nrather than assume one:\n  • The
            controller's own refusals — password changes not permitted for this member,
            or the\n    account locked/suspended — render the BARE shape (`{\"error\":
            \"...\"}`).\n  • The dest-only CSRF gate (`application_controller.rb:29`)
            renders the ENVELOPE shape\n    (`{\"error\": {\"code\": \"authorization_denied\",
            ...}}`) and fires as a `before_action`, so\n    it precedes the controller
            entirely for a session caller missing `X-Requested-With`.\nDeclared as
            a union because collapsing it to either one would make the other fail
            response\nvalidation. Both members are closed objects, so the two are
            unambiguous.\n"
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/Error"
        '422':
          description: |
            Current password incorrect, new password reused within the organization's password-history
            limit, or model validation failed (joined `full_messages`). Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/account_settings/sign_out_other_sessions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - account_settings
      operationId: account_settings.sign_out_other_sessions
      summary: Invalidate the caller's other sessions, keeping the current one.
      description: |
        Takes no body. The current session is preserved by passing its own id through, so the caller is
        not signed out of the request that made the call. Idempotent — a second call is a no-op.
        ⚠️ Being body-less does NOT exempt it from the dest-only CSRF gate: it is a POST, and the gate
        keys on the verb, not on the presence of a body. A session caller without `X-Requested-With`
        gets the `authorization_denied` envelope 403.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AccountSettingsSuccess"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/admin/account_lockouts":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.account_lockouts.index
      summary: List locked-out members.
      description: |
        `Api::V3::Admin::AccountLockoutsController` is real and complete (task #71, register #253
        pre-flight order). `Organization#get_locked_out_members`/`Member#is_locked_out?`/
        `Member#handle_reactivate_account!` all confirmed live doing the real thing, not just present.
        `Member#name` (distinct from `User#name`) carries no deprecation tripwire, and both call sites
        here pass `name_only: true`, which short-circuits its admin-term branch — no N+1 to preload.

        `accountLockedAt` is nullable: `Organization#get_locked_out_members`'s fallback branch
        (`members.locked_out`) matches on `failed_login_attempts` alone and compares against
        `IFNULL(account_locked_at, UTC_TIMESTAMP())`, so a member who has exceeded the attempt count
        but has no `account_locked_at` set yet can still be included.
      responses:
        '200':
          description: Locked-out member records, ordered by first/last name.
          content:
            application/json:
              schema:
                type: object
                required:
                - members
                - total
                additionalProperties: false
                properties:
                  members:
                    type: array
                    description: Locked-out member records.
                    items:
                      type: object
                      required:
                      - id
                      - firstName
                      - lastName
                      - name
                      - email
                      - accountLockedAt
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: Member ID.
                        firstName:
                          type: string
                        lastName:
                          type: string
                        name:
                          type: string
                          description: Full name (name_only
                          no admin-term suffix).: 
                        email:
                          type: string
                        accountLockedAt:
                          type:
                          - string
                          - 'null'
                          format: date-time
                  total:
                    type: integer
                    description: Total locked-out members.
  "/admin/account_lockouts/{id}/reactivate":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.account_lockouts.reactivate
      summary: Reactivate a locked-out member account.
      description: |
        Same controller, same findings as the index operation. `Member#handle_reactivate_account!`
        genuinely nils `account_locked_at` and zeroes `failed_login_attempts`.
      responses:
        '200':
          description: Member reactivated.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                - member
                additionalProperties: false
                properties:
                  message:
                    type: string
                  member:
                    type: object
                    required:
                    - id
                    - name
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                      name:
                        type: string
        '404':
          description: No member with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Member is not locked out.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/agent_approvals":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agent_approvals.index
      summary: List pending agent approvals.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agent_approvals/approve/{token}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.agent_approvals.approve
      summary: Approve an agent run by token.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agent_approvals/reject/{token}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.agent_approvals.reject
      summary: Reject an agent run by token.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agent_events":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agent_events.index
      summary: List agent events.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agent_events/test_fire":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.agent_events.test_fire
      summary: Fire a test agent event.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agent_runs":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agent_runs.index
      summary: List agent runs.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agent_runs/daily_stats":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agent_runs.daily_stats
      summary: Daily agent-run stats.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agent_runs/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agent_runs.show
      summary: Show an agent run.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agents/default":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agents.default
      summary: Return the organization's primary/active agentic-automation agent.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::AgentsController`\nis real and complete (dev-5, admin
        root family batch), verbatim — `Agent#for_organization`/\n`#primary`/`#by_status`
        and `FeatureName::AI_AGENTS` all already on dest. Route per source\n(`config/routes.rb:484-487`):
        `resources :agents, only: [] { collection { get :default } } ` — ROUTED on
        dest.\n\nLegacy/duplicate: the SPA actually consumes the member-facing `Api::V3::AgentsController#default`\nat
        `/api/v3/agents/default` for this same read; this admin-namespaced route is
        kept only to\npreserve the legacy path (source's own comment flags it as a
        future dead-equivalent cleanup\ncandidate, not something to build on).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agents/killswitch":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.agents.killswitch
      summary: Organization agent killswitch.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agents/{agent_id}/playbooks":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agent_playbooks.index
      summary: List playbooks attached to an agent.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.agent_playbooks.create
      summary: Attach a playbook to an agent.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/agents/{agent_id}/playbooks/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.agent_playbooks.reorder
      summary: Reorder playbooks attached to an agent.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/agents/{agent_id}/playbooks/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.agent_playbooks.replace
      summary: Replace an agent playbook attachment.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.agent_playbooks.update
      summary: Update an agent playbook attachment.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.agent_playbooks.destroy
      summary: Detach a playbook from an agent.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/agents/{agent_id}/workflow_state":
    parameters:
    - name: agent_id
      in: path
      required: true
      schema:
        type: integer
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.agents.workflow_state
      summary: Live engine state of a workflow agent's bound workflow.
      description: |
        Whether the bound workflow is published, how it triggers (schedule cron /
        webhook / app event / manual), whether its schedule is paused, the
        execution mode its steps derive (agentic when the flow contains an AI
        step, structured otherwise — persisted onto the agent as a side effect),
        plus the identifiers browser-direct engine calls need (slug, environment).
        404 when the agent is not a bound workflow agent; 503 when the engine is
        unconfigured; 502 when the engine cannot be reached.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - published
                - execution_mode
                - trigger_kind
                - trigger_cron
                - trigger_event_label
                - schedule_paused
                - step_count
                - environment
                - slug
                properties:
                  published:
                    type: boolean
                  execution_mode:
                    type: string
                    enum:
                    - agentic
                    - structured
                  trigger_kind:
                    type: string
                    enum:
                    - schedule
                    - webhook
                    - event
                    - manual
                  trigger_cron:
                    type:
                    - string
                    - 'null'
                    description: Cron expression when schedule-triggered.
                  trigger_event_label:
                    type:
                    - string
                    - 'null'
                    description: Application · event label for event triggers.
                  schedule_paused:
                    type: boolean
                  step_count:
                    type: integer
                  environment:
                    type:
                    - string
                    - 'null'
                    description: The engine environment the workflow lives in.
                  slug:
                    type:
                    - string
                    - 'null'
                    description: Application slug browser-direct execute calls address
                      the workflow by.
                additionalProperties: false
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '502':
          description: The workflow engine could not be reached.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                additionalProperties: false
        '503':
          description: The workflow engine is not configured for this environment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                additionalProperties: false
  "/admin/agents/{agent_id}/workflow_versions/{version_id}/restore":
    parameters:
    - name: agent_id
      in: path
      required: true
      schema:
        type: integer
    - name: version_id
      in: path
      required: true
      schema:
        type: string
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.agents.workflow_versions.restore
      summary: Restore a published version snapshot of the agent's workflow.
      description: |
        Git-revert semantics — the snapshot's steps are imported into the current
        draft; a published workflow is published again so the restored content
        becomes a NEW version (no history rewritten), and the agent's lifecycle is
        re-asserted (a paused schedule stays paused). An unpublished workflow takes
        the steps into its draft only. 404 when the agent is not a bound workflow
        agent or the version does not belong to its workflow.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - republished
                properties:
                  republished:
                    type: boolean
                    description: True when the restore published a new version; false
                      when it landed in the draft only.
                additionalProperties: false
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '502':
          description: The workflow engine refused or could not be reached.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                additionalProperties: false
        '503':
          description: The workflow engine is not configured for this environment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                additionalProperties: false
  "/admin/api_credentials":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.api_credentials.index
      summary: List the organization's ChronusApi basic (v2 API) credentials.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nApiCredentialsController` is real and complete (dev-5,
        admin root family batch) —\n`ChronusApi::BasicCredential`/`ChronusApi::CredentialsStrategy::Basic`
        already on dest. Route\nper source (`config/routes.rb:632-636`): `resources
        :api_credentials, only: [:index, :show,\n:create, :destroy] { member { post
        :regenerate } }`.\n\n✅ RESOLVED 2026-08-13 (register #288/#290, ruled 2026-08-02)
        — source's `destroy` was ONE route\nover legacy's TWO operations, and dest
        now draws both separately: `DELETE {id}` revokes a single\nOUTDATED credential
        (owner-or-staff), and `DELETE remove_authentication` is the organization-wide\nwipe
        (staff-only, `super_console?`). Each row carries `can_remove` for the caller
        so the client\ngates the affordance on the predicate the action enforces.\n\n⚠️
        `status: outdated` does NOT mean revoked — no part of the v2 auth chain filters
        on status\n(`chronus_api/base.rb:30`, `credentials_strategy/basic.rb:14-17`,\n`api/v2/basic_controller.rb:31-34`),
        so a superseded key authenticates until its row is deleted.\n`regenerate`
        therefore supersedes without revoking; `DELETE {id}` is the revocation.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.api_credentials.create
      summary: Create the organization's active API credential (rejected 422 if one
        already exists).
      description: Contract deferred — see the index operation's description. Body
        — `{ name?, description? }`. Response reveals the full key once (create only).
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/api_credentials/remove_authentication":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.api_credentials.remove_authentication
      summary: Remove the organization's API authentication — wipes every credential
        (staff-only).
      description: |
        Contract deferred — see the index operation's description.

        Legacy's "remove authentication" operation (`chronus_api/credentials_controller/
        crud_operations_concern.rb:38-48`), gated on `super_console?`. Deliberately NOT narrowed to one
        row. `super_console?` is session-only, so a machine token can never reach it (403).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/api_credentials/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.api_credentials.show
      summary: Get a single (active) API credential, key masked.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.api_credentials.destroy
      summary: Revoke one OUTDATED credential (owner-or-staff). 404 for an active
        or cross-org id.
      description: |
        Contract deferred — see the index operation's description.

        Deleting the row IS the revocation: an outdated key keeps authenticating the v2 API until then.
        The active credential cannot be deleted here — replace it with `regenerate`, or remove the
        organization's API authentication entirely with the staff-only operation below.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/api_credentials/{id}/regenerate":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.api_credentials.regenerate
      summary: Regenerate the active credential's key (marks the current one outdated,
        creates a new active one).
      description: |
        Contract deferred — see the index operation's description.

        A concurrent regenerate racing the per-org-per-status uniqueness constraint renders 409;
        response reveals the full new key once — so it is gated owner-or-staff (403 otherwise), matching
        legacy's `current_member`-scoped lookup (`crud_operations_concern.rb:76-79`).

        ⚠️ It does NOT revoke the key it replaces: the superseded row keeps authenticating until it is
        deleted (`DELETE {id}`).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/audit_events":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.audit_events.index
      summary: List Rumi audit events.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/auth_configs":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.auth_configs.index
      summary: List all auth configs for the organization (enabled and disabled).
      description: |
        `Api::V3::Admin::AuthConfigsController` is real and complete (dev-5, admin root family
        batch), verbatim — `AuthConfig` (pre-existing legacy model) already on dest with every method
        used, including the `default_scope -> { where(enabled: true) }` that's why source uses
        `.unscoped` throughout, and `saml_auth?` (an unconditionally-`include`d mixin, safe on any
        instance).

        Consumed by the SPA's Login & SSO management view
        (`packages/web/src/queries/admin/auth-configs/`); every field below is already camelCased
        server-side (`serialize_auth_config`) and read by that layer field-for-field.
      responses:
        '200':
          description: All auth configs (enabled and disabled).
          content:
            application/json:
              schema:
                type: object
                required:
                - authConfigs
                additionalProperties: false
                properties:
                  authConfigs:
                    type: array
                    items:
                      "$ref": "#/components/schemas/AuthConfig"
  "/admin/auth_configs/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.auth_configs.replace
      summary: Update an auth config's title/description.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin carries the same contract so
        the `PUT` route is not fenced out of the spec — see the `PATCH` sibling above.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                title:
                  type: string
                description:
                  type: string
      responses:
        '200':
          description: Updated auth config.
          content:
            application/json:
              schema:
                type: object
                required:
                - authConfig
                additionalProperties: false
                properties:
                  authConfig:
                    "$ref": "#/components/schemas/AuthConfig"
        '404':
          description: No auth config with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AuthConfigErrors"
    patch:
      tags:
      - admin
      operationId: admin.auth_configs.update
      summary: Update an auth config's title/description.
      description: |
        `params.permit(:title, :description)` — the SPA's `UpdateAuthConfigInput` (mutations.ts)
        sends exactly these two, both optional.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                title:
                  type: string
                description:
                  type: string
      responses:
        '200':
          description: Updated auth config.
          content:
            application/json:
              schema:
                type: object
                required:
                - authConfig
                additionalProperties: false
                properties:
                  authConfig:
                    "$ref": "#/components/schemas/AuthConfig"
        '404':
          description: No auth config with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AuthConfigErrors"
    delete:
      tags:
      - admin
      operationId: admin.auth_configs.destroy
      summary: Delete a custom auth config.
      description: |
        ⚠️ NOT the `{authConfig}` envelope the other operations here use — success renders a bare
        `{success: true}` literal (`render json: { success: true }`), and the SPA's `deleteAuthConfig`
        (mutations.ts) doesn't read the body at all, so this is described exactly as the controller
        emits it rather than normalized to match its siblings.
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                additionalProperties: false
                properties:
                  success:
                    type: boolean
                    const: true
        '404':
          description: No auth config with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Cannot delete (can_be_deleted? is false).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/auth_configs/{id}/password_policy":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.auth_configs.update_password_policy
      summary: Update the password policy message for an indigenous (email/password)
        auth config.
      description: |
        ⚠️ The controller reads `params[:passwordMessage]` directly — a raw param read, not
        `params.permit` — so the wire key is the literal camelCase `passwordMessage`, matching the
        SPA's `UpdatePasswordPolicyInput` (mutations.ts sends `{ passwordMessage }` verbatim). Not
        snake_case: this is a hand-rolled admin controller, not schema-derived.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - passwordMessage
              additionalProperties: false
              properties:
                passwordMessage:
                  type: string
      responses:
        '200':
          description: Updated password policy.
          content:
            application/json:
              schema:
                type: object
                required:
                - authConfig
                additionalProperties: false
                properties:
                  authConfig:
                    "$ref": "#/components/schemas/AuthConfig"
        '404':
          description: No auth config with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Not an indigenous auth config, or model validation failed.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/AuthConfigErrors"
  "/admin/auth_configs/{id}/toggle":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.auth_configs.toggle
      summary: Enable/disable an auth config.
      description: |
        No request body — the SPA's `toggleAuthConfig` (mutations.ts) sends a bare `PATCH` with no
        body, matched here by omitting `requestBody` rather than declaring one and risking a 400 on
        every real call.

        Rejected 422 if it cannot be disabled (`can_be_disabled?`), or if enabling would need an IDP
        that isn't configured (`can_prevent_auth_config_enable?`, SAML-specific). Both render the
        bare `{error}` shape. A third, distinct 422 comes from a rescued `ActiveRecord::RecordInvalid`
        on `enable!`/`disable!`, which renders the array `{errors}` shape instead.
      responses:
        '200':
          description: Toggled auth config.
          content:
            application/json:
              schema:
                type: object
                required:
                - authConfig
                additionalProperties: false
                properties:
                  authConfig:
                    "$ref": "#/components/schemas/AuthConfig"
        '404':
          description: No auth config with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Cannot toggle — see the operation description for the two possible
            shapes.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/AuthConfigErrors"
  "/admin/bot_apps":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.bot_apps.index
      summary: List bot-app workspaces.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/health":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.bot_apps.health
      summary: Bot-apps health.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/slack/callback":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.bot_apps.slack.callback
      summary: Slack OAuth callback.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/slack/connect":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.bot_apps.slack.connect
      summary: Start Slack install.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/teams/connect":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.bot_apps.teams.connect
      summary: Start Teams install.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/teams/manifest_download":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.bot_apps.teams.manifest_download
      summary: Download the org-stamped Teams manifest.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/teams/tenant_bind":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.bot_apps.teams.tenant_bind
      summary: Bind a Teams tenant to this organization.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.bot_apps.teams.tenant_unbind
      summary: Unbind the Teams tenant.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.bot_apps.show
      summary: Show a bot-app workspace.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.bot_apps.destroy
      summary: Disconnect a bot-app workspace.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/{id}/events":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.bot_apps.events.index
      summary: List workspace event log.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/bot_apps/{id}/skills":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.bot_apps.skills.index
      summary: List workspace skills.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.bot_apps.skills.update
      summary: Update workspace skill config.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/coaching_framework":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.coaching_framework.show
      summary: Coaching-framework coverage for the org.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/coaching_frameworks":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.coaching_frameworks.index
      summary: List coaching frameworks.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.coaching_frameworks.create
      summary: Create a coaching framework.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/coaching_frameworks/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.coaching_frameworks.show
      summary: Show a coaching framework.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.coaching_frameworks.replace
      summary: Replace a coaching framework.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.coaching_frameworks.update
      summary: Update a coaching framework.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.coaching_frameworks.destroy
      summary: Delete a coaching framework.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/coaching_quality":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.coaching_quality.show
      summary: Org-level structural coaching quality aggregates for admins.
      description: |
        Returns playbook-grounded rate, ungrounded-turn rate and refusal rate for
        evaluable coaching turns in a rolling window. Every count and rate routes
        through Privacy::MinimumCohort on the evaluable-turn count. Never exposes
        per-member or per-turn quality scores (A5). Definitions are labeled
        structural_v1 — rubric groundedness and refusal correctness are deferred.
      responses:
        '200':
          description: Coaching quality aggregate payload.
          content:
            application/json:
              schema:
                type: object
                required:
                - organization_id
                - state
                additionalProperties: true
                properties:
                  organization_id:
                    type: integer
                  state:
                    type: string
                    enum:
                    - ready
                  computed_at:
                    type: string
                    format: date-time
                  definitions_version:
                    type: integer
                  definitions_label:
                    type: string
                  weeks:
                    type: integer
                  population:
                    type: object
                    required:
                    - evaluable_turns_count
                    additionalProperties: false
                    properties:
                      evaluable_turns_count:
                        "$ref": "#/components/schemas/CoachingQualityCell"
                  metrics:
                    type: object
                    required:
                    - playbook_grounded
                    - ungrounded_turn
                    - refusal
                    additionalProperties: false
                    properties:
                      playbook_grounded:
                        "$ref": "#/components/schemas/CoachingQualityMetric"
                      ungrounded_turn:
                        "$ref": "#/components/schemas/CoachingQualityMetric"
                      refusal:
                        "$ref": "#/components/schemas/CoachingQualityMetric"
                  trend:
                    type: object
                    required:
                    - by_week
                    additionalProperties: false
                    properties:
                      by_week:
                        type: array
                        items:
                          type: object
                          required:
                          - iso_week
                          additionalProperties: true
                  definitions:
                    type: object
                    required:
                    - playbook_grounded
                    - ungrounded_turn
                    - refusal
                    additionalProperties: false
                    properties:
                      playbook_grounded:
                        type: string
                      ungrounded_turn:
                        type: string
                      refusal:
                        type: string
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: weeks
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 26
        description: Rolling window length in weeks (default 12).
  "/admin/compliance_records":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.compliance_records.index
      summary: List compliance records.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.compliance_records.create
      summary: Create a compliance record.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/config_audit_logs":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.config_audit_logs.index
      summary: Paginated, filterable audit log of config changes across the org/org-units/programs.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nConfigAuditLogsController` is authored (dev-5, admin
        root family batch) but has TWO open\nfindings reported for a ruling, not decided
        here:\n\n1. `allowed_entity_ids` calls `@current_organization.org_units` —
        **confirmed live\n   (`rails runner`) this raises `NoMethodError: undefined
        method 'org_units' for an\n   instance of Organization`**. No `has_many :org_units`
        (or equivalent) exists anywhere in\n   `app/models` on dest. This is a genuine
        missing association, not a naming trap — every\n   request to this action
        will fail until it's resolved.\n2. `Time.parse(params[:since])` has no rescue
        anywhere in the chain. Unlike every raise site\n   classified in the admin/content/*
        batch, `ArgumentError` is NOT one of the classes\n   `ActionDispatch::ExceptionWrapper::RESCUE_RESPONSES`
        maps to a 4xx — a malformed `since`\n   param would genuinely 500.\n\nRoute
        per source (`config/routes.rb:690`): `resources :config_audit_logs, only:
        [:index]` — ROUTED on dest (moot until finding 1 is resolved).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/connectors":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.connectors.index
      summary: The organization's connectable source systems, live from the workflow
        engine.
      description: |
        Exactly the applications enabled for the org on the workflow engine, with
        per-auth-method connection state and the field specs a connect form needs.
        Nothing is stored or seeded Chronus-side; enabling an app on the engine makes
        it appear here. Internal (`custom`) engine apps are excluded.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connectors
                properties:
                  connectors:
                    type: array
                    items:
                      type: object
                      properties:
                        slug:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          nullable: true
                        icon:
                          type: string
                          nullable: true
                          description: Engine-hosted logo URL.
                        category:
                          type: string
                          nullable: true
                          description: The engine's first tag (CRM, ATS, ...).
                        connected:
                          type: boolean
                        reauth_required:
                          type: boolean
                          description: True renders the Expired state.
                        auth_methods:
                          type: array
                          description: Every auth method the engine advertises, primary
                            first.
                          items:
                            type: object
                            properties:
                              auth_type:
                                type: string
                                description: oauth2 or keybased.
                              fields:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    name:
                                      type: string
                                    label:
                                      type: string
                                    placeholder:
                                      type: string
                                      nullable: true
                                    required:
                                      type: boolean
                                    type:
                                      type: string
                                      description: text or password.
                        connections:
                          type: array
                          description: Live authorizations, one per auth method.
                          items:
                            type: object
                            properties:
                              auth_type:
                                type: string
                              status:
                                type: string
                              identifier:
                                type: string
                                nullable: true
                                description: Provider-side principal the engine reports.
                              connected_at:
                                type: string
                                nullable: true
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '503':
          description: The workflow engine is not configured for this environment,
            or unreachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  "/admin/connectors/{slug}/connections":
    parameters:
    - name: slug
      in: path
      required: true
      description: The engine's application slug.
      schema:
        type: string
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.connectors.create_connection
      summary: Authorize a connector for the organization.
      description: |
        Key-based (`auth_type: keybased`): the opaque credentials hash passes straight
        through to the engine, which verifies it against the live provider before
        storing — the response is `connected: true` and nothing is persisted or logged
        Chronus-side. OAuth (`auth_type: oauth2`): returns the provider consent URL to
        open in a popup; the engine completes the token exchange on its own callback
        and the caller polls the catalog for the state flip. A successful key-based
        connect also stamps the connector's standard workflow (best-effort, never a
        failed connect).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - auth_type
              properties:
                auth_type:
                  type: string
                  description: oauth2 or keybased.
                credentials:
                  type: object
                  additionalProperties:
                    type: string
                  description: |
                    Keys are whatever the connector's auth schema declares (see the
                    catalog's auth_methods fields). For OAuth apps these become the
                    per-app prerequisite query params some providers require.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                description: Key-based answers `connected`; OAuth answers `auth_url`.
                properties:
                  connected:
                    type: boolean
                  auth_url:
                    type: string
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: The engine refused the connection (for example credential verification
            failed); the engine's own message is relayed verbatim.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Error"
        '503':
          description: The workflow engine is not configured for this environment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  "/admin/connectors/{slug}/connections/{auth_type}":
    parameters:
    - name: slug
      in: path
      required: true
      description: The engine's application slug.
      schema:
        type: string
    - name: auth_type
      in: path
      required: true
      description: The auth method to disconnect (oauth2 or keybased).
      schema:
        type: string
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.connectors.destroy_connection
      summary: Remove one auth method's authorization for a connector.
      description: |
        The engine cascades its caches and revokes tokens; idempotent on its side.
        Deliberately touches no agents and no workflows — a disconnected connector's
        standard workflow keeps its history.
      responses:
        '204':
          description: Disconnected.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: The engine refused the disconnect; its message is relayed verbatim.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Error"
        '503':
          description: The workflow engine is not configured for this environment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
  "/admin/content/page/{page_id}/blocks":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.page.blocks.index
      summary: List a Page's content blocks, ordered by position.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::Content::Page::\nBlocksController` is real and complete
        (dev-5, admin/content/* triage batch), unblocked by\n`Content::Page::PageBlockSerializer`
        (same batch). Route per source (`config/routes.rb:553-562`):\n`namespace :content
        { namespace :page, path: \"page/:page_id\" { resources :blocks, only:\n[:index]
        { collection { put :bulk_update } } } }` — ROUTED on dest.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/content/page/{page_id}/blocks/bulk_update":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.content.page.blocks.bulk_update
      summary: Bulk-save a Page's content blocks.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ blocks: [...] }` — blocked on published pages (422 "Cannot edit published page", via
        `ensure_draft!`); a missing `blocks` key is rejected 422. Delegates to `Content::Page::
        BlockSyncer` (already on dest); `content`/`settings` accept either JSON strings or nested
        param hashes.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/content/playbook/{playbook_id}/reference_files":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.playbook.reference_files.index
      summary: List playbook reference files.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.content.playbook.reference_files.create
      summary: Create a playbook reference file.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.content.playbook.reference_files.destroy_prefix
      summary: Delete playbook reference files by prefix.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/content/playbook/{playbook_id}/reference_files/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.playbook.reference_files.show
      summary: Show a playbook reference file.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.content.playbook.reference_files.replace
      summary: Replace a playbook reference file.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.content.playbook.reference_files.update
      summary: Update a playbook reference file.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.content.playbook.reference_files.destroy
      summary: Delete a playbook reference file.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/content/podcast/extract_metadata":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.podcast.metadata.extract
      summary: Extract podcast episode metadata from a URL for the content builder.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159's openapi DSL migration unit, same\nas every other admin/content/* row
        on this port. `Api::V3::Admin::Content::Podcast::\nMetadataController` is
        real and complete (dev-5, admin/content/* triage batch). Route per\nsource
        (`config/routes.rb:568-570`, nested under `namespace :v3 { namespace :admin
        }`):\n`scope 'content/podcast', module: 'content/podcast' { post :extract_metadata,
        action: :extract,\ncontroller: 'metadata' }` — ROUTED on dest; owned by whoever
        lands routes/v3.rb for this\nfamily.\n\nBody: `{ url: <string> }`. Delegates
        to `Content::Podcast::MetadataExtractor` (already landed\nwith the U2 content-core
        services unit) — no controller-side validation beyond blank-check.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/content/roleplay/creation_chat":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.creation_chat.create
      summary: Multi-turn AI chat that drafts a new roleplay scenario for the admin
        builder.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::Content::\nRoleplay::CreationChatController` is real
        and complete (dev-5, admin/content/* triage batch).\nRoute per source (`config/routes.rb:601-602`,
        inside `namespace :content { namespace :roleplay\n}`): `post :creation_chat,
        to: 'creation_chat#create'` — ROUTED on dest.\n\nBody: `{ message, chat_history?:
        [], current_draft? }`. Delegates to `Content::Roleplay::\nAICreationService`
        (already landed with the roleplay AI-services unit); `CreationResponseError`\nrenders
        422.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/content/roleplay/objectives":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.index
      summary: List roleplay practice objectives for the current organization.
      responses:
        '200':
          description: Paginated practice objectives list.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriteriaIndexResult"
      parameters:
      - name: page
        in: query
        schema:
          type: integer
      - name: per_page
        in: query
        schema:
          type: integer
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.create
      summary: Create a roleplay practice objective.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                name:
                  type: string
                description:
                  type: string
                objective_type:
                  type: string
                  enum:
                  - skill
                  - behavior
                  - framework
                config:
                  type:
                  - object
                  - 'null'
                  additionalProperties: true
                parent_objective_id:
                  type:
                  - integer
                  - 'null'
                proficiency_expectations:
                  type:
                  - object
                  - 'null'
                  additionalProperties: true
                tag_list:
                  type: array
                  items:
                    type: string
      responses:
        '201':
          description: Created practice objective.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionResult"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionError"
  "/admin/content/roleplay/objectives/ai_generate":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.ai_generate
      summary: AI-generate a roleplay practice objective (multi-turn).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                message:
                  type: string
                chat_history:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
                prompt:
                  type: string
                  description: Legacy single-shot input (still accepted).
      responses:
        '200':
          description: "{ type, message, draft? } — type is question | draft | refinement."
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAiGenerateResult"
        '422':
          description: Generation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionError"
  "/admin/content/roleplay/objectives/bulk_destroy":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.bulk_destroy
      summary: Bulk-delete objectives, skipping any still in use.
      description: |
        Deletes unused practice objectives and reports which ids were blocked because a
        roleplay still scores against them. Never a partial-fail 422 for in-use rows —
        those land in `blocked`. Distinct from `POST …/objectives/{id}` so openapi_first
        does not 405 this collection verb as an unknown method on the member path.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                ids:
                  type: array
                  items:
                    type: integer
      responses:
        '200':
          description: "{ deletedIds, deletedCount, blocked: [{ id, name, roleplays
            }] }"
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '422':
          description: No ids given.
          content:
            application/json:
              schema:
                type: object
                required:
                - error
                additionalProperties: false
                properties:
                  error:
                    type: string
  "/admin/content/roleplay/objectives/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.show
      summary: Show a roleplay practice objective.
      responses:
        '200':
          description: Criterion details.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionResult"
        '404':
          description: Criterion not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionError"
    put:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.replace
      summary: Update a roleplay practice objective.
      description: "`PUT` and `PATCH` route to the same controller action — same contract
        as the `PATCH` sibling above (armed together; a parity test enforces the pair
        stays in lockstep)."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                name:
                  type: string
                description:
                  type: string
                objective_type:
                  type: string
                  enum:
                  - skill
                  - behavior
                  - framework
                config:
                  type:
                  - object
                  - 'null'
                  additionalProperties: true
                parent_objective_id:
                  type:
                  - integer
                  - 'null'
                proficiency_expectations:
                  type:
                  - object
                  - 'null'
                  additionalProperties: true
                tag_list:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Updated practice objective.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionResult"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionError"
    patch:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.update
      summary: Update a roleplay practice objective.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                name:
                  type: string
                description:
                  type: string
                objective_type:
                  type: string
                  enum:
                  - skill
                  - behavior
                  - framework
                config:
                  type:
                  - object
                  - 'null'
                  additionalProperties: true
                parent_objective_id:
                  type:
                  - integer
                  - 'null'
                proficiency_expectations:
                  type:
                  - object
                  - 'null'
                  additionalProperties: true
                tag_list:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Updated practice objective.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionResult"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionError"
    delete:
      tags:
      - admin
      operationId: admin.content.roleplay.objectives.destroy
      summary: Delete a roleplay practice objective (refused if it has any roleplay
        assignments).
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '422':
          description: Criterion is in use and cannot be deleted.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionError"
  "/admin/content/roleplay/personas":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.personas.index
      summary: Paginated list of an organization's roleplay personas.
      description: "`Api::V3::Admin::Content::Roleplay::PersonasController`. Route
        per source (`config/routes.rb:576-604`): `resources :personas, only: [:index,
        :show, :create, :update, :destroy] { collection { post :ai_generate } } `
        — ROUTED on dest."
      responses:
        '200':
          description: Paginated persona list.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonasIndexEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: Page number.
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: Items per page (default 25, max 500).
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.personas.create
      summary: Create a roleplay persona.
      description: 'NOT VERBATIM per the captured DSL: the DSL''s `personality` request
        property does not exist in `persona_params` (`params.permit(:name, :role,
        :avatar_url, :background, :demeanor, :voice_id, tag_list: [])`) — the controller
        silently drops it via strong params. Dropped here; the controller is the real
        contract. Snake_case on the wire (`persona_params` reads snake_case directly;
        the SPA''s `createPersona`/`updatePersona` explicitly `deepSnake()` the body).
        Verified against the live caller (`packages/web/src/queries/admin/personas/mutations.ts#createPersona`),
        which sends `name`/`role?`/`demeanor?`/`voice_id?`/`background?`/`tag_list?`
        (no `avatar_url` today, though the controller accepts it).'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/PersonaCreateBody"
      responses:
        '201':
          description: Created persona.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: Validation error (`name` blank, or an invalid `demeanor`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
  "/admin/content/roleplay/personas/ai_generate":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.personas.ai_generate
      summary: AI-generate a roleplay persona (multi-turn).
      description: 'Delegates to `Content::Roleplay::PersonaAIGenerateService`. NOT
        VERBATIM per the captured DSL: none of `message`/`chat_history`/`prompt` are
        actually required by the controller (`message` defaults via `.presence`, `chat_history`
        via `Array(...)`, `prompt` is the legacy fallback used only when `message`
        is blank) — the service itself raises a 422 `AIGenerateError` ("Message is
        required") if both `message` and `prompt` end up blank, so the true constraint
        is "at least one of the two", which JSON Schema `required` cannot express
        as a single-field requirement; left both optional rather than over-claim either
        as always-required.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/PersonaAiGenerateBody"
      responses:
        '200':
          description: "`{ type, message, draft }` — `type` is question | draft |
            refinement; `draft` is null unless `type` is draft/refinement."
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaAiGenerateResult"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: Generation error (blank message, unparseable/invalid LLM response).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
  "/admin/content/roleplay/personas/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.personas.show
      summary: Get a single roleplay persona.
      responses:
        '200':
          description: Persona details.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Persona not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
    put:
      tags:
      - admin
      operationId: admin.content.roleplay.personas.replace
      summary: Update a roleplay persona.
      description: "`PUT` and `PATCH` route to the same controller action — same contract
        as the `PATCH` sibling above (armed together; a parity test enforces the pair
        stays in lockstep)."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/PersonaUpdateBody"
      responses:
        '200':
          description: Updated persona.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Persona not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags:
      - admin
      operationId: admin.content.roleplay.personas.update
      summary: Update a roleplay persona.
      description: Snake_case on the wire (same as `create`). Verified against the
        live caller (`personas/mutations.ts#updatePersona`), which always sends `name`
        plus whichever of `role`/`avatar_url`/`demeanor`/`voice_id`/`background`/`tag_list`
        the editor form has. All properties are optional here (a genuine partial update)
        — the caller's habit of always sending `name` is not a server requirement.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/PersonaUpdateBody"
      responses:
        '200':
          description: Updated persona.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Persona not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
    delete:
      tags:
      - admin
      operationId: admin.content.roleplay.personas.destroy
      summary: Delete a roleplay persona (rejected 422 if any persona assignment references
        it).
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaMessageResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Persona not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
        '422':
          description: Persona is referenced by a persona assignment.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PersonaSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
  "/admin/content/roleplay/prompt_versions":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.prompt_versions.index
      summary: Paginated list of an organization's roleplay prompt versions, optionally
        filtered by prompt_type.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::Content::\nRoleplay::PromptVersionsController` is real
        and complete (dev-5, admin/content/* triage\nbatch), unblocked by `Content::Roleplay::PromptVersionSerializer`
        (same batch). Route per\nsource (`config/routes.rb:576-604`): `resources :prompt_versions,
        only: [:index, :show,\n:create, :update] { member { post :activate } } ` —
        ROUTED on dest. Note there is no\n`destroy` action (source deliberately omits
        it — prompt versions are append-only/deactivated,\nnever deleted).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.prompt_versions.create
      summary: Create a roleplay prompt version.
      description: Contract deferred — see the index operation's description. Body
        — `{ prompt_type, name, content, version }`.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/content/roleplay/prompt_versions/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.prompt_versions.show
      summary: Get a single prompt version.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.content.roleplay.prompt_versions.replace
      summary: Update a prompt version.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.content.roleplay.prompt_versions.update
      summary: Update a prompt version.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/content/roleplay/prompt_versions/{id}/activate":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.prompt_versions.activate
      summary: Activate a prompt version, deactivating all other versions of the same
        prompt_type in the organization.
      description: |
        Contract deferred — see the index operation's description.

        `update_all(is_active: false)` deactivates siblings, then `update!(is_active: true)` activates
        this one. The bang call is kept verbatim (not client-triggerable — it writes a literal `true`,
        never a request-derived value; see the controller's own comment).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/content/roleplay/voice/transcript":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.voice_webhook.transcript
      security: []
      summary: Machine-to-machine webhook — the LiveKit voice agent posts a roleplay
        session transcript for persistence + scoring.
      description: |
        HMAC-authenticated (`X-Webhook-Signature` against `LIVEKIT_WEBHOOK_SECRET`).
        No tenant subdomain or member session — `resolve_organization`, `set_time_zone`,
        and `resolve_auth_context` are skipped; org context comes from `session_id` in
        the body. A missing secret fails closed (401), never HMAC against a coded default.

        Body — `{ session_id, attempt_id, messages?: [...], delivery_signals? }`.
        An in-progress attempt returns `{ status: "deferred" }` (accidental disconnect).
        Intentional End completes the attempt first; then this webhook scores (or, for
        reverse sessions, enqueues `GenerateReverseHighlightsJob`).
      responses:
        '200':
          description: Transcript accepted (deferred, persisted, or already_persisted).
        '401':
          description: Missing/invalid HMAC, or LIVEKIT_WEBHOOK_SECRET unset.
        '404':
          description: Session or attempt not found.
  "/admin/content/roleplay/voices/{voice_id}/preview":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.voices.preview
      summary: Preview a roleplay voice.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/content/spark/{spark_id}/spark-steps":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.spark.spark_steps.index
      summary: List a Spark's steps + theme for the admin builder, with the current
        optimistic lock version.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::Content::Spark::\nSparkStepsController` is real and
        complete (dev-5, admin/content/* triage batch). Route per\nsource (`config/routes.rb:539-543`):
        `namespace :content { namespace :spark, path:\n\"spark/:spark_id\" { get 'spark-steps',
        to: 'spark_steps#index' } }` — note the URL segment is\n`spark-steps` (hyphen),
        and `spark_id` is a path segment here, unlike `simulate`/`spark_insights`\nwhich
        key off the generic content `id`. ROUTED on dest.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.content.spark.spark_steps.bulk_update
      summary: Bulk-save a Spark's steps + theme with optimistic-concurrency and soft-cap
        enforcement.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ steps: [...], theme?: {}, lock_version? }`. Blocked on published sparks (409
        `immutable`, via `ensure_draft!`); lock-version mismatch returns 409 `concurrent_edit_conflict`
        with the current version so the client can resync; >50 steps returns 422 `soft_cap_exceeded`;
        unknown step `type` or invalid theme (unknown key / bad hex / >64KB) returns 422 with a named
        `error.code`. Writes an `AuthoringHistory` row only on a meaningful diff (theme/add/delete/
        reorder/edit), skipping no-op debounce saves.

        NOT VERBATIM (dev-5, two #27-class fixes):
        1. Source's bare `params.require(:steps)` raised `ActionController::ParameterMissing` with no
           rescue (this engine has zero `rescue_from`) — guarded with a presence check BEFORE the
           require so the raise is unreachable, per house pattern register #27. Missing/blank `steps`
           now renders 422 `{ error: { code: "missing_steps", message: "steps is required" } }`.
        2. A step entry outside Hash/Params (e.g. an array of strings) raised `ActionController::
           BadRequest` with no rescue — client-triggerable by construction. Now renders 422
           `{ error: { code: "invalid_step", ... } }` alongside the six existing named `Content::Spark`
           error rescues.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/content/video/extract_metadata":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.video.metadata.extract
      summary: Extract video metadata from a URL for the content builder.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159, same as every other\nadmin/content/* row. `Api::V3::Admin::Content::Video::MetadataController`
        is real and complete\n(dev-5, admin/content/* triage batch). Route per source
        (`config/routes.rb:565-567`):\n`scope 'content/video', module: 'content/video'
        { post :extract_metadata, action: :extract,\ncontroller: 'metadata' }` — ROUTED
        on dest.\n\nBody: `{ url: <string> }`. Delegates to `Content::Video::MetadataExtractor`
        (already landed).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/contents":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.contents.index
      summary: Paginated, filterable list of all content (any STI subtype) for the
        organization.
      description: |
        `Api::V3::Admin::ContentsController` — the family's largest row (402 L). Route per source
        (`config/routes.rb:513-518`): `resources :contents, only: [:index, :show, :create, :update,
        :destroy] { member { post :publish; post :archive; post :clone } } ` — ROUTED on dest.

        Excludes personal (Rumi-generated) roleplays from admin listings (a `type_data.visibility !=
        "personal"` filter). ⚠️ The DSL never captured the `search` query param — the controller reads
        it directly (`params[:search]`, a title/description LIKE match) alongside `page`/`per_page`/
        `type`/`status`; added here from the controller, not invented.
      responses:
        '200':
          description: Paginated content list.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentsIndexEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: Page number (default 1).
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: Items per page (default 25, max 500).
      - name: type
        in: query
        required: false
        schema:
          type: string
        description: Filter by STI subtype — short form (`roleplay`) or full (`Content::Roleplay`).
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - draft
          - published
          - archived
          - under_review
        description: Filter by lifecycle status.
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Title/description LIKE filter. Not in the captured DSL — read
          directly off the controller (`ContentsController#index`).
    post:
      tags:
      - admin
      operationId: admin.contents.create
      summary: Create content of a given STI subtype.
      description: |
        Body verified against the live caller (`packages/web/src/queries/admin/content/mutations.ts#createContent`),
        which sends only `type`/`title`/`description` — NOT `type_data`. NOT VERBATIM per the captured
        DSL: the DSL marked `type`, `description`, and `typeData` all `required`, but the controller
        disagrees — `content_class` defaults `type` to `"roleplay"` when absent, `content_params`
        permits `description`/`type_data` only `if params.key?(...)`, and `Content` has no presence
        validation on `description`. Requiring `type_data` here would 400 the live create dialog, which
        never sends it. `type` is kept required anyway because the one live caller always sends it.

        An unrecognized `type` falls back to base `::Content` (kept verbatim from source — a deliberate
        scope decision, not a crash risk). `program_id` is validated against `@current_organization.
        programs` (NOT VERBATIM, register #193/#191/#187/#177) — an id outside the organization renders
        422; blank is fine.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ContentWriteBody"
      responses:
        '201':
          description: Created content.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: Validation error (model errors, or an out-of-organization `program_id`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
  "/admin/contents/export":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.contents.export
      summary: CSV export of the admin roleplay list — same filters as the index,
        optional selection.
      description: |
        Was missing from this sidecar entirely, so request validation matched the path against
        `/admin/contents/{id}` and rejected it with `id: expected integer` before it reached the
        controller. Declared here so the route the controller has always served is actually reachable.
      responses:
        '200':
          description: CSV file.
          content:
            text/csv:
              schema:
                type: string
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: type
        in: query
        required: false
        schema:
          type: string
        description: Content type (roleplay).
      - name: ids
        in: query
        required: false
        schema:
          type: array
          items:
            type: integer
        description: Selected content ids. Omit to export everything matching the
          filters.
      - name: columns
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
        description: Visible column keys, in order.
  "/admin/contents/metrics_summary":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.contents.metrics_summary
      summary: Aggregate roleplay adoption + readiness for the filtered set (header
        metric cards).
      description: |
        Runs the SAME scope as the index, so the cards can never describe a different set than the
        table beneath them. Rates are nullable by design — a rate over zero attempts is undefined,
        not zero, and renders as N/A rather than 0%.
      responses:
        '200':
          description: Aggregate metrics for the filtered set.
          content:
            application/json:
              schema:
                type: object
                properties:
                  readyRate:
                    type: number
                    nullable: true
                  avgAttemptsToReady:
                    type: number
                    nullable: true
                  learners:
                    type: integer
                  attempts:
                    type: integer
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: type
        in: query
        required: false
        schema:
          type: string
        description: Content type (roleplay).
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Free-text search, same as the index.
      - name: statuses
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
        description: Filter by status (repeatable).
      - name: program_ids
        in: query
        required: false
        schema:
          type: array
          items:
            type: integer
        description: Filter by program (repeatable).
      - name: category_ids
        in: query
        required: false
        schema:
          type: array
          items:
            type: integer
        description: Filter by roleplay category (repeatable).
  "/admin/contents/playbook_import":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.contents.playbook_import
      summary: Import a playbook catalog.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Collection
        POST sibling of `/contents/{id}` — must be its own spec path so openapi_first
        does not treat `playbook_import` as a member id.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/contents/{content_id}/content/roleplay/availability":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.availability.show
      summary: Read a roleplay content's program/member-view availability.
      description: "`Api::V3::Admin::Content::Roleplay::AvailabilityController`. Route
        per source (`config/routes.rb:606-616`): `scope \"contents/:content_id\" {
        namespace :content { namespace :roleplay { resource :availability, only: [:show,
        :update], controller: 'availability' } } }`.\n\U0001F534 The router draws
        THREE verbs at this path — `GET`, `PATCH`, and `PUT` — all three resolving
        to this controller (`GET#show`, `PATCH#update`, `PUT#update`; confirmed live
        via `ChronusMentorApi::Engine.routes.routes`, not assumed from the route DSL).
        This file previously declared only `GET`/`PUT`, so the drawn `PATCH` had no
        entry. Its path was already covered by those two, and `openapi_first` treats
        a covered path with an undeclared verb as `:method_not_allowed`, which maps
        to `RequestInvalidError` (`openapi_first/failure.rb:9`). So every `PATCH`
        here returned **400** — the endpoint was BROKEN, not unguarded. Added below,
        mirroring `PUT` — same contract, `operationId` `.update` (PUT uses `.replace`)."
      responses:
        '200':
          description: Current availability.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilityEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilitySimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
    put:
      tags:
      - admin
      operationId: admin.content.roleplay.availability.replace
      summary: Set a roleplay content's program/member-view availability.
      description: |
        Body: `{ program_ids?: [id, ...], member_view_ids?: [id, ...] }` — either key replaces that
        availability set entirely (destroy-then-recreate in one transaction); omitting a key leaves it
        unchanged. NOT VERBATIM (#27-class fix, dev-5): both join models carry a `content_id`-scoped
        uniqueness validation, so a duplicate id within either array now renders 422 with this tier's
        error envelope instead of an unrescued 500 (source has no rescue at all here). The duplicate
        actually arrives as `ActiveRecord::RecordNotUnique` (a DB unique-index violation, not an AR
        validator — `Performance::UniqueIndexesValidatorTest` forbids a redundant validator), rescued
        alongside `ActiveRecord::RecordInvalid`.

        ⚠️ Known pre-existing source bug, kept as-is (out of scope for this row, flagged for tracking):
        `member_view_ids` resolution does `AdminView.where(program_id: @current_organization.id)` —
        compares an `AdminView#program_id` against an *organization* id, so it will not match any real
        `AdminView` in this org unless an id collision is coincidental. Functionally near-inert, not a
        security issue (only makes matches a false negative, never a false positive) — a submitted
        `member_view_ids` id therefore silently fails to attach rather than erroring.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AvailabilityWriteBody"
      responses:
        '200':
          description: Updated availability.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilityEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilitySimpleError"
        '422':
          description: Duplicate id within `program_ids`/`member_view_ids` (unique-index
            violation), rescued into this tier's error envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilitySimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags:
      - admin
      operationId: admin.content.roleplay.availability.update
      summary: Set a roleplay content's program/member-view availability.
      description: Drawn by the router (`PATCH#update`) but had no meta entry before
        this change. Its path was already covered by `GET`/`PUT`, so `openapi_first`
        treated it as `:method_not_allowed` and every `PATCH` returned 400 — broken,
        not unguarded. Same contract as the `PUT` sibling below (armed together; a
        parity test enforces the pair stays in lockstep). See `PUT` for the full behavior
        notes.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AvailabilityWriteBody"
      responses:
        '200':
          description: Updated availability.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilityEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilitySimpleError"
        '422':
          description: Duplicate id within `program_ids`/`member_view_ids` (unique-index
            violation), rescued into this tier's error envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AvailabilitySimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
  "/admin/contents/{content_id}/content/roleplay/materials":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.materials.index
      summary: List a roleplay content's materials, ordered by sort_order.
      description: |-
        `Api::V3::Admin::Content::Roleplay::MaterialsController`. Route per source (`config/routes.rb:606-616`): `resources :materials, only: [:index, :show, :create, :update, :destroy]` — ROUTED on dest.
        NOT VERBATIM (register #191): `find_content` is org-scoped (`.for_organization`), unlike source, which left this lookup unscoped across the whole family — a confirmed cross-organization read/write. Deliberate divergence; do not "restore" it to match source.
      responses:
        '200':
          description: Material list.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialsIndexEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.materials.create
      summary: Add a material to a roleplay content.
      description: "No live SPA caller today. NOT VERBATIM per the captured DSL: the
        DSL marked all seven body\nproperties `required`, but only `title` is unconditionally
        required by\n`Content::RoleplayMaterial`'s validations — `material_url` is
        required only `if :link?`,\n`material_id` only `if spark? || course?`, and
        `material_type`/`estimated_minutes`/\n`context_note`/`sort_order` are all
        optional (the column defaults `material_type` to \"link\",\n`sort_order` to
        0). Modeled here as `required: [title]`, matching the model, not the DSL.\n\n\U0001F534
        REGISTERED DEFECT, PORTED VERBATIM (not decided here, do not fix without an
        operator\nruling): `material.sort_order ||= @content.materials.count` can
        never fire — the column\ndefaults `sort_order` to `0`, and `0` is truthy in
        Ruby, so `||=` never assigns. Every material\ncreated via this endpoint silently
        gets `sort_order: 0` regardless of how many already exist.\nAn omitted `sort_order`
        does NOT append to the end, whatever the field name implies.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/MaterialWriteBody"
      responses:
        '201':
          description: Created material.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
        '422':
          description: Validation error (blank `title`, missing conditionally-required
            field for the given `material_type`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
  "/admin/contents/{content_id}/content/roleplay/materials/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.materials.show
      summary: Get a single material's details.
      responses:
        '200':
          description: Material details.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content, or material within it, not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
      - name: id
        in: path
        required: true
        schema:
          type: integer
    put:
      tags:
      - admin
      operationId: admin.content.roleplay.materials.replace
      summary: Update a material.
      description: "`PUT` and `PATCH` route to the same controller action — same contract
        as the `PATCH` sibling above (armed together; a parity test enforces the pair
        stays in lockstep)."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/MaterialUpdateBody"
      responses:
        '200':
          description: Updated material.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content, or material within it, not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
      - name: id
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags:
      - admin
      operationId: admin.content.roleplay.materials.update
      summary: Update a material.
      description: No live SPA caller today. Every property optional (partial update)
        — same fields as `create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/MaterialUpdateBody"
      responses:
        '200':
          description: Updated material.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content, or material within it, not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
      - name: id
        in: path
        required: true
        schema:
          type: integer
    delete:
      tags:
      - admin
      operationId: admin.content.roleplay.materials.destroy
      summary: Remove a material from a roleplay content.
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialMessageResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Roleplay content, or material within it, not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MaterialSimpleError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
      - name: id
        in: path
        required: true
        schema:
          type: integer
  "/admin/contents/{content_id}/content/roleplay/objective_assignments":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.objectiveAssignments.index
      summary: List a roleplay's practice objective assignments.
      responses:
        '200':
          description: The roleplay's assigned practice objectives.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentIndexResult"
        '404':
          description: Content not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.objectiveAssignments.create
      summary: Assign a practice objective to a roleplay.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                practice_objective_id:
                  type: integer
                is_required:
                  type: boolean
                weight:
                  type: integer
      responses:
        '201':
          description: Created assignment.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentResult"
        '404':
          description: Content not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
        '422':
          description: Validation error (e.g. this practice objective is already assigned
            to the roleplay).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
  "/admin/contents/{content_id}/content/roleplay/objective_assignments/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.content.roleplay.objectiveAssignments.replace
      summary: Update a roleplay's practice objective assignment (practice_objective_id
        / is_required / weight).
      description: "`PUT` and `PATCH` route to the same controller action — same contract
        as the `PATCH` sibling above (armed together; a parity test enforces the pair
        stays in lockstep)."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                practice_objective_id:
                  type: integer
                is_required:
                  type: boolean
                weight:
                  type: integer
      responses:
        '200':
          description: Updated assignment.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentResult"
        '404':
          description: Content or assignment not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
      - name: id
        in: path
        required: true
        schema:
          type: integer
    patch:
      tags:
      - admin
      operationId: admin.content.roleplay.objectiveAssignments.update
      summary: Update a roleplay's practice objective assignment (practice_objective_id
        / is_required / weight).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                practice_objective_id:
                  type: integer
                is_required:
                  type: boolean
                weight:
                  type: integer
      responses:
        '200':
          description: Updated assignment.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentResult"
        '404':
          description: Content or assignment not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
      - name: id
        in: path
        required: true
        schema:
          type: integer
    delete:
      tags:
      - admin
      operationId: admin.content.roleplay.objectiveAssignments.destroy
      summary: Remove a practice objective assignment from a roleplay.
      responses:
        '200':
          description: Removed.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '404':
          description: Content or assignment not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleplayCriterionAssignmentError"
      parameters:
      - name: content_id
        in: path
        required: true
        schema:
          type: integer
      - name: id
        in: path
        required: true
        schema:
          type: integer
  "/admin/contents/{content_id}/content/roleplay/persona_assignments":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.roleplay.persona_assignments.index
      summary: List a roleplay content's persona assignments, ordered by position.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::Content::\nRoleplay::PersonaAssignmentsController`
        is real and complete (dev-5, admin/content/* triage\nbatch), unblocked by
        `Content::Roleplay::PersonaAssignmentSerializer` (same batch). Route per\nsource
        (`config/routes.rb:606-616`): `resources :persona_assignments, only: [:index,
        :create,\n:update, :destroy]` — ROUTED on dest.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.content.roleplay.persona_assignments.create
      summary: Assign a persona to a roleplay content.
      description: Contract deferred — see the index operation's description. Body
        — `{ persona_id, position?, scenario_overrides? }`.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/contents/{content_id}/content/roleplay/persona_assignments/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.content.roleplay.persona_assignments.replace
      summary: Update a persona assignment.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.content.roleplay.persona_assignments.update
      summary: Update a persona assignment.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.content.roleplay.persona_assignments.destroy
      summary: Remove a persona assignment.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/contents/{content_id}/responses":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content_responses.all_for_content
      summary: List the current admin member's responses across every step of a content.
      description: |
        Contract deferred — see the index operation's description.

        Route per source (`config/routes.rb:530-531`): `get :responses, controller:
        'content_responses', action: :all_for_content` — a sibling of the steps/responses nesting
        above, mounted directly on `contents/:content_id`.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/contents/{content_id}/steps":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content_steps.index
      summary: List a content's steps, ordered by position.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nContentStepsController` is real and complete (dev-5,
        admin root family batch) — generic\nsingle-step CRUD for ANY `Content` subtype
        (`Content::ContentStep` already on dest), a sibling\nof the Spark-specific
        bulk `spark_steps` row rather than a duplicate of it. Route per source\n(`config/routes.rb:520-525`,
        nested under `resources :contents`): `resources :steps, only:\n[:index, :show,
        :create, :update, :destroy], controller: 'content_steps' { collection { patch\n:reorder
        } } ` — ROUTED on dest.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.content_steps.create
      summary: Create a content step (appends at the end unless a position is given).
      description: Contract deferred — see the index operation's description. Body
        — `{ title?, position?, mdx_source? }`.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/contents/{content_id}/steps/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.content_steps.reorder
      summary: Reorder a content's steps by an ordered list of {id, position} pairs.
      description: "Contract deferred — see the index operation's description.\n\nBody
        — `{ positions: [{ id, position }, ...] }`. NOT VERBATIM (register #193, directed):
        a\nmissing `positions` now renders this tier's 422 envelope instead of the
        framework's bare 400\nfor an unrescued `ParameterMissing`. \U0001F534 OPEN
        FINDING, not decided: an unknown/foreign `id` in\nthe array raises `ActiveRecord::RecordNotFound`
        (bang `.find`, no local rescue) — framework-\nmapped to 404 (not a 500 risk)
        but not this tier's envelope, and it aborts the whole batch on\nthe first
        bad id rather than validating the full set up front the way `skill_categories#reorder`\ndoes.
        Left verbatim pending a ruling.\n"
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/contents/{content_id}/steps/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content_steps.show
      summary: Get a single content step.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.content_steps.replace
      summary: Update a content step (optimistic-locked via lock_version).
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.content_steps.update
      summary: Update a content step (optimistic-locked via lock_version).
      description: |
        Contract deferred — see the index operation's description.

        A stale `lock_version` renders 409 (`ActiveRecord::StaleObjectError`, framework's built-in
        optimistic locking on the real `lock_version` column). Editing `mdx_source` clears the
        step's compiled-MDX cache.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.content_steps.destroy
      summary: Delete a content step and recompact the remaining steps' positions.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/contents/{content_id}/steps/{step_id}/responses":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content_responses.index
      summary: List the current admin member's responses for a content step.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nContentResponsesController` is real and complete
        (dev-5, admin root family batch), backed by\n`Content::Responses::Writer`
        (already on dest). Route per source\n(`config/routes.rb:520-534`, nested under
        `resources :contents`): `resources :steps, only: [],\ncontroller: 'content_steps'
        { resources :responses, only: [:index, :create], controller:\n'content_responses'
        }` — ROUTED on dest.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.content_responses.create
      summary: Upsert the current admin member's response to a content step.
      description: |
        Contract deferred — see the index operation's description.

        Body — `{ field_name, value }`. NOT VERBATIM (register #193, directed): a missing/blank
        `field_name` now renders this tier's 422 envelope with an i18n message instead of the
        framework's bare 400 for an unrescued `ParameterMissing` — envelope/i18n consistency, not
        crash-prevention. `ActiveRecord::RecordInvalid` (also framework-mapped to 422) is likewise
        rescued into the same envelope shape.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/contents/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.contents.show
      summary: Get content details, with subtype-specific fields (roleplay personas/goals/materials,
        spark config, page blocks, video/podcast/document/book/article fields).
      description: 'Renders `content_detail_json` — the summary fields plus subtype-specific
        extensions (see `ContentDetail`). Scoped only by organization: unlike `index`
        (which excludes personal Rumi roleplays), `show` has no visibility filter,
        so a personal roleplay''s id is still readable here.'
      responses:
        '200':
          description: Content details.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
    put:
      tags:
      - admin
      operationId: admin.contents.replace
      summary: Update content.
      description: "`PUT` and `PATCH` route to the same controller action — same contract
        as the `PATCH` sibling above (armed together; a parity test enforces the pair
        stays in lockstep)."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ContentUpdateBody"
      responses:
        '200':
          description: Updated content.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
    patch:
      tags:
      - admin
      operationId: admin.contents.update
      summary: Update content.
      description: Same `program_id` validation as `create` applies here too (shared
        `validate_program_id!` before_action). Every property in the body is optional
        — a caller sends only the slice it owns (the Settings tab sends `title`/`description`;
        the type builders send `type_data` verbatim to `content.type_data`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ContentUpdateBody"
      responses:
        '200':
          description: Updated content.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
        '422':
          description: Validation error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
    delete:
      tags:
      - admin
      operationId: admin.contents.destroy
      summary: Delete content.
      responses:
        '200':
          description: Deleted.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentMessageResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
  "/admin/contents/{id}/archive":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.contents.archive
      summary: Archive content (rejected 422 if not archivable from its current AASM
        status).
      responses:
        '200':
          description: Archived content.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
        '422':
          description: Cannot archive.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
  "/admin/contents/{id}/clone":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.contents.clone
      summary: Clone content (Spark, Page, and simple media types; rejected 422 for
        unsupported types, 403 for an unauthorized cross-org Spark clone).
      description: "No request body — the controller reads no params for this action.\n\n\U0001F534
        OPEN FINDING, not decided: `Content::Page::Cloner` raises a bare `ArgumentError`
        for a page\nwith more than 200 blocks (`app/services/content/page/cloner.rb:14-16`)
        — unlike\n`ActiveRecord::RecordInvalid`/`ActionController::BadRequest`, `ArgumentError`
        is NOT\nframework-mapped to a 4xx, so this would genuinely 500, unrescued.
        Left verbatim; not modeled\nas a response here since it is not a documented
        contract, it is a bug.\n"
      responses:
        '201':
          description: Cloned content.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Unauthorized cross-organization Spark clone (`Content::Spark::UnauthorizedCloneError`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
        '404':
          description: Content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
        '422':
          description: Content type cannot be cloned.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
  "/admin/contents/{id}/fork":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.contents.fork
      summary: Fork a content record.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/contents/{id}/generate":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.contents.generate
      summary: Generate content (SSE deferred; routed).
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/contents/{id}/insights":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.content.spark.insights.show
      summary: Live aggregate completion/response metrics for a Spark.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::Content::Spark::\nSparkInsightsController` is real
        and complete (dev-5, admin/content/* triage batch). Route per\nsource (`config/routes.rb:546-549`,
        mounted under `contents/:id/...` alongside `simulate`, NOT\nnested under `content/spark/:spark_id`):
        `get 'contents/:id/insights', to:\n'content/spark/spark_insights#show'` —
        ROUTED on dest.\n\nRead-only; every metric is a single grouped SQL query (no
        per-step N+1). Cross-org requests\n404 (`Content.for_organization` scope).
        Verbatim port — no raising idiom, no missing dependency.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/contents/{id}/publish":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.contents.publish
      summary: Publish content (rejected 422 with validation reasons if not publishable
        — e.g. an unpublishable Spark).
      description: Emits `spark.published`/`spark.validation_failed` telemetry for
        Spark content.
      responses:
        '200':
          description: Published content.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentShowEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Content not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentSimpleError"
        '422':
          description: Cannot publish.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ContentPublishError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
  "/admin/contents/{id}/roleplay_metrics":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.contents.roleplay_metrics
      summary: Per-roleplay readiness + mode breakdown for the detail panel's Insights
        tab.
      description: |
        Deliberately NOT `{id}/insights` — that path belongs to Spark, and a member route on
        `resources :contents` is declared earlier in the routes file, so it would shadow it.
        404s for a non-roleplay or unpublished id rather than returning an all-zero chart.
      responses:
        '200':
          description: Readiness distribution, mode distribution and performance.
          content:
            application/json:
              schema:
                type: object
                properties:
                  performance:
                    type: object
                    properties:
                      readyRate:
                        type: number
                        nullable: true
                      avgAttemptsToReady:
                        type: number
                        nullable: true
                      learners:
                        type: integer
                      attempts:
                        type: integer
                  readiness:
                    type: object
                    description: All three levels are always present, including zero-count
                      ones.
                    properties:
                      ready:
                        "$ref": "#/components/schemas/RoleplayMetricSlice"
                      almostThere:
                        "$ref": "#/components/schemas/RoleplayMetricSlice"
                      keepPracticing:
                        "$ref": "#/components/schemas/RoleplayMetricSlice"
                  mode:
                    type: object
                    properties:
                      voice:
                        "$ref": "#/components/schemas/RoleplayMetricSlice"
                      text:
                        "$ref": "#/components/schemas/RoleplayMetricSlice"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Content ID.
  "/admin/contents/{id}/simulate_next":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.spark.simulate.next_step
      summary: Resolve the next Spark step for the builder's test-drive modal, given
        branching answers.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::Content::Spark::\nSimulateController` is real and complete
        (dev-5, admin/content/* triage batch). Route per\nsource (`config/routes.rb:546-551`,
        mounted directly under `contents/:id/...` so the builder\ncan reuse the content
        id it already has, NOT nested under the `content/spark/:spark_id`\nnamespace
        `spark_steps` uses): `post 'contents/:id/simulate_next', to:\n'content/spark/simulate#next_step'`
        — ROUTED on dest.\n\nBody: `{ current_step_id?, answers?: {} }` → `{ next_step_id,
        terminal }`. Shadow-mode: no DB\nwrites, no notifications. NOT VERBATIM (#27-class
        fix, dev-5): `answers` outside\nHash/Params/nil raised `ActionController::BadRequest`
        with no rescue in source (a plain\nclient-triggerable malformed body) — now
        rendered 422 `{ error: { code: \"invalid_answers\", ... } }`\nlike every other
        named error path here, instead of an unhandled 500.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/contents/{id}/simulate_score":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.content.spark.simulate.score
      summary: Score a simulated Spark run against the test-drive modal's answers.
      description: |
        Contract deferred — see the simulate_next operation's description.

        Body: `{ answers?: {} }` → `{ raw, max, percent, passed, breakdown }`. Same
        `answers`-type #27-class fix as simulate_next applies here (shared `normalize_answers` helper).
      responses:
        '200':
          description: Deferred — see the simulate_next operation's description.
  "/admin/dashboards":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.dashboards.index
      summary: The organization's Insights dashboards, ordered.
      description: |
        Scoped to the caller's organization via `Insights::Dashboard.for_organization`, so another org's
        dashboards are unreachable rather than merely hidden.

        ⚠️ NOT VERBATIM: source gates every action on `GOODDATA_INSIGHTS` and 404s without it. That gate
        is dropped here — with `embed_token` unported there is no vendor call left for it to protect, and
        keeping it would have preserved the 404 this port exists to fix (register #313 records the flag as
        code-level rather than functional coupling).

        Dashboards are polymorphic over `gooddata` / `widget_grid` / `react_spa` kinds, so most rows never
        needed the vendor at all.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - dashboards
                additionalProperties: false
                properties:
                  dashboards:
                    type: array
                    items:
                      "$ref": "#/components/schemas/InsightsDashboard"
    post:
      tags:
      - admin
      operationId: admin.dashboards.create
      summary: Creates a dashboard in the caller's organization.
      description: |
        `organization_id` and the created/updated-by stamps are set server-side and are NOT accepted from
        the body, so a caller cannot author into another organization.

        ⚠️ Session callers must send `X-Requested-With` or the write 403s before the action runs.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                kind:
                  type: string
                  description: One of the Insights::Dashboard kinds (gooddata / widget_grid
                    / react_spa).
                gooddata_ref:
                  type: string
                component_key:
                  type: string
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - dashboard
                additionalProperties: false
                properties:
                  dashboard:
                    "$ref": "#/components/schemas/InsightsDashboard"
        '422':
          description: |
            Model validation failed, OR the org-scoped unique index on `gooddata_ref` was hit — the
            controller rescues `RecordNotUnique` so a race that the validation lost still answers 422
            rather than 500.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
  "/admin/dashboards/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.dashboards.show
      summary: One dashboard by id.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - dashboard
                additionalProperties: false
                properties:
                  dashboard:
                    "$ref": "#/components/schemas/InsightsDashboard"
        '404':
          description: No such dashboard, or it belongs to another organization —
            deliberately indistinguishable.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
    put:
      tags:
      - admin
      operationId: admin.dashboards.replace
      summary: Updates a dashboard.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin carries the same contract so
        the `PUT` route is not fenced out of the spec — see the `PATCH` sibling above.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                kind:
                  type: string
                gooddata_ref:
                  type: string
                component_key:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - dashboard
                additionalProperties: false
                properties:
                  dashboard:
                    "$ref": "#/components/schemas/InsightsDashboard"
        '404':
          description: Not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
    patch:
      tags:
      - admin
      operationId: admin.dashboards.update
      summary: Updates a dashboard.
      description: "⚠️ Session callers must send `X-Requested-With`."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                kind:
                  type: string
                gooddata_ref:
                  type: string
                component_key:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - dashboard
                additionalProperties: false
                properties:
                  dashboard:
                    "$ref": "#/components/schemas/InsightsDashboard"
        '404':
          description: Not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
    delete:
      tags:
      - admin
      operationId: admin.dashboards.destroy
      summary: Deletes a dashboard.
      description: "⚠️ Session callers must send `X-Requested-With`."
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '404':
          description: Not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
  "/admin/dashboards/{id}/embed_token":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.dashboards.embed_token
      summary: Mints a short-lived GoodData embed token for a gooddata-kind dashboard.
      description: |
        Only meaningful for `kind: gooddata`. A `widget_grid` or `react_spa` dashboard renders purely
        client-side and needs no server token, so those 422 rather than minting one.

        Not gated on `GOODDATA_INSIGHTS`: the kind check above is the real guard, and gating every action
        on the flag is what previously 404'd the whole Optimize section for orgs without it.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - embed
                additionalProperties: false
                properties:
                  embed:
                    "$ref": "#/components/schemas/GoodDataEmbedToken"
        '404':
          description: |
            No such dashboard in this organization, or GoodData no longer holds the referenced object
            (`Gooddata::NotFoundError`). Both render the same body — a cross-org id must not confirm
            existence.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
        '422':
          description: |
            The dashboard's kind cannot carry an embed token, or the vendor rejected the request
            arguments (`Gooddata::Error` — e.g. a blank `gooddata_ref`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
        '503':
          description: |
            BI service not configured (`ConfigError`) or unreachable (`AuthError`, `ConnectionError`,
            `RateLimitedError`, `ServerError`, `ProtocolError`). Retryable except for `ConfigError`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
      parameters:
      - name: mode
        in: query
        required: false
        description: |
          `player` renders the dashboard read-only; `editor` opens GoodData's edit chrome.
          Absent or unrecognized values fall back to `player` (the action defaults before minting).
        schema:
          type: string
          enum:
          - player
          - editor
          default: player
        example: player
  "/admin/data_imports":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.data_imports.create
      summary: Upload a CSV to start a users/profile_questions data import.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nDataImportsController` is real and complete (dev-5,
        admin root family batch). NOT VERBATIM\n(ruled): source also passed `created_by:
        current_member` to `DataImport.new` — confirmed live\nthat `DataImport` has
        no such attribute/association on dest, so this raised unconditionally.\nRuling:
        dropped the argument rather than invent a column — a 100%-crashing call recorded
        no\nprovenance anyway. Source intended to record who triggered the import;
        dest has nowhere to put\nthat today, and adding a column is a separate schema
        decision.\n\n\U0001F534 SEPARATE NEW FINDING, not decided: `create` is STILL
        blocked for a different reason.\nConfirmed live: assigning ANY ordinary file
        upload to `source_file=` raises `NoMethodError`\nfrom `DataImport::Utils#source_file_created_at`
        (`app/models/data_import/utils.rb:99`), which\nrequires a digit-prefixed filename
        (the SFTP feed importer's own convention) — an ordinary\nadmin-uploaded filename
        never matches. Unconditional for every real use of this endpoint.\n\nRoute
        per source (`config/routes.rb:693`): `resources :data_imports, only: [:create,
        :show]`\n— `show` is ROUTED on dest and fully functional; `create` remains\nunrouted
        and stays moot until the `created_by` finding above is resolved.\n"
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/data_imports/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.data_imports.show
      summary: Read a data import's status.
      description: |
        Contract deferred — see the create operation's description.

        NOT VERBATIM (register #191): source's `find_by(id: params[:id])` is unscoped — the third
        confirmed cross-organization read of the session (same class as `materials_controller.rb:89`).
        Fixed with `DataImport.where(organization_id: @current_organization.id).find_by(id: params[:id])`
        — `DataImport` has no `.for_organization` scope (that scope is `Content`-specific, does not
        generalize), so this uses the confirmed `organization_id` column directly via
        `belongs_to_organization`. ⛔ Do not "restore" the unscoped form to match source.
      responses:
        '200':
          description: Deferred — see the create operation's description.
  "/admin/email_logs/export":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.emailLogs.export
      summary: Trigger an async CSV export of email messages via the exporter job.
      description: |
        Enqueues `ChronusExporter::EmailLogs::MessagesExporter.delay(queue: DjQueues::LONG_RUNNING)`
        with the same search/sort params `messages` builds, read off `params` (query string) rather
        than a JSON body — no request body is declared, since none is genuinely accepted; declaring
        one would invent a contract the controller doesn't have.
      responses:
        '200':
          description: Export initiated.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
  "/admin/email_logs/filters":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.emailLogs.filters
      summary: Get possible filter values (filterable statuses, email titles, program
        options).
      responses:
        '200':
          description: Filter options.
          content:
            application/json:
              schema:
                type: object
                required:
                - filters
                additionalProperties: false
                properties:
                  filters:
                    type: object
                    required:
                    - statuses
                    - emailTitles
                    - programs
                    additionalProperties: false
                    properties:
                      statuses:
                        type: array
                        items:
                          type: object
                          required:
                          - value
                          - label
                          additionalProperties: false
                          properties:
                            value:
                              type: string
                            label:
                              type: string
                      emailTitles:
                        type: array
                        items:
                          type: string
                      programs:
                        type: array
                        items:
                          type: object
                          required:
                          - id
                          - name
                          additionalProperties: false
                          properties:
                            id:
                              type: integer
                            name:
                              type: string
        '503':
          description: The email-logs microservice call failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/email_logs/messages":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.emailLogs.messages
      summary: Get paginated, locally-enriched email message logs.
      description: |
        Enriches each microservice row with local `Member`/`AbstractProgram` lookups
        (`members_map`/`programs_map`); a sender or recipient id that no longer resolves locally sets
        `senderRemoved`/`recipientRemoved` and falls back to the microservice's own unenriched name
        string, rather than rendering blank.

        ⚠️ `email_title` is accepted but not read by `build_messages_request_params` — it plays no
        part in filtering; it only happens to share a name with the unrelated response field
        `emailTitle`.
      responses:
        '200':
          description: Paginated email messages.
          content:
            application/json:
              schema:
                type: object
                required:
                - messages
                - meta
                additionalProperties: false
                properties:
                  messages:
                    type: array
                    items:
                      "$ref": "#/components/schemas/EmailLogMessage"
                  meta:
                    "$ref": "#/components/schemas/EmailLogsPaginationMeta"
        '503':
          description: The email-logs microservice call failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number. Default: 1.'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page. Default: 30, max 100.'
      - name: sort_field
        in: query
        required: false
        schema:
          type: string
        description: 'Sort field. Default: sent_at.'
      - name: sort_dir
        in: query
        required: false
        schema:
          type: string
        description: 'Sort direction. Default: desc.'
      - name: sender
        in: query
        required: false
        schema:
          type: string
        description: Filter by sender name.
      - name: recipient
        in: query
        required: false
        schema:
          type: string
        description: Filter by recipient name.
      - name: sender_email
        in: query
        required: false
        schema:
          type: string
        description: Filter by sender email.
      - name: recipient_email
        in: query
        required: false
        schema:
          type: string
        description: Filter by recipient email.
      - name: program_id
        in: query
        required: false
        schema:
          type: string
        description: Filter by program ID(s), comma-separated.
      - name: email_title
        in: query
        required: false
        schema:
          type: string
        description: Accepted but currently inert — see the operation description.
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by status(es), comma-separated.
      - name: date_from
        in: query
        required: false
        schema:
          type: string
        description: Filter messages sent after this date (ISO 8601).
      - name: date_to
        in: query
        required: false
        schema:
          type: string
        description: Filter messages sent before this date (ISO 8601).
  "/admin/email_logs/overall_stats":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.emailLogs.overallStats
      summary: Get overall email delivery statistics (org- or program-scoped, optionally
        date-ranged).
      description: |
        `Api::V3::Admin::EmailLogsController` is real and complete (task #70, verbatim port).
        Dependencies verified live on dest, matching source exactly: `Microservices::EmailLogs`
        (URL_MAP paths confirmed by direct read — `overall_stats` → `cumulative_email_stats/
        stat_for_date_range`), `ChronusMentorMailgun::Event` constants,
        `ReportsFilterService`. A microservice-side failure surfaces as `503` with the bare
        `{error}` shape, not this API's `{error: {code, message}}` envelope — the controller renders
        it directly, not through the shared interface-stack error path.
      responses:
        '200':
          description: Overall email stats.
          content:
            application/json:
              schema:
                type: object
                required:
                - overallStats
                additionalProperties: false
                properties:
                  overallStats:
                    type: object
                    required:
                    - delivered
                    - accepted
                    - failed
                    - clicked
                    - opened
                    - spammed
                    - averageAccepted
                    - averageOpened
                    - averageClicked
                    additionalProperties: false
                    properties:
                      delivered:
                        type: integer
                      accepted:
                        type: integer
                      failed:
                        type: integer
                      clicked:
                        type: integer
                      opened:
                        type: integer
                      spammed:
                        type: integer
                      averageAccepted:
                        type: number
                        description: accepted / participant count, rounded to 1 decimal;
                          0 if there are no participants.
                      averageOpened:
                        type: number
                        description: opened / participant count, rounded to 1 decimal;
                          0 if there are no participants.
                      averageClicked:
                        type: number
                        description: clicked / participant count, rounded to 1 decimal;
                          0 if there are no participants.
        '503':
          description: The email-logs microservice call failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
      parameters:
      - name: program_id
        in: query
        required: false
        schema:
          type: integer
        description: Filter by program ID (optional; defaults to every program the
          caller can filter on).
      - name: date_range
        in: query
        required: false
        schema:
          type: string
        description: Date range filter (MM/DD/YYYY - MM/DD/YYYY).
  "/admin/evidence_packages/{review_type}":
    parameters:
    - name: review_type
      in: path
      required: true
      schema:
        type: string
      description: Review type slug. Unknown values 422 from the controller with `known`.
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.evidence_packages.show
      summary: Assemble a go-live evidence package for a review type.
      responses:
        '200':
          description: Assembled evidence package.
        '422':
          description: Unknown review_type.
  "/admin/global_connections_report":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.globalConnectionsReport.index
      summary: Get a paginated, cross-program report of every connection (group or
        flash meeting) in the organization.
      description: "`Api::V3::Admin::GlobalConnectionsReportController` is real and
        complete (task #70, verbatim\nport). `test_index_returns_real_summary_and_paginated_connections`
        and its siblings exercise\nit against real fixture data (`org_primary`'s real
        groups and flash meetings).\n\nA group row and a meeting row share an identical
        key set (`build_group_row`/`build_meeting_\nrow`), so both are modeled as
        one closed row shape rather than a `oneOf` — `objectType`\ndiscriminates which
        kind of object it is, and `status`'s enum is the union of both label sets\n(`status_label_for_group`/`meeting_status_label`).\n\n\U0001F534
        CONFIRMED DEFECT, inherited verbatim from source (not port-introduced, not
        fixed here — a\ncall for the lead, per this row's own header comment): `meeting_status_label`'s\n`meeting.active?`
        is an ActiveModel-generated predicate for the plain `active` boolean COLUMN,\nnot
        a time-based check. Nothing in this codebase ever flips it false for completing/cancelling,\nso
        a meeting reports `\"upcoming\"` regardless of its real completed/cancelled
        state as long as\n`active` stays true (the default) — confirmed by `test_index_filters_by_connection_type_flash_\nmeeting_and_labels_status_correctly`,
        which pins `\"upcoming\"` for a completed AND a cancelled\nfixture meeting.
        `status`'s enum below includes `completed`/`cancelled`/`past` anyway, because\nthey
        are real code paths (reachable the moment something flips `active` false),
        just not\nreachable by any fixture today.\n\n⚠️ Response keys are camelCase,
        not the snake_case this spec's convention states — this\ncontroller renders
        literal Ruby symbol keys directly. See this row's coordinator report for\nthe
        cross-cutting note shared by all 8 controllers in this batch.\n"
      responses:
        '200':
          description: Org-wide connection summary, paginated rows, and the program
            list for filtering.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GlobalConnectionsReportIndex"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: program_id
        in: query
        required: false
        schema:
          type: integer
        description: Filter by program ID.
      - name: connection_type
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by type: ongoing, circle, flash_meeting.'
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by status: active, inactive, closed, drafted, pending.'
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Search by connection name or member name.
      - name: sort
        in: query
        required: false
        schema:
          type: string
        description: Sort column (e.g. name, program, status, type, started_on).
      - name: sort_order
        in: query
        required: false
        schema:
          type: string
        description: 'asc or desc (default: desc).'
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 25, max: 100).'
  "/admin/global_connections_report/export":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.globalConnectionsReport.export
      summary: Export the current (filtered) global connections report as CSV.
      description: Same filters as `index`, minus pagination and sort.
      responses:
        '200':
          description: CSV file download (`global_connections_report_<date>.csv`).
          content:
            text/csv:
              schema:
                type: string
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: program_id
        in: query
        required: false
        schema:
          type: integer
        description: Filter by program ID.
      - name: connection_type
        in: query
        required: false
        schema:
          type: string
        description: Filter by type.
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by status.
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Search by name.
  "/admin/global_connections_report/select_all_ids":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.globalConnectionsReport.selectAllIds
      summary: Get every connection id ("groups:N" / "meetings:N") matching the current
        filters, unpaginated.
      description: Same filters as `index`, minus pagination and sort.
      responses:
        '200':
          description: Every matching id, in "groups:N" / "meetings:N" form.
          content:
            application/json:
              schema:
                type: object
                required:
                - ids
                - total
                additionalProperties: false
                properties:
                  ids:
                    type: array
                    items:
                      type: string
                  total:
                    type: integer
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: program_id
        in: query
        required: false
        schema:
          type: integer
        description: Filter by program ID.
      - name: connection_type
        in: query
        required: false
        schema:
          type: string
        description: Filter by type.
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by status.
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Search by name.
  "/admin/goal_analytics":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.goal_analytics.show
      summary: Org-level goal analytics for admins — participation, lifecycle funnel,
        alignment.
      description: |
        Returns aggregate goal-setting participation, a draft/scored/finalized lifecycle
        funnel, and org-goal alignment adoption for the organization's current goals.
        Every count and rate routes through Privacy::MinimumCohort — a numerator (not
        just the population) below the org's minimum cohort size is withheld. Never
        reads GoalQualityScore or SmartRubric: an individual's SMART score and
        per-criterion feedback stay employee-private (A5). `state: feature_disabled`
        when PERFORMANCE_GOALS is off for the organization.
      responses:
        '200':
          description: Goal analytics payload, or a feature_disabled marker.
          content:
            application/json:
              schema:
                type: object
                required:
                - organization_id
                - state
                additionalProperties: true
                properties:
                  organization_id:
                    type: integer
                  state:
                    type: string
                    enum:
                    - ready
                    - feature_disabled
                  computed_at:
                    type: string
                    format: date-time
                  definitions_version:
                    type: integer
                  population:
                    type: object
                    required:
                    - eligible_members_count
                    additionalProperties: false
                    properties:
                      eligible_members_count:
                        "$ref": "#/components/schemas/GoalAnalyticsCell"
                  participation:
                    type: object
                    required:
                    - members_with_goal_count
                    - participation_rate
                    additionalProperties: false
                    properties:
                      members_with_goal_count:
                        "$ref": "#/components/schemas/GoalAnalyticsCell"
                      participation_rate:
                        "$ref": "#/components/schemas/GoalAnalyticsCell"
                  funnel:
                    type: object
                    required:
                    - total_current_goals_count
                    - by_state
                    additionalProperties: false
                    properties:
                      total_current_goals_count:
                        "$ref": "#/components/schemas/GoalAnalyticsCell"
                      by_state:
                        "$ref": "#/components/schemas/GoalAnalyticsFunnelBreakdown"
                  alignment:
                    type: object
                    required:
                    - aligned_goals_count
                    - alignment_rate
                    additionalProperties: false
                    properties:
                      aligned_goals_count:
                        "$ref": "#/components/schemas/GoalAnalyticsCell"
                      alignment_rate:
                        "$ref": "#/components/schemas/GoalAnalyticsCell"
                  definitions:
                    type: object
                    required:
                    - participation
                    - funnel
                    - alignment
                    additionalProperties: false
                    properties:
                      participation:
                        type: string
                      funnel:
                        type: string
                      alignment:
                        type: string
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
  "/admin/goal_imports":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.goal_imports.create
      summary: Import member goals (cutover).
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/groups":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.groups.index
      summary: Paginated, filterable list of the organization's static/dynamic member
        groups.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::GroupsController`\nis real and complete (dev-5, admin
        root family batch, unblocked by the scope-guard mini-unit —\nregister #280).
        `GroupNew`/`AdminView`/`MemberGroupRefreshJob`/`MemberGroup::Evaluator`/the
        AASM\ntransitions all already on dest (dev-4 pre-flight). ROUTED on dest —
        all 14 operations\nresolve against the live route set. Route per source (`config/routes.rb:654-666`,
        nested\nunder `namespace :admin`): `resources :groups, only: [:index, :show,
        :create, :update, :destroy]\n{ member { post :archive; post :activate; post
        :pause; post :snapshot; post :refresh; get\n:members; post :members, action:
        :add_members; delete 'members/:member_id', action:\n:remove_member }; collection
        { post :preview_members } }`.\n⚠️ These keys carry the `admin/` segment because
        the whole family routes under\n`/api/v3/admin/…` — verify a key against the
        live route set once drawn, never the controller's\ndirectory.\n\nQuery params
        — `group_type` (`static`/`dynamic`), `status`, `search` (name/description
        LIKE),\n`page`, `per_page` (max 100, default 25).\n\nTwo things a future reader
        should know before touching this row:\n- \U0001F534 INHERITED, NOT PORT-INTRODUCED:
        `member_groups.status` defaults to `\"active\"` at the\n  column level while
        `GroupNew`'s AASM declares `:draft` as its initial state — every group is\n
        \ born active, and `:draft` is unreachable via the normal create path. A develop-side
        model bug,\n  not fixed here; see the controller's own header comment.\n-
        NOT VERBATIM: source calls `MemberGroupRefreshJob.perform_later`; dest's job
        (already ported\n  separately) is a plain `Sidekiq::Job`, which deliberately
        doesn't implement `perform_later` —\n  `activate`/`refresh` below use `.perform_async`
        instead. `archive` IS verbatim\n  (`update!(status: \"archived\")`, not the
        AASM event) — see the controller's own comment for why\n  routing it through
        `archive!` was tried and reverted (`whiny_transitions: false`).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.groups.create
      summary: Create a static or dynamic group.
      description: |
        Contract deferred — see the index operation's description. Body — `{ name, description?,
        group_type? ("static"/"dynamic", default "static"), filter_params? (required for "dynamic") }`.
        A dynamic group also creates an `AdminView` to hold `filter_params`; created born "active" (see
        the index operation's note on the inherited draft/active default bug).
      responses:
        '201':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/preview_members":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.groups.preview_members
      summary: Dry-run a filter_params rule set and return the paginated members it
        would resolve, without creating a group.
      description: |
        Contract deferred — see the index operation's description. Body — `{ filter_params }`.
        ⚠️ OPEN FINDING, not decided here: this is the ONE action with no `require_scope!` call in
        source at all — absent from both the `read:groups` and `write:groups` before_action lists.
        Ported verbatim; see the controller's own header comment.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.groups.show
      summary: Get a single group's detail (includes decoded filterParams for dynamic
        groups).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.groups.replace
      summary: Update a group's name/description, or a dynamic group's filter_params
        (422 if archived).
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.groups.update
      summary: Update a group's name/description, or a dynamic group's filter_params
        (422 if archived).
      description: Contract deferred — see the index operation's description. Body
        — `{ name?, description?, filter_params? }` (dynamic only).
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.groups.destroy
      summary: Delete a group (and its AdminView, for dynamic groups).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}/activate":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.groups.activate
      summary: Activate a paused (nominally draft-or-paused) group; queues a refresh
        if dynamic.
      description: |
        Contract deferred — see the index operation's description. In practice only fires from
        `:paused` — see the index operation's note on the inherited draft/active default bug. NOT
        VERBATIM: enqueues via `.perform_async`, not source's `.perform_later` — see the index
        operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}/archive":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.groups.archive
      summary: Archive a group (422 if already archived).
      description: |
        Contract deferred — see the index operation's description. Verbatim raw status write, not the
        AASM `archive!` event — see the controller's own comment on why (`whiny_transitions: false`
        makes the event's failure mode worse than the write it would replace).
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}/members":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.groups.members
      summary: Paginated, sorted list of a group's resolved members (static via memberships,
        dynamic via AdminView).
      description: |
        Contract deferred — see the index operation's description. Query params — `sort_field` (default
        `first_name`), `sort_order` (default `asc`), `page`, `per_page`.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    post:
      tags:
      - admin
      operationId: admin.groups.add_members
      summary: Add members to a static group by id (422 for a dynamic group).
      description: |
        Contract deferred — see the index operation's description. Body — `{ member_ids: [...] }`.
        ⚠️ OPEN FINDING, not decided here: `member_ids` is client-supplied and uncapped — one request
        does 2 synchronous in-request queries per id (a lookup + an insert), with no size limit. Ported
        verbatim; see the controller's own header comment.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}/members/{member_id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - name: member_id
      in: path
      required: true
      description: The `Member` id to remove from the group's memberships.
      schema:
        type: integer
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.groups.remove_member
      summary: Remove one member from a static group (422 for a dynamic group, 404
        if not a member).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}/pause":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.groups.pause
      summary: Pause an active group.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}/refresh":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.groups.refresh
      summary: Admin on-demand re-evaluation of a dynamic group (bypasses the nightly
        cron); 422 for a static group.
      description: |
        Contract deferred — see the index operation's description. NOT VERBATIM: enqueues via
        `.perform_async`, not source's `.perform_later` — see the index operation's description.
      responses:
        '202':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/groups/{id}/snapshot":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.groups.snapshot
      summary: Freeze a dynamic group's membership at this point in time.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/insights":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.insights.index
      summary: The organization's Insights, ordered.
      description: |
        Same org scoping and the same dropped `GOODDATA_INSIGHTS` gate as dashboards.

        ⚠️ ASYMMETRY WITH DASHBOARDS: `Insights::Insight` unconditionally validates
        `gooddata_ref` presence, so every insight row is vendor-BOUND at the data level even though this
        controller no longer calls the vendor. Insights cannot be authored independently of GoodData the
        way dashboards can.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - insights
                additionalProperties: false
                properties:
                  insights:
                    type: array
                    items:
                      "$ref": "#/components/schemas/InsightsInsight"
    post:
      tags:
      - admin
      operationId: admin.insights.create
      summary: Creates an insight in the caller's organization.
      description: |
        ⚠️ `gooddata_ref` is REQUIRED by the model, so a create without one 422s. That is source's own
        constraint, not something this port added.

        ⚠️ Session callers must send `X-Requested-With`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - gooddata_ref
              properties:
                name:
                  type: string
                description:
                  type: string
                gooddata_ref:
                  type: string
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - insight
                additionalProperties: false
                properties:
                  insight:
                    "$ref": "#/components/schemas/InsightsInsight"
        '422':
          description: Model validation failed (including a missing `gooddata_ref`),
            or the unique index was hit.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
  "/admin/insights/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.insights.show
      summary: One insight by id.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - insight
                additionalProperties: false
                properties:
                  insight:
                    "$ref": "#/components/schemas/InsightsInsight"
        '404':
          description: Not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
    put:
      tags:
      - admin
      operationId: admin.insights.replace
      summary: Updates an insight.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin carries the same contract so
        the `PUT` route is not fenced out of the spec — see the `PATCH` sibling above.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                gooddata_ref:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - insight
                additionalProperties: false
                properties:
                  insight:
                    "$ref": "#/components/schemas/InsightsInsight"
        '404':
          description: Not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
    patch:
      tags:
      - admin
      operationId: admin.insights.update
      summary: Updates an insight.
      description: "⚠️ Session callers must send `X-Requested-With`."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                gooddata_ref:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - insight
                additionalProperties: false
                properties:
                  insight:
                    "$ref": "#/components/schemas/InsightsInsight"
        '404':
          description: Not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
    delete:
      tags:
      - admin
      operationId: admin.insights.destroy
      summary: Deletes an insight.
      description: "⚠️ Session callers must send `X-Requested-With`."
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '404':
          description: Not found in this organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
  "/admin/insights/{id}/embed_token":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.insights.embed_token
      summary: Mints a short-lived GoodData Analytical Designer token for an insight.
      description: |
        Always `entity: insight, mode: editor` — insights have no player in v1; clicking one opens
        GoodData's Analytical Designer directly. Takes no `mode` parameter, unlike the dashboards sibling.

        No kind guard is needed here: `Insights::Insight` validates `gooddata_ref` presence
        unconditionally, so every row is vendor-bound by construction.

        ⚠️ This action does NOT rescue the generic `Gooddata::Error`, so a vendor argument rejection
        surfaces as a 500 rather than the 422 its dashboards sibling renders. That asymmetry is source
        behavior, documented here rather than silently normalized.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - embed
                additionalProperties: false
                properties:
                  embed:
                    "$ref": "#/components/schemas/GoodDataEmbedToken"
        '404':
          description: |
            No such insight in this organization, or GoodData no longer holds the referenced object
            (`Gooddata::NotFoundError`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
        '503':
          description: |
            BI service not configured (`ConfigError`) or unreachable (`AuthError`, `ConnectionError`,
            `RateLimitedError`, `ServerError`, `ProtocolError`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InsightsShellError"
  "/admin/integrations":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.integrations.index
      summary: List available integrations for the organization.
      description: |
        `Admin::IntegrationsController#index` is real, complete, and landed. It was the chosen
        DSL-inertness proof for register row #159, and that proof held: deleting all 120 source-verbatim
        `openapi do...end` blocks left `v3.yaml` byte-identical, because the spec pipeline reads
        `*.meta.yaml` and never read `OpenapiDsl::Registry`. The blocks and the registry are now gone;
        the schema below is this row's only contract.

        Consumed by the SPA's connectors list (`packages/web/src/queries/admin/integrations/`);
        `available` is currently a hardcoded `true` for every listed code (source parity, not a bug
        introduced by this port) — the client-visible signal is `connected`.
      responses:
        '200':
          description: Available integrations with sync status.
          content:
            application/json:
              schema:
                type: object
                required:
                - integrations
                additionalProperties: false
                properties:
                  integrations:
                    type: array
                    description: Available integrations with sync status.
                    items:
                      type: object
                      required:
                      - code
                      - name
                      - description
                      - category
                      - available
                      - connected
                      additionalProperties: false
                      properties:
                        code:
                          type: string
                          description: Integration code identifier.
                        name:
                          type: string
                          description: Display name.
                        description:
                          type: string
                          description: Short description.
                        category:
                          type: string
                          enum:
                          - video_conferencing
                          - calendar
                          - communication
                          - api
                          - other
                          description: other covers any code absent from INTEGRATION_META's
                            static map.
                        available:
                          type: boolean
                          description: Whether the integration is available for this
                            organization.
                        connected:
                          type: boolean
                          description: Whether the integration is currently connected/synced.
  "/admin/longitudinal_growth":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.longitudinal_growth.show
      summary: Longitudinal growth — metric series plus qualitative shift for two
        named members.
      description: |
        Returns a metric series (PerformanceSignals::Signal points, with Pattern-derived
        inflection marks) and an optional qualitative before/after from skill evaluations.
        Per-section `state` is `ready` | `feature_disabled` | `no_data`. Never exposes
        SMART scores, ratings, rubrics or criterion fields (A5). Foreign ids return
        invalid_request 400 rather than a distinguishable 404.
      responses:
        '200':
          description: Longitudinal growth payload with per-section state.
          content:
            application/json:
              schema:
                type: object
                required:
                - organization_id
                - computed_at
                - metric_series
                - qualitative_shift
                additionalProperties: false
                properties:
                  organization_id:
                    type: integer
                  computed_at:
                    type: string
                    format: date-time
                  metric_series:
                    type: object
                    required:
                    - state
                    additionalProperties: true
                    properties:
                      state:
                        type: string
                        enum:
                        - ready
                        - feature_disabled
                        - no_data
                      points:
                        type: array
                        items:
                          "$ref": "#/components/schemas/LongitudinalGrowthPoint"
                  qualitative_shift:
                    type: object
                    required:
                    - state
                    additionalProperties: true
                    properties:
                      state:
                        type: string
                        enum:
                        - ready
                        - feature_disabled
                        - no_data
                      before:
                        "$ref": "#/components/schemas/LongitudinalGrowthObservation"
                      after:
                        "$ref": "#/components/schemas/LongitudinalGrowthObservation"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: series_member_id
        in: query
        required: true
        schema:
          type: integer
      - name: performance_metric_id
        in: query
        required: true
        schema:
          type: integer
      - name: narrative_member_id
        in: query
        required: false
        schema:
          type: integer
      - name: taxonomy_skill_id
        in: query
        required: false
        schema:
          type: integer
      - name: period_start
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: period_end
        in: query
        required: false
        schema:
          type: string
          format: date
  "/admin/mcp/tools":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.mcp.tools.index
      summary: List MCP tools for admin authoring.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/members/{id}/org_unit":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.members.org_unit.update
      summary: Move a member to an org unit.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/members/{id}/profile":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.members.profile.show
      summary: Admin composed member profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/mentoring_adoption":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.mentoring_adoption.show
      summary: Go/no-go coaching adoption metrics for the current organization.
      description: |
        Returns activation, weekly-active, and satisfaction rates against org-configurable
        thresholds (defaults 60/35/95). Aggregates route through Privacy::MinimumCohort.
        Satisfaction uses ChronusAI::Feedback on AGENT Interactions (TMO-T10) — quality
        review only, not model training. Optional `program_id` scopes the population;
        `refresh=true` recomputes today's snapshot.
      responses:
        '200':
          description: Go/no-go payload for the named authority.
          content:
            application/json:
              schema:
                type: object
                required:
                - organization_id
                - metrics
                - overall
                - thresholds
                - definitions
                - population
                additionalProperties: true
                properties:
                  organization_id:
                    type: integer
                  overall:
                    type: string
                    enum:
                    - go
                    - no_go
                    - suppressed
                  metrics:
                    type: object
                    required:
                    - activation
                    - weekly_active
                    - satisfaction
                    additionalProperties: false
                    properties:
                      activation:
                        "$ref": "#/components/schemas/MentoringAdoptionMetric"
                      weekly_active:
                        "$ref": "#/components/schemas/MentoringAdoptionMetric"
                      satisfaction:
                        "$ref": "#/components/schemas/MentoringAdoptionMetric"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: program_id
        in: query
        required: false
        schema:
          type: integer
      - name: refresh
        in: query
        required: false
        schema:
          type: boolean
  "/admin/oauth_applications":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.oauth_applications.index
      summary: List OAuth applications.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.oauth_applications.create
      summary: Create an OAuth application.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/oauth_applications/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.oauth_applications.show
      summary: Show an OAuth application.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.oauth_applications.replace
      summary: Replace an OAuth application.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.oauth_applications.update
      summary: Update an OAuth application.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.oauth_applications.destroy
      summary: Delete an OAuth application.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/oauth_applications/{id}/regenerate_secret":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.oauth_applications.regenerate_secret
      summary: Regenerate an OAuth application secret.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/org_taxonomies":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.org_taxonomies.index
      summary: List an organization's taxonomy reference documents (job role catalog,
        level definitions, etc.) used by Rumi context injection.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nOrgTaxonomiesController` is real and complete (dev-5,
        admin root family batch) — `OrgTaxonomy`\nmodel (`for_org` scope, `category_label`)
        already on dest. Route per source\n(`config/routes.rb:759-760`): `resources
        :org_taxonomies, only: [:index, :create, :destroy],\ncontroller: 'org_taxonomies'`
        — ROUTED on dest.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.org_taxonomies.create
      summary: Create an org taxonomy entry.
      description: |
        Contract deferred — see the index operation's description.

        Body — `{ category, title, body_text? }`. NOT VERBATIM (register #193, directed): source's two
        bare `params.require(:category)`/`params.require(:title)` are each guarded with a presence
        check first, rendering this tier's 422 envelope with an i18n message instead of the
        framework's bare 400 for an unrescued `ParameterMissing` — envelope/i18n consistency, not
        crash-prevention.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/org_taxonomies/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.org_taxonomies.destroy
      summary: Delete an org taxonomy entry.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/org_units/{id}/assign_members":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.org_units.assign_members
      summary: Bulk-assign members to an org unit.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nOrgUnitsController` is authored verbatim (dev-5,
        admin root family batch) but is NON-FUNCTIONAL\non dest as written — TWO confirmed
        schema/association gaps, not decided here:\n\n1. `members.org_unit_id` — the
        pre-flagged #87/#88 gap. Confirmed live: this column does not\n   exist on
        dest's `members` table.\n2. `Organization#org_units` — confirmed live: this
        association does not exist on dest. Same\n   gap already reported against
        `config_audit_logs_controller.rb`, now confirmed on a second\n   controller.
        `OrgUnit` itself is fully present (members/children/ancestry/depth/status/\n
        \  position all respond) — only the Organization-side reverse association
        is missing.\n\nRoute per source (`config/routes.rb:677-681`): `resources :org_units,
        only: [] { member { post\n:assign_members; post \"settings/preview\" } }`
        — ROUTED on dest. `not_implemented` stays\nbecause the action cannot succeed
        until `members.org_unit_id` exists; G6 still requires the\npath in the document
        now that the route is drawn.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/org_units/{id}/settings/preview":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.org_units.settings.preview
      summary: Preview an org-unit settings change.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/org_units/{org_unit_id}/features":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.org_unit_features.index
      summary: List organization-level features and their tier/inheritance state for
        an org unit.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nOrgUnitFeaturesController` is real and complete (dev-5,
        admin root family batch), verbatim.\nUnblocked by two prior gaps clearing:
        `Organization#org_units` (dev-4) and\n`FeatureAccessManager` (dev-5, ported
        alone). Route per source: `resources :org_units, only:\n[] { resources :features,
        only: [:index, :update], controller: 'org_unit_features' } ` — WIRED\non dest,
        with source's `param: :feature_name` preserved (the member param is the feature's
        name,\nnot an id, so a default `:id` param would rename it and break every
        real request).\n\nLists every ORGANIZATION-level feature (PROGRAM-level ones
        are excluded), each with its\n`tier` (`admin`/`entitled`/`internal`/`deprecated`,
        via `FeatureAccessManager`), whether it's\nenabled (own override, falling
        back to the org-level `enabled_db_features`), and whether\nit's inherited
        vs overridden at this org unit.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/org_units/{org_unit_id}/features/{feature_name}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.org_unit_features.replace
      summary: Toggle a feature override for an org unit.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '403':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.org_unit_features.update
      summary: Toggle a feature override for an org unit.
      description: |
        Contract deferred — see the index operation's description. Body — `{ enabled }`. Gated by
        `FeatureAccessManager.can_toggle?`; a caller without tier access gets 403 with the manager's
        own `reason`/`tier`, not a generic forbidden.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '403':
          description: Deferred — see the index operation's description.
  "/admin/org_units/{org_unit_id}/settings":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.org_unit_settings.show
      summary: Show every non-features config namespace for an org unit, with inheritance/default
        info.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nOrgUnitSettingsController` is real (dev-5, admin
        root family batch). Unblocked by\n`Organization#org_units` (dev-4). Route
        per source: `resources :org_units, only: []\n{ resource :settings, only: [:show,
        :update], controller: 'org_unit_settings' { collection\n{ delete ':namespace/:key',
        to: 'org_unit_settings#clear'; get :trace } } } ` — WIRED on dest,\nminus
        `preview` (below). The `only: []` wrapper on `org_units` is load-bearing:
        it draws no\nroute of its own but is what nests these under `org_units/:org_unit_id`.\n\n\U0001F534
        ONE ACTION DELIBERATELY EXCLUDED FROM THIS CONTRACT: `preview`\n(`POST /api/v3/admin/org_units/{org_unit_id}/settings/preview`)
        is NOT declared here and MUST NOT be\nrouted. Three things, per the `data_imports#create`
        pattern: (1) the action exists in source\nand in this controller's code: it
        counts affected descendant org units/members for a\nprospective config change;
        (2) it cannot work today — `Member.where(org_unit_id:\naffected_ou_ids)` depends
        on `members.org_unit_id`, confirmed absent (register #87/#88, same\ngap blocking
        `org_units#assign_members`); (3) it is therefore deliberately unrouted. ⛔
        Do NOT\n\"complete the contract\" by adding a `preview` entry here later without
        first confirming\n`members.org_unit_id` exists — declaring it would name a
        path no route serves (G6 would flag\nthe mismatch, correctly).\n\n`show` resolves
        every `ConfigOverride::SchemaRegistry` namespace except `features` (which
        has\nits own `org_unit_features` row), each key showing its resolved value,
        inheritance source,\nand system default.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.org_unit_settings.replace
      summary: Write a config override for an org unit.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the show operation's description.
        '422':
          description: Deferred — see the show operation's description.
    patch:
      tags:
      - admin
      operationId: admin.org_unit_settings.update
      summary: Write a config override for an org unit.
      description: |
        Contract deferred — see the show operation's description. Body — `{ namespace, key, value }`;
        422 for an unknown namespace or an unknown key within a known namespace.
      responses:
        '200':
          description: Deferred — see the show operation's description.
        '422':
          description: Deferred — see the show operation's description.
  "/admin/org_units/{org_unit_id}/settings/trace":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.org_unit_settings.trace
      summary: Trace a single config key's resolution up the org unit's ancestor chain.
      description: |
        Contract deferred — see the show operation's description. Query params — `namespace`, `key`.
        ⚠️ NOT camelCase, unlike every other response in this batch: `ConfigResolution.ancestry_trace`
        builds this hash with snake_case keys (`entity_type`, `entity_id`, `entity_name`, `value`,
        `is_source`) and the controller renders it verbatim — kept as-is (verbatim port), not a case
        to "fix" into camelCase here.
      responses:
        '200':
          description: Deferred — see the show operation's description.
  "/admin/org_units/{org_unit_id}/settings/{namespace}/{key}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.org_unit_settings.clear
      summary: Clear a config override for an org unit, re-inheriting the ancestor/system
        default.
      description: Contract deferred — see the show operation's description.
      responses:
        '200':
          description: Deferred — see the show operation's description.
  "/admin/personas":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.personas.index
      summary: List personas.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.personas.create
      summary: Create a persona.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/personas/ai_generate":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.personas.ai_generate
      summary: Generate a persona with AI.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/personas/bulk_destroy":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.personas.bulk_destroy
      summary: Bulk-destroy personas.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/personas/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.personas.show
      summary: Show a persona.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.personas.replace
      summary: Replace a persona.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.personas.update
      summary: Update a persona.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.personas.destroy
      summary: Delete a persona.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/privileged_access":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.privileged_access.index
      summary: List privileged-access holders.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/privileged_access/reviews":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.privileged_access.reviews.create
      summary: Record a privileged-access review.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/profile_fields":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.profile_fields.index
      summary: List an organization's profile question fields, grouped by section.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nProfileFieldsController` is real and complete (register
        #244, pre-flighted by the operator,\nall findings re-verified live before
        authoring).\n\nNOT VERBATIM (ruled): source's `serialize_question` emitted
        a top-level `required:\nquestion.required?` — `ProfileQuestion` has no such
        method or column; requiredness here is\nrole-scoped (`RoleQuestion#required?`,
        already emitted correctly under `roles: [{roleId,\nrequired}]`). Dropped the
        top-level key rather than invent a value — the action had never once\nreturned
        a response, so nothing could depend on it.\n\nCORRECTION to a second pre-flighted
        claim: source's `question_text`/`QuestionChoice#text` are\nreal Globalize-translated
        attributes, but neither needs an explicit `:translations` preload —\n`ProfileQuestion`
        carries its own `default_scope { includes(:translations) }`\n(`profile_question.rb:98`),
        and `question_choices` carries the identical pattern at the\nassociation level
        (`has_question_choices.rb:14`). Confirmed by raw SQL query count on real\nfixture
        data (35 questions, one with 147 choices): constant query count regardless.
        No N+1\nexisted here to fix.\n\n`import` (`POST /api/v3/admin/profile_fields/import`,
        source's routes.rb:699) is deliberately\nNOT routed — see the controller's
        own header comment: it delegates to nothing and silently\ndiscards the uploaded
        file while reporting success, strictly worse than a visible crash.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/profile_fields/export":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.profile_fields.export
      summary: Export an organization's profile question fields as CSV.
      description: |
        Contract deferred — see the index operation's description. Same controller, same findings;
        `generate_csv`'s "Required" column is likewise dropped for the same reason as the JSON
        serializer's top-level `required` key.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/profile_fields/import":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.profile_fields.import
      summary: Import profile fields.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/program_authoring_templates":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.program_authoring_templates.index
      summary: Gallery listing of program-authoring templates (globals + this organization's
        own) for the AI-authoring "fork a template" flow.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nProgramAuthoringTemplatesController` is real and
        complete (dev-5, admin root family batch) —\n`Programs::Authoring::Template#visible_to_org`
        already on dest. Route per source\n(`config/routes.rb:1003`): `resources :program_authoring_templates,
        only: [:index]` — NOT YET\nWIRED on dest. Gated on `FeatureName::PROGRAM_AI_AUTHORING`.
        `spec_json` is deliberately\nomitted from this list shape (10+ KB per row)
        — a per-template show endpoint is a future row,\nnot this one.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.index
      summary: List an organization's programs, paginated, with status filter and
        name search.
      description: |
        `Api::V3::Admin::ProgramsController` is real and complete — bespoke admin CRUD (not the
        Interface stack). Routed now (`config/routes/v3.rb`'s `resources :programs, only: %i[index
        show create]` inside `namespace :admin`), so the contract is armed here.

        Two defects fixed during the port, both confirmed live (RAILS_ENV=development, direct
        `rails runner` query-count/SQL checks — not assumed from source):

        1. N+1 — `program_summary_json`'s `read_attribute(:name)` is a working Globalize idiom on dest,
           but it lazily fires one `program_translations` query per record whose translation isn't
           preloaded. Fixed by adding `:translations` to `index`'s `.includes`.
        2. `apply_search`'s raw `"programs.name LIKE :q"` targets a column that does not exist on
           `programs` on EITHER tree (`name`/`description` are Globalize-only, backed by
           `program_translations`). This is a genuine pre-existing crash in source, latent only because
           source ships no test file for this controller at all. Fixed with
           `.joins(:translations).where("program_translations.name LIKE ?", ...)`.

           ⚠️ CONTRACT NOTE — the fix chooses semantics that never existed. `?search=` carries no locale
           predicate, so it matches ANY locale's name: a program found by its French name is returned
           and rendered with its English one. Consumers should not assume the matched text appears in
           the response.

        NOT a defect: the unqualified `.order(:name)` does NOT crash — Globalize rewrites a symbol-form
        `.order`/`.where` for a translated attribute into a join automatically; only a raw SQL string
        bypasses that rewrite, which is defect 2 above.

        `openapi :action do` blocks on the controller are kept verbatim per register #159 — they are
        inert on dest; this file is the real contract.
      responses:
        '200':
          description: Paginated programs list.
          content:
            application/json:
              schema:
                type: object
                required:
                - programs
                - meta
                additionalProperties: false
                properties:
                  programs:
                    type: array
                    description: Programs array.
                    items:
                      type: object
                      required:
                      - id
                      - name
                      - status
                      - engagementType
                      - userCount
                      - groupCount
                      - createdAt
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: Program ID.
                        name:
                          type: string
                          description: Program name.
                        status:
                          type: string
                          enum:
                          - active
                          - inactive
                          - archived
                          - drafted
                          - unknown
                          description: Status; `unknown` when the AR state doesn't
                            map (defensive fallback in the controller).
                        engagementType:
                          type: string
                          enum:
                          - career_based
                          - project_based
                          - career_based_with_ongoing
                          - unknown
                          description: Engagement type; `unknown` when the AR value
                            doesn't map (defensive fallback in the controller).
                        userCount:
                          type: integer
                          description: Number of users.
                        groupCount:
                          type: integer
                          description: Number of groups/connections.
                        createdAt:
                          type:
                          - string
                          - 'null'
                          format: date-time
                          description: ISO 8601 created date.
                  meta:
                    type: object
                    required:
                    - total
                    - page
                    - perPage
                    - totalPages
                    additionalProperties: false
                    description: Pagination metadata.
                    properties:
                      total:
                        type: integer
                        description: Total matching programs.
                      page:
                        type: integer
                        description: Current page.
                      perPage:
                        type: integer
                        description: Items per page.
                      totalPages:
                        type: integer
                        description: Total pages.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: Page number (default 1).
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: Items per page (default 10, max 100).
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Free-text match against the program's name, any locale (see the
          cross-locale note above).
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by status (active / inactive / archived / drafted, case-insensitive).
          An unrecognized value is silently ignored — the list is returned unfiltered.
    post:
      tags:
      - admin
      operationId: admin.programs.create
      summary: Create a program from a minimal spec ("Create from Scratch"), via the
        authoring Compiler.
      description: |
        Gated on `FeatureName::PROGRAM_AI_AUTHORING` (carries `AI_MASTER_CONSTRAINT` —
        `organization.chronus_ai_active?`). Builds a minimum-viable `ProgramSpec` from 3 fields
        (name/engagement_type/description) + schema defaults and commits it through
        `Programs::Authoring::Compiler` — the same path the AI authoring canvas uses.

        Request and response bodies are snake_case on the wire (verified against the SPA's own
        `createProgram`/`CreateProgramDialog` tests, which assert a `deepSnake` request and a
        `deepCamel`'d response) — this endpoint does NOT follow the camelCase convention `index`/`show`
        use, because the controller builds this JSON by hand rather than through `program_summary_json`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - engagement_type
              - description
              additionalProperties: false
              properties:
                name:
                  type: string
                  description: Program name.
                engagement_type:
                  type: string
                  description: One of the ProgramSpec engagement types (one_to_one
                    / peer_circle / group / open_community) — NOT the legacy Program
                    engagement-type enum.
                description:
                  type: string
                  description: Program description.
      responses:
        '201':
          description: Program created. `root` is the program's URL slug the web SPA
            routes detail by (/manage/p/<root>); `program_url` is the legacy admin
            URL.
          content:
            application/json:
              schema:
                type: object
                required:
                - program_id
                - program_url
                - root
                - draft_id
                - source
                - warnings
                - duration_ms
                additionalProperties: false
                properties:
                  program_id:
                    type: integer
                  program_url:
                    type: string
                  root:
                    type: string
                  draft_id:
                    type: integer
                  source:
                    type: string
                    const: scratch
                  warnings:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                    description: Compiler warnings, shape defined by `Programs::Authoring::Compiler`'s
                      result.
                  duration_ms:
                    type: number
        '400':
          description: Missing a required field, or an unrecognized `engagement_type`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramCreateError"
        '403':
          description: Insufficient scope (`write:programs`), or `FeatureName::PROGRAM_AI_AUTHORING`
            is not enabled for the organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramCreateError"
        '422':
          description: The built spec failed schema validation (a defaults bug), the
            Compiler rejected it, or the created `Program` failed AR validation.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramCreateError"
  "/admin/programs/authoring/drafts":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.authoring.drafts.index
      summary: The current member's active AI-authored program drafts, most-recently-updated
        first.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::Authoring::DraftsController`
        is real, complete, and landed. This stub is the\nminimum needed to satisfy
        G6 without authoring the full contract, which is deferred to the same\nmigration
        unit as row #159's openapi DSL blocks. Drop `not_implemented` and add the
        real schema\nwhen that unit is taken, not before.\n\nPost `program-via-rumi`
        cleanup this surface is read + commit + destroy only — draft creation,\nprompt
        streaming, template fork, and the clarify round-trip all moved to the Rumi
        chat surface.\n`?limit=` clamps to 1-100 (default 50). Scoped to drafts owned
        by the current member, in an\nactive status (drafting/committing/compile_failed).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/authoring/drafts/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.authoring.drafts.show
      summary: One draft, with its full prompt, spec_json, and last_compile_error.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.authoring.drafts.destroy
      summary: Archive a draft (refused if already committed).
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/programs/authoring/drafts/{id}/commit":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.authoring.drafts.commit
      summary: Compile a drafting/compile_failed draft into a real Program.
      description: |
        Contract deferred — see the index operation's description.

        Delegates to `Programs::Authoring::Compiler#commit!`. On success returns
        `{ program_id, warnings, duration_ms, draft }`; on a `CompilerError` (409/422 family, per the
        error's own `http_status`) returns `{ error: { code, message, detail } }` and marks the draft
        `compile_failed` rather than failing the request with a generic 500. A draft already committed
        or not in `drafting`/`compile_failed` status is rejected with 409.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/counts":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.counts
      summary: Get aggregate program counts by status.
      description: Pure `.count` aggregates, no N+1 surface. Same controller, same
        gate (`require_scope! ('read:programs')`) as index/show.
      responses:
        '200':
          description: Aggregate counts.
          content:
            application/json:
              schema:
                type: object
                required:
                - total
                - active
                - drafted
                - archived
                - inactive
                additionalProperties: false
                properties:
                  total:
                    type: integer
                    description: Total programs.
                  active:
                    type: integer
                    description: Active programs.
                  drafted:
                    type: integer
                    description: Drafted programs.
                  archived:
                    type: integer
                    description: Archived programs.
                  inactive:
                    type: integer
                    description: Inactive programs.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/admin/programs/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.show
      summary: Get a single program's details.
      description: Same controller, same findings as the index operation above.
      responses:
        '200':
          description: Program details.
          content:
            application/json:
              schema:
                type: object
                required:
                - program
                additionalProperties: false
                properties:
                  program:
                    type: object
                    description: Program object.
                    required:
                    - id
                    - name
                    - description
                    - status
                    - engagementType
                    - userCount
                    - groupCount
                    - createdAt
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: Program ID.
                      name:
                        type: string
                        description: Program name.
                      description:
                        type:
                        - string
                        - 'null'
                        description: Program description.
                      status:
                        type: string
                        enum:
                        - active
                        - inactive
                        - archived
                        - drafted
                        - unknown
                      engagementType:
                        type: string
                        enum:
                        - career_based
                        - project_based
                        - career_based_with_ongoing
                        - unknown
                      userCount:
                        type: integer
                        description: Number of users.
                      groupCount:
                        type: integer
                        description: Number of groups/connections.
                      createdAt:
                        type:
                        - string
                        - 'null'
                        format: date-time
                        description: ISO 8601 created date.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: Program not found (or belongs to another organization).
          content:
            application/json:
              schema:
                type: object
                required:
                - error
                additionalProperties: false
                properties:
                  error:
                    type: string
                    description: Error message.
  "/admin/programs/{program_id}/articles":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.articles.index
      summary: List articles published to a program, filterable by status/type, sortable.
      description: |
        `article.author&.name(name_only: true)` is SAFE — `Article#author` is `class_name: 'Member'`,
        not the deprecated `User#name`. Tests create real articles end to end, not vacuous.

        ⚠️ Response keys are camelCase, NOT this spec's usual snake_case (verbatim-ported controller
        quirk, same as sibling rows in this tier). Several fields stay nullable even though `author`
        and `article_content` are presence-validated on the model — the controller reaches them via
        safe-navigation (`article.author&.…`, `content&.…`), so the contract follows that same
        defensive posture rather than the validation's promise.
      responses:
        '200':
          description: Paginated article list.
          content:
            application/json:
              schema:
                type: object
                required:
                - articles
                - meta
                additionalProperties: false
                properties:
                  articles:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ArticleSummary"
                  meta:
                    "$ref": "#/components/schemas/ArticlesPagination"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 25, max: 100).'
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - draft
          - published
        description: Filter by status.
      - name: type
        in: query
        required: false
        schema:
          type: string
          enum:
          - text
          - media
          - list
          - upload
        description: Filter by type.
      - name: sort
        in: query
        required: false
        schema:
          type: string
          enum:
          - recent
          - oldest
          - most_viewed
          - most_helpful
        description: 'Sort order (default: recent).'
  "/admin/programs/{program_id}/articles/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.articles.show
      summary: Get article detail (body, embed code, attachment metadata).
      responses:
        '200':
          description: Article details.
          content:
            application/json:
              schema:
                type: object
                required:
                - article
                additionalProperties: false
                properties:
                  article:
                    "$ref": "#/components/schemas/ArticleDetail"
        '404':
          description: No article published to this program with that id.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    delete:
      tags:
      - admin
      operationId: admin.programs.articles.destroy
      summary: Remove the program's publication of an article (destroys the article
        itself if no publications remain).
      responses:
        '200':
          description: Article's publication (and the article itself, if that was
            the last one) removed.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '404':
          description: No article published to this program with that id.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/programs/{program_id}/bulk_match":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.bulkMatch.show
      summary: Get bulk match configuration and state (lazily creates the default
        config on first access).
      description: |
        `Api::V3::Admin::Programs::BulkMatchController` (dev-5, `programs/*` family).

        ⚠️ `show`/`admin_views`/`update` are tested against real fixtures. ⛔ `compute` and
        `update_pair` — the two most complex, highest-risk actions (real matching computation,
        group creation/publish/discard, and the only two places `current_member.user_in_program`
        and `serialize_computation_results`'s `user.name` calls live) — have ZERO test coverage.
        A green result after routing says nothing about either action's actual runtime correctness.
      responses:
        '200':
          description: Bulk match config, program capability flags, and available
            orientation types.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchShowPayload"
    put:
      tags:
      - admin
      operationId: admin.programs.bulkMatch.updatePut
      summary: Update bulk match settings (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — even a SINGULAR `resource ..., only: [..., :update]` draws BOTH PATCH
        and PUT at this path, dispatching to the same action. Documented rather than left implicit —
        same precedent as `rumi_settings`/`playbook_settings`/`training`. Armed identically to PATCH
        (parity-enforced: this route pair must stay armed/unarmed together).
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BulkMatchUpdateBody"
      responses:
        '200':
          description: Updated config.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchUpdatePayload"
        '422':
          description: The updated attributes failed a model validation (e.g. duplicate
            orientation for the program).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchErrorResponse"
    patch:
      tags:
      - admin
      operationId: admin.programs.bulkMatch.update
      summary: Update bulk match settings (orientation, slots, sort, visibility flags).
      description: "\U0001F534 Found running the real suite, not on the measured list:
        `bulk_matches` validates\n`program_id` unique within `[:type, :orientation_type]`
        — a program with more than one\nbulk-match config can have an `orientationType`
        change collide with a sibling row and raise\n`ActiveRecord::RecordInvalid`,
        rescued here to a 422.\n"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BulkMatchUpdateBody"
      responses:
        '200':
          description: Updated config.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchUpdatePayload"
        '422':
          description: The updated attributes failed a model validation (e.g. duplicate
            orientation for the program).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchErrorResponse"
  "/admin/programs/{program_id}/bulk_match/admin_views":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.bulkMatch.adminViews
      summary: List admin views partitioned by mentor/mentee/connection role for entity
        selection.
      responses:
        '200':
          description: Admin views by role.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchAdminViewsPayload"
  "/admin/programs/{program_id}/bulk_match/compute":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.bulkMatch.compute
      summary: Configure and trigger bulk match computation.
      description: |
        ⚠️ UNTESTED — see the `show` operation's description for why this is the row's biggest
        coverage gap. `minMenteesPerGroup`/`mentorsPerGroup` are read by the controller
        (`update_bulk_entry`) but were absent from the source `openapi` DSL block — a real
        DSL/controller disagreement, controller followed.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BulkMatchComputeBody"
      responses:
        '200':
          description: Computation results.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchComputationResults"
        '409':
          description: Computation already in progress for this config.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchErrorResponse"
        '422':
          description: 'Computation failed — `error` carries "Computation failed:
            <exception message>".'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchErrorResponse"
  "/admin/programs/{program_id}/bulk_match/update_pair":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.bulkMatch.updatePair
      summary: Draft, publish, or discard a single match pair.
      description: "⚠️ UNTESTED — see the `show` operation's description. `current_member.user_in_program(@program)`\nis
        already the correct accessor (not the bare `@current_member` ivar bug found
        elsewhere in\nthis port), but that correctness is unverified by a real request
        since nothing exercises this\naction.\n\n\U0001F534 The DSL-captured body
        omitted `bulkMatchId`, but the controller reads\n`params[:bulkMatchId]` unconditionally
        (`BulkMatch.find_by!(program_id:, id: params[:bulkMatchId])`)\nbefore dispatching
        on `updateType` — a real DSL/controller disagreement, controller followed;\na
        request missing it 404s via the rescued `RecordNotFound`. The DSL also omitted
        the 404 response\nentirely — `mentor`/`student`/`group` lookups (`@program.mentor_users.find`,\n`.groups.find`/`.groups.drafted.find`)
        can all raise `RecordNotFound`, rescued to 404 here.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BulkMatchUpdatePairBody"
      responses:
        '200':
          description: Pair update result.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchUpdatePairResult"
        '404':
          description: The bulk match config, mentor, student, or group was not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchErrorResponse"
        '422':
          description: Invalid `updateType`, or the record failed a model validation.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BulkMatchErrorResponse"
  "/admin/programs/{program_id}/campaigns":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.campaigns.index
      summary: List a program's campaigns (user/program_invitation/survey), filterable
        by status/type.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::CampaignsController`
        is real and complete, landed as part of the\nwrite-bearing family batch (this
        row has no `:update` in its route — `only: [:index, :show,\n:destroy]` plus
        a `stop` member `POST` — no PUT/PATCH duality concern here). This stub is
        the\nminimum needed to satisfy G6 without authoring the full contract. Drop
        `not_implemented` and\nadd the real schema when that unit is taken, not before.\n\n\U0001F534
        Already-fixed source defect, tested: `stop!` only exists on `CampaignManagement::\nUserCampaign`
        — stopping an active survey/program-invitation campaign previously raised
        a bare\n`NoMethodError`; now guarded to 422. Not implementing `stop!` on the
        other two types was a\ndeliberate scope call (what \"stopping\" means for
        them is a product question). No `User#name`/\n`@current_member` risk found.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/campaigns/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.campaigns.show
      summary: Get campaign detail with its messages.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.campaigns.destroy
      summary: Delete a campaign.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/campaigns/{id}/stop":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.campaigns.stop
      summary: Stop an active campaign (UserCampaign only — other types 422).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/checkin_report":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.checkinReport.index
      summary: Check-in report — paginated table + summary hours, filterable by date/role/type/search.
      description: |
        `Api::V3::Admin::Programs::CheckinReportController` is real and complete.
        `test_index_returns_the_real_report_with_a_nonzero_summary` confirms the real, non-empty shape
        against `albers`' 12 fixture `GroupCheckin` rows (the row's own test previously asserted an
        empty list, which was a false premise — corrected before this contract was written).

        ⚠️ Response keys are camelCase, not the snake_case this spec's convention states — this
        controller renders literal Ruby symbol keys directly. See this row's coordinator report for
        the cross-cutting note shared by all 8 controllers in this batch.
      responses:
        '200':
          description: Paginated check-ins, summary hours, and the roles eligible
            to track hours.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/CheckinReportIndex"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/CheckinReportBareError"
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          type: string
        description: Start date (YYYY-MM-DD).
      - name: end_date
        in: query
        required: false
        schema:
          type: string
        description: End date (YYYY-MM-DD).
      - name: role_id
        in: query
        required: false
        schema:
          type: integer
        description: Filter by role ID.
      - name: checkin_type
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by type: task or meeting.'
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Search member name, group name, or title.
      - name: sort
        in: query
        required: false
        schema:
          type: string
        description: Sort column (e.g. date, member, duration).
      - name: sort_order
        in: query
        required: false
        schema:
          type: string
        description: 'asc or desc (default: desc).'
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 25, max: 100).'
  "/admin/programs/{program_id}/checkin_report/export":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.checkinReport.export
      summary: Export the check-in report as CSV.
      description: |
        Same filters as `index`, minus pagination and sort. `test_export_streams_a_csv_with_the_real_
        rows` confirms the `text/csv; charset=utf-8` content type and header row.

        ⚠️ NOT a documented defect fix, but worth knowing: `export` streams via `find_each`, which
        forces primary-key batch order regardless of `sort`/`sort_order` — those two params have no
        effect on this action (`test_export_ignores_the_requested_sort_and_returns_primary_key_order`
        pins this as the real, current behavior). Not accepted as parameters here since they are
        silently ignored, not because they're rejected.
      responses:
        '200':
          description: CSV file download (`Check_In_Report.csv`).
          content:
            text/csv:
              schema:
                type: string
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/CheckinReportBareError"
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          type: string
        description: Start date (YYYY-MM-DD).
      - name: end_date
        in: query
        required: false
        schema:
          type: string
        description: End date (YYYY-MM-DD).
      - name: role_id
        in: query
        required: false
        schema:
          type: integer
        description: Filter by role ID.
      - name: checkin_type
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by type: task or meeting.'
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Search member name, group name, or title.
  "/admin/programs/{program_id}/cohorts":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.cohorts.index
      summary: A program's cohorts, ordered by start date.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::CohortsController` is real, complete,
        and landed (P2-7..N batch 2b). This\nstub is the minimum needed to satisfy
        G6 without authoring the full contract, which is\ndeferred to the same migration
        unit as row #159's openapi DSL blocks. Drop `not_implemented`\nand add the
        real schema when that unit is taken, not before.\n\nGated behind the org-level
        PROGRAM_COHORTS_V2 feature (404, not 403, when off — via the\nshared `Concerns::FeatureGated`).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.programs.cohorts.create
      summary: Create a :draft Cohort.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ cohort: { name, description?, starts_at?, ends_at?, enrollment_window_starts_at?,
        enrollment_window_ends_at?, capacity?, waitlist_enabled?, grace_period_days?, metadata? } }`.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/cohorts/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.cohorts.show
      summary: One cohort, with its enrollment counts and metadata.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.programs.cohorts.replace
      summary: Update a Cohort (full field set while :draft, a restricted set afterward).
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.cohorts.update
      summary: Update a Cohort (full field set while :draft, a restricted set afterward).
      description: |
        Contract deferred — see the index operation's description.

        Once past :draft, only `capacity`/`waitlist_enabled`/`grace_period_days`/`description` are
        editable — a capacity increase enqueues `WaitlistPromoteJob` to fill the newly-freed seats.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.cohorts.destroy
      summary: Delete a :draft Cohort (refused once opened — cancel instead).
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/cohorts/{id}/cancel":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.cohorts.cancel
      summary: Cancel a non-terminal Cohort.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ reason: string (required) }` — 400 if blank. Withdraws every active/pending
        enrollment and emails affected members.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/cohorts/{id}/close":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.cohorts.close
      summary: Admin early-close a :running Cohort (equivalent to its end date + grace
        elapsing).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/cohorts/{id}/enrollments":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.cohorts.enrollments
      summary: A cohort's enrollments (max 200, most recent first), with state counts.
      description: |
        Contract deferred — see the index operation's description.

        `?state=` filters to one of active/pending/completed/withdrawn (400 on an unknown value).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/cohorts/{id}/open":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.cohorts.open
      summary: Open a :draft Cohort for enrollment (snapshots every published Path).
      description: |
        Contract deferred — see the index operation's description.

        Transactional: snapshots every published Path in the program, sets the default snapshot from
        the program's default Path, and transitions :draft → :enrolling. 422 if the program has no
        published paths, or if any individual snapshot fails (retried async; the cohort stays
        :draft).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/connection_activity_report":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.connectionActivityReport.index
      summary: Connection activity report — per-connection message/post/task/meeting/survey
        counts.
      description: |
        `Api::V3::Admin::Programs::ConnectionActivityReportController` is real and complete.
        `test_index_returns_a_summary_and_paginated_connections` and `test_index_reports_real_non_
        zero_message_counts` exercise it against real fixture data (`albers`' 11 real groups).

        `load_groups_with_activity`'s `group.mentors.map { |u| ... name: u.name }` hits dest's
        `User#name` deprecation tripwire on real data — kept verbatim (not switched to
        `unanonymized_name`, which would keep the anonymity bypass the deprecation flags); the
        controller's own test expects `ErrorAgent.notify`, not silencing it.

        ⚠️ Response keys are camelCase, not the snake_case this spec's convention states — this
        controller renders literal Ruby symbol keys directly. See this row's coordinator report for
        the cross-cutting note shared by all 8 controllers in this batch.
      responses:
        '200':
          description: Activity summary plus a paginated table of connections with
            per-connection counts.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionActivityReportIndex"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionActivityReportBareError"
      parameters:
      - name: start_date
        in: query
        required: false
        schema:
          type: string
        description: 'Start date (YYYY-MM-DD). Default: 3 months ago.'
      - name: end_date
        in: query
        required: false
        schema:
          type: string
        description: 'End date (YYYY-MM-DD). Default: today.'
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by status: active, inactive, closed, drafted, pending.'
      - name: sort
        in: query
        required: false
        schema:
          type: string
        description: Sort column (e.g. messages_count, started_on).
      - name: sort_order
        in: query
        required: false
        schema:
          type: string
        description: 'asc or desc (default: desc).'
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 25, max: 100).'
  "/admin/programs/{program_id}/connection_questions":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.connectionQuestions.index
      summary: List a program's connection questions with choices, ordered by position.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::ConnectionQuestionsController`
        is real and complete, landed as part\nof the write-bearing family batch. This
        stub is the minimum needed to satisfy G6 without\nauthoring the full contract.
        Drop `not_implemented` and add the real schema when that unit is\ntaken, not
        before.\n\n\U0001F534 Already-fixed source defect, tested end to end: `create`/`update`
        with `choices` on a\nchoice-type question had NEVER once worked (a silent,
        unrescued 500) until this port's fix —\nthree stacked shape requirements (`{0
        => ...}` row-group wrap, string-keyed choice hash,\nsibling `new_order` key),
        each verified empirically against a real fixture, not derived from\nreading
        alone. No `User#name`/`@current_member` risk found. `question_text`/`help_text`
        are\nGlobalize-translated attributes, not base-table columns — confirmed live
        (`respond_to?` true)\nbefore trusting the permitted-param mapping.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.programs.connectionQuestions.create
      summary: Create a connection question (optionally with choices for choice-type
        questions).
      description: Contract deferred — see the index operation's description.
      responses:
        '201':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/connection_questions/sort":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.connectionQuestions.sort
      summary: Reorder connection questions by an ordered list of ids.
      description: |
        Contract deferred — see the index operation's description. Backed by the shared
        `ReorderService` (confirmed safe against malformed/garbage ids — intersects with the program's
        real question ids, drops anything invalid, never raises).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/connection_questions/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.programs.connectionQuestions.updatePut
      summary: Update a connection question (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — a plural `resources ..., only: [..., :update, ...]` draws BOTH PATCH
        and PUT at this path, dispatching to the same action. Documented rather than left implicit —
        same precedent as `rumi_settings`/`playbook_settings`/`connections/budget`.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.connectionQuestions.update
      summary: Update a connection question (optionally replacing its choices).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.connectionQuestions.destroy
      summary: Delete a connection question.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/demographic_report":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.demographicReport.index
      summary: Demographic report — geographic user distribution, map + table data.
      description: |
        `Api::V3::Admin::Programs::DemographicReportController` is real and complete.
        `test_index_returns_the_real_report_when_a_location_question_is_configured` exercises the full
        `available: true` path (map + table + roles) against real fixture data; `test_index_reports_
        unavailable_when_no_role_has_location_data_configured` exercises the `available: false` guard.

        Two structurally different bodies share this one 200, both real per the tests above:
        - unavailable: `{available: false, message}` — no location question configured for the
          organization, or no role links one for this program.
        - available: `{available: true, roles, showForAllRoles, ...}`, where `mapData` (present unless
          `table_only=true`), and `columns`/`tableData`/`meta`/`mostUsersStats` (present unless
          `map_only=true`) are genuinely conditional on the request's `map_only`/`table_only` params —
          modeled as optional rather than forced-required, because the controller really does omit
          them from the rendered hash, not merely send them empty.

        `tableData[].counts` and `mostUsersStats` are keyed by `column_key` — a data-driven string
        (`country`, `all_users_count`, `<role_name>_count`, …) confirmed snake_case directly by
        `test_index_returns_the_real_report_when_a_location_question_is_configured`'s own
        `counts["all_users_count"]` assertion — genuinely dynamic, not a fixed field list, so modeled
        as an open integer-valued map rather than invented fixed properties.

        ⚠️ The OUTER envelope keys (`mapData`, `showForAllRoles`, …) are still camelCase, unlike
        `counts`' keys — this controller renders literal Ruby symbol keys for its own hash, but reads
        `column_key` values straight from `ReportViewColumn` records for the nested map. See this
        row's coordinator report for the cross-cutting camelCase note shared by all 8 controllers in
        this batch.
      responses:
        '200':
          description: Either the unavailable guard body, or the real demographic
            report body.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/DemographicReportUnavailable"
                - "$ref": "#/components/schemas/DemographicReportAvailable"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/DemographicReportBareError"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 10, max: 50).'
      - name: sort_param
        in: query
        required: false
        schema:
          type: string
        description: 'Sort column key (default: country).'
      - name: sort_order
        in: query
        required: false
        schema:
          type: string
        description: 'asc or desc (default: asc).'
      - name: role
        in: query
        required: false
        schema:
          type: string
        description: Filter the map by role name.
      - name: countries
        in: query
        required: false
        schema:
          type: string
        description: Filter the map by country name(s).
      - name: map_only
        in: query
        required: false
        schema:
          type: string
        description: '"true" to return only map data (omits columns/tableData/meta/mostUsersStats).'
      - name: table_only
        in: query
        required: false
        schema:
          type: string
        description: '"true" to return only table data (omits mapData).'
  "/admin/programs/{program_id}/engagement":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.engagement.show
      summary: Summary counts for each engagement section (surveys, announcements,
        campaigns, etc).
      description: |
        `Api::V3::Admin::Programs::EngagementController` (#50, engagement sub-unit).

        ⚠️ EVERY count in this summary is wrapped by the controller's own `count_for` blanket rescue,
        which returns 0 on any error — verbatim from source, and applied uniformly to all 13 sections so
        that one failing count cannot 500 the whole summary. ⇒ a zero here means "zero or broken", and a
        test asserting a count must assert the specific value against known data, never merely that the
        request succeeded.
      responses:
        '200':
          description: Engagement summary counts.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementSummary"
  "/admin/programs/{program_id}/engagement/{section}":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.engagement.section
      summary: Paginated list for one engagement section.
      description: "`{section}` is one of `surveys`, `announcements`, `campaigns`,
        `resources`, `events`, `forums`,\n`articles`, `meeting_guides`, `certificates`,
        `qa`, `moderate_content`, `integrations`,\n`three_sixty_surveys` — any other
        value 404s. Every declared section here has a real\nimplementing method. `page`
        is min-clamped to 1; `per_page` is max-clamped to 100 with no\nlower-bound
        clamp. `integrations` ignores both (a fixed 6-item constant, no pagination
        at all).\n\n⚠️ Three sections do not behave as their names suggest, all faithfully
        ported and each documented\nat its own method:\n  * `moderate_content` — \U0001F534
        NOT verbatim. Source filters `forums.type`, a column that exists on\n    NEITHER
        tree, so the list path raised `ActiveRecord::StatementInvalid` on every call
        while\n    the count path silently reported 0 through the blanket rescue.
        Corrected to\n    `forums.group_id IS NULL` — the same predicate dest's own
        `moderatable_posts` row already\n    uses for the identical \"program forums,
        not group forums\" semantic.\n  * `meeting_guides` — always renders an EMPTY
        list. Both of source's fallback associations\n    (`mentoring_model_templates`,
        `meeting_templates`) exist on neither tree. Permanently\n    non-functional
        but safe; the real feature is the separately-landed `meeting_guides` row.\n
        \ * `certificates` — renders BROADER than source intends. Its first branch
        filters on a\n    `certificate_type` column dest's `Certificate::Template`
        does not have; source's rescue\n    falls through to an unfiltered per-program
        query, which works — so the list is right-shaped\n    but unfiltered by type.\n\n⚠️
        `campaigns` here is NARROWER than the separately-landed `campaigns` row: this
        section reads\n`user_campaigns` only, while that row reads `abstract_campaigns`
        (all three types). Both surfaces\nexist and are not synchronised — a fix to
        one does not reach the other.\n"
      responses:
        '200':
          description: Section data (shape keyed by which of the 13 sections was requested).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementSectionReadPayload"
        '404':
          description: Unknown section.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
          enum:
          - surveys
          - announcements
          - campaigns
          - resources
          - events
          - forums
          - articles
          - meeting_guides
          - certificates
          - qa
          - moderate_content
          - integrations
          - three_sixty_surveys
      - name: page
        in: query
        required: false
        schema:
          type: integer
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by status — `surveys`/`announcements`/`campaigns`/`articles`/`events`
          only.
      - name: type
        in: query
        required: false
        schema:
          type: string
        description: Filter by type — `surveys`/`campaigns` only.
      - name: time
        in: query
        required: false
        schema:
          type: string
          enum:
          - upcoming
          - past
        description: Filter by time window — `events` only; any other value is a no-op.
    post:
      tags:
      - admin
      operationId: admin.programs.engagement.create_item
      summary: Create an item within an engagement section (currently `announcements`
        only).
      description: "Only `announcements` implements a create — it is the sole entry
        in `CREATABLE_SECTIONS`; every\nother section 404s.\n\n\U0001F534 NOT verbatim:
        source reads the bare ivar `@current_member` to resolve the authoring user.\nDest's
        `current_member` is a plain method with no ivar side effect, so that read
        is always nil\nand announcement creation raised `NoMethodError` on every call.
        Fixed to use the method — the\nthird instance of this class in the port, after
        `connections/budget` and `connections/expenses`\n(register #221).\n\n\U0001F534
        The DSL-captured responses omitted a real 403: when the resolved user is nil
        (no user in this\nprogram), the controller renders `{ error: \"Member has
        no user in this program\" }` at 403 —\nadded here since the controller genuinely
        has that guard. The created announcement's `viewCount`\nis always `0` in this
        response (the view-count lookup is only ever run for the list/detail\nreads,
        not on create).\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - title
              - body
              properties:
                title:
                  type: string
                body:
                  type: string
                recipientRoleIds:
                  type: array
                  items:
                    type: integer
                  description: Program role IDs to target; omitted/empty means no
                    recipient-role restriction.
      responses:
        '201':
          description: Created announcement.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AnnouncementDetailPayload"
        '403':
          description: The authenticated member has no user in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementErrorResponse"
        '404':
          description: Section is not creatable.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementErrorResponse"
        '422':
          description: Validation failed on the announcement record.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementValidationErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
        description: Section name — only `announcements` is actually supported.
  "/admin/programs/{program_id}/engagement/{section}/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.engagement.section_item
      summary: Detail view for one item within an engagement section.
      description: |
        Routes to the SAME action as the list above; the section serializer branches on the presence of
        `{id}` and whether that section defines a `_detail` method — only `surveys` and `announcements`
        do, so every other section ignores `{id}` and returns its plain list payload.

        ⚠️ Source draws this detail route for `engagement` and deliberately does NOT for `people`, whose
        equivalent serializer is therefore dead code on both trees. Both are ported as source has them —
        the asymmetry is source's, not an oversight here, and ⛔ the two should not be harmonised
        without a decision.
      responses:
        '200':
          description: Detail payload (surveys/announcements) or the section's plain
            list payload (every other section).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementSectionItemReadPayload"
        '404':
          description: Unknown section, or (for `surveys`/`announcements`) the item
            itself was not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
          enum:
          - surveys
          - announcements
          - campaigns
          - resources
          - events
          - forums
          - articles
          - meeting_guides
          - certificates
          - qa
          - moderate_content
          - integrations
          - three_sixty_surveys
        description: Only `surveys` and `announcements` actually use `{id}` for a
          detail view; every other value ignores it and returns the plain list payload.
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: page
        in: query
        required: false
        schema:
          type: integer
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
      - name: status
        in: query
        required: false
        schema:
          type: string
      - name: type
        in: query
        required: false
        schema:
          type: string
      - name: time
        in: query
        required: false
        schema:
          type: string
          enum:
          - upcoming
          - past
  "/admin/programs/{program_id}/goal_setting":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.goalSetting.show
      summary: Get goal setting configuration (lazily creates the program's default
        on first access).
      description: "`Api::V3::Admin::Programs::GoalSettingController` is real and
        complete.\n\n\U0001F534 Already-fixed source defect, tested: THREE unscoped
        params-finds (register #187/#191 class)\n— `role_question_id`/`configuration_id`
        from a client could be attached to/loaded as THIS\nprogram's goal config with
        no ownership check. Fixed by scoping each `.find` through\n`@program`'s own
        associations. No named single-path routes here draw a PUT twin (`publish`/\n`save_changes`/etc.
        are explicit `patch`/`post`/`get`/`delete` single-verb routes, not a\n`resources`
        block) — no PUT/PATCH duality concern anywhere in this row.\n"
      responses:
        '200':
          description: Goal setting configuration data.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GoalSettingConfigurationResponse"
  "/admin/programs/{program_id}/goal_setting/add_additional_question":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.goalSetting.addAdditionalQuestion
      summary: Add an additional question (program-scoped role_question_id, fixed
        per
      description: |
        `role_question_id` is looked up through `@program.role_questions` (not a bare
        `RoleQuestion.find`) — see the `show` operation's description for why. A profile question
        already attached is a silent no-op (idempotent), not a 422.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - role_question_id
              additionalProperties: false
              properties:
                role_question_id:
                  type: integer
                  description: A role question belonging to this program.
      responses:
        '200':
          description: Updated additional questions.
          content:
            application/json:
              schema:
                type: object
                required:
                - additional_questions
                additionalProperties: false
                properties:
                  additional_questions:
                    type: array
                    items:
                      "$ref": "#/components/schemas/GoalSettingAdditionalQuestion"
        '422':
          description: The role question is not attachable as an additional question.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
  "/admin/programs/{program_id}/goal_setting/additional_questions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.goalSetting.additionalQuestions
      summary: List attachable additional questions.
      description: |
        Same listing shape as `intent_questions`, filtered instead through
        `GoalConfiguration::ValidationService#validate_additional_question_attachable`.
      responses:
        '200':
          description: List of attachable additional questions.
          content:
            application/json:
              schema:
                type: object
                required:
                - questions
                additionalProperties: false
                properties:
                  questions:
                    type: array
                    items:
                      "$ref": "#/components/schemas/GoalSettingAttachableQuestion"
  "/admin/programs/{program_id}/goal_setting/detach_intent_question":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.programs.goalSetting.detachIntentQuestion
      summary: Remove the intent question.
      description: |
        `question_reference_id` is optional — when present it must match the configuration's own
        intent-question-reference id, or the request 404s (mismatch is treated the same as absence).

        404 (no intent question to detach, or a mismatch) raises an unrescued
        `ActiveRecord::RecordNotFound` — this engine has zero `rescue_from` for it, so the body is the
        framework default, not this tier's envelope. Documented via the shared `ErrorResponse` as the
        spec's best-known approximation; UNCONFIRMED against a live response.
      responses:
        '200':
          description: Intent question removed.
          content:
            application/json:
              schema:
                type: object
                required:
                - intent_question
                additionalProperties: false
                properties:
                  intent_question:
                    type: 'null'
                    description: Always null — confirms detachment.
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: question_reference_id
        in: query
        required: false
        schema:
          type: integer
        description: Expected id of the current intent-question reference.
  "/admin/programs/{program_id}/goal_setting/discard_changes":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.programs.goalSetting.discardChanges
      summary: Discard the update-in-progress configuration.
      responses:
        '200':
          description: Changes discarded.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '403':
          description: No configuration update is in progress.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/programs/{program_id}/goal_setting/edit_active":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.goalSetting.editActive
      summary: Create or get an update-in-progress clone of the active configuration
        for editing.
      description: |
        Clones the program's active mentee configuration into an `update_in_progress` one on first
        call (`GoalConfiguration::Cloner`); a later call returns the existing clone.
      responses:
        '200':
          description: Configuration data for editing.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GoalSettingConfigurationResponse"
        '403':
          description: Editing the mentee goal configuration is not allowed for this
            program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/programs/{program_id}/goal_setting/intent_questions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.goalSetting.intentQuestions
      summary: List attachable intent questions.
      description: |
        Role questions from the target roles, already filtered to what
        `GoalConfiguration::ValidationService#validate_intent_question_attachable` accepts and to
        profile questions not already used elsewhere on this configuration.
      responses:
        '200':
          description: List of attachable intent questions.
          content:
            application/json:
              schema:
                type: object
                required:
                - questions
                additionalProperties: false
                properties:
                  questions:
                    type: array
                    items:
                      "$ref": "#/components/schemas/GoalSettingAttachableQuestion"
  "/admin/programs/{program_id}/goal_setting/publish":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.goalSetting.publish
      summary: Publish the configuration.
      description: Takes no body.
      responses:
        '200':
          description: Published configuration status.
          content:
            application/json:
              schema:
                type: object
                required:
                - status
                additionalProperties: false
                properties:
                  status:
                    type: string
                    enum:
                    - draft
                    - active
                    - archived
                    - update_in_progress
        '422':
          description: Validation errors preventing publish.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
  "/admin/programs/{program_id}/goal_setting/publishable":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.goalSetting.publishable
      summary: Check if the configuration is publishable.
      responses:
        '200':
          description: Publishable status (validation passed).
          content:
            application/json:
              schema:
                type: object
                required:
                - status
                additionalProperties: false
                properties:
                  status:
                    type: string
                    enum:
                    - draft
                    - active
                    - archived
                    - update_in_progress
        '422':
          description: Validation errors preventing publish.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
  "/admin/programs/{program_id}/goal_setting/remove_additional_question":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.programs.goalSetting.removeAdditionalQuestion
      summary: Remove a secondary or additional question.
      description: |
        404 raises an unrescued `ActiveRecord::RecordNotFound` (this engine has zero `rescue_from`
        for it) — the body is the framework default, not this tier's envelope. Documented via the
        shared `ErrorResponse` as the spec's best-known approximation; UNCONFIRMED against a live
        response.
      responses:
        '200':
          description: Updated additional questions.
          content:
            application/json:
              schema:
                type: object
                required:
                - additional_questions
                additionalProperties: false
                properties:
                  additional_questions:
                    type: array
                    items:
                      "$ref": "#/components/schemas/GoalSettingAdditionalQuestion"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: question_reference_id
        in: query
        required: false
        schema:
          type: integer
        description: A secondary-or-additional question-reference id belonging to
          this configuration.
  "/admin/programs/{program_id}/goal_setting/save_changes":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.goalSetting.saveChanges
      summary: Save and publish changes to the active configuration.
      description: Takes no body.
      responses:
        '200':
          description: Saved configuration status.
          content:
            application/json:
              schema:
                type: object
                required:
                - status
                additionalProperties: false
                properties:
                  status:
                    type: string
                    enum:
                    - draft
                    - active
                    - archived
                    - update_in_progress
        '403':
          description: No configuration update is in progress.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Validation errors.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
  "/admin/programs/{program_id}/goal_setting/toggle_secondary_question":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.goalSetting.toggleSecondaryQuestion
      summary: Toggle a question between secondary and additional.
      description: |
        404 raises an unrescued `ActiveRecord::RecordNotFound` (this engine has zero `rescue_from`
        for it) — the body is the framework default, not this tier's envelope. Documented via the
        shared `ErrorResponse` as the spec's best-known approximation; UNCONFIRMED against a live
        response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - question_reference_id
              additionalProperties: false
              properties:
                question_reference_id:
                  type: integer
                  description: A secondary-or-additional question-reference id belonging
                    to this configuration.
      responses:
        '200':
          description: Updated additional questions, plus the toggled reference's
            id.
          content:
            application/json:
              schema:
                type: object
                required:
                - additional_questions
                - updated_question_reference_id
                additionalProperties: false
                properties:
                  additional_questions:
                    type: array
                    items:
                      "$ref": "#/components/schemas/GoalSettingAdditionalQuestion"
                  updated_question_reference_id:
                    type: integer
        '404':
          "$ref": "#/components/responses/ErrorResponse"
  "/admin/programs/{program_id}/goal_setting/update_intent_question":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.goalSetting.updateIntentQuestion
      summary: Attach or replace the intent question (program-scoped role_question_id,
        fixed per
      description: |
        `role_question_id` is looked up through `@program.role_questions` (not a bare
        `RoleQuestion.find`) — see the `show` operation's description for why.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - role_question_id
              additionalProperties: false
              properties:
                role_question_id:
                  type: integer
                  description: A role question belonging to this program.
      responses:
        '200':
          description: Updated intent question data.
          content:
            application/json:
              schema:
                type: object
                required:
                - intent_question
                additionalProperties: false
                properties:
                  intent_question:
                    anyOf:
                    - "$ref": "#/components/schemas/GoalSettingIntentQuestion"
                    - type: 'null'
        '422':
          description: The role question is not attachable as an intent question.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
  "/admin/programs/{program_id}/match_report":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.matchReport.show
      summary: Full match report data (current status, mentee actions, mentor distribution).
      description: |
        Backed by pre-existing full-stack `MatchReport::{CurrentStatus,MenteeActions,
        MentorDistribution}` and `ReportDateFilter` (reused unmodified from the legacy reporting
        feature). `test_show_returns_the_full_report_when_supported` forces `can_have_match_report?`
        true and asserts all three top-level keys present — the happy path this schema describes.

        `currentStatus.metrics` is `{}` when the program's engagement shape matches none of the three
        known report types (`detect_program_type` returns `"unknown"`); otherwise it always carries
        exactly `first`/`second`/`third` (confirmed against every branch of
        `Program::MatchReportUtils#get_data_values_for_*`, which only ever write those three keys).

        ⚠️ Response keys are camelCase, not the snake_case this spec's convention states — this
        controller renders literal Ruby symbol keys directly (`programType`, `menteeCount`, …). See
        this row's coordinator report for the cross-cutting note shared by all 8 controllers in this
        batch.
      responses:
        '200':
          description: Current status, mentee actions, and mentor distribution for
            a supported program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchReportShow"
        '403':
          description: |
            `{error: "Match report is not available for this program"}` — a controller-level guard
            (`@program.can_have_match_report?`), rendered BARE, not this API's `{error: {code,
            message}}` envelope. Distinct from the framework `require_admin!` 403 (engage-mode
            callers), which DOES use the standard envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchReportBareError"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchReportBareError"
  "/admin/programs/{program_id}/match_report/discrepancy":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.matchReport.discrepancy
      summary: Gap analysis discrepancy data for a match config.
      description: |
        `mentorAnswerChoices` is present (a choice-text → count map) only for a set-matching-type
        match config; for the default type the source hash never sets that key, so the mapped field
        is `null` (Ruby `hash[:missing_key]` reads `nil`). Only the not-found path
        (`match_config_id: -1`) is covered by this row's tests; the happy path (a real match config
        with a discrepancy cache) has no test coverage yet — schema is derived directly from
        `MatchReportController#discrepancy` and `MatchReport::MentorDistribution
        #get_needs_offers_discrepancy_for_{default,set}_type`, not from a test assertion.
      responses:
        '200':
          description: Discrepancy rows for the match config, ordered by gap (or by
            student need).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchReportDiscrepancy"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: |
            `{error: "Match config not found or not eligible"}` — no match config with this id in the
            program, or it fails `can_create_match_config_discrepancy_cache?`. Rendered BARE.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchReportBareError"
      parameters:
      - name: match_config_id
        in: query
        required: true
        schema:
          type: integer
        description: Match config ID.
      - name: order_by_student_need
        in: query
        required: false
        schema:
          type: string
        description: Order by student need count instead of discrepancy gap.
  "/admin/programs/{program_id}/matching":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.matching.show
      summary: Summary counts for each matching data section (connections, mentor
        requests, etc).
      description: |
        `Api::V3::Admin::Programs::MatchingController` (#50, matching sub-unit, the last of the four
        settings/engagement/people/matching dispatchers).

        Response: `{ summary: { connections: {total}, mentorRequests: {total}, mentorOffers: {total},
        meetingRequests: {total}, projectRequests: {total}, recommendations: {total},
        bulkMatches: {total}, auditLogs: {total} } }` — the 8 `LIST_SECTIONS` only; the 6
        config/detail-style sections below are not summarized here.

        ⚠️ Each count is independently `rescue`-wrapped to `0`, verbatim from source — a zero here
        means "zero or broken", same caveat as `engagement`'s summary; a test asserting a count must
        assert the specific value against known data, never merely that the request succeeded.
      responses:
        '200':
          description: Matching summary counts.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSummary"
  "/admin/programs/{program_id}/matching/{section}":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.matching.section
      summary: Paginated list or config for one matching section (data list or config).
      description: |
        `{section}` is one of `connections`, `mentor_requests`, `mentor_offers`, `meeting_requests`,
        `project_requests`, `recommendations`, `bulk_matches`, `audit_logs`, `connection_questions`,
        `request_form`, `match_config`, `mentoring_models`, `connection_settings`, `matching_settings`
        — any other value 404s. Every declared section here has a real implementing method.
        `connections` and `mentoring_models` also support a detail view at the `{id}` sibling route
        below (`DETAIL_SECTIONS`).

        ⚠️ Four sections here overlap already-landed rows, each narrower here than its standalone
        controller:
          * `connection_questions` — read-only, no `choices` field. The full CRUD
            `connection_questions_controller` (a separate route) had a 3-part choices-write-shape
            bug, already fixed there — not applicable here, since this section never writes.
          * `bulk_matches` — a read-only list. `bulk_match_controller` (a separate route) has the full
            show/update/compute/update_pair machinery.
          * `recommendations` — a read-only list, already `program_id`-scoped from the start.
            `recommend_mentors_controller` (a separate route) needed a cross-program IDOR fix on its
            own, different query (`past_recommendations`) — this section's query never had that gap.
          * `matching_settings` duplicates the CONCEPT (not the code) of the already-landed
            `SettingsController`'s narrower `matching` section — both write independently to
            `mentor_request_style`/`allow_mentoring_requests`/`max_connections_for_mentee`. The two
            endpoints are not synchronised as one concept; that is source's own design, not drift here.

        Query params vary by section: `page`/`per_page` (list sections), `status` (`connections`/
        request-shaped sections), `search` (`connections` only).
      responses:
        '200':
          description: Section data — shape keyed by which of the 14 sections was
            requested.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionReadPayload"
        '404':
          description: Unknown section.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
          enum:
          - connections
          - mentor_requests
          - mentor_offers
          - meeting_requests
          - project_requests
          - recommendations
          - bulk_matches
          - audit_logs
          - connection_questions
          - request_form
          - match_config
          - mentoring_models
          - connection_settings
          - matching_settings
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: Page number (default 1, min-clamped to 1).
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: Items per page (default 25, max-clamped to 100; no lower-bound
          clamp).
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by status — `connections`/`mentor_requests`/`mentor_offers`/`meeting_requests`/`project_requests`
          only.
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Free-text search — `connections` only.
    patch:
      tags:
      - admin
      operationId: admin.programs.matching.update_section
      summary: Update a matching config section (only `UPDATABLE_SECTIONS` accept
        writes).
      description: |
        `{section}` must be one of `match_config`, `connection_settings`, `matching_settings`,
        `request_form` — any other declared section 405s, any undeclared value 404s (via the `section`
        dispatch first). Every field in the request body is optional — each updater guards its own
        fields with `params.key?`, so a request naming only its own section's fields validates cleanly
        regardless of which section is targeted (`useUpdateMatchingSection` on the SPA side sends the
        camelCase body as-is, no case conversion).
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/MatchingConfigUpdateBody"
      responses:
        '200':
          description: Updated section data.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingUpdateSectionResponse"
        '405':
          description: Section not updatable.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
        '422':
          description: Validation failed on the underlying Program/ConnectionSetting/CalendarSetting
            record.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionValidationErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
          enum:
          - match_config
          - connection_settings
          - matching_settings
          - request_form
  "/admin/programs/{program_id}/matching/{section}/bulk_close":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.matching.bulk_close
      summary: Bulk close items in a matching section (currently `meeting_requests`
        only).
      description: |
        Only `meeting_requests` implements a bulk close (`bulk_close_meeting_requests`); every other
        section 405s with "Bulk close not supported for '{section}'".
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - ids
              properties:
                ids:
                  type: array
                  items:
                    type:
                    - string
                    - integer
                  description: Meeting-request IDs to close.
                reason:
                  type: string
                  description: Reason for closing (defaults to "Closed via API" when
                    absent/blank).
                notifySender:
                  type: boolean
                  description: Notify the sender (defaults to true — only an explicit
                    `false` suppresses it).
                notifyReceiver:
                  type: boolean
                  description: Notify the receiver (defaults to true — only an explicit
                    `false` suppresses it).
      responses:
        '200':
          description: Bulk close result.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required:
                - closedCount
                properties:
                  closedCount:
                    type: integer
        '405':
          description: Bulk close not supported for this section.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
        '422':
          description: "`ids` was missing, not an array, or empty."
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
        description: Section name — only `meeting_requests` is actually supported.
  "/admin/programs/{program_id}/matching/{section}/export":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.matching.export_section
      summary: Export a matching section as CSV (currently `meeting_requests` only).
      description: |
        Only `meeting_requests` implements an export (`export_meeting_requests`); every other section
        405s with "Export not supported for '{section}'". Columns: ID, Sender, Sender Email, Receiver,
        Receiver Email, Status, Topic/Message, Created At, Closed By, Closed At, Response. Rows come
        out in primary-key order — the scope's own `created_at DESC` is silently dropped by
        `find_each` (a known, registered, not-fixed-here export-ordering gap, same class as
        `checkin_report#export`).
      responses:
        '200':
          description: CSV file download.
          content:
            text/csv:
              schema:
                type: string
                format: binary
        '405':
          description: Export not supported for this section.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
        description: Section name — only `meeting_requests` is actually supported.
      - name: status
        in: query
        required: false
        schema:
          type: string
  "/admin/programs/{program_id}/matching/{section}/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.matching.sectionItem
      summary: Detail view for one item in a matching section, when the section supports
        one.
      description: "\U0001F534 Closes a genuine fence gap: this route is drawn by
        the router (`section` action, same as the\n`{section}`-only route above) but
        had no `GET` entry. Its path was already covered by the `PATCH`\nentry, and
        `openapi_first` treats a covered path with an undeclared verb as\n`:method_not_allowed`,
        which maps to `RequestInvalidError` (`openapi_first/failure.rb:9`). So\nevery
        `GET` here returned **400** — the endpoint was BROKEN, not unguarded. Only
        a path with no\ndeclared verb at all raises `NotFoundError`, which the middleware
        rescues into a passthrough.\n\nRoutes to the SAME `section` action as the
        `{section}`-only route: `matching_controller.rb`\ncalls `serialize_<section>_detail(id)`
        only when BOTH `{id}` is present AND that method exists\n(`DETAIL_SECTIONS
        = %w[connections mentoring_models]`) — every other section falls through to\nits
        plain list/config method, `{id}` silently ignored. So the response is the
        same 14-way union\nas the `{section}`-only route, plus the two detail shapes
        for `connections`/`mentoring_models`.\n"
      responses:
        '200':
          description: Detail payload (connections/mentoring_models) or the section's
            plain list/config payload (every other section).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionItemReadPayload"
        '404':
          description: Unknown section, or (for `connections`/`mentoring_models`)
            the item itself was not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
          enum:
          - connections
          - mentor_requests
          - mentor_offers
          - meeting_requests
          - project_requests
          - recommendations
          - bulk_matches
          - audit_logs
          - connection_questions
          - request_form
          - match_config
          - mentoring_models
          - connection_settings
          - matching_settings
        description: Only `connections` and `mentoring_models` actually use `{id}`
          for a detail view; every other value ignores it and returns the plain list/config
          payload.
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: page
        in: query
        required: false
        schema:
          type: integer
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
      - name: status
        in: query
        required: false
        schema:
          type: string
      - name: search
        in: query
        required: false
        schema:
          type: string
    patch:
      tags:
      - admin
      operationId: admin.programs.matching.update_item
      summary: State-change update on one item in a matching section (currently `connections`
        and `meeting_requests` only).
      description: "Only `connections` (`state: \"closed\"|\"active\"`) and `meeting_requests`
        (`state: \"closed\"`)\nimplement an item update; every other section 405s
        with \"Items in '{section}' cannot be\nupdated\".\n\n\U0001F534 Two confirmed
        source-side defects on `connections`, both escalated (not decided here):\n
        \ * `state: \"active\"` (reactivate a closed connection): source's trailing
        `group.reactivate!`\n    call (which doesn't exist on either tree) was deleted
        rather than substituted — the\n    preceding `change_expiry_date` call already
        performs the full CLOSED→ACTIVE flip via the\n    model's own observer, confirmed
        empirically. Zero functional change from source's intent,\n    but source's
        `clear_closure_reason: true` behavior is NOT carried — a reactivated\n    connection
        keeps its stale `closure_reason_id`, held open for a ruling.\n  * The detail
        view's `messageCount` reads `Chat.where(ref_obj: group)` (ported from a dead\n
        \   `Scrap` read, task #59) — `rescue`'d to `0` on any failure, not expected
        to fail in\n    practice.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - state
              properties:
                state:
                  type: string
                  description: "`connections`: closed|active. `meeting_requests`:
                    closed."
                reason:
                  type: string
                  description: Reason for the state change (defaults to "Closed via
                    API" when absent/blank).
      responses:
        '200':
          description: Updated item.
          content:
            application/json:
              schema:
                oneOf:
                - type: object
                  additionalProperties: false
                  required:
                  - connection
                  properties:
                    connection:
                      type: object
                      additionalProperties: false
                      required:
                      - id
                      - status
                      properties:
                        id:
                          type: integer
                        status:
                          type: string
                          enum:
                          - active
                          - inactive
                          - closed
                          - drafted
                          - pending
                          - proposed
                          - rejected
                          - withdrawn
                - type: object
                  additionalProperties: false
                  required:
                  - meetingRequest
                  properties:
                    meetingRequest:
                      "$ref": "#/components/schemas/MeetingRequestItem"
        '404':
          description: Unknown section, or the item itself was not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
        '422':
          description: Invalid state, or the state transition is not valid from the
            item's current state.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MatchingSectionErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
        description: Section name — only `connections` and `meeting_requests` are
          actually supported.
      - name: id
        in: path
        required: true
        schema:
          type: integer
  "/admin/programs/{program_id}/meeting_guides":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.meetingGuides.index
      summary: List assigned guide packages for the program.
      description: "\U0001F534 Already-fixed, tested cross-program IDOR (register
        #187/#191 class): `destroy` did an\nUNSCOPED `Package::Info.find_by(id:)`
        — any admin in any program could delete any other\nprogram's custom guide
        package. Fixed via `@program.package_infos.find_by(id:)`.\n\n⚠️ `remove_assignments`
        (a write action, `Package::Assignment.chr_destroy`) has ZERO test\ncoverage
        in this row — `index`/`manage`/`save_assignments`/`destroy` are all tested
        against\nreal fixtures, but this one action isn't exercised at all.\n"
      responses:
        '200':
          description: Assigned guide packages.
          content:
            application/json:
              schema:
                type: object
                required:
                - guidePackages
                - meta
                additionalProperties: false
                properties:
                  guidePackages:
                    type: array
                    items:
                      "$ref": "#/components/schemas/MeetingGuidePackage"
                  meta:
                    type: object
                    required:
                    - applicablePackageTypes
                    additionalProperties: false
                    properties:
                      applicablePackageTypes:
                        type: array
                        items:
                          type: integer
                        description: Applicable package type IDs.
  "/admin/programs/{program_id}/meeting_guides/manage":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.meetingGuides.manage
      summary: Get all applicable guide packages with assignment status.
      description: |
        The captured DSL declared `guidePackages` as an untyped array and an additional required
        `tags` object — neither matches the real controller. `guidePackages` items are the same
        package shape as `index`, plus `deletable` and a `categories` breakdown; there is no `tags`
        key anywhere in `manage`'s `render json:`. Documented per the controller, which is the truth.
      responses:
        '200':
          description: Manage data for guide packages.
          content:
            application/json:
              schema:
                type: object
                required:
                - guidePackages
                additionalProperties: false
                properties:
                  guidePackages:
                    type: array
                    items:
                      "$ref": "#/components/schemas/MeetingGuidePackageManage"
  "/admin/programs/{program_id}/meeting_guides/remove_assignments":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.meetingGuides.removeAssignments
      summary: Remove all assignments for a guide package.
      description: |
        ⚠️ UNTESTED — no test in this row exercises this action at all.

        The captured DSL declared only a 200; the controller also rescues any `StandardError` from
        `Package::Assignment.chr_destroy` into a 422 `{success: false, error}` — added here as the
        controller is the truth.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - packageId
              additionalProperties: false
              properties:
                packageId:
                  type: integer
                  description: Package info ID.
      responses:
        '200':
          description: Removal result.
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                additionalProperties: false
                properties:
                  success:
                    type: boolean
        '422':
          description: An error was raised while removing assignments; `error` is
            the raw exception message.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MeetingGuideFailure"
  "/admin/programs/{program_id}/meeting_guides/save_assignments":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.meetingGuides.saveAssignments
      summary: Sync guide package group assignments.
      description: |
        `packageGroupIds` tolerates omission (`Array(params[:packageGroupIds])` treats a missing
        value as empty, syncing to no assignments) — not marked schema-required, unlike the DSL.
        `success` can be `false` on a plain 200 (`sync_assignments!`'s own return value) — this action
        has no rescue and never 422s.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                packageGroupIds:
                  type: array
                  items:
                    type: string
                  description: Package group IDs to assign.
      responses:
        '200':
          description: Assignment result.
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                additionalProperties: false
                properties:
                  success:
                    type: boolean
  "/admin/programs/{program_id}/meeting_guides/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.programs.meetingGuides.destroy
      summary: Delete a custom guide package and its guides (program-scoped, fixed
        per
      description: |
        The captured DSL declared no 404, but an out-of-scope or missing id 404s before the
        `deletable?` guard even runs (`@program.package_infos.find_by(id:)` — added here as the
        controller is the truth. A global (program-less) package and a package belonging to a
        different program both fall out of scope and land on this same 404, not a 403 — the fix
        doesn't distinguish "exists elsewhere" from "doesn't exist".
      responses:
        '200':
          description: Deletion result.
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                additionalProperties: false
                properties:
                  success:
                    type: boolean
        '404':
          description: No package with that id in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            Two distinct causes share this status: the package isn't custom or has active assignments
            (`{error}` only), or an exception was raised while destroying it (`{success: false, error}`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MeetingGuideFailure"
  "/admin/programs/{program_id}/mentoring_calendar_report":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.mentoringCalendarReport.index
      summary: Mentoring calendar report — meeting occurrences by tab (upcoming/overdue/completed/cancelled).
      description: |
        `Api::V3::Admin::Programs::MentoringCalendarReportController` is real and complete.
        `test_index_returns_the_real_occurrences_and_all_tab_counts` exercises the full row-building
        path (mentors/mentees/RSVP lists) against 13 real fixture occurrences.

        `mentors_and_mentees_for`/`rsvp_lists_for` call dest's deprecated `User#name` (kept verbatim,
        not switched to `unanonymized_name`, which would keep the anonymity bypass the deprecation
        flags); the controller's own test expects `ErrorAgent.notify`, not silencing it.

        `state` is one of exactly `completed`/`cancelled`/`overdue`/`upcoming`
        (`meeting_state_label`). `meetingMode` reads `MeetingModeCodeHelper::MEETING_MODE_NAME`, which
        can return `nil` for a mode code it doesn't cover.

        ⚠️ Response keys are camelCase, not the snake_case this spec's convention states — this
        controller renders literal Ruby symbol keys directly. See this row's coordinator report for
        the cross-cutting note shared by all 8 controllers in this batch.
      responses:
        '200':
          description: Paginated meeting occurrences, tab counts, and summary meeting
            time.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MentoringCalendarReportIndex"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MentoringCalendarReportBareError"
      parameters:
      - name: tab
        in: query
        required: false
        schema:
          type: string
        description: 'Tab filter: all (default), upcoming, overdue, completed, cancelled.'
      - name: start_date
        in: query
        required: false
        schema:
          type: string
        description: 'Start date (YYYY-MM-DD). Default: 6 months ago.'
      - name: end_date
        in: query
        required: false
        schema:
          type: string
        description: 'End date (YYYY-MM-DD). Default: today.'
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 25, max: 100).'
  "/admin/programs/{program_id}/mentoring_tips":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.mentoringTips.index
      summary: List a program's mentoring tips, optionally filtered by role.
      description: |
        `bulk_update`'s `tips.update_all(enabled: params[:enabled])` looked like a register #220-class
        risk (`update_all` bypasses normal attribute assignment) — checked live rather than assumed:
        Rails DOES cast the raw string "true"/"false" through the column's type before the UPDATE,
        confirmed by replaying it against a real fixture tip. Safe.

        ⚠️ Response keys are camelCase, NOT this spec's usual snake_case (verbatim-ported controller
        quirk) — and the REQUEST side matches: `roleNames` is read camelCase too
        (`params.permit(:message, :enabled, roleNames: [])`), so this row has no case mismatch to
        reconcile, unlike some sibling rows.
      responses:
        '200':
          description: List of mentoring tips, plus the program's filterable roles.
          content:
            application/json:
              schema:
                type: object
                required:
                - mentoringTips
                - meta
                additionalProperties: false
                properties:
                  mentoringTips:
                    type: array
                    items:
                      "$ref": "#/components/schemas/MentoringTip"
                  meta:
                    type: object
                    required:
                    - roles
                    additionalProperties: false
                    properties:
                      roles:
                        type: array
                        items:
                          "$ref": "#/components/schemas/MentoringTipRoleOption"
      parameters:
      - name: role
        in: query
        required: false
        schema:
          type: string
        description: Filter by role name (e.g. mentor, student).
    post:
      tags:
      - admin
      operationId: admin.programs.mentoringTips.create
      summary: Create a mentoring tip for one or more roles.
      description: |
        `enabled` defaults to `true` when omitted; `roleNames` is only set when present (an omission
        saves an unsaved-without-roles tip, which fails the model's `roles` presence validation — a
        422, not a 400). Only `message` is genuinely required to form the request.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - message
              additionalProperties: false
              properties:
                message:
                  type: string
                  description: Tip message (max 350 chars).
                enabled:
                  type: boolean
                  description: Whether the tip is enabled. Defaults to true when omitted.
                roleNames:
                  type: array
                  items:
                    type: string
                  description: Role names to associate.
      responses:
        '201':
          description: Created mentoring tip.
          content:
            application/json:
              schema:
                type: object
                required:
                - mentoringTip
                additionalProperties: false
                properties:
                  mentoringTip:
                    "$ref": "#/components/schemas/MentoringTip"
        '422':
          description: |
            Model validation failed (e.g. no message, or no roles attached). The captured DSL declared
            `errors` as an `Object` — the controller actually renders `tip.errors.full_messages`, a
            flat string array; documented per the controller, which is the truth here.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
  "/admin/programs/{program_id}/mentoring_tips/bulk_update":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.mentoringTips.bulkUpdate
      summary: Enable or disable all mentoring tips for a role.
      description: |
        Both fields are required in this contract deliberately (not merely per the DSL): `role` and
        `enabled` are read as plain, undefaulted `params[...]` and fed straight into
        `tips.update_all(enabled:)` with no presence check — an omitted `role` updates every tip in
        the program, and an omitted `enabled` writes `NULL` to all of them. Requiring both at the
        schema level closes that footgun at the 400 gate rather than at the database.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - role
              - enabled
              additionalProperties: false
              properties:
                role:
                  type: string
                  description: Role name to filter by.
                enabled:
                  type: boolean
                  description: Enable (true) or disable (false) all tips for the role.
      responses:
        '200':
          description: Bulk update result.
          content:
            application/json:
              schema:
                type: object
                required:
                - updatedCount
                additionalProperties: false
                properties:
                  updatedCount:
                    type: integer
  "/admin/programs/{program_id}/mentoring_tips/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.programs.mentoringTips.updatePut
      summary: Update a mentoring tip (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — a plural `resources ..., only: [..., :update, ...]` draws BOTH PATCH
        and PUT at this path, dispatching to the same action. Documented rather than left implicit —
        same precedent as `rumi_settings`/`playbook_settings`/`connections/budget`. See the PATCH
        operation's description for the nil-out footgun.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                message:
                  type: string
                enabled:
                  type: boolean
      responses:
        '200':
          description: Updated mentoring tip.
          content:
            application/json:
              schema:
                type: object
                required:
                - mentoringTip
                additionalProperties: false
                properties:
                  mentoringTip:
                    "$ref": "#/components/schemas/MentoringTip"
        '404':
          description: No mentoring tip with that id in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            Model validation failed. Same DSL/controller disagreement as `create` — `errors` is a flat
            string array (`full_messages`), not an `Object`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
    patch:
      tags:
      - admin
      operationId: admin.programs.mentoringTips.update
      summary: Update a mentoring tip's message/enabled state.
      description: |
        ⚠️ Both fields are passed to `.update` unconditionally
        (`@mentoring_tip.update(message: tip_params[:message], enabled: tip_params[:enabled])`) — an
        OMITTED field is passed through as `nil`, not left unchanged. Sending only one of the two
        risks nil-ing out the other (message nil fails a presence validation → 422; enabled nil may
        not). Neither is marked schema-required, since the request is well-formed either way — this is
        a data-safety footgun, not a 400-worthy shape violation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                message:
                  type: string
                enabled:
                  type: boolean
      responses:
        '200':
          description: Updated mentoring tip.
          content:
            application/json:
              schema:
                type: object
                required:
                - mentoringTip
                additionalProperties: false
                properties:
                  mentoringTip:
                    "$ref": "#/components/schemas/MentoringTip"
        '404':
          description: No mentoring tip with that id in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            Model validation failed. Same DSL/controller disagreement as `create` — `errors` is a flat
            string array (`full_messages`), not an `Object`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareErrors"
    delete:
      tags:
      - admin
      operationId: admin.programs.mentoringTips.destroy
      summary: Delete a mentoring tip.
      description: |
        The captured DSL declared no 404 for this action, but `find_mentoring_tip` runs as a
        `before_action` on `destroy` too (same as `update`) — a missing/foreign id 404s here as well.
      responses:
        '200':
          description: Deletion confirmation.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '404':
          description: No mentoring tip with that id in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/programs/{program_id}/migrate_to_paths":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.migrate_to_paths.create
      summary: Flip a Program's read source from MentoringModel to Path.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::MigrateToPathsController` is
        real, complete, and landed (#34, P2-7..N batch\n2, Row 6/8). This stub is
        the minimum needed to satisfy G6 without authoring the full contract,\nwhich
        is deferred to the same migration unit as row #159's openapi DSL blocks. Drop\n`not_implemented`
        and add the real schema when that unit is taken, not before.\n\nIdempotent
        — re-running is safe. Populates a shadow Path for every MentoringModel that
        lacks\none, then sets `default_path_id` if unset (preferring the default MentoringModel's
        shadow Path,\nfalling back to the first published Path). Per-MentoringModel
        shadow-write failures are\ncollected in the response `errors` array rather
        than failing the whole request.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/moderatable_posts":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.moderatablePosts.index
      summary: List unpublished posts in the program's own forums awaiting moderation.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::ModeratablePostsController`
        is real and complete, landed as part of\nthe last remaining-rows batch (no
        `:update` in this row's routes — `only: [:index]` plus\nmember `publish`/`decline`
        — no PUT/PATCH duality concern here). This stub is the minimum\nneeded to
        satisfy G6 without authoring the full contract. Drop `not_implemented` and
        add the\nreal schema when that unit is taken, not before.\n\n\U0001F534 Register
        #223, confirmed and RE-MEASURED (not assumed): `post_json`'s `post.user&.name(\nname_only:
        true)` hits dest's `User#name` deprecation tripwire. Checked live for pre-existing\nunpublished
        program-forum posts on `albers` before trusting a count: there are ZERO —
        so\nunlike `qa_questions`, \"once\" IS the accurate count here. Two tests
        are affected and neither\nhas `ErrorAgent.expects` treatment yet: `test_index_returns_unpublished_program_forum_posts`\n(fires
        once, via the created post) and `test_publish_marks_the_post_published` (fires
        once,\nvia the success render). `test_publish_not_found_for_an_already_published_post`/`test_decline_\n*`
        don't render `post_json` on their path and are unaffected.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/moderatable_posts/{id}/decline":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - admin
      operationId: admin.programs.moderatablePosts.decline
      summary: Decline (destroy) a post with a required reason, notifying the author
        if different from the actor.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/moderatable_posts/{id}/publish":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.moderatablePosts.publish
      summary: Publish an unpublished post.
      description: |
        Contract deferred — see the index operation's description. Hits the same `User#name` tripwire
        as index — see above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '404':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/outcomes_report/connection_outcomes":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.outcomesReport.connectionOutcomes
      summary: Connection outcomes report data.
      description: "Traced live, same method as `user_outcomes` — constructed\n`OutcomesReport::ConnectionOutcomesReport`
        across the active/completed/positive-outcomes\nbranches and every `data_side`
        value, and inspected the real `#as_json` output.\n\nTwelve fields are always
        present regardless of branch or `data_side`: `mentoring_roles`\n(see the leak
        note below — snake_case here, since this ivar has no accessor override, unlike\n`mentoringRolesForCharts`
        on `user_outcomes`), `roleGraphColorMapping`,\n`enabledStatusMapping`, `startDate`,
        `endDate`, `startDayIndex`, `daysSpan`, `oldEndTime`,\n`oldStartTime`, `getOldData`,
        `startDateForGraph`, `forStatus`.\n\n`totalCount`/`overallChange`/`userSummary`/`rolewiseSummary`/`graphData`
        are each present or\nabsent independently, gated by `data_side` — but ONLY
        on the default (active-connections)\nbranch; `status` set to anything other
        than blank/`active` (the \"completed\" branch) or `type`\nset to exactly `positive_outcomes`
        (which overrides `status` entirely) compute all five\nregardless of `data_side`.
        Modeled as optional rather than conditioned on the branch, since\nthey're
        a truthful superset either way. `marginError`/`responseRate`/\n`responseRateInScopedConnections`
        appear only for the `positive_outcomes` branch.\n\n\U0001F534 `forStatus`
        is NOT always a string — confirmed live: with no `status` param it's the raw\ninteger
        `Group::Status::ACTIVE` (`0`); with one supplied, it's that string, echoed
        verbatim\n(never validated against a known set of statuses).\n\n\U0001F534
        `mentoring_roles` is the same raw, unfiltered `Role` leak as `user_outcomes`'\n`mentoringRolesForCharts`
        — see that operation's description for why it's deliberately left\nopen rather
        than itemized.\n"
      responses:
        '200':
          description: Connection outcomes report data.
          content:
            application/json:
              schema:
                type: object
                required:
                - connectionOutcomes
                additionalProperties: false
                properties:
                  connectionOutcomes:
                    "$ref": "#/components/schemas/OutcomesReportConnectionOutcomes"
      parameters:
      - name: date_range
        in: query
        required: false
        schema:
          type: string
        description: "`MM/DD/YYYY - MM/DD/YYYY`. Defaults to the program's creation
          date through today."
      - name: data_side
        in: query
        required: false
        schema:
          type: string
          enum:
          - non_graph_data
          - graph_data
          - all_data
        description: |
          Which half of the report to compute. Only affects the default (active-connections)
          branch — the completed and positive-outcomes branches always compute everything.
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: |
          Connection status filter. Blank or `active` selects the active-connections branch;
          anything else selects the completed-connections branch (echoed back verbatim as
          `forStatus`, not validated against a known set).
      - name: type
        in: query
        required: false
        schema:
          type: string
        description: Only `positive_outcomes` has special meaning (selects that branch,
          overriding `status`); any other value is ignored.
  "/admin/programs/{program_id}/outcomes_report/meeting_outcomes":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.outcomesReport.meetingOutcomes
      summary: Meeting outcomes report data (flash programs).
      description: "Traced live, same method as `user_outcomes` — constructed\n`OutcomesReport::MeetingOutcomesReport`
        across the default and `positive_outcomes` branches\nand inspected the real
        `#as_json` output. Unlike `user_outcomes`/`connection_outcomes`, this\naction
        takes no `data_side` — every field below is always present, since the underlying
        PORO\nalways computes the completed-meetings shape unconditionally.\n\n`marginError`/`responseRate`/`responseRateInScopedConnections`
        appear only for the\n`positive_outcomes` branch. `memberIds` is `null` when
        the request carries no cached/scoped\nmember segment (confirmed live against
        a plain request).\n\n\U0001F534 `mentoring_roles` is the same raw, unfiltered
        `Role` leak described on `user_outcomes` —\nsee that operation's description
        for why it's deliberately left open rather than itemized.\n"
      responses:
        '200':
          description: Meeting outcomes report data.
          content:
            application/json:
              schema:
                type: object
                required:
                - meetingOutcomes
                additionalProperties: false
                properties:
                  meetingOutcomes:
                    "$ref": "#/components/schemas/OutcomesReportMeetingOutcomes"
      parameters:
      - name: date_range
        in: query
        required: false
        schema:
          type: string
        description: "`MM/DD/YYYY - MM/DD/YYYY`. Defaults to the program's creation
          date through today."
      - name: type
        in: query
        required: false
        schema:
          type: string
          enum:
          - closed
          - positive_outcomes
        description: Only `positive_outcomes` has special meaning; `closed` (and any
          other value, including omission) is the same default report.
  "/admin/programs/{program_id}/outcomes_report/seed_data":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.outcomesReport.seedData
      summary: Seed data for the outcomes report (date presets, program info).
      description: "`Api::V3::Admin::Programs::OutcomesReportController` is real and
        complete, landed as part of\nthe read-only report family batch. `presets`
        is the fixed 8-entry list built from\n`OUTCOME_PRESETS` (confirmed by `test_seed_data_returns_presets_and_program_info`);
        only the\n`custom` preset is editable/pre-selected.\n\n⚠️ Response keys are
        camelCase (`programCreatedAt`, not `program_created_at`) — this\ncontroller
        renders literal Ruby symbol keys, not schema-projected fields, so it never
        passes\nthrough the snake_case wire convention `.claude`'s `openapi/CLAUDE.md`
        states for this spec.\nDocumented as observed, not \"fixed\" — see this row's
        report to the coordinator for the\ncross-cutting note (all 8 controllers in
        this batch share the same pre-existing camelCase\nbehavior).\n\n\U0001F534
        N+1, allow-listed not fixed (register #225 family): `role.customized_term.pluralized_term`\nresolves
        through the shared `Role::Resolver` utility, which re-queries regardless of\ncaller-side
        preloading. Allow-listed per-action, matching 30+ existing callers of this
        exact\npair.\n"
      responses:
        '200':
          description: Date-range presets, program creation date, and mentoring-role
            listing.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OutcomesReportSeedData"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OutcomesReportBareError"
  "/admin/programs/{program_id}/outcomes_report/user_outcomes":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.outcomesReport.userOutcomes
      summary: User outcomes report data.
      description: "Traced live rather than from source alone: constructed `OutcomesReport::UserOutcomesReport`\nwith
        every `data_side`/`include_rolewise_summary` combination this controller can
        actually\nproduce and inspected the real `#as_json` output (`bundle exec rails
        runner`, against\n`programs(:albers)`), rather than reading the PORO branches
        and guessing.\n\nThe response is `{ userOutcomes: report }`, `report` rendered
        via Rails' generic\n`Object#as_json` (`instance_values` — every set `@ivar`,
        keyed by its bare name). Confirmed\nlive: `fetch_user_state`/`only_rolewise_summary`
        are PORO options this controller never passes\n(always falsy here), so their
        branches — including the `userState` key — are unreachable\nthrough this endpoint
        and are correctly absent from this contract, not merely undocumented.\n\nFive
        fields are always present (`startDate`, `endDate`, `mentoringRolesForCharts`,\n`programId`,
        `nMentoringRoles`). The rest split on `data_side`: `intervalInDays`, `totalCount`,\n`overallChange`,
        `prevWindowValid` appear only when `data_side` is `non_graph_data` or\n`all_data`;
        `enabledStatusMapping`, `graphData`, `roleGraphColorMapping`,\n`startDateForGraph`,
        `startDayIndex` only when it's `graph_data` or `all_data`;\n`rolewiseSummary`
        only when `include_rolewise_summary=true` AND the non-graph half ran. An\nunrecognized
        `data_side` value isn't rejected — it silently skips BOTH halves, matching
        the\ncontroller's own untyped `.in?` check, which is why `data_side` isn't
        required here either.\n\n\U0001F534 `mentoringRolesForCharts` is a raw, unfiltered
        `Role` ActiveRecord array — confirmed live to\ncarry all ~23 current `roles`
        columns (id, name, `description`/`eligibility_message`\nGlobalize-translated
        text, program_id, timestamps, and ~18 admin-configuration\nbooleans/settings
        that have nothing to do with an outcomes report). `Role` has no\n`as_json`/`serializable_hash`
        override, so this is `instance_values` dumping the model\nverbatim — almost
        certainly an unintended leak, not a designed field set. Left OPEN\n(`additionalProperties:
        true`) rather than itemized: closing it would couple this contract to\nthe
        `roles` table schema, and the next unrelated migration adding a column would
        silently start\nrejecting a real, valid response — the exact failure mode
        `additionalProperties: false` is\nsupposed to prevent, not cause.\n"
      responses:
        '200':
          description: User outcomes report data.
          content:
            application/json:
              schema:
                type: object
                required:
                - userOutcomes
                additionalProperties: false
                properties:
                  userOutcomes:
                    "$ref": "#/components/schemas/OutcomesReportUserOutcomes"
      parameters:
      - name: date_range
        in: query
        required: false
        schema:
          type: string
        description: "`MM/DD/YYYY - MM/DD/YYYY`. Defaults to the program's creation
          date through today."
      - name: data_side
        in: query
        required: false
        schema:
          type: string
          enum:
          - non_graph_data
          - graph_data
          - all_data
        description: Which half of the report to compute. Defaults to `all_data`.
          An unrecognized value computes neither half.
      - name: include_rolewise_summary
        in: query
        required: false
        schema:
          type: boolean
        description: Include the per-role summary breakdown (only takes effect alongside
          the non-graph half).
  "/admin/programs/{program_id}/path_assignments":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.path_assignments.index
      summary: List a Program's path assignments, ordered by priority.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::PathAssignmentsController` is
        real, complete, and landed (#34, P2-7..N batch\n2, Row 4/8). This stub is
        the minimum needed to satisfy G6 without authoring the full contract,\nwhich
        is deferred to the same migration unit as row #159's openapi DSL blocks. Drop\n`not_implemented`
        and add the real schema when that unit is taken, not before.\n\n⚠️ NOT nested
        under `paths` — a sibling resource of `programs`. `path_id` is a body field\n(`path_assignment.path_id`),
        not a URL segment.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.programs.path_assignments.create
      summary: Create a path assignment routing a MemberGroup to a Path.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ path_assignment: { path_id, member_group_id, priority, allow_self_switch?,
        on_group_exit_policy? } }` — a missing/blank `path_assignment` key is rejected with 422.
        Unlike `paths`/`path_modules`/`path_nodes`, `priority` has NO auto-default here — an omitted
        value fails the model's own presence validation with a normal 422.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/path_assignments/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.path_assignments.reorder
      summary: Reorder a Program's path assignments by an ordered list of ids (evaluation
        priority).
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ assignment_ids: [id1, id2, ...] }` — note the payload key is `assignment_ids`, not
        `ids`/`node_ids`/`module_ids`.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/path_assignments/set_default_path":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.path_assignments.set_default_path
      summary: Set (or clear) a Program's default Path.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ path_id: <int> | null }` — a null/omitted `path_id` clears the program's default path
        rather than being rejected. `path_id` must belong to this program or the request 422s.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/path_assignments/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.programs.path_assignments.replace
      summary: Update a path assignment.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.path_assignments.update
      summary: Update a path assignment.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.path_assignments.destroy
      summary: Delete a path assignment.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.paths.index
      summary: List a program's Paths.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::PathsController` is real, complete,
        and landed (#34, P2-7..N batch 2,\nRow 1/8). This stub is the minimum needed
        to satisfy G6 without authoring the full contract,\nwhich is deferred to the
        same migration unit as row #159's openapi DSL blocks. Drop\n`not_implemented`
        and add the real schema when that unit is taken, not before.\n\nGated behind
        the org-level PROGRAM_PATHS feature — 404 (not 403) when off, matching source.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.programs.paths.create
      summary: Create a Path.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ path: { kind, name, description?, metadata? } }` — a missing/blank `path` key is
        rejected with 422.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.paths.show
      summary: One Path, with its modules and nodes.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.programs.paths.replace
      summary: Update a Path.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.paths.update
      summary: Update a Path.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.paths.destroy
      summary: Delete a Path (refused if it is the program's default path or has active
        enrollments).
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{id}/archive":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.paths.archive
      summary: Archive a Path (refused for the program's current default path).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{id}/duplicate":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.paths.duplicate
      summary: Duplicate a Path (a new draft copy, including its modules and nodes).
      description: Contract deferred — see the index operation's description.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{id}/publish":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.paths.publish
      summary: Publish a Path (becomes the program's default path if none is set yet).
      description: |
        Contract deferred — see the index operation's description.

        Requires at least one node, all with a resolvable asset; 422 otherwise. Also 422 when the
        path is not in a publishable state (only `draft`/`published` transition; an `archived` path
        cannot be published without first reactivating).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{path_id}/path_modules":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.path_modules.index
      summary: List a Path's modules, ordered by position.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::PathModulesController` is real,
        complete, and landed (#34, P2-7..N batch 2,\nRow 2/8). This stub is the minimum
        needed to satisfy G6 without authoring the full contract,\nwhich is deferred
        to the same migration unit as row #159's openapi DSL blocks. Drop\n`not_implemented`
        and add the real schema when that unit is taken, not before.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.programs.path_modules.create
      summary: Create a module on a Path.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ module: { name, description?, position?, config? } }` — a missing/blank `module` key
        is rejected with 422. `position` defaults to one past the path's current max.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{path_id}/path_modules/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.path_modules.reorder
      summary: Reorder a Path's modules by an ordered list of ids.
      description: "Contract deferred — see the index operation's description.\n\n\U0001F534
        This is a PATCH, not a POST (opposite of the taxonomy batch's reorder verb).
        Body:\n`{ module_ids: [id1, id2, ...] }` — note the payload key is `module_ids`,
        not `ids`.\n"
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{path_id}/path_modules/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.programs.path_modules.replace
      summary: Update a module.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.path_modules.update
      summary: Update a module.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.path_modules.destroy
      summary: Delete a module.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{path_id}/path_nodes":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.path_nodes.index
      summary: List a Path's nodes, ordered by position.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::PathNodesController` is real,
        complete, and landed (#34, P2-7..N batch 2,\nRow 3/8). This stub is the minimum
        needed to satisfy G6 without authoring the full contract,\nwhich is deferred
        to the same migration unit as row #159's openapi DSL blocks. Drop\n`not_implemented`
        and add the real schema when that unit is taken, not before.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.programs.path_nodes.create
      summary: Create a node on a Path.
      description: |
        Contract deferred — see the index operation's description.

        Body: `{ node: { type, asset_type?, asset_id?, path_module_id?, position?, title?, description?,
        config? } }`. `type` must be one of `PathNode::Content`, `PathNode::SkillPractice`,
        `PathNode::MeetingTemplate` or the request is rejected with 422. `position` defaults to one past
        the path's current max.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{path_id}/path_nodes/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.path_nodes.reorder
      summary: Reorder a Path's nodes by an ordered list of ids.
      description: "Contract deferred — see the index operation's description.\n\n\U0001F534
        This is a PATCH, matching `path_modules`' reorder verb (not the taxonomy batch's
        POST). Body:\n`{ node_ids: [id1, id2, ...] }` — note the payload key is `node_ids`,
        not `ids`/`module_ids`.\nPositions are re-numbered PER path_module (each module's
        nodes restart at 0), not per path.\n"
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/paths/{path_id}/path_nodes/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.programs.path_nodes.replace
      summary: Update a node.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.path_nodes.update
      summary: Update a node.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.programs.path_nodes.destroy
      summary: Delete a node.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/people":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.people.show
      summary: Summary counts for each people section (members, admins, invitations,
        etc).
      description: |
        `Api::V3::Admin::Programs::PeopleController` (#50, people sub-unit).

        Response: `{ summary: { members: {total}, admins: {total}, invitations: {total},
        membershipRequests: {total}, blockedUsers: {total}, profileQuestions: {total} } }`.
      responses:
        '200':
          description: People summary counts.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleSummary"
  "/admin/programs/{program_id}/people/{section}":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.people.section
      summary: Paginated list for one people section.
      description: "`{section}` is one of `members`, `admins`, `invitations`, `membership_requests`,\n`blocked_users`,
        `profile_questions` — any other value 404s.\n\n\U0001F534 NOT verbatim: source's
        `SECTIONS` also lists `onboarding`/`membership_settings`, neither of\nwhich
        has an implementing method anywhere in `Concerns::People::Sections` — a client-supplied\n`:section`
        of either 500ed via this action's ungated `send`. Dropped both from `SECTIONS`\nrather
        than implementing them (source never wrote those two sections); a request
        for either now\n404s the same as any other unknown section.\n\nQuery params
        vary by section: `page`/`per_page` (all except `profile_questions`, which
        has no\npagination at all), `role`/`search`/`status` (`members` only), `status`
        (`invitations`/\n`membership_requests` only, defaults to `pending` when blank).\n"
      responses:
        '200':
          description: Paginated section list (shape keyed by which of the 6 sections
            was requested).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleSectionReadPayload"
        '404':
          description: Unknown section.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
          enum:
          - members
          - admins
          - invitations
          - membership_requests
          - blocked_users
          - profile_questions
      - name: page
        in: query
        required: false
        schema:
          type: integer
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
      - name: role
        in: query
        required: false
        schema:
          type: string
        description: Filter by role — `members` only (mentor/mentee/admin).
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Search by name or email — `members` only.
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by status — `invitations`/`membership_requests` only.
    post:
      tags:
      - admin
      operationId: admin.programs.people.create_item
      summary: Create an item within a people section (currently `invitations` only).
      description: |
        Only `invitations` implements a create (`{ emails: [string], role, message? }`, `role` one of
        `mentor`/`mentee`/`admin`); every other section 404s with "Create not supported for this
        section".
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - emails
              - role
              properties:
                emails:
                  type: array
                  items:
                    type: string
                  description: Email addresses to invite (deduplicated, blanks rejected).
                role:
                  type: string
                  description: "`mentor`, `mentee`, or `admin`, matched case-insensitively
                    by `resolve_invitation_role_name` — kept as a plain string rather
                    than an `enum` so mixed-case input doesn't 400 before reaching
                    that check."
                message:
                  type: string
      responses:
        '201':
          description: Created invitations.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleInvitationsCreatePayload"
        '404':
          description: Unsupported section.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleErrorResponse"
        '422':
          description: "`emails` was blank, `role` was invalid, or the role does not
            exist in this program."
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
        description: Section name — only `invitations` is actually supported.
  "/admin/programs/{program_id}/people/{section}/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.people.update_item
      summary: Update an item within a people section (currently `members` only —
        suspend/reactivate).
      description: "Only `members` implements an update (`{ state: \"suspended\"|\"active\",
        reason? }`); every other\nsection 404s with \"Update not supported for this
        section\" — a section-scoped guard already\npresent on source, unaffected
        by the `SECTIONS` trim above.\n\n\U0001F534 NOT verbatim: source reads the
        bare ivar `@current_member` when suspending/reactivating,\npopulated only
        as a side effect of source's OWN memoized `current_member`. Dest's\n`current_member`
        is a plain method with no such ivar side effect, so `@current_member` is\nalways
        nil on dest — `NoMethodError` on every call. Fixed to use the method, same
        substitution\nalready applied to `connections/budget` and `connections/expenses`.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - state
              properties:
                state:
                  type: string
                  description: "`suspended` or `active`, matched case-insensitively
                    by the controller — kept as a plain string rather than an `enum`
                    so mixed-case input doesn't 400 before reaching that check."
                reason:
                  type: string
      responses:
        '200':
          description: Updated member.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleMemberUpdatePayload"
        '404':
          description: Unsupported section, or the member itself was not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleErrorResponse"
        '422':
          description: Invalid `state`, or the member is already in the requested
            state (already suspended / not suspended).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PeopleErrorResponse"
      parameters:
      - name: section
        in: path
        required: true
        schema:
          type: string
        description: Section name — only `members` is actually supported.
      - name: id
        in: path
        required: true
        schema:
          type: integer
  "/admin/programs/{program_id}/playbook_settings":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.playbook_settings.index
      summary: List a program's per-playbook enable/disable overrides.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::PlaybookSettingsController`
        is real, complete, and landed (#48).\nThis stub is the minimum needed to satisfy
        G6 without authoring the full contract, which is\ndeferred to the same migration
        unit as row #159's openapi DSL blocks. Drop `not_implemented`\nand add the
        real schema when that unit is taken, not before.\n\n⚠️ NOT this family's shared
        `Api::V3::Admin::Programs::BaseController` — inherits the root\n`Api::V3::Admin::BaseController`
        instead, with its own `find_program` (org-scoped, not a\nsecurity gap — see
        the controller's header comment). Only rows with existing\n`ProgramPlaybookSetting`
        overrides are listed; a playbook with no row is enabled by default and\nsimply
        absent from this list.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/playbook_settings/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.programs.playbook_settings.update_put
      summary: Enable or disable one playbook for a program (PUT alias of the PATCH
        above).
      description: |
        Contract deferred — see the index operation's description.

        ⚠️ Declared because `resources … only: %i[index update]` draws BOTH `PATCH` and `PUT` at this
        path, and an undocumented verb enters the generated spec as an EMPTY operation — no
        operationId, no responses — which the SPA then codegens against. Same dual-verb trap as
        `rumi_settings` and `connections/budget`; the sibling `rumi_settings.meta.yaml` declares its
        `PUT` for exactly this reason. Behaviour is identical to the `PATCH`: Rails routes both verbs
        to the same action.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.playbook_settings.update
      summary: Enable or disable one playbook for a program.
      description: |
        Contract deferred — see the index operation's description.

        `{id}` is the playbook's `key` (a string), not a numeric row id — there may be no existing
        `ProgramPlaybookSetting` row yet, in which case one is created. Body: `{ enabled: <bool> }`.
        Foundation/operating (`always_load: true`) playbooks reject `enabled: false` with 422.
      responses:
        '200':
          description: Deferred — see the index operation's description.
        '422':
          description: Deferred — see the index operation's description.
  "/admin/programs/{program_id}/qa_questions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.qaQuestions.index
      summary: List a program's Q&A questions, sortable, with author and answer counts.
      description: "\U0001F534 Register #223, carried forward: `question_list_json`'s
        `question.user&.name(name_only:\ntrue)` and `answer_json`'s `answer.user&.name(name_only:
        true)` both hit dest's `User#name`\ndeprecation tripwire (`QaQuestion#user`/`QaAnswer#user`
        are real `User`, not `Member`) — kept\nverbatim, consistent with other landed
        rows reading the same deprecated method. The test file\nalready stubs `ErrorAgent.expects(:notify).at_least_once`
        around every request that triggers it.\n\n⚠️ Response keys are camelCase,
        NOT this spec's usual snake_case (verbatim-ported controller\nquirk, same
        as sibling rows in this tier).\n"
      responses:
        '200':
          description: Paginated question list.
          content:
            application/json:
              schema:
                type: object
                required:
                - qaQuestions
                - meta
                additionalProperties: false
                properties:
                  qaQuestions:
                    type: array
                    items:
                      "$ref": "#/components/schemas/QaQuestionSummary"
                  meta:
                    "$ref": "#/components/schemas/QaQuestionsPagination"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 25, max: 100).'
      - name: sort
        in: query
        required: false
        schema:
          type: string
          enum:
          - id
          - summary
          - qa_answers_count
          - views
          - created_at
        description: Sort column.
      - name: order
        in: query
        required: false
        schema:
          type: string
          enum:
          - asc
          - desc
        description: 'Sort direction (default: desc).'
  "/admin/programs/{program_id}/qa_questions/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.qaQuestions.show
      summary: Get Q&A question detail with paginated answers.
      responses:
        '200':
          description: Question detail with answers.
          content:
            application/json:
              schema:
                type: object
                required:
                - qaQuestion
                - answers
                - answersMeta
                additionalProperties: false
                properties:
                  qaQuestion:
                    "$ref": "#/components/schemas/QaQuestionDetail"
                  answers:
                    type: array
                    items:
                      "$ref": "#/components/schemas/QaAnswer"
                  answersMeta:
                    "$ref": "#/components/schemas/QaQuestionsPagination"
        '404':
          description: No question with that id in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
      parameters:
      - name: answers_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Answers page number (default: 1).'
      - name: answers_per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Answers per page (default: 25, max: 100).'
    delete:
      tags:
      - admin
      operationId: admin.programs.qaQuestions.destroy
      summary: Delete a Q&A question.
      responses:
        '200':
          description: Question deleted.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '404':
          description: No question with that id in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/programs/{program_id}/recommend_mentors":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.show
      summary: Get recommend-mentors configuration and state (lazily creates the default
        config).
      description: "`Api::V3::Admin::Programs::RecommendMentorsController` (dev-5,
        `programs/*` family).\n\n\U0001F534 Already-fixed, tested cross-program IDOR
        (register #187/#191 class): `past_recommendations`'\n`MentorRecommendation.unscoped`
        query had NO program scope on a client-supplied\n`secondaryEntityId` — an
        admin of Program A could read Program B's past mentor-recommendation\nhistory.
        Fixed by adding `program_id: @program.id`.\n\n⛔ `compute`, `update_pair`,
        and `bulk_update_pairs` — same class of gap as `bulk_match` (this\ncontroller
        shares its shape) — have ZERO test coverage.\n"
      responses:
        '200':
          description: Recommendation config, program capability flags, and static
            settings.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsShowPayload"
    put:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.updatePut
      summary: Update recommend-mentors settings (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — even a SINGULAR `resource ..., only: [..., :update]` draws BOTH PATCH
        and PUT at this path, dispatching to the same action. Documented rather than left implicit —
        same precedent as `rumi_settings`/`playbook_settings`/`training`/`bulk_match`. Armed identically
        to PATCH (parity-enforced: this route pair must stay armed/unarmed together).
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RecommendMentorsUpdateBody"
      responses:
        '200':
          description: Updated config.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsUpdatePayload"
        '422':
          description: The updated attributes failed a model validation.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsErrorResponse"
    patch:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.update
      summary: Update recommend-mentors settings.
      description: |
        Sibling of `BulkMatchController#update`'s `program_id`-uniqueness fix: `bulk_matches` (the
        shared table backing `BulkRecommendation` via STI) validates `program_id` unique within
        `[:type, :orientation_type]`. This action never sets `orientation_type`, so the exact
        collision `BulkMatchController` hit isn't reachable here today — but `update!` can still raise
        on any other validation, rescued to 422 for consistency with that sibling.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RecommendMentorsUpdateBody"
      responses:
        '200':
          description: Updated config.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsUpdatePayload"
        '422':
          description: The updated attributes failed a model validation.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsErrorResponse"
  "/admin/programs/{program_id}/recommend_mentors/admin_views":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.adminViews
      summary: List available admin views partitioned by mentor/mentee role for entity
        selection.
      responses:
        '200':
          description: Admin views by role.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsAdminViewsPayload"
  "/admin/programs/{program_id}/recommend_mentors/bulk_update_pairs":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.bulkUpdatePairs
      summary: Bulk draft, publish, or discard mentor recommendations.
      description: |
        ⚠️ UNTESTED — see the `show` operation's description. On `publish`, when
        `chemistry_check_applicable?`, queues `MentorRecommendation.handle_published_recommendation_for_chemistry_check`
        and always queues `send_delayed_bulk_publish_mails` as delayed jobs — neither is awaited by
        this response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RecommendMentorsBulkUpdatePairsBody"
      responses:
        '200':
          description: Bulk update result — `count` is the number of mentee IDs processed
            (not necessarily the number successfully changed).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsBulkUpdatePairsResult"
        '422':
          description: Invalid `updateType`, or a record failed a model validation.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsErrorResponse"
  "/admin/programs/{program_id}/recommend_mentors/compute":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.compute
      summary: Trigger recommendation computation.
      description: "⚠️ UNTESTED — see the `show` operation's description."
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RecommendMentorsComputeBody"
      responses:
        '200':
          description: Computation results.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsComputationResults"
        '409':
          description: Computation already in progress for this config.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsErrorResponse"
        '422':
          description: 'Computation failed — `error` carries "Computation failed:
            <exception message>".'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsErrorResponse"
  "/admin/programs/{program_id}/recommend_mentors/past_recommendations":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.pastRecommendations
      summary: Get past recommendations for a mentee (program-scoped, fixed per
      description: |
        Hits the NEW `User#name` site described in the `show` operation's description. `perPage` is a
        hardcoded `10`, not client-configurable — the `page` query param is the only pagination input.
      responses:
        '200':
          description: Past recommendations for the named mentee.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsPastRecommendationsPayload"
      parameters:
      - name: secondaryEntityId
        in: query
        required: true
        schema:
          type: integer
        description: Mentee user ID.
      - name: page
        in: query
        required: false
        schema:
          type: integer
  "/admin/programs/{program_id}/recommend_mentors/update_pair":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.recommendMentors.updatePair
      summary: Draft, publish, or discard a single mentor recommendation.
      description: "⚠️ UNTESTED — see the `show` operation's description. `current_member.user_in_program(@program)`\nis
        already the correct accessor here, but unverified by a real request.\n\n\U0001F534
        The DSL-captured 422-only response omitted a real 404: `@program.mentor_recommendations\n.find_by(receiver_id:
        ...).try(:past_or_destroy!)` — a `try` — never raises on discard, but\n`draft_mentor_recommendation`'s
        own lookups can still surface `RecordNotFound` (rescued to 404\nhere for consistency
        with `bulk_match#update_pair`'s sibling shape); kept as a documented\npossibility
        rather than dropped, since the controller has the rescue clause wired.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RecommendMentorsUpdatePairBody"
      responses:
        '200':
          description: Pair update result.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsUpdatePairResult"
        '404':
          description: A referenced recommendation record was not found.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsErrorResponse"
        '422':
          description: Invalid `updateType`, or the record failed a model validation.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RecommendMentorsErrorResponse"
  "/admin/programs/{program_id}/reports/{section}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.reports.section
      summary: One of 11 report sections (overview, outcomes, match_report, emails,
        connection_activity, demographics, virtual_mentoring, calendar, check_in,
        meeting_report, flags).
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::ReportsController` is
        real and complete, landed earlier this batch\n(dev-5) with its own test file
        already covering all 11 sections against real fixture data. This\nstub is
        the minimum needed to satisfy G6 without authoring the full contract. Drop\n`not_implemented`
        and add the real schema when that unit is taken, not before.\n\n\U0001F534
        Two source-side defects already found, registered, and pinned by regression
        tests (not\nfixed — escalated, per the controller's own header comment): `render_flags`'s
        `@program.flags`\n(no such association on `Program`, self-masked by its own
        `rescue`, always reports 0);\n`render_connection_activity`'s `groups_forums`
        join (table doesn't exist on either tree, self\n-masked, `totalMessages` always
        0). Both degrade to zero rather than crash, and both have a\ndedicated test
        (`test_flags_degrades_to_zero_without_crashing`,\n`test_connection_activity_degrades_messages_to_zero_without_crashing`)
        that also handles the\n`Rails.logger.warn` expectation dest's warn-gate requires.\n\nNo
        `User#name` risk (`serialize_email`'s `msg.sender&.name` resolves through\n`CommonMessageExtensions#sender`,
        `class_name: 'Member'` — confirmed, not `User`). No\n`@current_member` risk.
        This row's tests create real data (a real active group, a real\nadmin_message)
        for the sections that need it — not vacuous.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
        '404':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/rumi_settings":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.rumi_settings.show
      summary: The org-context fields Rumi is allowed to surface for members in this
        program.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::RumiSettingsController` is real,
        complete, and landed (#34, P2-7..N batch 2,\nRow 7/8). This stub is the minimum
        needed to satisfy G6 without authoring the full contract,\nwhich is deferred
        to the same migration unit as row #159's openapi DSL blocks. Drop\n`not_implemented`
        and add the real schema when that unit is taken, not before.\n\nBacked by
        the `rumi_org_context_fields` JSON column (Group 16.7 migration). Response:\n`{
        rumi_settings: { <field>: <bool>, ... } }`, one key per toggled field — an
        untouched field\nis simply absent, not defaulted to `false`.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.programs.rumi_settings.update_put
      summary: Update which org-context fields Rumi may surface for members in this
        program (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — the route's singular `resource ... only: [:show, :update]` draws
        PATCH and PUT at the same path, both dispatching to the same action (same precedent as
        `connections/budget`'s `update_put`). Documented rather than left implicit because a routed
        verb with no operation publishes an empty contract for a live endpoint.
      responses:
        '200':
          description: Deferred — see the `show` operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.rumi_settings.update
      summary: Update which org-context fields Rumi may surface for members in this
        program.
      description: |
        Contract deferred — see the `show` operation's description.

        Body: `{ rumi_settings: { <field>: <bool>, ... } }` — allowed fields: `title`, `manager`,
        `years_of_experience`, `career_history`, `skills`, `skill_evaluations`, `purpose_profile`,
        `next_role_skills`. Values coerce to boolean. Merges into the existing toggle set rather than
        replacing it; a missing/blank `rumi_settings` key is rejected with 422.
      responses:
        '200':
          description: Deferred — see the `show` operation's description.
  "/admin/programs/{program_id}/settings":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.settings.show
      summary: All 17 program settings sections in one payload (general, features,
        security, etc).
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::SettingsController`
        is real, complete, and landed (#50, settings\nsub-unit, 949 L source across
        4 concerns). This stub is the minimum needed to satisfy G6 without\nauthoring
        the full contract, which is deferred to the same migration unit as row #159's
        openapi\nDSL blocks. Drop `not_implemented` and add the real schema when that
        unit is taken, not before.\n\nResponse: `{ settings: { general: {...}, features:
        [...], terminology: [...],\npermissions: {...}, security: {...}, branding:
        {...}, systemEmails: {...}, events: {...},\nbudget: {...}, analytics: {...},
        membership: {...}, matching: {...}, authConfigs: [...],\ntranslations: [...],
        dataImports: {...}, pages: {...}, contactAdmin: {...} } }`.\n\n⚠️ Three sections
        carry CONFIRMED, RULED masked bugs — all pre-existing on source, all\n`rescue`-swallowed
        to a fixed wrong default rather than erroring, all already tracked at\n`docs/releases/proj-rumi-to-develop-port/common-notes.md:292`:\n
        \ * `budget.budgetingEnabled` — always `false`. `budgeting_enabled?` doesn't
        exist on `Program`\n    on either tree.\n  * `membership.joinSettings.allowJoin`
        / `.requireApproval` — always `true` / `false`.\n    `membership_eligibility_rules_enabled`/`membership_request_auto_approval`
        don't exist on\n    `Program` on either tree.\n  * `matching.maxConnectionsForMentor`
        — always `null`. Not a missing column so much as a\n    category error: the
        concept is real but lives on `User` (`users.max_connections_limit`,\n    written
        via the existing `admin_views` bulk action), never on `Program`. Ruled: kept\n
        \   rescue-swallowed on read (asserting the wrong default is the correct test,
        not routing\n    around it); write support was dropped entirely (see the `update`
        operation below).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/settings/{section}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.programs.settings.update
      summary: Update one settings section (`{section}` is a path segment, not a query
        param).
      description: |
        Contract deferred — see the `show` operation's description.

        `{section}` must be one of the 11 `WRITABLE_SECTIONS` (`general`, `features`, `terminology`,
        `permissions`, `security`, `branding`, `events`, `budget`, `analytics`, `membership`,
        `matching`) — any other declared-but-read-only section 405s, any undeclared value 404s.

        ⚠️ Two write-side defects, both RULED and FIXED (not merely rescued):
          * `matching` — `max_connections_for_mentor` used to be a permitted param that crashed
            `@program.update` with `ActiveModel::UnknownAttributeError` on every call (it's a per-user
            field name, never a `Program` attribute — see the `show` operation's description). Dropped
            from the allowlist entirely; sending it is now a silent no-op, not a 500.
          * `budget` — used to call `@program.enable_features(["budgeting"])` on `budgeting_enabled:
            true`, unrescued, raising `ActiveRecord::RecordInvalid` on every `Program` ("budgeting" is
            registered ORGANIZATION-level-only). Write support for `budgeting_enabled` was removed
            entirely rather than rescued or redirected to `Organization` — a rescue would convert a loud
            500 into a silent no-op on a feature toggle (the costliest failure mode this port has seen),
            and calling `enable_features` on the `Organization` instead would enable an org-level feature
            from a program-scoped endpoint, affecting every sibling program (a privilege-scope
            escalation, not a fix). If organization-level budgeting toggling is wanted, it belongs on an
            organization-scoped surface with its own authorization — not built here.

            ⭐ Worth noting for the next reader: this is the SECOND distinct failure mode
            `enable_features` has produced in this port. Elsewhere (register #198) it silently no-ops
            when a constraint proc filters the feature out; here it hard-raises on an
            organization/program level mismatch. The same call fails two different ways depending on
            *why* the feature is ineligible, and only one of those ways is loud.
      responses:
        '200':
          description: Deferred — see the `show` operation's description.
        '404':
          description: Deferred — see the `show` operation's description.
        '405':
          description: Deferred — see the `show` operation's description.
        '422':
          description: Deferred — see the `show` operation's description.
  "/admin/programs/{program_id}/simulate_enrollment":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.simulate_enrollment.create
      summary: Dry-run PathAssignment resolution for a member, with a per-assignment
        trace.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Programs::SimulateEnrollmentController`
        is real, complete, and landed (#34, P2-7..N\nbatch 2, Row 5/8). This stub
        is the minimum needed to satisfy G6 without authoring the full\ncontract,
        which is deferred to the same migration unit as row #159's openapi DSL blocks.
        Drop\n`not_implemented` and add the real schema when that unit is taken, not
        before.\n\nBody: `{ member_id: <int> }`. Resolves the Path a member would
        get WITHOUT creating an\nInteraction or snapshot — pure read, no side effects.
        A 3-second internal timeout guards\nagainst a pathological MemberGroup rule;
        on timeout the response reports `timed_out: true`\nrather than the request
        itself timing out.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/skills-gap":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.skillsGap.show
      summary: Skills gap analysis — mentee needs vs mentor offers.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::SkillsGapController`
        is real and complete, landed as part of the\nlast remaining-rows batch (read-only,
        no writes — no PUT/PATCH duality concern). This stub is\nthe minimum needed
        to satisfy G6 without authoring the full contract. Drop `not_implemented`\nand
        add the real schema when that unit is taken, not before.\n\n⚠️ Route path
        is `skills-gap` (HYPHENATED), not `skills_gap` — confirmed against source's
        own\n`get 'skills-gap', to: 'programs/skills_gap#show'`. Easy to miss when
        wiring.\n\n\U0001F534 Already-registered, escalated (not fixed) source-side
        defect: `SkillsGapAnalyzer` (ported\nalongside this controller) references
        a bare `RoleName` constant that always raises, self\n-masked by its own rescue
        — `analyze` currently ALWAYS returns empty skill sets on both source\nand
        dest, regardless of real data. The one test in this row pins that as the current,
        honest\nbehavior rather than trying to fix it here.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/programs/{program_id}/three_sixty_surveys":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.threeSixtySurveys.index
      summary: List 360 surveys for a program's organization (own program + org-wide,
        deliberately).
      description: |
        `organization_surveys` deliberately includes org-wide surveys (`program_id IS NULL`)
        alongside this program's own — confirmed a deliberate scope, not a missing one; org-isolation
        is intact (still scoped through `organization.three_sixty_surveys`).

        ⚠️ Response keys are camelCase, NOT this spec's usual snake_case — a verbatim-ported
        controller quirk (request-body params it reads, e.g. `reviewers_addition_type`, are still
        snake_case; only what it renders back is camelCase). Documented as the controller actually
        behaves, not normalized.
      responses:
        '200':
          description: Paginated survey list.
          content:
            application/json:
              schema:
                type: object
                required:
                - surveys
                - meta
                additionalProperties: false
                properties:
                  surveys:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ThreeSixtySurveySummary"
                  meta:
                    "$ref": "#/components/schemas/ThreeSixtySurveysPagination"
      parameters:
      - name: state
        in: query
        required: false
        schema:
          type: string
          enum:
          - drafted
          - published
        description: Filter by survey state.
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number (default: 1).'
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: 'Items per page (default: 25, max: 100).'
    post:
      tags:
      - admin
      operationId: admin.programs.threeSixtySurveys.create
      summary: Create a new drafted 360 survey.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ThreeSixtySurveyWriteBody"
      responses:
        '201':
          description: Created survey.
          content:
            application/json:
              schema:
                type: object
                required:
                - survey
                additionalProperties: false
                properties:
                  survey:
                    "$ref": "#/components/schemas/ThreeSixtySurveyDetail"
        '422':
          description: Model validation failed (e.g. missing title).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ThreeSixtySurveyError"
  "/admin/programs/{program_id}/three_sixty_surveys/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.threeSixtySurveys.show
      summary: Get 360 survey details.
      responses:
        '200':
          description: Survey details.
          content:
            application/json:
              schema:
                type: object
                required:
                - survey
                additionalProperties: false
                properties:
                  survey:
                    "$ref": "#/components/schemas/ThreeSixtySurveyDetail"
        '404':
          description: Survey not found (or belongs to another organization).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    put:
      tags:
      - admin
      operationId: admin.programs.threeSixtySurveys.updatePut
      summary: Update a 360 survey (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — a plural `resources ..., only: [..., :update, ...]` draws BOTH PATCH
        and PUT at this path, dispatching to the same action. Documented rather than left implicit —
        same precedent as `rumi_settings`/`playbook_settings`/`bulk_match`/`recommend_mentors`.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ThreeSixtySurveyWriteBody"
      responses:
        '200':
          description: Updated survey.
          content:
            application/json:
              schema:
                type: object
                required:
                - survey
                additionalProperties: false
                properties:
                  survey:
                    "$ref": "#/components/schemas/ThreeSixtySurveyDetail"
        '422':
          description: Not drafted, or model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ThreeSixtySurveyError"
    patch:
      tags:
      - admin
      operationId: admin.programs.threeSixtySurveys.update
      summary: Update a 360 survey (drafted state only).
      description: |
        Only a `drafted` survey may be edited — a `published` one 422s with a bare `{error}` before
        any model validation runs.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ThreeSixtySurveyWriteBody"
      responses:
        '200':
          description: Updated survey.
          content:
            application/json:
              schema:
                type: object
                required:
                - survey
                additionalProperties: false
                properties:
                  survey:
                    "$ref": "#/components/schemas/ThreeSixtySurveyDetail"
        '422':
          description: Not drafted, or model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ThreeSixtySurveyError"
    delete:
      tags:
      - admin
      operationId: admin.programs.threeSixtySurveys.destroy
      summary: Delete a 360 survey (drafted state only).
      responses:
        '200':
          description: Survey deleted.
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    type: string
        '422':
          description: Only a drafted survey can be deleted.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ThreeSixtySurveyError"
  "/admin/programs/{program_id}/three_sixty_surveys/{id}/publish":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.programs.threeSixtySurveys.publish
      summary: Publish a drafted 360 survey.
      description: |
        Takes no body. ⚠️ Only the "incomplete survey" 422 guard is tested — the success path
        (`create_survey_assessees_from_admin_view!` → `publish!` →
        `.delay.notify_assessees`/`.delay.notify_reviewers`) is not. A green result after arming
        doesn't confirm the actual publish flow works.
      responses:
        '200':
          description: Survey published.
          content:
            application/json:
              schema:
                type: object
                required:
                - survey
                additionalProperties: false
                properties:
                  survey:
                    "$ref": "#/components/schemas/ThreeSixtySurveyDetail"
        '422':
          description: |
            Not drafted, incomplete (missing questions/reviewer groups/assessees), or an unrescued
            `StandardError` during publish (caught and reported as this same bare shape).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ThreeSixtySurveyError"
  "/admin/programs/{program_id}/training":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.training.show
      summary: Aggregated training data — plans, certificate templates, resources,
        settings.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Api::V3::Admin::Programs::TrainingController`
        is real and complete, landed as part of the\nwrite-bearing family batch. This
        stub is the minimum needed to satisfy G6 without authoring the\nfull contract.
        Drop `not_implemented` and add the real schema when that unit is taken, not\nbefore.\n\nA
        NEW aggregating \"show all training data\" endpoint, not one of the excluded
        pre-existing\n`training_plans`/`certificate_templates` rows — it only reads
        those already-landed,\nprogram-scoped associations. Fully tested against real
        fixtures (3 training plans, 2\ncertificate templates on `albers`, both counts
        confirmed live). No `User#name`/\n`@current_member` risk found. `update`'s
        params already use `as: :json` correctly in the test.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.programs.training.updatePut
      summary: Update program-level training settings (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — even a SINGULAR `resource ..., only: [:show, :update]` draws BOTH
        PATCH and PUT at this path, dispatching to the same action. Documented rather than left
        implicit — same precedent as `rumi_settings`/`playbook_settings`/`connections/budget`.
      responses:
        '200':
          description: Deferred — see the show operation's description.
        '422':
          description: Deferred — see the show operation's description.
    patch:
      tags:
      - admin
      operationId: admin.programs.training.update
      summary: Update program-level training settings (access_any_course, allow_users_to_share_certificates).
      description: Contract deferred — see the show operation's description.
      responses:
        '200':
          description: Deferred — see the show operation's description.
        '422':
          description: Deferred — see the show operation's description.
  "/admin/programs/{program_id}/virtual_mentoring_dashboard":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.virtualMentoringDashboard.index
      summary: List all virtual mentoring features with availability and status.
      description: |
        `Api::V3::Admin::Programs::VirtualMentoringDashboardController` is real and complete.
        `test_index_lists_every_listable_feature` confirms the real, filtered set (only features
        `VirtualMentoringDashboard::Features.feature_available?` allows for the program's actual
        config — NOT the full static `FEATURE_TITLES`/`FEATURE_CATEGORIES` list).

        `category` is one of the four keys `FEATURE_CATEGORIES` maps every known feature id to.
        `status` is `connected`/`not_connected` for integration-based features (slack/teams), else
        `enabled`/`not_enabled`.

        ⚠️ Response keys are camelCase, not the snake_case this spec's convention states — this
        controller renders literal Ruby symbol keys directly. See this row's coordinator report for
        the cross-cutting note shared by all 8 controllers in this batch.
      responses:
        '200':
          description: Every listable virtual-mentoring feature for this program,
            with status.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/VirtualMentoringDashboardIndex"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: The program does not exist in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/VirtualMentoringDashboardBareError"
  "/admin/programs/{program_id}/virtual_mentoring_dashboard/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.programs.virtualMentoringDashboard.show
      summary: Get metrics for a specific virtual mentoring feature.
      description: "`{id}` is a feature key string (e.g. `zoom`, `slack`, `real_time_chat`),
        not a numeric row id.\n`test_show_returns_metrics_for_a_known_feature` runs
        the full metrics-calculation body — the\nstrongest-tested row in this batch.\n\n`metrics`'
        shape is determined entirely by the feature's `category`\n(`normalize_metrics`)
        — exactly one of four closed shapes, never a mix, so modeled as a\n`oneOf`
        rather than one shape with optional fields from all four.\n\n\U0001F534 NOT
        verbatim (fixed, register #193): source's `Time.zone.parse` on `start_date`/`end_date`\nwas
        unrescued, a real 500 on any unparseable caller-supplied date. Fixed to 422
        here\n(`test_show_returns_422_for_an_unparseable_{start,end}_date` prove it)
        — the only 500-surface\nfix across this whole 8-row family.\n"
      responses:
        '200':
          description: Metrics for the named feature over the requested date range.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/VirtualMentoringDashboardShow"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: |
            `{error: "Unknown feature: <id>"}` — `id` is not in `VirtualMentoringDashboard::Features
            .all`. Rendered BARE, not this API's `{error: {code, message}}` envelope. Also covers the
            program-not-found case from `Programs::BaseController#resolve_program`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/VirtualMentoringDashboardBareError"
        '422':
          description: |
            `start_date` or `end_date` failed to parse. Rendered BARE via a translated message, not
            this API's `{error: {code, message}}` envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/VirtualMentoringDashboardBareError"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: Feature key (e.g., zoom, slack, real_time_chat).
      - name: start_date
        in: query
        required: false
        schema:
          type: string
        description: Start date (YYYY-MM-DD), defaults to program creation.
      - name: end_date
        in: query
        required: false
        schema:
          type: string
        description: End date (YYYY-MM-DD), defaults to today.
  "/admin/refold/connector_manifest":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.refold.connector_manifest
      summary: Refold connector manifest.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/refold/session":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.refold.session
      summary: Refold session.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/rollout_status":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.rollout_status.show
      summary: Per-site AI coach rollout status for the organization.
      description: |
        Lists every org unit with a resolved `live` or `pending` status from the
        `rollout` config namespace (default pending). Member counts route through
        Privacy::MinimumCohort.
      responses:
        '200':
          description: Rollout status payload.
          content:
            application/json:
              schema:
                type: object
                required:
                - organization_id
                - computed_at
                - sites
                additionalProperties: false
                properties:
                  organization_id:
                    type: integer
                  computed_at:
                    type: string
                    format: date-time
                  sites:
                    type: array
                    items:
                      "$ref": "#/components/schemas/RolloutStatusSite"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/admin/rumi/activity":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.rumi.activity.gone
      summary: Retired Rumi activity surface (410).
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/rumi/content_gaps":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.rumi.content_gaps.index
      summary: List content gaps.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/rumi/content_gaps/{id}/acknowledge":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.rumi.content_gaps.acknowledge
      summary: Acknowledge a content gap.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/rumi/content_gaps/{id}/dismiss":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.rumi.content_gaps.dismiss
      summary: Dismiss a content gap.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/rumi/content_gaps/{id}/resolve":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.rumi.content_gaps.resolve
      summary: Resolve a content gap.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/rumi/guardrails":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.rumi.guardrails.index
      summary: List Rumi guardrails.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.rumi.guardrails.create
      summary: Create a Rumi guardrail.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/rumi/guardrails/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.rumi.guardrails.show
      summary: Show a Rumi guardrail.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.rumi.guardrails.replace
      summary: Replace a Rumi guardrail.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.rumi.guardrails.update
      summary: Update a Rumi guardrail.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.rumi.guardrails.destroy
      summary: Delete a Rumi guardrail.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/rumi/members/{member_id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.rumi.members.gone
      summary: Retired Rumi member surface (410).
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/skill_categories":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.skill_categories.index
      summary: List an organization's skill categories, ordered by position.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nSkillCategoriesController` is real and complete (dev-5,
        admin root family batch). Unblocked by\n`SkillCategorySerializer` (dev-5,
        this batch — genuine absence, verified directly, not a\nleaf-hidden root-resolves
        trap). Route per source (`config/routes.rb:704-708`): `resources\n:skill_categories,
        only: [:index, :show, :create, :update, :destroy] { collection { patch\n:reorder
        } } ` — ROUTED on dest.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.skill_categories.create
      summary: Create a skill category.
      description: Contract deferred — see the index operation's description. Body
        — `{ name, description?, position?, icon?, color? }`.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/skill_categories/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - admin
      operationId: admin.skill_categories.reorder
      summary: Reorder an organization's skill categories by an ordered list of {id,
        position} pairs.
      description: |
        Contract deferred — see the index operation's description.

        Body — `{ positions: [{ id, position }, ...] }`. NOT VERBATIM (register #193, directed):
        source's bare `params.require(:positions)` is guarded with a presence check first — envelope/
        i18n consistency, not crash-prevention. Rejects (422) if the id set doesn't exactly match the
        organization's own category ids (prevents a partial/foreign-id reorder).
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/skill_categories/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.skill_categories.show
      summary: Get a single skill category.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.skill_categories.replace
      summary: Update a skill category.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.skill_categories.update
      summary: Update a skill category.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.skill_categories.destroy
      summary: Delete a skill category (rejected 422 if any skill is still assigned
        to it).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/slack_archie/{path}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.slack_archie.gone
      summary: Retired Slack Archie surface (410).
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.slack_archie.gone_patch
      summary: Retired Slack Archie surface (410).
      description: |
        Same 410 stub as GET. PATCH must stay documented so the validator does
        not 405 leftover admin clients.
      responses:
        '200':
          description: Deferred — see the GET operation description above.
  "/admin/taxonomy/career_visibility":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.career_visibility.show
      summary: The organization's per-audience career-visibility config (member_self,
        manager).
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::CareerVisibilitiesController`
        is real, complete, and landed (#31, P2-7..N\nfirst landing batch, Row 9/9).
        This stub is the minimum needed to satisfy G6 without authoring\nthe full
        contract, which is deferred to the same migration unit as row #159's openapi
        DSL\nblocks. Drop `not_implemented` and add the real schema when that unit
        is taken, not before.\n\nSingular resource (no `{id}`) — one config per organization.
        Response also carries\n`featureEnabled`, since the CAREER_FRAMEWORK feature
        is the master gate applied at READ time\nby `Organization#career_visible_to?`,
        not by this endpoint — an admin may always view/edit the\ntoggles regardless
        of whether the feature is on.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.career_visibility.update_put
      summary: Update the per-audience career-visibility toggles (PUT alias — a singular
        `resource` draws both verbs to the same action).
      description: Contract deferred — see the show operation's description.
      responses:
        '200':
          description: Deferred — see the show operation's description.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.career_visibility.update
      summary: Update the per-audience career-visibility toggles (member_self, manager).
      description: |
        Contract deferred — see the show operation's description.

        Body: `{ career_visibility: { member_self: bool, manager: bool } }` — either key may be
        omitted (a partial update merges over the existing config); an entirely missing or blank
        `career_visibility` key is rejected with 422.
      responses:
        '200':
          description: Deferred — see the show operation's description.
  "/admin/taxonomy/imports":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.imports.create
      summary: Import job architecture or skill evaluations.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_categories":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_categories.index
      summary: List job categories.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_categories.create
      summary: Create a job category.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_categories/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_categories.reorder
      summary: Reorder job categories.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_categories/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_categories.show
      summary: Show a job category.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.job_categories.replace
      summary: Replace a job category.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.job_categories.update
      summary: Update a job category.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.job_categories.destroy
      summary: Delete a job category.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_families":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_families.index
      summary: List the organization's job families, optionally filtered by job_family_group_id.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::JobFamiliesController` is real,
        complete, and landed (#31, P2-7..N first\nlanding batch, Row 2/9). This stub
        is the minimum needed to satisfy G6 without authoring the\nfull contract,
        which is deferred to the same migration unit as row #159's openapi DSL blocks\n(this
        row keeps its source `openapi do...end` blocks in place per that ruling, though
        this\ncontroller happens to declare none). Drop `not_implemented` and add
        the real schemas when that\nunit is taken, not before.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_families.create
      summary: Create a job family under a job family group.
      description: Contract deferred — see the index operation's description.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_families/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_families.reorder
      summary: Reorder job families by an ordered list of ids.
      description: |
        Contract deferred — see the index operation's description.

        `ids` must be sent in bracket form (`ids[]=5&ids[]=3`, or a JSON array body) — a bare
        `ids=5` parses as the scalar string "5", which the controller rejects with 422 rather than
        coercing into a single-element reorder.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_families/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_families.show
      summary: One job family.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.job_families.update_put
      summary: Update a job family (PUT alias — `resources` draws both verbs to the
        same action).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.job_families.update
      summary: Update a job family.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.job_families.destroy
      summary: Delete a job family.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_family_groups":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_family_groups.index
      summary: List the organization's job family groups, ordered by position.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::JobFamilyGroupsController` is
        real, complete, and landed (#31, P2-7..N first\nlanding batch). This stub
        is the minimum needed to satisfy G6 without authoring the full\ncontract,
        which is deferred to the same migration unit as row #159's openapi DSL blocks
        (this\nrow keeps its source `openapi do...end` blocks in place per that ruling,
        though this\ncontroller happens to declare none). Drop `not_implemented` and
        add the real schemas when that\nunit is taken, not before.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_family_groups.create
      summary: Create a job family group.
      description: Contract deferred — see the index operation's description.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_family_groups/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_family_groups.reorder
      summary: Reorder job family groups by an ordered list of ids.
      description: |
        Contract deferred — see the index operation's description.

        `ids` must be sent in bracket form (`ids[]=5&ids[]=3`, or a JSON array body) — a bare
        `ids=5` parses as the scalar string "5", which the controller rejects with 422 rather than
        coercing into a single-element reorder.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_family_groups/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_family_groups.show
      summary: One job family group.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.job_family_groups.update_put
      summary: Update a job family group (PUT alias — `resources` draws both verbs
        to the same action).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.job_family_groups.update
      summary: Update a job family group.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.job_family_groups.destroy
      summary: Delete a job family group.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_levels":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_levels.index
      summary: List the organization's job levels, optionally filtered by job_role_id.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::JobLevelsController` is real,
        complete, and landed (#31, P2-7..N first\nlanding batch, Row 4/9). This stub
        is the minimum needed to satisfy G6 without authoring the\nfull contract,
        which is deferred to the same migration unit as row #159's openapi DSL blocks\n(this
        row keeps its source `openapi do...end` blocks in place per that ruling, though
        this\ncontroller happens to declare none). Drop `not_implemented` and add
        the real schemas when that\nunit is taken, not before.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_levels.create
      summary: Create a job level under a job role.
      description: Contract deferred — see the index operation's description.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_levels/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_levels.reorder
      summary: Reorder job levels by an ordered list of ids.
      description: |
        Contract deferred — see the index operation's description.

        `ids` must be sent in bracket form (`ids[]=5&ids[]=3`, or a JSON array body) — a bare
        `ids=5` parses as the scalar string "5", which the controller rejects with 422 rather than
        coercing into a single-element reorder.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_levels/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_levels.show
      summary: One job level.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.job_levels.update_put
      summary: Update a job level (PUT alias — `resources` draws both verbs to the
        same action).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.job_levels.update
      summary: Update a job level.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.job_levels.destroy
      summary: Delete a job level.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_profiles":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_profiles.index
      summary: List job profiles.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_profiles.create
      summary: Create a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_profiles/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_profiles.reorder
      summary: Reorder job profiles.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_profiles/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_profiles.show
      summary: Show a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.job_profiles.replace
      summary: Replace a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.job_profiles.update
      summary: Update a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.job_profiles.destroy
      summary: Delete a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_profiles/{job_profile_id}/skills":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.index
      summary: List skills on a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.create
      summary: Add a skill to a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '201':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_profiles/{job_profile_id}/skills/new":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.new
      summary: New job-profile skill form.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_profiles/{job_profile_id}/skills/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.show
      summary: Show a job-profile skill.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.replace
      summary: Replace a job-profile skill.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.update
      summary: Update a job-profile skill.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.destroy
      summary: Remove a skill from a job profile.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '204':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_profiles/{job_profile_id}/skills/{id}/edit":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_profile_skills.edit
      summary: Edit job-profile skill form.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/job_roles":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_roles.index
      summary: List the organization's job roles, optionally filtered by job_family_id.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::JobRolesController` is real,
        complete, and landed (#31, P2-7..N first\nlanding batch, Row 3/9). This stub
        is the minimum needed to satisfy G6 without authoring the\nfull contract,
        which is deferred to the same migration unit as row #159's openapi DSL blocks\n(this
        row keeps its source `openapi do...end` blocks in place per that ruling, though
        this\ncontroller happens to declare none). Drop `not_implemented` and add
        the real schemas when that\nunit is taken, not before.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_roles.create
      summary: Create a job role under a job family.
      description: Contract deferred — see the index operation's description.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_roles/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.job_roles.reorder
      summary: Reorder job roles by an ordered list of ids.
      description: |
        Contract deferred — see the index operation's description.

        `ids` must be sent in bracket form (`ids[]=5&ids[]=3`, or a JSON array body) — a bare
        `ids=5` parses as the scalar string "5", which the controller rejects with 422 rather than
        coercing into a single-element reorder.
      responses:
        '200':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/job_roles/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.job_roles.show
      summary: One job role.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.job_roles.update_put
      summary: Update a job role (PUT alias — `resources` draws both verbs to the
        same action).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.job_roles.update
      summary: Update a job role.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.job_roles.destroy
      summary: Delete a job role.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/member_skill_evaluations":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.member_skill_evaluations.index
      summary: List a member's skill evaluation history (?member_id=), oldest first.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::MemberSkillEvaluationsController`
        is real, complete, and landed (#31, P2-7..N\nfirst landing batch, Row 8/9).
        This stub is the minimum needed to satisfy G6 without authoring\nthe full
        contract, which is deferred to the same migration unit as row #159's openapi
        DSL\nblocks. Drop `not_implemented` and add the real schema when that unit
        is taken, not before.\n\n\U0001F534 Only `index` and `create` are routed —
        this is an append-only performance-matrix table (one\nrow per member × skill
        × assessed_at); there is no show/update/destroy.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.member_skill_evaluations.create
      summary: Upsert a skill evaluation for (member_id, taxonomy_skill_id, assessed_at).
      description: |
        Contract deferred — see the index operation's description.

        Upsert semantics: `find_or_initialize_by(member_id:, taxonomy_skill_id:, assessed_at:)` — a
        repeat call for the same (member, skill, timestamp) triple updates the existing row and
        returns 200, not 201; only the first call for a triple returns 201. This is the re-import path
        (idempotent per feed cycle), not a general update endpoint.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/members/{member_id}/career":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.member_careers.show
      summary: An admin's resolved Career Framework view for one member.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::MemberCareersController` is real,
        complete, and landed (#31, P2-7..N first\nlanding batch, Row 10/10 — the batch's
        final row). This stub is the minimum needed to satisfy\nG6 without authoring
        the full contract, which is deferred to the same migration unit as row\n#159's
        openapi DSL blocks. Drop `not_implemented` and add the real schema when that
        unit is\ntaken, not before.\n\n`career: null` is a DESIGNED response, not
        an edge case — it is returned whenever the member\nhas no assigned job level
        (or the assignment points at a deleted level), not only as an error\nfallback.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/skill_anchors":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.skill_anchors.index
      summary: List the organization's skill anchors, optionally filtered by skill_id
        or job_level_id.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::SkillAnchorsController` is real,
        complete, and landed (#31, P2-7..N first\nlanding batch, Row 7/9). This stub
        is the minimum needed to satisfy G6 without authoring the\nfull contract,
        which is deferred to the same migration unit as row #159's openapi DSL blocks\n(this
        row keeps its source `openapi do...end` blocks in place per that ruling, though
        this\ncontroller happens to declare none). Drop `not_implemented` and add
        the real schemas when that\nunit is taken, not before.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.skill_anchors.create
      summary: Upsert a skill anchor for a (skill, job level) pair.
      description: |
        Contract deferred — see the index operation's description.

        Upsert semantics: `find_or_initialize_by(taxonomy_skill_id:, taxonomy_job_level_id:)` — a
        repeat call for the same pair updates the existing anchor and returns 200, not 201; only the
        first call for a pair returns 201.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/skill_anchors/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.skill_anchors.show
      summary: One skill anchor.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.skill_anchors.update_put
      summary: Update a skill anchor (PUT alias — `resources` draws both verbs to
        the same action).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.skill_anchors.update
      summary: Update a skill anchor.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.skill_anchors.destroy
      summary: Delete a skill anchor.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/skill_categories/reorder":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - admin
      operationId: admin.taxonomy.skill_categories.reorder
      summary: Reorder skill categories by an ordered list of ids.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::SkillCategoriesController#reorder`
        is real, complete, and landed (#31,\nP2-7..N first landing batch, Row 6/9).
        This stub is the minimum needed to satisfy G6 without\nauthoring the full
        contract, which is deferred to the same migration unit as row #159's\nopenapi
        DSL blocks. Drop `not_implemented` and add the real schema when that unit
        is taken,\nnot before.\n\n\U0001F534 This is the ONLY operation on this path
        — CRUD (index/show/create/update/destroy) was\ndeliberately retired here and
        migrated to `Api::V3::Resources::Taxonomy::SkillCategoriesController`\n(already
        routed at `/api/v3/taxonomy/skill_categories`); do not expect the sibling
        verbs.\n\n\U0001F534 Gated behind the org-level CAREER_FRAMEWORK feature (rider
        #61), in addition to the ordinary\nApp-Mode/admin 403 — a 403 here can come
        from either gate, distinguishable only by the\nresponse body's error string.\n\n`ids`
        must be sent in bracket form (`ids[]=5&ids[]=3`, or a JSON array body) — a
        bare\n`ids=5` parses as the scalar string \"5\", which the controller rejects
        with 422 rather than\ncoercing into a single-element reorder.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/admin/taxonomy/skills":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.skills.index
      summary: List the organization's skills, optionally filtered by skill_category_id.
      description: "\U0001F534 `not_implemented: true` means \"contract deferred\",
        not \"controller stub\" —\n`Admin::Taxonomy::SkillsController` is real, complete,
        and landed (#31, P2-7..N first\nlanding batch, Row 5/9). This stub is the
        minimum needed to satisfy G6 without authoring the\nfull contract, which is
        deferred to the same migration unit as row #159's openapi DSL blocks\n(this
        row keeps its source `openapi do...end` blocks in place per that ruling, though
        this\ncontroller happens to declare none). Drop `not_implemented` and add
        the real schemas when that\nunit is taken, not before.\n\n\U0001F534 Every
        action on this controller (not just index) is gated behind the org-level\nCAREER_FRAMEWORK
        feature (rider #61) — OFF by default for every org — in addition to the\nordinary
        App-Mode/admin 403. A 403 here can come from EITHER gate; the two are\nindistinguishable
        by status code alone, only by the response body's error string.\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - admin
      operationId: admin.taxonomy.skills.create
      summary: Create a skill under a skill category.
      description: Contract deferred — see the index operation's description.
      responses:
        '201':
          description: Deferred — see the index operation's description.
  "/admin/taxonomy/skills/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.taxonomy.skills.show
      summary: One skill.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    put:
      tags:
      - admin
      operationId: admin.taxonomy.skills.update_put
      summary: Update a skill (PUT alias — `resources` draws both verbs to the same
        action).
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    patch:
      tags:
      - admin
      operationId: admin.taxonomy.skills.update
      summary: Update a skill.
      description: Contract deferred — see the index operation's description.
      responses:
        '200':
          description: Deferred — see the index operation's description.
    delete:
      tags:
      - admin
      operationId: admin.taxonomy.skills.destroy
      summary: Delete a skill.
      description: Contract deferred — see the index operation's description.
      responses:
        '204':
          description: Deferred — see the index operation's description.
  "/admin/training_resources":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.training_resources.index
      summary: List training resources for the organization.
      description: |
        `Api::V3::Admin::TrainingResourcesController` is real and complete (task #71, register #253
        pre-flight order). Two confirmed defects fixed from source: `tag_list` doesn't exist for this
        model's custom `:labels` taggable context (`label_list` does) — but `label_list` itself can't
        be preloaded via `.includes` (traced into the gem: `tag_list_on` always issues a fresh
        `.where` query, preloaded or not), so labels are bulk-fetched per page the same way
        `associatedCoursesCount` already is. `title` is Globalize-translated with no default_scope
        preload — `:translations` added to `.includes`, confirmed via raw query count.

        ⚠️ `sort` is accepted but not read by the controller — it always orders by `title`; only
        `order` (asc/desc) affects the result. Both `content_type` and `order` silently fall back to
        their defaults on an unrecognized value rather than rejecting the request.
      responses:
        '200':
          description: Training resources list.
          content:
            application/json:
              schema:
                type: object
                required:
                - trainingResources
                - meta
                additionalProperties: false
                properties:
                  trainingResources:
                    type: array
                    description: List of training resources.
                    items:
                      type: object
                      required:
                      - id
                      - title
                      - contentType
                      - labels
                      - hasQuiz
                      - isDefault
                      - associatedCoursesCount
                      - createdAt
                      - updatedAt
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: Resource ID.
                        title:
                          type: string
                          description: Resource title.
                        contentType:
                          type: string
                          enum:
                          - document
                          - scorm
                          - unknown
                          description: unknown covers any TrainingResource::ContentType
                            not in the document/scorm map.
                        labels:
                          type: array
                          items:
                            type: string
                          description: Tag/label names.
                        hasQuiz:
                          type: boolean
                          description: Whether resource has an attached quiz.
                        isDefault:
                          type: boolean
                          description: Whether resource is a Chronus default.
                        associatedCoursesCount:
                          type: integer
                          description: Number of published training plans using this
                            resource.
                        createdAt:
                          type:
                          - string
                          - 'null'
                          format: date-time
                        updatedAt:
                          type:
                          - string
                          - 'null'
                          format: date-time
                  meta:
                    "$ref": "#/components/schemas/TrainingResourcesPaginationMeta"
      parameters:
      - name: label
        in: query
        required: false
        schema:
          type: string
        description: Filter by tag/label name.
      - name: content_type
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by content type: document or scorm. Unrecognized values
          are ignored (no filter applied).'
      - name: sort
        in: query
        required: false
        schema:
          type: string
        description: Accepted but currently inert — see the operation description.
      - name: order
        in: query
        required: false
        schema:
          type: string
        description: 'Sort order: asc or desc. Default: asc; any other value also
          falls back to asc.'
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: 'Page number. Default: 1.'
  "/admin/translations":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.translations.index
      summary: List organization languages with status and program assignments.
      description: |
        `Api::V3::Admin::TranslationsController` is real and complete (register #68, pre-flighted
        CLEAN by dev-5, re-verified live before authoring). `OrganizationLanguage`/`Language`/
        `ProgramLanguage` live in `vendor/engines/chronus_translations/app/models/`, not
        `app/models/`. `OrganizationLanguage#program_ids_to_enable=` fires
        `handle_enabled_program_languages` via `OrganizationLanguageObserver#after_save`
        (`rails-observers`), not from within the model itself — confirmed live end-to-end.

        Every action 403s bare (`check_language_settings!`) when
        `Organization#language_settings_enabled?` (gated on `FeatureName::LANGUAGE_SETTINGS`) is off.

        NOT VERBATIM (fixed, found running this row): `index`'s program select was missing `:state`,
        which `Program#name`'s `archived_or_drafted?` override reads — the missing column raised
        `ActiveModel::MissingAttributeError` on every program, silently caught and reported via
        `ErrorAgent.notify_custom_error` on every request. `:state` was added to the select list;
        confirmed live this eliminates the notification with an identical rendered result.
      responses:
        '200':
          description: Languages list.
          content:
            application/json:
              schema:
                type: object
                required:
                - languages
                - programs
                additionalProperties: false
                properties:
                  languages:
                    type: array
                    description: Organization languages, including a synthetic English
                      entry first.
                    items:
                      "$ref": "#/components/schemas/OrganizationLanguageEntry"
                  programs:
                    type: array
                    description: Available programs for language assignment.
                    items:
                      type: object
                      required:
                      - id
                      - name
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                        name:
                          type: string
        '403':
          description: The organization's language-settings feature is disabled.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/admin/translations/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - admin
      operationId: admin.translations.replace
      summary: Update an organization language's status and program assignments.
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin carries the same contract so
        the `PUT` route is not fenced out of the spec — see the `PATCH` sibling above.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                enabled:
                  type: string
                  enum:
                  - enabled
                  - drafted
                  - disabled
                title:
                  type: string
                display_title:
                  type: string
                program_ids:
                  type: array
                  items:
                    type: integer
      responses:
        '200':
          description: Updated language.
          content:
            application/json:
              schema:
                type: object
                required:
                - language
                additionalProperties: false
                properties:
                  language:
                    "$ref": "#/components/schemas/OrganizationLanguageEntry"
        '403':
          description: The organization's language-settings feature is disabled.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: No organization language with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OrganizationLanguageErrors"
    patch:
      tags:
      - admin
      operationId: admin.translations.update
      summary: Update an organization language's status and program assignments.
      description: |
        Same controller, same findings as the index operation. `params.permit(:enabled, :title,
        :display_title, program_ids: [])` — these are the literal wire keys the controller reads;
        this is a hand-rolled admin controller, not schema-derived, so casing is not auto-converted.
        `enabled` accepts the wire strings `enabled`/`drafted`/`disabled` (mapped onto
        `OrganizationLanguage::EnabledFor`); any other value is coerced with `.to_i`, which is dead for
        a real caller and not documented here.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                enabled:
                  type: string
                  enum:
                  - enabled
                  - drafted
                  - disabled
                title:
                  type: string
                display_title:
                  type: string
                program_ids:
                  type: array
                  items:
                    type: integer
      responses:
        '200':
          description: Updated language.
          content:
            application/json:
              schema:
                type: object
                required:
                - language
                additionalProperties: false
                properties:
                  language:
                    "$ref": "#/components/schemas/OrganizationLanguageEntry"
        '403':
          description: The organization's language-settings feature is disabled.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: No organization language with this id in the caller's organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Model validation failed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OrganizationLanguageErrors"
  "/admin/workspace_settings":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: admin.workspace_settings.show
      summary: Full workspace settings snapshot — general, regional, security, features,
        branding, notifications, mobile.
      description: "\U0001F534 `not_implemented: true` — contract deferred per row
        #159. `Api::V3::Admin::\nWorkspaceSettingsController` is real and complete
        (dev-5, admin root family batch). Unblocked\nby `Api::V3::BrandingPresenter`
        and `FeatureAccessManager` (both dev-5, landed alone). Admin\ngate declared
        as `require_admin!` directly rather than porting source's\n`require_workspace_admin!`
        (source's own `alias_method :require_workspace_admin!,\n:require_admin!` —
        a naming-only alias, not a distinct tier; dest already enforces\n`require_admin!`
        at `Api::V3::Admin::ApplicationController`). Route per source: `get\n'workspace_settings',
        to: 'workspace_settings#show'; patch 'workspace_settings', to:\n'workspace_settings#update'
        ` — ROUTED on dest.\n\n`branding` reuses `Api::V3::BrandingPresenter` (the
        same shape served by the pre-auth\n`/api/v3/branding` endpoint and the post-auth
        session response), plus an admin-only\n`emailTheme` key. `features` lists
        only ORGANIZATION-level admin/entitled-tier features (via\n`FeatureAccessManager.features_by_tier`).\n"
      responses:
        '200':
          description: Deferred — see the operation description above.
    put:
      tags:
      - admin
      operationId: admin.workspace_settings.replace
      summary: Update one workspace settings namespace (general, security, branding,
        regional, notifications, mobile, or features).
      description: |
        `PUT` and `PATCH` route to the same controller action. This twin is declared so the `PUT` route
        is not fenced out of the spec — see the `PATCH` sibling above.
      responses:
        '200':
          description: Deferred — see the show operation's description.
        '422':
          description: Deferred — see the show operation's description.
    patch:
      tags:
      - admin
      operationId: admin.workspace_settings.update
      summary: Update one workspace settings namespace (general, security, branding,
        regional, notifications, mobile, or features).
      description: |
        Contract deferred — see the show operation's description. Body — `{ namespace, updates }`
        (shape of `updates` depends on `namespace`); 422 for an unknown namespace.

        `branding` writes get hex-color validation (`#RRGGBB` only, case-insensitive; the 3-char short
        form is rejected) on `primary_color`/`secondary_color` before reaching the config store — a
        `nil`/empty value clears the override instead of writing it. `security` writes are audit-
        logged via `ConfigAuditLog`. `features` writes are gated per-feature by
        `FeatureAccessManager.can_toggle?` — a feature the caller can't toggle is silently skipped,
        not rejected (matches source; a partial-success response for a multi-feature body is
        deliberate, not a bug).
      responses:
        '200':
          description: Deferred — see the show operation's description.
        '422':
          description: Deferred — see the show operation's description.
  "/admin_messages":
    "$ref": v3/resources/admin_messages.yaml#/paths/~1admin_messages
  "/admin_views":
    "$ref": v3/resources/admin_views.yaml#/paths/~1admin_views
  "/admin_views/{id}":
    "$ref": v3/resources/admin_views.yaml#/paths/~1admin_views~1{id}
  "/agents":
    "$ref": v3/resources/agents.yaml#/paths/~1agents
  "/agents/by_name/{name}":
    parameters:
    - name: name
      in: path
      required: true
      description: |
        Deliberately UNCONSTRAINED (`type: string`, no pattern, no enum). Source declared no schema
        for this param, so any constraint added here would become a middleware 400 in front of a
        controller that answers a genuine miss with 404 — converting a not-found into a bad-request
        for callers source never rejected. The route's own `/[^\/]+/` constraint is what admits
        dots and spaces, which Rails' default segment pattern would otherwise split as a format.
      schema:
        type: string
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - agents
      operationId: agents.by_name
      summary: Look up an active agent by its canonical name, case-insensitively.
      description: |
        An exact match on `LOWER(name)`, not a search — there is no partial or fuzzy matching. Lets a
        caller reach a seeded agent by name instead of hardcoding its id.
        ⚠️ The `400` is reachable ONLY through a segment that is non-empty in the URL but blank after
        `String#strip` — in practice a percent-encoded space (`by_name/%20`). An empty segment
        (`by_name/`) does not route here at all, and so 404s at the router rather than returning this
        400.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/LegacyAgentResponse"
        '400':
          description: The name segment was blank after trimming. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: No active agent of that name. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/agents/default":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - agents
      operationId: agents.default
      summary: The organization's primary active agent, falling back to any active
        agent.
      description: |
        Two-step resolution: the active `primary` agent if one exists, otherwise the FIRST active
        agent by no explicit order — so with several active agents and no primary, which one comes
        back is not deterministic. 404s only when the organization has no active agent at all.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/LegacyAgentResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: No active agent exists for the organization. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/agents/{agent_id}/conversations":
    parameters:
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - conversations
      operationId: agents.conversations.index
      summary: List conversations.
    post:
      tags:
      - conversations
      operationId: agents.conversations.create
      summary: Create a conversation.
  "/agents/{agent_id}/conversations/hydrate_demo":
    parameters:
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - conversations
      operationId: agents.conversations.hydrate_demo
      summary: Hydrate demo a conversation.
  "/agents/{agent_id}/conversations/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - conversations
      operationId: agents.conversations.show
      summary: Retrieve a conversation.
    put:
      tags:
      - conversations
      operationId: agents.conversations.replace
      summary: Replace a conversation.
    patch:
      tags:
      - conversations
      operationId: agents.conversations.update
      summary: Update a conversation.
    delete:
      tags:
      - conversations
      operationId: agents.conversations.destroy
      summary: Delete a conversation.
  "/agents/{agent_id}/conversations/{id}/archive":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - conversations
      operationId: agents.conversations.archive
      summary: Archive a conversation.
  "/agents/{agent_id}/conversations/{id}/messages":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - conversations
      operationId: agents.conversations.messages.create
      summary: Create a message.
  "/agents/{agent_id}/conversations/{id}/wear":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - conversations
      operationId: agents.conversations.wear
      summary: Wear a conversation.
  "/agents/{agent_id}/voice/rooms":
    parameters:
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - agents
      operationId: agents.voice.rooms.create
      summary: Create a room.
  "/agents/{agent_id}/voice/token":
    parameters:
    - "$ref": "#/components/parameters/AgentId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - agents
      operationId: agents.voice.token
      summary: Token a voice.
  "/agents/{id}":
    "$ref": v3/resources/agents.yaml#/paths/~1agents~1{id}
  "/agents/{id}/clone":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: agents.clone
      summary: Duplicate an agent.
      responses:
        '200':
          description: The cloned agent.
          content:
            application/json:
              schema:
                "$ref": v3/resources/agents.yaml#/components/schemas/ResourceEnvelopeAgentDetail
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
        '503':
          "$ref": "#/components/responses/ErrorResponse"
  "/ai_output_contests":
    "$ref": v3/resources/ai_output_contests.yaml#/paths/~1ai_output_contests
  "/ai_output_contests/{id}":
    "$ref": v3/resources/ai_output_contests.yaml#/paths/~1ai_output_contests~1{id}
  "/ai_output_contests/{id}/resolve":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: ai_output_contests.resolve
      summary: Resolve an ai output contest.
  "/analytics/mentoring":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - analytics
      operationId: analytics.mentoring.index
      summary: List mentoring.
  "/announcements":
    "$ref": v3/resources/announcements.yaml#/paths/~1announcements
  "/announcements/aggregates":
    "$ref": v3/resources/announcements.yaml#/paths/~1announcements~1aggregates
  "/announcements/test_email":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - test_emails
      operationId: announcements.test_email
      summary: Send a preview of an announcement to one or more program administrators.
      description: |
        Sends the announcement currently being composed, as a preview, to the given addresses. Creates no
        record and takes the unsaved `subject` (the announcement title) and `body` rather than an
        announcement id, so the form can send a test before the announcement exists.

        The announcement notification template is temporarily enabled for the duration of the send when the
        program has it turned off, matching legacy: a test send is how an admin verifies the content renders
        in Outlook/Gmail/Yahoo before publishing, and it would otherwise be silently suppressed.

        Recipients must be administrators of this program, at most 10 at a time. A malformed or non-admin
        address is reported as a `422` naming it — never silently dropped. Both a subject and a body are
        required, and an unknown merge tag is rejected before anything is sent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - subject
              - body
              - emails
              additionalProperties: false
              properties:
                subject:
                  type: string
                  description: The unsaved announcement title, merge tags included.
                body:
                  type: string
                  description: The unsaved HTML body, merge tags included.
                emails:
                  type: array
                  description: Recipient addresses. Each must belong to an administrator
                    of this program.
                  items:
                    type: string
      responses:
        '200':
          description: The test email was sent to each listed recipient.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AnnouncementTestEmailResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/announcements/{id}":
    "$ref": v3/resources/announcements.yaml#/paths/~1announcements~1{id}
  "/auth/token":
    post:
      tags:
      - auth
      operationId: auth.token
      summary: Issue an access token (OAuth 2.0 client_credentials grant).
      security: []
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
              - grant_type
              additionalProperties: true
              properties:
                grant_type:
                  type: string
                  enum:
                  - client_credentials
          application/json:
            schema:
              type: object
              required:
              - grant_type
              additionalProperties: true
              properties:
                grant_type:
                  type: string
                  enum:
                  - client_credentials
      responses:
        '200':
          description: Token issued.
          content:
            application/json:
              schema:
                type: object
                required:
                - access_token
                - token_type
                - expires_in
                additionalProperties: false
                properties:
                  access_token:
                    type: string
                  token_type:
                    type: string
                    enum:
                    - Bearer
                  expires_in:
                    type: integer
        '400':
          "$ref": "#/components/responses/OAuthErrorResponse"
        '401':
          "$ref": "#/components/responses/OAuthErrorResponse"
  "/authorized_applications":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - authorized_applications
      operationId: authorized_applications.index
      summary: List authorized applications.
  "/authorized_applications/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - authorized_applications
      operationId: authorized_applications.destroy
      summary: Delete an authorized application.
  "/bot_apps/actions":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - bot_apps
      operationId: bot_apps.events.action
      summary: Inbound Slack/Teams interactive action.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/bot_apps/events":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - bot_apps
      operationId: bot_apps.events.create
      summary: Inbound Slack/Teams event.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/bot_apps/streams/{thread_id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/ThreadId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - bot_apps
      operationId: bot_apps.streams.show
      summary: Short-poll stream drain for a bot-apps thread.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/bot_apps/teams/tab_session":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - tab_sessions
      operationId: bot_apps.teams.tab_session.create
      summary: Teams personal-tab SSO session.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/branding":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - branding
      operationId: branding.index
      summary: List branding.
  "/bulk_actions":
    "$ref": v3/resources/progress_statuses.yaml#/paths/~1bulk_actions
  "/bulk_actions/{id}":
    "$ref": v3/resources/progress_statuses.yaml#/paths/~1bulk_actions~1{id}
  "/bulk_actions/{id}/status":
    "$ref": v3/resources/progress_statuses.yaml#/paths/~1bulk_actions~1{id}~1status
  "/campaign_messages":
    "$ref": v3/resources/campaign_messages.yaml#/paths/~1campaign_messages
  "/campaign_messages/test_email":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - test_emails
      operationId: campaign_messages.test_email
      summary: Send a preview of a campaign email to one or more program administrators.
      description: |
        Sends the email currently being composed, as a preview, to the given addresses. Creates no record,
        and takes the unsaved `subject`/`body` rather than a message id — so the campaign studio can send a
        test before the campaign exists.

        `campaign_id` is optional: with it, merge tags are validated against that campaign; without it,
        against the program. The two are equivalent, because a campaign's supported tag set is delegated
        straight to its program.

        Recipients must be administrators of this program, at most 10 at a time. A malformed or non-admin
        address is reported as a `422` naming it — never silently dropped. Both a subject and a body are
        required, and an unknown merge tag or widget is rejected before anything is sent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - subject
              - body
              - emails
              additionalProperties: false
              properties:
                subject:
                  type: string
                  description: The unsaved subject line, merge tags included.
                body:
                  type: string
                  description: The unsaved HTML body, merge tags included.
                emails:
                  type: array
                  description: Recipient addresses. Each must belong to an administrator
                    of this program.
                  items:
                    type: string
                sender_id:
                  type:
                  - integer
                  - 'null'
                  description: 'Who the mail appears to come from. A null id (or omitting
                    it) sends as the program.

                    '
                campaign_id:
                  type:
                  - integer
                  - 'null'
                  description: |
                    The campaign whose tag set to validate against. Omit from the studio, where the
                    campaign does not exist yet; the program's tag set is used instead.
      responses:
        '200':
          description: The test email was sent to each listed recipient.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/CampaignMessageTestEmailResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/campaign_messages/{id}":
    "$ref": v3/resources/campaign_messages.yaml#/paths/~1campaign_messages~1{id}
  "/career_playbooks":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - resources
      operationId: career_playbooks.create
      summary: Create a career playbook.
  "/career_playbooks/current":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - resources
      operationId: career_playbooks.current.index
      summary: List current.
  "/career_playbooks/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - resources
      operationId: career_playbooks.show
      summary: Retrieve a career playbook.
  "/certificate_criteria_configs":
    "$ref": v3/resources/certificate_criteria_configs.yaml#/paths/~1certificate_criteria_configs
  "/certificate_criteria_configs/{id}":
    "$ref": v3/resources/certificate_criteria_configs.yaml#/paths/~1certificate_criteria_configs~1{id}
  "/certificate_templates":
    "$ref": v3/resources/certificate_templates.yaml#/paths/~1certificate_templates
  "/certificate_templates/{id}":
    "$ref": v3/resources/certificate_templates.yaml#/paths/~1certificate_templates~1{id}
  "/chapters":
    "$ref": v3/resources/groups.yaml#/paths/~1chapters
  "/chapters/{id}":
    "$ref": v3/resources/groups.yaml#/paths/~1chapters~1{id}
  "/chat_rooms":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - inbox
      operationId: inbox.chat_rooms.index
      summary: The caller's chat rooms, most recently active first.
      description: |
        Scoped through `current_member.chat_rooms` (has_many :through `chat_room_members`), so a room the
        member does not belong to is not merely hidden — it is unreachable.

        A DIRECT room has no stored name: `name` resolves to the other participant's name, falling back
        to "Unknown" when that member is unresolvable (a suspended or deleted counterpart). GROUP rooms
        render their stored name.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - chatRooms
                - meta
                additionalProperties: false
                properties:
                  chatRooms:
                    type: array
                    items:
                      "$ref": "#/components/schemas/InboxChatRoom"
                  meta:
                    "$ref": "#/components/schemas/InboxPaginationMeta"
      parameters:
      - "$ref": "#/components/parameters/Page"
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
          description: Per `PaginationConcern`'s default and clamp.
  "/chat_rooms/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - inbox
      operationId: inbox.chat_rooms.show
      summary: One chat room with a page of its messages, oldest-first.
      description: |
        Messages are fetched newest-first to apply the LIMIT, then reversed so the page renders
        chronologically — so `meta.page` 1 is the room's MOST RECENT page, not its first.

        Only root chats are returned (`parent_id: nil`); threaded replies are not part of this payload.

        Unlike `index`, this action preloads participants — `ChatRoomSerializer` returns an EMPTY
        participants array rather than N+1-ing when the association is unloaded, which would render a
        blank header that reads as "no participants" instead of as a bug.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - chatRoom
                - messages
                - meta
                additionalProperties: false
                properties:
                  chatRoom:
                    "$ref": "#/components/schemas/InboxChatRoom"
                  messages:
                    type: array
                    items:
                      "$ref": "#/components/schemas/InboxChat"
                  meta:
                    "$ref": "#/components/schemas/InboxPaginationMeta"
        '404':
          description: No such room, or the caller is not a member of it — deliberately
            indistinguishable.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
  "/chat_rooms/{id}/mark_read":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - inbox
      operationId: inbox.chat_rooms.mark_read
      summary: Marks every unread message in the room as read for the caller.
      description: |
        Idempotent — a second call is a no-op and still answers `{success: true}`.

        ⚠️ Session callers must send `X-Requested-With`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                additionalProperties: false
                properties:
                  success:
                    type: boolean
        '404':
          description: Not a member of this room.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
  "/chat_rooms/{id}/messages":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - inbox
      operationId: inbox.chat_rooms.create_message
      summary: Posts a message to a chat room.
      description: |
        Broadcasts over ActionCable on success (`send_broadcast: true`).

        A room with no active connection is legitimate — a closed connection's room stays readable — so
        the `ref_obj` lookup failing is a normal outcome, not an error path.

        ⚠️ Session callers must send `X-Requested-With` or the write 403s before the action runs.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - body
              properties:
                body:
                  type: string
                  description: Trimmed; must be non-blank and at most 10,000 characters.
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    "$ref": "#/components/schemas/InboxChat"
        '404':
          description: Not a member of this room.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
        '422':
          description: Blank body, body over 10,000 characters, or model validation
            failure.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
  "/check_in_cycles":
    "$ref": v3/resources/check_in_cycles.yaml#/paths/~1check_in_cycles
  "/check_in_cycles/{id}":
    "$ref": v3/resources/check_in_cycles.yaml#/paths/~1check_in_cycles~1{id}
  "/check_in_cycles/{id}/clone":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: check_in_cycles.clone
      summary: Duplicate a check-in cycle
      description: |
        Creates a new draft cycle copying population and description (TMO-T124 /
        A-CFG-10). Window dates are not carried over unless `opens_at` / `closes_at`
        are supplied; otherwise draft placeholders are used and the admin must set
        the real window before scheduling.
  "/check_in_reference_materials":
    "$ref": v3/resources/check_in_reference_materials.yaml#/paths/~1check_in_reference_materials
  "/check_in_reference_materials/{id}":
    "$ref": v3/resources/check_in_reference_materials.yaml#/paths/~1check_in_reference_materials~1{id}
  "/check_ins":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: check_ins.index
      summary: List the caller's check-in pairs
      description: |
        Member-facing index of pairs where the caller is employee or leader.
        Gated by PERFORMANCE_CHECK_INS (404 when off). View envelope.
  "/check_ins/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: check_ins.show
      summary: Show one check-in pair for a party member
      description: |
        Party-only read via CheckInContentProjection. Non-parties get 404.
        Wire shape mirrors report_check_in plus viewer_role.
  "/check_ins/{id}/meeting_link":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - resources
      operationId: check_ins.meeting_link
      summary: Meeting link a check in.
  "/check_ins/{id}/submissions":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - resources
      operationId: check_ins.submissions.create
      summary: Submit a check-in narrative for the caller's party role
      description: |
        Body `{ narrative: { summary, responses } }`. Runs NarrativeGuardrail for
        leaders, then CheckIns::SubmissionService. Idempotent per party.
  "/coaching_feedbacks":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - coaching_feedbacks
      operationId: coaching_feedbacks.index
      summary: Org-scoped aggregate of coaching-turn ratings (admin).
      description: |
        Returns recent Interaction ratings for members in the current organization, plus
        helpful/unhelpful counts. Admin-only. No Insights UI — customer-facing aggregates
        need minimum-cohort suppression (TMO-T09) first.
      responses:
        '200':
          description: Aggregate ratings for the organization.
          content:
            application/json:
              schema:
                type: object
                required:
                - feedbacks
                - summary
                additionalProperties: false
                properties:
                  feedbacks:
                    type: array
                    items:
                      "$ref": "#/components/schemas/CoachingFeedback"
                  summary:
                    "$ref": "#/components/schemas/CoachingFeedbackSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
    post:
      tags:
      - coaching_feedbacks
      operationId: coaching_feedbacks.create
      summary: Rate a coaching turn (helpful / unhelpful with optional comment).
      description: |
        Creates or updates the current member's rating for one AGENT coaching Interaction.
        Change-your-mind updates the existing unique (rater, rateable) row in place.
        Feedback is for Talent & Learning quality improvement only — not model training (Q13 / Q35).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - interaction_id
              - rating
              additionalProperties: false
              properties:
                interaction_id:
                  type: integer
                rating:
                  type: string
                  enum:
                  - helpful
                  - unhelpful
                reason_codes:
                  type: array
                  items:
                    type: string
                  description: Required when rating is unhelpful.
                comment:
                  type:
                  - string
                  - 'null'
      responses:
        '201':
          description: Rating persisted.
          content:
            application/json:
              schema:
                type: object
                required:
                - feedback
                additionalProperties: false
                properties:
                  feedback:
                    "$ref": "#/components/schemas/CoachingFeedback"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/commit_actions/{id}/complete":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: commit_actions.complete
      summary: Complete a commit action.
  "/compose":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - compose
      operationId: compose.create
      summary: Execute a bundle of resource queries.
      description: |
        Each key in `queries` is a label the client chooses; each value is a
        `ComposeQuerySpec`. Each per-query result carries its own HTTP status —
        partial failures do not break the bundle. The outer envelope is always
        200 unless the whole request was malformed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - queries
              additionalProperties: false
              properties:
                queries:
                  type: object
                  description: Map of client-chosen labels to query specifications.
                  minProperties: 1
                  maxProperties: 5
                  additionalProperties:
                    "$ref": "#/components/schemas/ComposeQuerySpec"
      responses:
        '200':
          description: Bundle executed; each per-query result has its own status.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - results
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  results:
                    type: object
                    description: Map keyed by the same labels supplied in the request.
                    additionalProperties:
                      "$ref": "#/components/schemas/ComposeQueryResult"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/connection_goals":
    "$ref": v3/resources/mentoring_model/goals.yaml#/paths/~1connection_goals
  "/connection_goals/{id}":
    "$ref": v3/resources/mentoring_model/goals.yaml#/paths/~1connection_goals~1{id}
  "/connection_goals/{id}/log_activity":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: connection_goals.log_activity
      summary: Log activity a connection goal.
  "/connection_messages":
    "$ref": v3/resources/chats.yaml#/paths/~1connection_messages
  "/connection_messages/{id}":
    "$ref": v3/resources/chats.yaml#/paths/~1connection_messages~1{id}
  "/connection_milestones":
    "$ref": v3/resources/mentoring_model/milestones.yaml#/paths/~1connection_milestones
  "/connection_milestones/{id}":
    "$ref": v3/resources/mentoring_model/milestones.yaml#/paths/~1connection_milestones~1{id}
  "/connection_private_notes":
    "$ref": v3/resources/connection/private_notes.yaml#/paths/~1connection_private_notes
  "/connection_private_notes/{id}":
    "$ref": v3/resources/connection/private_notes.yaml#/paths/~1connection_private_notes~1{id}
  "/connection_settings/current":
    "$ref": v3/resources/connection_settings.yaml#/paths/~1connection_settings~1current
  "/connection_tasks":
    "$ref": v3/resources/mentoring_model/tasks.yaml#/paths/~1connection_tasks
  "/connection_tasks/{id}":
    "$ref": v3/resources/mentoring_model/tasks.yaml#/paths/~1connection_tasks~1{id}
  "/connection_tasks/{id}/complete":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: connection_tasks.complete
      summary: Complete a connection task.
  "/connections":
    "$ref": v3/resources/groups.yaml#/paths/~1connections
  "/connections/performance_case_process":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - performance_case_process
      operationId: connections.performance_case_process.index
      summary: List performance case process.
  "/connections/reports":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: connections.reports.index
      summary: Leader Connections roster
      description: |
        ReportsQuery members plus per-report check-in summary fields for the SPA
        ReportSummary shape. Empty list when the caller has no leader visibility.
  "/connections/reports/readiness":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: connections.reports.readiness.index
      summary: List overall readiness reports.
  "/connections/reports/{member_id}/case_offer":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: connections.reports.case_offer.index
      summary: List case offer.
  "/connections/reports/{member_id}/check_in":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: connections.reports.check_in.index
      summary: List check in.
  "/connections/reports/{member_id}/goals":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: connections.reports.goals.index
      summary: List goals.
  "/connections/reports/{member_id}/goals/{goal_id}/support_offer":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/GoalId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - goals
      operationId: connections.reports.goals.support_offer
      summary: Support offer a goal.
  "/connections/reports/{member_id}/readiness":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: connections.reports.member_readiness.show
      summary: Retrieve readiness report for a specific member.
  "/connections/reports/{member_id}/talk_tracks":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: connections.reports.talk_tracks.index
      summary: List talk tracks.
  "/connections/reports/{member_id}/talk_tracks/act":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - talk_tracks
      operationId: connections.reports.talk_tracks.act
      summary: Act a talk track.
  "/connections/reports/{member_id}/talk_tracks/check":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - talk_tracks
      operationId: connections.reports.talk_tracks.check
      summary: Check a talk track.
  "/connections/reports/{member_id}/talk_tracks/drafts":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - talk_tracks
      operationId: connections.reports.talk_tracks.drafts.create
      summary: Create a draft.
  "/connections/{id}":
    "$ref": v3/resources/groups.yaml#/paths/~1connections~1{id}
  "/content/discover":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.discover.index
      summary: List discover.
  "/content/pages":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.pages.index
      summary: List pages.
  "/content/pages/{slug}":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.pages.show
      summary: Retrieve a page.
  "/content/pages/{slug}/ai_blocks/{block_id}/refresh":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.pages.ai_blocks.refresh
      summary: Refresh an ai block.
  "/content/pages/{slug}/directives/practise":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.pages.directives.practise
      summary: Practise a directive.
  "/content/pages/{slug}/directives/set_goal":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.pages.directives.set_goal
      summary: Set goal a directive.
  "/content/pages/{slug}/directives/start_check_in":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.pages.directives.start_check_in
      summary: Start check in a directive.
  "/content/roleplay/attempts":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.attempts.index
      summary: All attempts across every session the caller owns for one roleplay
        content — the content-scoped variant.
      description: |
        Query param `content_id` (required in practice — omitting it routes to the session-scoped
        behavior instead, which then requires `session_id`; sending NEITHER renders `"Session not
        found"` since `params[:session_id]` is blank). Used by the Overview page to show forward
        attempts and reverse reference runs side-by-side. Member-scoped the same way as the
        session-scoped variant above, via `Content::RoleplaySession.for_member`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - attempts
                additionalProperties: false
                properties:
                  attempts:
                    type: array
                    items:
                      "$ref": "#/components/schemas/RoleplayAttemptSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Session not found"` when `content_id` is omitted and `session_id`
            does not resolve either. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
      parameters:
      - name: content_id
        in: query
        required: false
        schema:
          type: integer
  "/content/roleplay/attempts/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.attempts.show
      summary: One roleplay attempt's full debrief — the caller's own, only.
      description: |
        Member-scoped via `find_attempt` (`session.member_id == current_member.id`) — a foreign
        attempt id renders the SAME `"Attempt not found"` 404 an unknown id would, never a 403 (which
        would confirm existence).
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - attempt
                additionalProperties: false
                properties:
                  attempt:
                    "$ref": "#/components/schemas/RoleplayAttemptDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Attempt not found"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/attempts/{id}/feedback":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.attempts.feedback
      summary: Feedback an attempt.
  "/content/roleplay/attempts/{id}/mark_readiness_viewed":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.attempts.mark_readiness_viewed
      summary: Mark readiness viewed an attempt.
  "/content/roleplay/attempts/{id}/messages":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.attempts.messages
      summary: One roleplay attempt's ordered message transcript — the caller's own,
        only.
      description: Same member-scoped `find_attempt` as `show` above.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - messages
                additionalProperties: false
                properties:
                  messages:
                    type: array
                    items:
                      "$ref": "#/components/schemas/RoleplaySharedMessage"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Attempt not found"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/attempts/{id}/messages/{message_id}/rephrase":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.attempts.messages.rephrase
      summary: Rephrase a message.
  "/content/roleplay/attempts/{id}/retry_feedback":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.attempts.retry_feedback
      summary: Retry feedback an attempt.
  "/content/roleplay/catalog":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.catalog.index
      summary: The member-facing catalog of published roleplays for the org — Discover
        → Roleplays.
      description: "NET-NEW on this port; no source counterpart. It exists because
        the SPA's engage surfaces were\ncalling `GET /api/v3/admin/contents?type=roleplay&status=published`
        directly\n(`packages/web/src/queries/user/roleplays/queries.ts`). `Api::V3::Admin::ContentsController`\ngates
        on `require_admin!`, which tests `app_mode == MANAGE`, and every `/new/*`
        page sends\n`App-Mode: engage` — so those reads 403'd as soon as the admin
        gate was hardened from source's\n`except: [:index, :show]` to unconditional.\n\n⛔
        The fix is NOT to reopen the admin endpoint. That `except:` did not narrow
        the inherited gate,\nit REPLACED it (ActiveSupport dedupes callbacks by kind+filter,
        ignoring the `:if`/`:unless` that\n`except:` compiles to), which is what let
        any authenticated member read the org's whole content\nlibrary, drafts included.
        And a client cannot work around the 403 by asking for `manage`:\napp_mode
        is resolved server-side and a non-admin requesting MANAGE is refused outright.\n\nAuthorization:
        `Api::V3::Content::Roleplay::BaseController` — authenticated member, no admin\nrole.
        The entire security surface is the scope, in one method:\n`Content::Roleplay`
        (the CLASS, so `?type=` cannot widen it) → `where(organization_id:)` →\n`visible`
        (`status: \"published\"`) → the personal-visibility exclusion.\n\n\U0001F534
        That exclusion uses `COALESCE(JSON_UNQUOTE(JSON_EXTRACT(type_data,'$.visibility')),
        '')`,\ncopied verbatim from the admin controller, and must NOT be replaced
        with the model's own\n`Content::Roleplay.public_only`. `public_only` compares
        without COALESCE, so for a row with no\n`visibility` key the comparison is
        SQL NULL, `NOT NULL` is NULL, and the row is DROPPED — which\nwould silently
        empty the catalog of exactly the roleplays an admin authored normally.\n`public_only`
        has zero callers anywhere in the app.\n\n`status` and `type` request params
        are deliberately unsupported: both are decided server-side.\n"
      responses:
        '200':
          description: Published roleplays in the caller's organization.
          content:
            application/json:
              schema:
                type: object
                required:
                - contents
                - meta
                properties:
                  contents:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - type
                      - title
                      - description
                      - status
                      - typeData
                      properties:
                        id:
                          type: integer
                        type:
                          type: string
                          enum:
                          - roleplay
                        title:
                          type: string
                          nullable: true
                        description:
                          type: string
                          description: Truncated to 300 characters, matching the admin
                            summary.
                        status:
                          type: string
                          enum:
                          - published
                        typeData:
                          type: object
                          nullable: true
                          additionalProperties: true
                        publishedAt:
                          type: string
                          format: date-time
                          nullable: true
                        updatedAt:
                          type: string
                          format: date-time
                          nullable: true
                  meta:
                    type: object
                    required:
                    - page
                    - perPage
                    - total
                    properties:
                      page:
                        type: integer
                      perPage:
                        type: integer
                      total:
                        type: integer
        '401':
          description: No authenticated member.
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
        description: 1-based page number; anything below 1 coerces to 1.
      - name: perPage
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
        description: Page size, default 50, capped at 100. An absent or non-positive
          value means the default.
  "/content/roleplay/catalog/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.catalog.show
      summary: One published roleplay from the member catalog.
      description: |
        Same scope as the index — org-scoped, published-only, personal excluded — so a draft, an archived
        row, another org's roleplay, or a non-roleplay content id are all indistinguishable 404s. That
        uniformity is deliberate: a 403-vs-404 split here would be an existence oracle across the tenant
        boundary.

        ⚠️ Returns the SUMMARY shape, not the admin detail shape. Personas, goals and materials are
        AUTHORING data and are served to the play surface by the sibling controllers in this namespace;
        `RoleplayContentApi` marks all three optional, so this satisfies the SPA's declared contract
        without growing a second admin-shaped serializer here.
      responses:
        '200':
          description: The roleplay.
          content:
            application/json:
              schema:
                type: object
                required:
                - content
                properties:
                  content:
                    type: object
                    required:
                    - id
                    - type
                    - title
                    - description
                    - status
                    - typeData
                    properties:
                      id:
                        type: integer
                      type:
                        type: string
                        enum:
                        - roleplay
                      title:
                        type: string
                        nullable: true
                      description:
                        type: string
                      status:
                        type: string
                        enum:
                        - published
                      typeData:
                        type: object
                        nullable: true
                        additionalProperties: true
                      publishedAt:
                        type: string
                        format: date-time
                        nullable: true
                      updatedAt:
                        type: string
                        format: date-time
                        nullable: true
        '401':
          description: No authenticated member.
        '404':
          description: Not found, not published, not this org's, or not a roleplay.
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
  "/content/roleplay/coaching_chat":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.coaching_chat
      summary: Coaching chat a roleplay.
  "/content/roleplay/discover":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.discover.index
      summary: List discover.
  "/content/roleplay/roleplays/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.roleplays.show
      summary: Retrieve a roleplay.
  "/content/roleplay/roleplays/{roleplay_id}/progress":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.progress.show
      summary: A member's cross-session readiness progress on one roleplay content
        — timeline + last-5 scorecards.
      description: "#28 Row 4, the last of the roleplay four. NOT `/api/v3/admin/content/roleplay/…`:
        the\ncontroller's own source header comment says `admin/` — the third of #161's
        three false\nadmin/ header comments in this family (shared_session's and this
        one are now fixed as\ncomments only; coaching's, #9, is still open). There
        is no admin gate here: `BaseController`\nrequires only an authenticated member,
        scoped entirely by the member-owns-this-session check\nbelow.\n\nMember-scoped
        via `Content::RoleplaySession.for_member(current_member.id).for_content(...)`
        —\nnever another member's sessions or attempts. Only `scored?` attempts contribute
        to the\ntimeline/scorecards; `in_progress`/`completed`-but-unscored attempts
        are excluded.\n\nNOT verbatim: source's content lookup has no organization
        scope at all. The member-scoped\nsession query already prevents any actual
        data leak (a foreign org's content id just yields\nempty progress, identical
        in shape to a genuinely unknown one) — but the STATUS CODE\ndiffered (200-empty
        vs 404), a low-severity existence oracle across the tenant boundary.\nAdded
        the same `.where(organization_id: ...)` scope `sessions_controller.rb#create`
        already\nestablishes for content lookups in this family.\n\n\U0001F534 `crossAttemptSummary`
        is ALWAYS `null` on this landing, and always was on source too:\n`cross_attempt_summary`
        has zero occurrences in `db/migrate/` — the column backing it was\nnever created.
        Both the sole writer (`ScoreAttemptJob`) and this reader guard with\n`respond_to?(:cross_attempt_summary)`,
        which is always false, so the branch that would\npopulate it never runs. A
        source-side abandoned feature, landed as-is per the ruling: don't\ninvent
        the column, don't remove the guard.\n\nLive-but-mostly-empty on landing: nothing
        on dest currently drives an attempt to `scored`\noutside a test (`ScoreAttemptJob`/`Conductor`
        are dark elsewhere in this port), so\n`totalAttempts` is `0` for every real
        caller today. Rider, not a blocker — the row lands\nregardless, per this family's
        own rule.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - progress
                additionalProperties: false
                properties:
                  progress:
                    type: object
                    required:
                    - totalAttempts
                    - readinessTimeline
                    - stackedScorecards
                    - crossAttemptSummary
                    additionalProperties: false
                    properties:
                      totalAttempts:
                        type: integer
                      readinessTimeline:
                        type: array
                        items:
                          "$ref": "#/components/schemas/RoleplayReadinessTimelineEntry"
                      stackedScorecards:
                        type: array
                        description: The caller's last 5 scored attempts, oldest first.
                        items:
                          "$ref": "#/components/schemas/RoleplayStackedScorecard"
                      crossAttemptSummary:
                        type: 'null'
                        description: Always `null` — see the operation description;
                          the backing column was never migrated.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Content not found"` — no roleplay content with this id in
            the caller''s organization. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/sessions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.sessions.index
      summary: The caller's own roleplay sessions, newest first.
      description: |
        #28 Row 2 — drops `AgentTokenAuthenticatable` from this whole controller (see the `POST .../end`
        entry below for the security-relevant detail). This action itself is unaffected by that: it
        only ever serves the standard member/Doorkeeper-authenticated caller.

        Member-scoped via `Content::RoleplaySession.for_member(current_member.id)` before pagination —
        never another member's sessions. Optional `content_id` narrows to one roleplay content's
        sessions (forward + reverse both included; this endpoint does not filter by `sessionKind`).
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - sessions
                - meta
                additionalProperties: false
                properties:
                  sessions:
                    type: array
                    items:
                      "$ref": "#/components/schemas/RoleplaySharedSession"
                  meta:
                    "$ref": "#/components/schemas/RoleplayPaginationMeta"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: content_id
        in: query
        required: false
        schema:
          type: integer
      - "$ref": "#/components/parameters/Page"
      - "$ref": "#/components/parameters/PerPage"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.create
      summary: Start a new roleplay session (+ its first attempt) on one roleplay
        content.
      description: |
        Org-scope guard: `content_id` is looked up within the CALLER's own organization — the
        Conductor performs no tenant check of its own, so without this a member could start a session
        on cross-tenant content by guessing an id. Any authenticated member may create a session; there
        is no admin-only gate (ROLEPLAY-T01 moved this whole surface out from under `/admin`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - content_id
              additionalProperties: false
              properties:
                content_id:
                  type: integer
                mode:
                  type: string
                  description: Defaults to `"text"` when omitted.
                source_surface:
                  type: string
                  description: |
                    Entry surface the Start came from (`?src=` on legacy links, forwarded by the SPA).
                    Deliberately NOT an enum: `ChrTrackData::Source::Roleplay.normalize` collapses
                    anything outside its closed set to `unknown`, so a spec enum would 400 the request
                    the server is designed to accept and normalise.
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - session
                - attempt
                additionalProperties: false
                properties:
                  session:
                    "$ref": "#/components/schemas/RoleplaySessionDetail"
                  attempt:
                    "$ref": "#/components/schemas/RoleplayAttemptSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Content not found"` — no roleplay content with this id in
            the caller''s organization. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: '`{"error": "<Conductor rate-limit message>"}` — e.g. max concurrent/daily
            sessions reached. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/sessions/preview":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.preview
      summary: Preview a session.
  "/content/roleplay/sessions/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.sessions.show
      summary: One roleplay session's full detail — the caller's own, only.
      description: |
        Member-scoped the same way as `index` — a foreign session id renders `"Session not found"`,
        never another member's session.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - session
                additionalProperties: false
                properties:
                  session:
                    "$ref": "#/components/schemas/RoleplaySessionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Session not found"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/sessions/{id}/abandon":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.abandon
      summary: Abandon a session.
  "/content/roleplay/sessions/{id}/coach/invoke":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.coach.invoke
      summary: Invoke a coach.
  "/content/roleplay/sessions/{id}/coaching_assist":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.coaching_assist
      summary: Coaching assist a session.
  "/content/roleplay/sessions/{id}/end":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.end_session
      summary: End a roleplay session — completes its last attempt and enqueues scoring
        (or, for a reverse session, highlight generation).
      description: "\U0001F534 NOT verbatim — a feature is dropped, not just a route
        detail: source also lets the LiveKit\nvoice agent authenticate here with a
        roleplay_agent message-verifier Bearer token, via\n`AgentTokenAuthenticatable#find_current_member`.
        That module is DROPPED on this landing: it\noverrides a hook (`find_current_member`)
        dest's auth stack does not have — dest resolves\n`current_member` from `auth_context&.member`,
        not a per-controller override chain, so the\nmodule's own `super` call would
        `NameError` if included as-is. The controller body itself never\ncalls any
        of the module's methods directly, so dropping it is a clean omission (verified,
        not\nassumed) — but it means the voice agent's own end-of-call request (no
        session cookie) 401s here\nuntil the voice/LiveKit unit re-adds an equivalent.
        Every other caller (the standard\nmember/Doorkeeper-authenticated one) is
        unaffected.\n\nMember-scoped via the same `find_session` as `show`. `422`
        when the session isn't in a state\n`may_complete?` allows (already ended),
        or when it has no attempt yet.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - session
                - attempt
                additionalProperties: false
                properties:
                  session:
                    "$ref": "#/components/schemas/RoleplaySessionDetail"
                  attempt:
                    "$ref": "#/components/schemas/RoleplayAttemptSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Session not found"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: '`"Session cannot be ended from current status"` or `"No attempt
            found"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/sessions/{id}/messages":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.messages.create
      summary: Create a message.
  "/content/roleplay/sessions/{id}/nudge_message":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.nudge_message
      summary: Nudge message a session.
  "/content/roleplay/sessions/{id}/restart":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.restart
      summary: Restart a session.
  "/content/roleplay/sessions/{id}/reverse":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.reverse_sessions.create
      summary: Start a reverse roleplay session from a scored forward attempt — Rumi
        plays the learner, the human plays the persona.
      description: |
        CLEAN row (#28 Row 3) — landed verbatim; no security fix needed. `{id}` is the SOURCE
        (forward) session's own id — despite sitting inside `resources :sessions`, this is a `member`
        route, not a nested-resource one, so it binds the singular `:id`, not `:session_id`. Authority
        is ownership of the source: `Content::RoleplaySession.for_member(current_member.id)` before the
        session lookup even runs, so a foreign `id` renders `"Session not found"`, never another
        member's session or attempt.

        Gate is verb-correct: the source session must be `sessionKind: forward` (reverse sessions
        cannot themselves be reversed) and its `source_attempt_id` attempt must be `scored?` — a
        debrief the caller hasn't finished reviewing/scoring yet cannot spawn a reverse demo from it.

        Idempotent within 30 minutes: if a reverse session already exists for this source session and
        still has an in-progress attempt, it is returned (`reused: true`, `200`) instead of creating a
        duplicate; otherwise a new reverse session + attempt is created (`reused: false`, `201`). Unlike
        `POST /sessions` and `.../end`, the response carries only the new attempt's bare `attemptId`,
        not a rendered attempt object — verbatim from source, which has nothing to show yet for a
        freshly spawned reverse attempt.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - source_attempt_id
              additionalProperties: false
              properties:
                source_attempt_id:
                  type: integer
      responses:
        '200':
          description: 'An existing, still-in-progress reverse session was reused
            (`reused: true`).'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ReverseSessionResult"
        '201':
          description: 'A new reverse session + attempt was created (`reused: false`).'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ReverseSessionResult"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Session not found"` (source session, member-scoped) or `"Source
            attempt not found"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: '`"Source attempt must be scored before starting reverse"`.
            Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/sessions/{id}/stream":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.sessions.stream.index
      summary: List stream.
  "/content/roleplay/sessions/{id}/suggest_turn":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.sessions.suggest_turn
      summary: Suggest turn a session.
  "/content/roleplay/sessions/{session_id}/attempts":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.sessions.attempts.index
      summary: All attempts for one roleplay session, oldest first.
      description: |
        CLEAN row — landed verbatim, no security fix needed here (its own idiom is what row #41's
        fix on the sibling `shared_session_controller.rb` was modeled on). Member-scoped via
        `Content::RoleplaySession.for_member(current_member.id)` before the session lookup even runs,
        so a foreign `session_id` renders `"Session not found"`, never another member's attempts.

        ⚠️ TWO SEPARATE ROUTES reach `AttemptsController#index` — this one (session-scoped, nested
        under `sessions`) and the content-scoped one below (`GET /attempts?content_id=`, a query
        param, not a path segment). Same action, same response shape, different scoping query.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - attempts
                additionalProperties: false
                properties:
                  attempts:
                    type: array
                    items:
                      "$ref": "#/components/schemas/RoleplayAttemptSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Session not found"` — no session with this id owned by the
            calling member. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/shared_sessions/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.roleplay.shared_sessions.show
      summary: A completed roleplay attempt's full debrief transcript — the caller's
        own, only.
      description: "\U0001F534 #41 (ruled — the port's only ruled-and-unfixed security
        defect, fixed on this landing):\nsource scoped this lookup to the caller's
        ORGANIZATION only, with no member/share-token check\n— any authenticated member
        of the org could enumerate `id` and read another member's roleplay\ntranscript.
        `Content::RoleplayAttempt` has no `member_id` of its own; scoped instead to
        the\ncalling member's OWN sessions (`Content::RoleplaySession.for_member(current_member.id)`),
        the\nsame idiom the sibling `attempts_controller.rb#find_attempt` already
        uses on source. The\npre-existing organization check is KEPT alongside the
        new member scope, not replaced by it —\nit guards a different axis (the content's
        `organization_id`) and costs one branch.\n\nNOT `/api/v3/admin/...`: despite
        the controller's own source header comment (also wrong about\nthe id param
        name), this sits under `namespace :content > namespace :roleplay` with no
        admin\ngate — `Api::V3::Content::Roleplay::BaseController` requires only an
        authenticated member,\nscoped entirely by the member-owns-this-session check
        above. NOT `/api/v3/rumi/...` either —\na follow-up on the original landing
        (ab9341872b1) found that path claim was itself wrong: a\nprecise do/end nesting
        check showed `scope 'rumi'` (source routes.rb:401-408) is a closed\nSIBLING
        block, not an ancestor of this namespace.\n\nBoth denial branches render the
        bare `{\"error\": \"Not found\"}` shape at 404 — never 403, since\n403 would
        confirm someone else's attempt id EXISTS (an existence oracle, a lesser version
        of\nthe same defect this fixes).\n\n\U0001F534 Live-but-empty on landing,
        a rider not a blocker: nothing on dest currently CREATES a\n`Content::RoleplayAttempt`
        row (the writer, `Conductor`, has zero callers; the endpoints that\nwould
        create one through the player — sessions/attempts — are separately unported),
        so this\nendpoint 404s for every id today. The security fix is real and lands
        with the file regardless\nof reachability, per the row's own rule.\n\n`content.title`/`description`/`goal`/`userRole`
        can render `null` even though the DB's\n`contents.title` column is `NOT NULL`
        — `Content::Roleplay` overrides these to read `type_data`\nJSON instead (identical
        behavior on both trees; not a port defect).\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - attempt
                - session
                - messages
                - content
                additionalProperties: false
                properties:
                  attempt:
                    "$ref": "#/components/schemas/RoleplayAttemptDetail"
                  session:
                    "$ref": "#/components/schemas/RoleplaySharedSession"
                  messages:
                    type: array
                    items:
                      "$ref": "#/components/schemas/RoleplaySharedMessage"
                  content:
                    type: object
                    required:
                    - title
                    - description
                    - goal
                    - userRole
                    additionalProperties: false
                    properties:
                      title:
                        type:
                        - string
                        - 'null'
                      description:
                        type:
                        - string
                        - 'null'
                      goal:
                        type:
                        - string
                        - 'null'
                      userRole:
                        type:
                        - string
                        - 'null'
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: |
            Two causes render the SAME bare shape: the attempt doesn't exist, or exists but isn't
            owned by the calling member (the #41 fix — this is the security-relevant branch), or its
            session's content belongs to a different organization (the pre-existing, kept check).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/content/roleplay/voice/preview_rooms":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.voice.preview_rooms.create
      summary: Create a preview room.
  "/content/roleplay/voice/rooms":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.voice.rooms.create
      summary: Create a room.
  "/content/roleplay/voice/token":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - content
      operationId: content.roleplay.voice.token
      summary: Token a voice.
  "/content/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - content
      operationId: content.show
      summary: Retrieve a content.
  "/content_flags":
    "$ref": v3/resources/content_flags.yaml#/paths/~1content_flags
  "/content_flags/{id}":
    "$ref": v3/resources/content_flags.yaml#/paths/~1content_flags~1{id}
  "/conversations":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - all_conversations
      operationId: all_conversations.index
      summary: The caller's own AI-agent conversations across the organization, newest
        first.
      description: |
        F38 Row A (#16). Hand-written, not an Interface-stack resource — `AllConversationsController`
        inherits `Api::V3::BaseController` directly (the org-level base, not
        `Programs::Member::BaseController`), so this endpoint is reachable with no program/enrollment
        context at all; it is scoped entirely by `Interaction.for_member(current_member.id)`.

        NOT verbatim: source's feature gate accepts EITHER `AI_AGENTS` or `RUMI_MAAS`. Dest's own
        committed `RUMI_MAAS` description reads "Companion to AI_AGENTS; both must be ON" — an OR
        contradicts that, so this requires `AI_AGENTS` alone (strictly tighter than verbatim on a
        never-shipped endpoint; cannot regress anything). Confirmed the RUMI_MAAS description's further
        claim — that it additionally admits members without a program enrollment — is vacuous for this
        row: it has no enrollment/program logic at all, and dest's tenant resolver never attaches a
        program to a request without a `/p/<root>/` prefix, which this endpoint's flat path never has.

        Only `active` conversations are listed (`with_status("active")`); `agentId` is scoped to the
        caller's own organization's agents (`Agent.for_organization`) before the interaction lookup even
        runs, so a foreign-org agent's conversations are never reachable regardless of member id.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - conversations
                - meta
                additionalProperties: false
                properties:
                  conversations:
                    type: array
                    items:
                      "$ref": "#/components/schemas/AgentConversationSummary"
                  meta:
                    type: object
                    required:
                    - total
                    - page
                    - perPage
                    - totalPages
                    additionalProperties: false
                    properties:
                      total:
                        type: integer
                      page:
                        type: integer
                      perPage:
                        type: integer
                      totalPages:
                        type: integer
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: '`"Feature not available"` — the organization does not have
            AI_AGENTS enabled. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
      parameters:
      - "$ref": "#/components/parameters/Page"
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
          description: Defaults to 25; clamped to [1, 500].
  "/demo_reseeds":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - demo_reseeds
      operationId: demo_reseeds.create
      summary: Create a demo reseed.
  "/demo_tech_runbook_resets":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - demo_tech_runbook_resets
      operationId: demo_tech_runbook_resets.create
      summary: Create a demo tech runbook reset.
  "/design_editor/attachments":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - design_editor
      operationId: design_editor.attachments.index
      summary: Browse the program's design-studio images (stock + uploaded).
      description: |
        Lists the caller program's uploaded design-studio images of the requested `used_for`, MERGED with
        the built-in stock images/backgrounds, paginated. Stock entries carry `is_stock: true` and a null
        `id`/`attachment_id`. **Manage-only**, program-scoped.

        Chronus-only (global) assets are listed only in the super console, and only for
        `issued_for_type=training_plan` — matching the legacy picker's `default: false` filter.
      responses:
        '200':
          description: A page of the program's images of the requested used_for (stock
            + uploaded).
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                - pagination
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    type: array
                    items:
                      "$ref": "#/components/schemas/DesignEditorAttachment"
                  pagination:
                    type: object
                    required:
                    - total
                    - page
                    - limit
                    - has_more
                    additionalProperties: false
                    properties:
                      total:
                        type: integer
                      page:
                        type: integer
                      limit:
                        type: integer
                      has_more:
                        type: boolean
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: used_for
        in: query
        required: true
        description: Which image library to browse.
        schema:
          type: string
          enum:
          - background_image
          - image
      - name: issued_for_type
        in: query
        required: false
        description: Which certificate type's studio is browsing. Only used to decide
          whether Chronus-only assets are visible; omit it and they are excluded.
        schema:
          type: string
          enum:
          - training_plan
          - connection
      - name: page
        in: query
        required: false
        description: 1-based page number (default 1).
        schema:
          type: integer
          minimum: 1
      - name: limit
        in: query
        required: false
        description: Page size (1–100; default 25; out-of-range falls back to 25).
        schema:
          type: integer
          minimum: 1
          maximum: 100
    post:
      tags:
      - design_editor
      operationId: design_editor.attachments.create
      summary: Upload an image for the certificate design studio.
      description: |
        Stores an uploaded image to S3 via the ChronusDesignEditor::Attachment model, tenant-stamped to
        the caller's program, and returns the serving URL. `used_for` selects the library
        (`background_image` or `image`). Enforces the same size/content-type/extension limits as the legacy
        design editor. **Manage-only.**

        A retried POST with the same `Idempotency-Key` and body replays the stored `201` exactly once.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              - used_for
              additionalProperties: false
              properties:
                file:
                  type: string
                  format: binary
                  description: The image to upload.
                used_for:
                  type: string
                  enum:
                  - background_image
                  - image
                  description: "`background_image` → a canvas background; `image`
                    → an inline image."
                issued_for_type:
                  type: string
                  enum:
                  - training_plan
                  - connection
                  description: Which certificate type's studio is uploading. Only
                    used to gate `chronus_only`; omit it and the upload is a plain
                    program asset.
                chronus_only:
                  type: string
                  enum:
                  - 'true'
                  - 'false'
                  description: Mark the upload a Chronus-shipped (global) default
                    asset, visible across programs. Honoured only in the super console
                    and only for `issued_for_type=training_plan` (legacy's "make this
                    a Chronus asset" gate); ignored otherwise. A multipart form value,
                    so it's a STRING ("true"/"false"), not a JSON boolean; the controller
                    casts it. (Named `chronus_only`, not `default` — the latter is
                    a reserved JSON Schema keyword and can't be a property.)
      responses:
        '201':
          description: The image was stored; the serving URL is returned.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    "$ref": "#/components/schemas/DesignEditorAttachment"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '409':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - "$ref": "#/components/parameters/IdempotencyKey"
  "/design_editor/attachments/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - design_editor
      operationId: design_editor.attachments.destroy
      summary: Delete one uploaded design-studio image.
      description: |
        Deletes one of the caller program's uploaded images by id. **Manage-only**, program-scoped. An
        image still referenced by a certificate template can't be deleted and returns `409` with
        `error.code: "image_in_use"` — that code is the discriminator to branch on. The linked-template
        tally rides along as a supplementary `error.details` entry,
        `{ field: "certificate_templates", message: "<count>" }`, purely so a client can render the
        singular/plural message; treat it as optional and never surface it as prose.
      responses:
        '200':
          description: The image was deleted.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    type: object
                    required:
                    - id
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '409':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: id
        in: path
        required: true
        description: The uploaded image's attachment id.
        schema:
          type: integer
      - name: issued_for_type
        in: query
        required: false
        description: Which certificate type's studio is deleting. Only used to decide
          whether Chronus-only assets are reachable; omit it and deleting one returns
          `404`.
        schema:
          type: string
          enum:
          - training_plan
          - connection
  "/design_editor/config":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - design_editor
      operationId: design_editor.config.show
      summary: The certificate design studio's editor configuration.
      description: |
        Returns everything the SPA design editor needs to render its toolbars and seed a blank canvas for
        the given `issued_for_type`: the available fonts and font sizes, the background colour palette, the
        stock background/clipart image URLs, the applicable merge tags (each with a human label and a
        program-contextual example), and the type's default design JSON. **Manage-only** (the same gate as
        managing certificate templates), program-scoped.

        `connection` always includes the `certificate_details` merge tag; `training_plan` includes it only
        in the super console, matching the legacy editor's tag gating.
      responses:
        '200':
          description: The editor configuration for the requested type.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    "$ref": "#/components/schemas/DesignEditorConfig"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: issued_for_type
        in: query
        required: true
        description: Which certificate type's configuration to return.
        schema:
          type: string
          enum:
          - training_plan
          - connection
  "/discover/members":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.discover.members
      summary: Discover members the caller could connect with.
      responses:
        '200':
          description: The discover/members view envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/DiscoverMembersViewResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/discover/programs":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.discover.programs
      summary: Discover programs the caller could join.
      responses:
        '200':
          description: The discover/programs view envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/DiscoverProgramsViewResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/eligibility_screenings":
    "$ref": v3/resources/eligibility_screenings.yaml#/paths/~1eligibility_screenings
  "/eligibility_screenings/aggregates":
    "$ref": v3/resources/eligibility_screenings.yaml#/paths/~1eligibility_screenings~1aggregates
  "/eligibility_screenings/{id}":
    "$ref": v3/resources/eligibility_screenings.yaml#/paths/~1eligibility_screenings~1{id}
  "/email_campaigns":
    "$ref": v3/resources/email_campaigns.yaml#/paths/~1email_campaigns
  "/email_campaigns/aggregates":
    "$ref": v3/resources/email_campaigns.yaml#/paths/~1email_campaigns~1aggregates
  "/email_campaigns/{id}":
    "$ref": v3/resources/email_campaigns.yaml#/paths/~1email_campaigns~1{id}
  "/ergs":
    "$ref": v3/resources/ergs.yaml#/paths/~1ergs
  "/events":
    "$ref": v3/resources/events.yaml#/paths/~1events
  "/events/discover":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: events.discover
      summary: List discoverable upcoming events for the current member (not yet invited
        to).
      description: Same query params and response shape as `my`. Shows every published,
        upcoming event in the organization — it does not filter by program membership
        or by invite list; that breadth is the controller's own `discover` semantic,
        not narrowed here.
      responses:
        '200':
          description: Paginated list of discoverable events.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MemberEventsEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: Page number (default 1).
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: Items per page (default 25, max 100).
  "/events/my":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - admin
      operationId: events.my
      summary: List the current member's upcoming events they're invited to.
      responses:
        '200':
          description: Paginated list of the member's events.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MemberEventsEnvelope"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
        description: Page number (default 1).
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
        description: Items per page (default 25, max 100).
  "/events/{id}":
    "$ref": v3/resources/events.yaml#/paths/~1events~1{id}
  "/events/{id}/publish":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: events.publish
      summary: Publish an event.
  "/flags":
    "$ref": v3/resources/flags.yaml#/paths/~1flags
  "/flags/aggregates":
    "$ref": v3/resources/flags.yaml#/paths/~1flags~1aggregates
  "/flags/{id}":
    "$ref": v3/resources/flags.yaml#/paths/~1flags~1{id}
  "/for_you":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: for_you.index
      summary: List for you.
  "/for_you/check_ins/{check_in_id}/engage":
    parameters:
    - "$ref": "#/components/parameters/CheckInId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - for_you
      operationId: for_you.check_ins.engage
      summary: Engage a check in.
  "/for_you/nudges/{interaction_id}/act":
    parameters:
    - "$ref": "#/components/parameters/InteractionId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - for_you
      operationId: for_you.nudges.act
      summary: Act a nudge.
  "/goal_exemplars":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - goal_exemplars
      operationId: goalExemplars.index
      summary: Strong goal exemplars for the signed-in member
      description: |
        Always via Goals::ExemplarResolver.for_member. Structurally strong-only;
        empty list when none are configured. Weak exemplars never appear here.
      responses:
        '200':
          description: Strong exemplars for the member's role/level fallback ladder.
          content:
            application/json:
              schema:
                type: object
                required:
                - profileLabel
                - exemplars
                additionalProperties: false
                properties:
                  profileLabel:
                    type:
                    - string
                    - 'null'
                  exemplars:
                    type: array
                    items:
                      "$ref": "#/components/schemas/GoalExemplar"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/goals":
    "$ref": v3/resources/goals.yaml#/paths/~1goals
  "/goals/available_skills":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1available_skills
  "/goals/dismiss_suggestion":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1dismiss_suggestion
  "/goals/suggestions":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1suggestions
  "/goals/{id}":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1{id}
  "/goals/{id}/attach_evidence":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1{id}~1attach_evidence
  "/goals/{id}/close":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: goals.close
      summary: Close a goal.
  "/goals/{id}/evidence":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1{id}~1evidence
  "/goals/{id}/quality_score":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1{id}~1quality_score
  "/goals/{id}/recommendations":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1{id}~1recommendations
  "/goals/{id}/replace_skills":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1{id}~1replace_skills
  "/goals/{id}/skill_focus_suggestions":
    "$ref": v3/resources/goals.yaml#/paths/~1goals~1{id}~1skill_focus_suggestions
  "/group_views":
    "$ref": v3/resources/group_views.yaml#/paths/~1group_views
  "/inbox/badge_counts":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - inbox
      operationId: inbox.badge_counts
      summary: Unread counts for the member's chats, admin-message threads and push
        notifications.
      description: |
        Feeds the inbox bell in the persistent user footer (`pages/user-layout/UserFooterMenu.tsx`),
        which fetches on mount — so this is the first v3 call a member makes on any page.

        `messages` counts DISTINCT threads, not messages: a 40-reply thread is one unread item, matching
        what the UI renders. `chats` counts unread `ChatReceiver` rows across every room the member
        belongs to; `notifications` counts `push_based` unread only, so email-only notification rows
        never inflate the badge.
      responses:
        '200':
          description: The three counts plus their sum.
          content:
            application/json:
              schema:
                type: object
                required:
                - chats
                - messages
                - notifications
                - total
                additionalProperties: false
                properties:
                  chats:
                    type: integer
                  messages:
                    type: integer
                    description: Distinct unread THREADS, not messages.
                  notifications:
                    type: integer
                  total:
                    type: integer
                    description: Sum of the three.
  "/inbox/notifications":
    "$ref": v3/resources/inbox/notifications.yaml#/paths/~1inbox~1notifications
  "/inbox/notifications/{id}/read":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - commands
      operationId: inbox.notifications.read.replace
      summary: Replace a read.
  "/mailer_templates":
    "$ref": v3/resources/mailer_templates.yaml#/paths/~1mailer_templates
  "/mailer_templates/{uid}":
    "$ref": v3/resources/mailer_templates.yaml#/paths/~1mailer_templates~1{uid}
  "/mailer_templates/{uid}/preview_emails":
    "$ref": v3/resources/mailer_templates.yaml#/paths/~1mailer_templates~1{uid}~1preview_emails
  "/mailer_widgets":
    "$ref": v3/resources/mailer_widgets.yaml#/paths/~1mailer_widgets
  "/mailer_widgets/{uid}":
    "$ref": v3/resources/mailer_widgets.yaml#/paths/~1mailer_widgets~1{uid}
  "/manager/reports":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - manager_reports
      operationId: manager.reports.index
      summary: List reports.
  "/manager/reports/practice_invitations":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - practice_invitations
      operationId: manager.reports.practice_invitations.create
      summary: Create a practice invitation.
  "/manager/reports/{member_id}/career":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - manager_career
      operationId: manager.reports.career.index
      summary: List career.
  "/manager/reports/{member_id}/notes":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - manager_report_workspace
      operationId: manager.reports.notes.create
      summary: Create a note.
  "/manager/reports/{member_id}/workspace":
    parameters:
    - "$ref": "#/components/parameters/MemberId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - manager_report_workspace
      operationId: manager.reports.workspace.index
      summary: List workspace.
  "/match/recommend":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - match
      operationId: match.recommend
      summary: Recommend a match.
  "/match/{id}/accept":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - match
      operationId: match.accept
      summary: Accept a match.
  "/match/{id}/decline":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - match
      operationId: match.decline
      summary: Decline a match.
  "/match/{id}/status":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - match
      operationId: match.status.index
      summary: List status.
  "/mcp":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - mcp
      operationId: mcp.index
      summary: List mcp.
    post:
      tags:
      - mcp
      operationId: mcp.send
      summary: Send a request to the MCP endpoint.
    delete:
      tags:
      - mcp
      operationId: mcp.close
      summary: Close an MCP connection.
  "/mcp/.well-known/oauth-authorization-server":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - well_known
      operationId: mcp.well_known.oauth_authorization_server
      summary: OAuth authorization-server metadata for the MCP resource.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/mcp/.well-known/oauth-protected-resource":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - well_known
      operationId: mcp.well_known.oauth_protected_resource
      summary: OAuth protected-resource metadata for the MCP resource.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/mcp/.well-known/openid-configuration":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - well_known
      operationId: mcp.well_known.openid_configuration
      summary: OpenID discovery document (ChatGPT well-known probe).
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
  "/mcp/tool_call":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mcp
      operationId: mcp.tool_call
      summary: Tool call a mcp.
  "/me/ai_coaches":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - me
      operationId: me.ai_coaches.index
      summary: List ai coaches.
  "/me/circle":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - me
      operationId: me.circle.index
      summary: List circle.
  "/me/coaching_consent":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - coaching_consent
      operationId: coachingConsent.show
      summary: Read the member's coaching consent decision for the current disclosure
        version.
      description: |
        Absence of a row is the pending state. Never 404s for a pending member —
        that is the state the gate exists to detect.
      responses:
        '200':
          description: Current consent decision (or well-formed pending).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/CoachingConsentDecision"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
    post:
      tags:
      - coaching_consent
      operationId: coachingConsent.create
      summary: Record an accepted or declined coaching consent decision.
      description: |
        Appends a ledger row. `pending` is not a valid wire value — absence of a
        row remains the only pending representation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - decision
              additionalProperties: false
              properties:
                decision:
                  type: string
                  enum:
                  - accepted
                  - declined
      responses:
        '201':
          description: Decision recorded; returns the updated decision shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/CoachingConsentDecision"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/me/communication_preferences":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - me
      operationId: me.communication_preferences.index
      summary: List communication preferences.
    patch:
      tags:
      - me
      operationId: me.communication_preferences.update
      summary: Update a communication preference.
  "/me/communities":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - me
      operationId: me.communities.index
      summary: List communities.
  "/me/connection_goals":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: me.connection_goals.index
      summary: List connection goals.
  "/me/connections":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - me
      operationId: me.connections.index
      summary: List connections.
  "/me/growth":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: me.growth.index
      summary: List growth.
  "/me/growth/whats_next_dismissals":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - member_growth
      operationId: me.growth.whats_next_dismissals.create
      summary: Create a whats next dismissal.
  "/me/journal":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: me.journal.index
      summary: List journal.
  "/me/mentor":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - my_mentor
      operationId: me.mentor.show
      summary: The organization's primary AI mentor, with the caller's preferences
        and its skills.
      responses:
        '200':
          description: |
            ⚠️ ALSO the "no mentor configured" response: when no active primary agent exists the body
            is `{mentor: null}` with NO `preferences` or `skills` keys — a 200, not a 404. Clients must
            branch on `mentor` being null rather than on status.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MyMentorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/me/mentor/onboarding":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - my_mentor
      operationId: me.mentor.onboarding
      summary: Complete mentor onboarding, REPLACING preferences and context rather
        than merging.
      description: |
        Same dest-only `403` as `PUT preferences`: a session-cookie caller without `X-Requested-With`
        is denied with the `authorization_denied` envelope. Bearer callers are exempt.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/MyMentorPreferencesUpdateRequest"
      responses:
        '200':
          description: |
            ⚠️ Semantics differ from the PUT above: this ASSIGNS `preferences` and `context` outright
            (falling back to the class defaults and `{}`), so omitting a key ERASES it rather than
            leaving it alone. Same request body, different merge behaviour — do not treat them as
            interchangeable.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MyMentorPreferences"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: No active primary agent is configured for the organization.
            Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Model validation failed. Bare error shape with joined full_messages.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/me/mentor/preferences":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - my_mentor
      operationId: me.mentor.preferences
      summary: The caller's preferences for the primary mentor.
      responses:
        '200':
          description: |
            Preferences are synthesised when no row exists (AgentMemberPreference.for_member returns
            an unsaved default-filled record), so this never 404s for a member who has set nothing.
            With no primary agent at all it returns the class defaults with an empty context.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MyMentorPreferences"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
    put:
      tags:
      - my_mentor
      operationId: me.mentor.update_preferences
      summary: Merge preference and context keys for the caller's primary mentor.
      description: |
        The `403` is dest-only and has no source equivalent: a **session-cookie** caller must send
        `X-Requested-With` (`require_csrf_header_for_session_writes`), and without it the request is
        denied with the `authorization_denied` envelope before the action runs. Bearer callers are exempt.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/MyMentorPreferencesUpdateRequest"
      responses:
        '200':
          description: The merged, persisted preferences.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MyMentorPreferences"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: No active primary agent is configured for the organization.
            Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            Model validation failed — in practice `context.custom_instructions` over
            AgentMemberPreference::CUSTOM_INSTRUCTIONS_MAX_LENGTH (500). Bare error shape carrying
            joined `full_messages`, NOT this API's field-level `details` array.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/me/programs":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - me
      operationId: me.programs.index
      summary: List programs.
  "/me/rumi_context":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - rumi_context
      operationId: me.rumi_context.show
      summary: The caller's own Rumi memory, rendered as forgettable sections for
        the transparency UI.
      responses:
        '200':
          description: |
            Only sections with content are returned, so an empty `sections` array is the normal
            response for a member Rumi has learned nothing about — not an error.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RumiContextResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
    delete:
      tags:
      - rumi_context
      operationId: me.rumi_context.destroy
      summary: Forget every forgettable key in the caller's Rumi memory.
      responses:
        '200':
          description: |
            All nine FORGETTABLE_KEYS reset to their schema defaults. Always 200: a key that fails
            schema validation is skipped silently rather than failing the batch, so this cannot
            report a partial reset. That is source's behaviour, kept deliberately.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedOkAck"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/me/rumi_context/{key}":
    parameters:
    - "$ref": "#/components/parameters/RumiContextKeyParam"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - rumi_context
      operationId: me.rumi_context.destroy_key
      summary: Forget one key of the caller's Rumi memory.
      responses:
        '200':
          description: The key was reset to its schema default.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RumiContextForgetKeyResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: |
            The key is not one of the nine forgettable keys. Reachable despite the route constraint,
            which only narrows the alphabet — e.g. `career_aspiration` is a real schema key, matches
            `[a-z_]+`, and is deliberately NOT forgettable.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/me/rumi_data_export":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - rumi_data_export
      operationId: me.rumi_data_export
      summary: DSAR export of the caller's own Rumi audit trail and member-context
        snapshot.
      responses:
        '200':
          description: The caller's full export. Self-only — there is no admin bypass
            and no member id parameter.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RumiDataExportResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/me/visibility_matrix":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - visibility_matrix
      operationId: me.visibility_matrix.index
      summary: List visibility matrix.
  "/media/avatar_history":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - media
      operationId: media.avatar_history.index
      summary: List avatar history.
  "/media/avatar_history/presets/{preset_key}":
    parameters:
    - "$ref": "#/components/parameters/PresetKey"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - media
      operationId: media.avatar_history.presets.destroy
      summary: Delete a preset.
  "/media/confirm":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - media
      operationId: media.confirm
      summary: Confirm a media.
  "/media/presign":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - media
      operationId: media.presign
      summary: Presign a media.
  "/media/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - media
      operationId: media.destroy
      summary: Delete a media.
  "/media/{id}/url":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - media
      operationId: media.url.index
      summary: List url.
  "/meeting_guide_imports":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - meeting_guides
      operationId: meeting_guide_imports.create
      summary: Validate or import a meeting-guide CSV/ZIP upload.
      description: |
        One endpoint branched on `validate`: when `validate=true` the upload is parsed and checked, returning
        the blocking / non-blocking errors, an optional error-report download URL, and the parsed guide count;
        otherwise the upload is committed (imported) in a transaction. Manage-only (admins with
        `manage_meeting_guides`). The `attachment` is an uploaded-file reference (`file_name` + `code`) — the
        client first POSTs the raw file to `/file_uploads` (the shared FileUploader temp-upload endpoint,
        `class_name=guided_conversation/template/standalone_guides_imports`, `attr_name=attachment`), which
        returns the `code`; this endpoint then resolves the file from that reference, not raw file bytes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - attachment
              - package_type
              additionalProperties: false
              properties:
                attachment:
                  type: object
                  description: Reference to the file already uploaded via /file_uploads.
                  required:
                  - file_name
                  - code
                  additionalProperties: false
                  properties:
                    file_name:
                      type: string
                    code:
                      type: string
                validate:
                  type: boolean
                  description: When true, only validate the upload; otherwise commit
                    the import.
                package_type:
                  type: string
                  enum:
                  - ongoing
                  - one_time
                  description: The guide package type the import targets.
      responses:
        '200':
          description: |
            Validation result (errors + count) when `validate=true`, or the import result otherwise. The
            payload shape varies by branch, so `data` is an open object.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    type: object
                    additionalProperties: true
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - "$ref": "#/components/parameters/IdempotencyKey"
  "/meeting_guide_packages":
    "$ref": v3/resources/standalone_guide_packages.yaml#/paths/~1meeting_guide_packages
  "/meeting_guide_packages/aggregates":
    "$ref": v3/resources/standalone_guide_packages.yaml#/paths/~1meeting_guide_packages~1aggregates
  "/meeting_guide_packages/{id}":
    "$ref": v3/resources/standalone_guide_packages.yaml#/paths/~1meeting_guide_packages~1{id}
  "/meeting_requests":
    "$ref": v3/resources/meeting_requests.yaml#/paths/~1meeting_requests
  "/meeting_requests/aggregates":
    "$ref": v3/resources/meeting_requests.yaml#/paths/~1meeting_requests~1aggregates
  "/meeting_requests/close":
    "$ref": v3/resources/meeting_requests.yaml#/paths/~1meeting_requests~1close
  "/meeting_requests/select_all":
    "$ref": v3/resources/meeting_requests.yaml#/paths/~1meeting_requests~1select_all
  "/meeting_requests/{id}":
    "$ref": v3/resources/meeting_requests.yaml#/paths/~1meeting_requests~1{id}
  "/member/career":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: member.career.index
      summary: List career.
  "/member_context":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - member_context
      operationId: member_context.update
      summary: Update the caller's first-time-experience context keys.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/MemberContextUpdateRequest"
      responses:
        '200':
          description: Every supplied key was written.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedOkAck"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          description: |
            Source's bare error shape. NOT reachable from the request body: `params.permit` admits
            only scalars and both keys are `Types::String.optional`, so any accepted value validates.
            It fires when the member's ALREADY-STORED `context` fails the schema — the write
            re-validates the whole document, so a legacy/corrupt row surfaces here.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/member_context/onboarding_status":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - member_context
      operationId: member_context.onboarding_status
      summary: Whether the caller has finished the first-time-experience overlay.
      responses:
        '200':
          description: The caller's onboarding timestamp, or null if never completed.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MemberContextOnboardingStatus"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/member_relationships":
    "$ref": v3/resources/member_relationships.yaml#/paths/~1member_relationships
  "/member_relationships/{id}":
    "$ref": v3/resources/member_relationships.yaml#/paths/~1member_relationships~1{id}
  "/members":
    "$ref": v3/resources/members.yaml#/paths/~1members
  "/members/aggregates":
    "$ref": v3/resources/members.yaml#/paths/~1members~1aggregates
  "/members/current":
    "$ref": v3/resources/members.yaml#/paths/~1members~1current
  "/members/{id}":
    "$ref": v3/resources/members.yaml#/paths/~1members~1{id}
  "/membership_request_views":
    "$ref": v3/resources/membership_request_views.yaml#/paths/~1membership_request_views
  "/membership_request_views/{id}":
    "$ref": v3/resources/membership_request_views.yaml#/paths/~1membership_request_views~1{id}
  "/membership_requests":
    "$ref": v3/resources/membership_requests.yaml#/paths/~1membership_requests
  "/membership_requests/accept":
    "$ref": v3/resources/membership_requests.yaml#/paths/~1membership_requests~1accept
  "/membership_requests/aggregates":
    "$ref": v3/resources/membership_requests.yaml#/paths/~1membership_requests~1aggregates
  "/membership_requests/ignore":
    "$ref": v3/resources/membership_requests.yaml#/paths/~1membership_requests~1ignore
  "/membership_requests/reject":
    "$ref": v3/resources/membership_requests.yaml#/paths/~1membership_requests~1reject
  "/membership_requests/select_all":
    "$ref": v3/resources/membership_requests.yaml#/paths/~1membership_requests~1select_all
  "/membership_requests/{id}":
    "$ref": v3/resources/membership_requests.yaml#/paths/~1membership_requests~1{id}
  "/mentor/sessions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentor
      operationId: mentor.sessions.create
      summary: Create a session.
  "/mentor/sessions/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - mentor
      operationId: mentor.sessions.show
      summary: Retrieve a session.
  "/mentor/sessions/{id}/complete":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentor
      operationId: mentor.sessions.complete
      summary: Complete a session.
  "/mentor/sessions/{id}/feedback":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentor
      operationId: mentor.sessions.feedback
      summary: Feedback a session.
  "/mentor/sessions/{id}/messages":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentor
      operationId: mentor.sessions.messages.create
      summary: Create a message.
  "/mentoring/briefs":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - mentoring
      operationId: mentoring.briefs.index
      summary: Pre-session briefs where the caller is the named mentor.
      responses:
        '200':
          description: |
            Most-recent-first, hard-capped at 50 rows. There are NO pagination parameters and no
            total count — a mentor with more than 50 briefs silently cannot reach the older ones.
            Source's behaviour; recorded rather than fixed at port time.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MentorBriefsListResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/mentoring/briefs/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - mentoring
      operationId: mentoring.briefs.show
      summary: One pre-session brief, including its generated content.
      responses:
        '200':
          description: The brief plus its `content` payload.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MentorBriefResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            The caller is not the brief's mentor. Source's bare error shape. ⚠️ An organization admin
            gets this too: authorization is `brief.mentor_id == current_member.id` and nothing else,
            so unlike every other V3 surface admin grants NO access here.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            No brief with that id exists ANYWHERE — the lookup is a global `find_by(id:)` with no
            tenant or mentor scope, and authorization runs as a separate later callback. So a brief
            belonging to another organization returns 403, not 404; only a truly absent id is 404.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/mentoring/development_records":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - mentoring
      operationId: mentoring.development_records.index
      summary: The caller's own recent development cycles that have at least one paired
        mentoring artifact.
      description: |
        F38 Row C (#18), closes the F38 batch. Lists only cycles with a paired `auto_update` /
        `mentor_brief` / `commit_action` — plain Rumi chats auto-mint a `cycle_id` at create time, so
        without this filter every conversation would surface here. Member-scoped throughout
        (`for_member(current_member.id)` plus three `member_id:` filters on the paired-artifact scans).
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - development_records
                additionalProperties: false
                properties:
                  development_records:
                    type: array
                    items:
                      "$ref": "#/components/schemas/CycleSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/mentoring/development_records/{cycle_id}":
    parameters:
    - "$ref": "#/components/parameters/CycleId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - mentoring
      operationId: mentoring.development_records.show
      summary: One development cycle's full artifact set — shape depends on the caller's
        relationship to it.
      description: "\U0001F534 `{cycle_id}` is a 36-char UUID, NOT a numeric record
        id.\n\nTwo-tier read authority, CONFIRMED sound (not a #146(2)-shaped defect
        — the inverse of it):\n`full` view when the caller IS the cycle's mentee (`conversation.member_id
        ==\ncurrent_member.id`); `summary` view when the caller is the mentee's assigned
        human mentor\n(`MentorshipAuthorization#mentor_of?`); `403` otherwise. Default-deny
        by construction — the\nmode is derived from the READER's relationship to the
        record, not assumed. `summary` never\nincludes raw `Interaction(:message)`
        content or full brief/auto-update text — only counts and\nAI-summarized previews,
        per the privacy posture.\n\nNOT verbatim (fixed): source's fallback lookup
        (used when the caller's own\nmember-scoped query finds nothing, so a mentor's
        cross-member view can still resolve) had no\norganization filter — source's
        own comment says cycle_id can collide across tenants in seed\ndata. Not a
        leak (the mode check still fails closed for a genuine stranger), but on a\ncollision
        `.first` could pick another tenant's conversation, false-403ing a legitimately\nauthorized
        same-org mentor. Scoped the fallback to the caller's own organization instead,\nthe
        same tenant boundary `all_conversations` uses.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - development_record
                - view_mode
                additionalProperties: false
                properties:
                  development_record:
                    "$ref": "#/components/schemas/DevelopmentRecord"
                  view_mode:
                    type: string
                    enum:
                    - full
                    - summary
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Cycle not found"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '403':
          description: '`"Forbidden"` — the caller is neither the cycle''s mentee
            nor their assigned mentor. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: '`"cycle_id is required"`. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/mentoring/development_records/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - mentoring
      operationId: mentoring.development_records.show_get
      summary: Retrieve a development record.
  "/mentoring/notes":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentoring
      operationId: mentoring.notes.create
      summary: Submit a mentor's structured post-session note about one of their mentees.
      description: "F38 Row B (#17). Authority is `MentorshipAuthorization#mentor_of?(params[:member_id])`
        — the\nONLY assigned human mentor of the mentee may submit a note; `mentor_id`
        is hard-set to\n`current_member.id` server-side, so the actor cannot forge
        the mentor side. Landed alongside\n`MentorshipAuthorization` (#12 Leg 1).\n\n`freeform_text`
        is encrypted at rest (`encrypts :freeform_text` on `MentorNote`). On success,\nenqueues
        `MentorNoteIngestJob` (fenced LLM extraction into `Member.context`); an enqueue\nfailure
        (`Redis::BaseError`) is logged and does not fail the request — the note is
        already\npersisted.\n\n\U0001F534 CREATE-ONLY: this endpoint has no index/show/update/destroy,
        so a note, once written, is not\nreadable through this API by itself — the
        read surface is `mentoring/development_records`\n(F38 Row C).\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - member_id
              additionalProperties: false
              properties:
                member_id:
                  type: integer
                  description: The mentee's member id.
                scheduled_session_id:
                  type: integer
                  nullable: true
                how_showed_up:
                  type: string
                  description: Free text, max 200 chars.
                confidence_going_in:
                  type: string
                  enum:
                  - low
                  - moderate
                  - high
                topics_covered:
                  type: array
                  items:
                    type: string
                freeform_text:
                  type: string
                  description: Free text, max 2000 chars. Encrypted at rest.
                cycle_id:
                  type: string
                  description: Denormalized active development-cycle id.
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - mentor_note
                additionalProperties: false
                properties:
                  mentor_note:
                    "$ref": "#/components/schemas/MentorNoteSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: '`"Forbidden"` — the caller is not the mentee''s assigned human
            mentor. Bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            Two distinct shapes, both bare (neither is this API's `{error: {code, message}}` envelope):
            `{"error": "member_id is required"}` when the param is missing, or
            `{"errors": ["<model validation message>", ...]}` when `MentorNote#save` fails validation.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/PortedBareErrors"
  "/mentoring/pending_debriefs/{conversation_id}":
    parameters:
    - "$ref": "#/components/parameters/ConversationId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentoring
      operationId: mentoring.pending_debriefs.create
      summary: Create a pending debrief.
  "/mentoring/pending_debriefs/{conversation_id}/trigger":
    parameters:
    - "$ref": "#/components/parameters/ConversationId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentoring
      operationId: mentoring.pending_debriefs.trigger
      summary: Trigger a pending debrief.
  "/mentoring/pending_proactives/{conversation_id}":
    parameters:
    - "$ref": "#/components/parameters/ConversationId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentoring
      operationId: mentoring.pending_proactives.create
      summary: Create a pending proactive.
  "/mentoring/seed_roleplay_debriefs/{conversation_id}":
    parameters:
    - "$ref": "#/components/parameters/ConversationId"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - mentoring
      operationId: mentoring.seed_roleplay_debriefs.create
      summary: Create a seed roleplay debrief.
  "/mentoring_models":
    "$ref": v3/resources/mentoring_models.yaml#/paths/~1mentoring_models
  "/mentoring_models/{id}":
    "$ref": v3/resources/mentoring_models.yaml#/paths/~1mentoring_models~1{id}
  "/messages":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - inbox
      operationId: inbox.messages.index
      summary: The caller's admin-message threads, most recently active first.
      description: |
        A thread is a ROOT message (`parent_id: nil`). Visibility is the union of two sets: threads where
        the caller is a receiver, and threads the caller sent — the latter explicitly bounded to this
        organization's programs plus the organization itself.

        `updatedAt` is the latest activity across the thread's replies, falling back to the root's own
        `createdAt` for a reply-less thread, so ordering is stable rather than null-punned.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - threads
                - meta
                additionalProperties: false
                properties:
                  threads:
                    type: array
                    items:
                      "$ref": "#/components/schemas/InboxMessageThread"
                  meta:
                    "$ref": "#/components/schemas/InboxPaginationMeta"
      parameters:
      - "$ref": "#/components/parameters/Page"
      - name: per_page
        in: query
        required: false
        schema:
          type: integer
    post:
      tags:
      - inbox
      operationId: inbox.messages.create
      summary: Contact Admin — opens a new thread addressed to a program's or the
        organization's admins.
      description: |
        `program_id` is optional and forgiving by design: an absent, unknown, or cross-organization id
        degrades to the organization itself rather than 404ing. So a caller cannot use it to reach
        another tenant, and cannot fail on a stale program reference.

        Created as `USER_TO_ADMIN` with `admin_contact: true`, and an empty receiver row is built so the
        thread resolves to that program's admins rather than a named member.

        ⚠️ Session callers must send `X-Requested-With`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - subject
              - body
              properties:
                subject:
                  type: string
                  description: Trimmed; must be non-blank.
                body:
                  type: string
                  description: Trimmed; must be non-blank.
                program_id:
                  type: integer
                  description: Optional. Falls back to the organization when absent,
                    unknown, or foreign.
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    "$ref": "#/components/schemas/InboxMessage"
        '422':
          description: Blank subject or body, or model validation failure.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
  "/messages/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - inbox
      operationId: inbox.messages.show
      summary: One thread with every message in it, oldest-first — and marks it read.
      description: |
        ⚠️ This GET is deliberately SIDE-EFFECTFUL and that is load-bearing: it calls
        `mark_siblings_as_read`, and the SPA has no separate mark-read mutation for threads. Opening a
        thread is the only thing that clears it from the badge count, so making this read-only would
        leave the badge permanently stuck.

        NOT VERBATIM — the thread lookup is scoped to the caller's organization. Source resolved the
        thread GLOBALLY (`AdminMessage.find_by(id:, parent_id: nil)`), and the visibility check behind it
        did not close the hole: for ORG-LEVEL messages `is_member_admin_for_this_msg?` is a bare
        `member.admin?` — the members-table flag, blind to WHICH org owns the message. So an org-A admin
        could read an org-B org-level thread and reply into it. Program-level threads were already safe.

        ⚠️ KNOWN GAP, not fixed here: sibling messages are returned unfiltered, whereas the full-stack
        controller filters them through `viewable_siblings`. On a bulk announcement (one root, many
        receivers) a co-recipient can therefore read another recipient's private replies to admins. That
        is a behavioural change beyond this port's scope and is registered separately.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - thread
                - messages
                additionalProperties: false
                properties:
                  thread:
                    "$ref": "#/components/schemas/InboxMessageThread"
                  messages:
                    type: array
                    items:
                      "$ref": "#/components/schemas/InboxMessage"
        '404':
          description: No such thread, not visible to the caller, or outside the caller's
            organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
  "/messages/{id}/reply":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - inbox
      operationId: inbox.messages.reply
      summary: Replies to a thread.
      description: |
        The reply's program is derived from the THREAD, not from the request — which is why the
        org-scoped thread lookup above matters for writes as well as reads.

        ⚠️ Session callers must send `X-Requested-With`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - body
              properties:
                body:
                  type: string
                  description: Trimmed; must be non-blank.
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - message
                additionalProperties: false
                properties:
                  message:
                    "$ref": "#/components/schemas/InboxMessage"
        '404':
          description: Thread not visible to the caller, or outside their organization.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
        '422':
          description: Blank body, or model validation failure.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/InboxBareError"
  "/my_space":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.my_space
      summary: The member's "Your Space" view — working-on, learning, and shared-artifact
        blocks.
      responses:
        '200':
          description: The home/my_space view envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MySpaceViewResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: conversation_id
        in: query
        required: false
        schema:
          type: string
        description: Current conversation id — floats matching items to the top of
          their block.
  "/notifications/read_all":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - inbox
      operationId: inbox.notifications.mark_all_read
      summary: Marks every push-based notification for the caller as read.
      description: |
        Deliberately NOT under `namespace :inbox`, even though the notifications it acts on are read
        from `/api/v3/inbox/notifications`. The SPA hand-builds this exact path
        (`queries/inbox/queries.ts`), so the bulk verb lives at a different root from its resource.
        Moving it under `inbox` would 404 the live caller.

        Single mark-read is a separate Interface-stack command
        (`PUT /api/v3/inbox/notifications/{id}/read`); only the bulk verb is served here.

        ⚠️ A session-strategy caller must send `X-Requested-With` — dest's
        `require_csrf_header_for_session_writes` rejects the write with 403 before the action runs.
        Bearer callers are exempt.
      responses:
        '200':
          description: 'Always `{success: true}`; a no-op when nothing was unread.'
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                additionalProperties: false
                properties:
                  success:
                    type: boolean
  "/oauth/authorize":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - oauth
      operationId: oauth.authorize.index
      summary: List authorize.
    post:
      tags:
      - oauth
      operationId: oauth.authorize
      summary: Authorize an oauth.
    delete:
      tags:
      - oauth
      operationId: oauth.authorize.destroy
      summary: Delete authorize.
  "/oauth/authorize/native":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - oauth
      operationId: oauth.authorize.native.index
      summary: List native.
  "/oauth/introspect":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - oauth
      operationId: oauth.introspect
      summary: Introspect an oauth.
  "/oauth/register":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - oauth
      operationId: oauth.register
      summary: Register an oauth.
  "/oauth/revoke":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - oauth
      operationId: oauth.revoke
      summary: Revoke an oauth.
  "/oauth/token":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - oauth
      operationId: oauth.token
      summary: Token an oauth.
  "/oauth/token/info":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - doorkeeper
      operationId: oauth.token.info.index
      summary: List info.
  "/onboarding_flow_images":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - onboarding_flow_images
      operationId: onboarding_flow_images.create
      summary: Upload a background or profile image for the onboarding flow editor.
      description: |
        Stores an uploaded image as an ORGANIZATION-scoped `Layout::Image` (background or profile, per
        `image_type`) and returns its record + serving URL. The editor references the returned id from a
        flow component and persists that reference atomically on the flow `update`.

        A retried POST with the same `Idempotency-Key` and body replays the stored `201` exactly once
        (a mismatched body returns `422 idempotency_key_reuse`); omit the header for at-least-once.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              - imageType
              additionalProperties: false
              properties:
                file:
                  type: string
                  format: binary
                  description: The image to upload.
                imageType:
                  type: string
                  enum:
                  - '1'
                  - '2'
                  description: "`1` → background, `2` → profile (Layout::Image::ImageType)."
      responses:
        '201':
          description: The image was stored; its record and serving URL are returned.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    "$ref": "#/components/schemas/OnboardingFlowImage"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - "$ref": "#/components/parameters/IdempotencyKey"
  "/onboarding_flows":
    "$ref": v3/resources/onboarding_flows.yaml#/paths/~1onboarding_flows
  "/onboarding_flows/{id}":
    "$ref": v3/resources/onboarding_flows.yaml#/paths/~1onboarding_flows~1{id}
  "/onboarding_testimonial_photos":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - onboarding_testimonial_photos
      operationId: onboarding_testimonial_photos.create
      summary: Upload a testimonial avatar for the onboarding flow editor.
      description: |
        Parks an uploaded PNG in the shared temp area and returns the `{file_name, code}` pair that
        identifies it, plus a short-lived `preview_url` the editor can render immediately.

        Unlike `onboarding_flow_images`, this does NOT create a record: a testimonial photo is an
        attachment on the `Testimonial` itself, which may not exist yet. The caller echoes `file_name`
        and `code` back on the testimonial entry of the next flow `update`, and that save moves the file
        onto the record atomically. Sending a blank `file_name` on the update removes an existing photo.

        The pair is only valid until the temp file is reaped, so save the document in the same session.

        A retried POST with the same `Idempotency-Key` and body replays the stored `201` exactly once
        (a mismatched body returns `422 idempotency_key_reuse`); omit the header for at-least-once.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              additionalProperties: false
              properties:
                file:
                  type: string
                  format: binary
                  description: The PNG to upload (Testimonial accepts PNG only).
      responses:
        '201':
          description: The photo was parked; echo `file_name` + `code` on the next
            flow update to persist it.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    "$ref": "#/components/schemas/OnboardingTestimonialPhoto"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - "$ref": "#/components/parameters/IdempotencyKey"
  "/optimize/coaching_lift":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: optimize.coaching_lift.index
      summary: List coaching lift.
  "/org_goal_ladder":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - org_goal_ladder
      operationId: orgGoalLadder.show
      summary: Read-only company-goal ladder visible at the member's level
      description: |
        Depth-capped org goals the member can align to (same resolver as MCP
        list_org_goals). Gated by FeatureName::ORG_GOAL_LADDER — 403 when the
        flag is off. Never returns owner emails or inactive goals.
      responses:
        '200':
          description: Depth-capped org goals the member can align to.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OrgGoalLadder"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/org_goals":
    "$ref": v3/resources/org_goals.yaml#/paths/~1org_goals
  "/org_goals/{id}":
    "$ref": v3/resources/org_goals.yaml#/paths/~1org_goals~1{id}
  "/org_units":
    "$ref": v3/resources/org_units.yaml#/paths/~1org_units
  "/org_units/{id}":
    "$ref": v3/resources/org_units.yaml#/paths/~1org_units~1{id}
  "/organizations/current":
    "$ref": v3/resources/organizations.yaml#/paths/~1organizations~1current
  "/performance_case_offers/{id}/respond":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - performance_case_offers
      operationId: performance_case_offers.respond
      summary: Respond a performance case offer.
  "/performance_cases":
    "$ref": v3/resources/performance_cases.yaml#/paths/~1performance_cases
  "/performance_cases/{id}":
    "$ref": v3/resources/performance_cases.yaml#/paths/~1performance_cases~1{id}
  "/performance_cases/{id}/evaluation":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - performance_case_evaluations
      operationId: performance_cases.evaluation
      summary: Evaluation a performance cas.
  "/performance_cases/{id}/events":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - performance_case_events
      operationId: performance_cases.events.index
      summary: List events.
  "/performance_metric_skill_mappings":
    "$ref": v3/resources/performance_metric_skill_mappings.yaml#/paths/~1performance_metric_skill_mappings
  "/performance_metric_skill_mappings/{id}":
    "$ref": v3/resources/performance_metric_skill_mappings.yaml#/paths/~1performance_metric_skill_mappings~1{id}
  "/performance_metric_thresholds":
    "$ref": v3/resources/performance_metric_thresholds.yaml#/paths/~1performance_metric_thresholds
  "/performance_metric_thresholds/{id}":
    "$ref": v3/resources/performance_metric_thresholds.yaml#/paths/~1performance_metric_thresholds~1{id}
  "/performance_metrics":
    "$ref": v3/resources/performance_metrics.yaml#/paths/~1performance_metrics
  "/performance_metrics/{id}":
    "$ref": v3/resources/performance_metrics.yaml#/paths/~1performance_metrics~1{id}
  "/posts":
    "$ref": v3/resources/posts.yaml#/paths/~1posts
  "/posts/{id}":
    "$ref": v3/resources/posts.yaml#/paths/~1posts~1{id}
  "/program_invitations":
    "$ref": v3/resources/program_invitations.yaml#/paths/~1program_invitations
  "/program_invitations/aggregates":
    "$ref": v3/resources/program_invitations.yaml#/paths/~1program_invitations~1aggregates
  "/program_invitations/{id}":
    "$ref": v3/resources/program_invitations.yaml#/paths/~1program_invitations~1{id}
  "/programs":
    "$ref": v3/resources/programs.yaml#/paths/~1programs
  "/programs/aggregates":
    "$ref": v3/resources/programs.yaml#/paths/~1programs~1aggregates
  "/programs/current":
    "$ref": v3/resources/programs.yaml#/paths/~1programs~1current
  "/programs/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.show
      summary: Retrieve a program.
  "/programs/{program_id}/connections/{connection_id}/rumi_nudges":
    parameters:
    - name: program_id
      in: path
      required: true
      description: Program id. Resolved within the caller's organization; unknown
        or non-numeric yields 404.
      schema:
        type: string
    - name: connection_id
      in: path
      required: true
      description: Connection (group) id, resolved within the program. See the existence-oracle
        note above.
      schema:
        type: string
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - connections
      operationId: connections.rumi_nudges.show
      summary: Armed Rumi nudge cards for one connection, for the calling member.
      description: "Returns the calling member's ARMED `pre_session_brief` / `post_meeting_debrief`
        timed triggers\nwhose payload `meeting_id` belongs to this connection, projected
        as CTA cards. Always a `200`\nwith a `nudges` array once the connection resolves
        — an empty array is the normal answer, not\nan error.\n\n\U0001F534 ON THIS
        DEPLOYMENT THE ONLY POSSIBLE ANSWER IS `{\"nudges\": []}`, and a client MUST
        NOT read\nthat as \"this member has no nudges\". Nothing on this tree arms
        a trigger this endpoint can\nselect:\n  • `pre_session_brief` — its sole armer
        is the mentoring-intelligence seam on `Meeting`, which\n    is not present
        here (neither the extensions file nor its include). That seam belongs to an\n
        \   unowned porting family, so there is no scheduled date at which this fills
        in.\n  • `post_meeting_debrief` — UNREACHABLE BY DATA, and not only here:
        every armer of that kind\n    writes a payload of `commit_action_id` + `commit_text`
        and NO `meeting_id` key, so\n    `JSON_EXTRACT(payload, '$.meeting_id')` is
        NULL and `NULL IN (...)` never matches. That is\n    inherited behaviour,
        identical on the tree this was ported from, and it does not resolve\n    when
        the `pre_session_brief` armer lands.\n⇒ the `post_meeting_debrief` variant
        below is documented from the projection code, not from any\nproducible row.\n\n⚠️
        THIS ENDPOINT IS A GROUP-ID EXISTENCE ORACLE, and that is source's behaviour,
        ported\ndeliberately rather than hardened. `find_group` checks organization
        → program → group but\nNEVER that the caller belongs to the group, so `404`-vs-`200`
        distinguishes valid connection\nids across EVERY program in the organization
        for any authenticated member.\n**Data exposure is nil**: the trigger query
        is self-scoped (`TimedTrigger.for_member(current_member.id)`)\nand peer names
        derive from the caller's own meetings, so a non-member receives `{\"nudges\":
        []}`.\nWhat leaks is existence only.\n\n⚠️ Path params are declared as bare
        strings ON PURPOSE. Source declared no schema for them and\nresolves both
        via `find_by(id:)`, which answers `404` for a non-numeric segment; a\n`type:
        integer` here would instead `400` that probe at the always-on request validator,
        turning\na source `404` into a status source never returned. It is a reachable
        case, not hypothetical:\nsource's own dead route path resolves against this
        route with `program_id: \"member\"`.\n"
      responses:
        '200':
          description: |
            The armed nudges for this connection. Empty whenever the connection has no meetings, or no
            armed trigger of either kind references one of them.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RumiNudgesResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: |
            No such program in the caller's organization, or no such group in that program. Bare error
            shape (`{"error": "Connection not found"}`), not this API's error envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/contents/{content_id}/complete":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.contents.responses.complete
      summary: Mark this content complete for the caller — idempotent, scores on first
        call only.
      description: |
        Idempotent on the `(member, context, interaction_type: completion)` tuple: a second call finds
        the existing completion `Interaction` and short-circuits with `already_completed: true`,
        re-scoring nothing and re-emitting no notification. The first call scores every response via
        `Content::Spark::Scorer`, persists an `Interaction`, runs `Reflection::SparkTrigger`, and
        instruments `spark.completed` (payload: `spark_id`, `organization_id`, `member_id`, `score`,
        `passed`, `completed_at` — not itself part of the HTTP response).

        Shares the routing-landmine note and the row #150 program-scoping fix documented on `index`
        above. Takes no body.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/SparkCompletionResult"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census — see `index` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Base census — see `index` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/contents/{content_id}/next_step":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.contents.responses.next_step
      summary: Resolve the next step id via the content's branching rules (or positional
        order).
      description: |
        Body: `{"current_step_id"?: string|null, "answers"?: {field: value}}`. `current_step_id: null`
        (or omitted) asks for the FIRST step. `Content::Spark::FlowEngine.next` evaluates branching
        rules against `answers` first, falling back to positional (declaration) order when a step has
        none. `terminal: true` with `next_step_id: null` means either the content has no steps at all,
        or the current step was the last one / branched to `"end"`.

        Shares the routing-landmine note and the row #150 program-scoping fix documented on `index`
        above.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                current_step_id:
                  type:
                  - string
                  - 'null'
                answers:
                  type: object
                  additionalProperties: true
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - next_step_id
                - terminal
                additionalProperties: false
                properties:
                  next_step_id:
                    type:
                    - string
                    - 'null'
                  terminal:
                    type: boolean
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census — see `index` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            Base census (see `index`) plus `"Step not found"` — here meaning the SUPPLIED
            `current_step_id` does not match any step of this content (distinct from `create`'s
            identically-worded `find_step` 404, a different lookup on a different param).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/contents/{content_id}/responses":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.contents.responses.index
      summary: The caller's own saved responses across every step of this content
        (Spark player resume).
      description: "\U0001F534 ROUTING LANDMINE (verified, not assumed): this row
        and its three siblings below are drawn\nby BARE `get`/`post` calls directly
        inside `resources :contents, only: [], module:\n'programs/member' do ... end`
        — NOT wrapped in a `member do...end` block. Measured (not\nassumed) by generating
        both forms in an isolated `ActionDispatch::Routing::RouteSet` and\nreading
        the resolved params: the bare form binds this segment as `:content_id` (what\n`find_content`
        reads), while the \"idiomatic\" `member do...end` rewrite binds plain `:id`\ninstead
        — which would 404 `find_content` on EVERY request despite looking like a harmless\ntidy-up.
        Kept bare to match source's ACTUAL resolved route (source routes.rb:238-246),
        same\nmethod already used for `connections_controller.rb`'s `activity_graph`/`progress`.\n\nNOT
        VERBATIM (fixed, row #150 ruled): source scoped content lookup to\n`Content.for_organization(@current_organization.id)`
        alone — ORG-wide, not program-scoped.\n`Content#program_id` is optional (org-wide
        content shares NULL), but when set it belongs to\nexactly one program; an
        org-only lookup let a member of Program B fetch Program A's\nprogram-specific
        content by id — a cross-program leak within the same org. Scoped to\n`where(program_id:
        [nil, @program.id])` instead, applied identically across all four\noperations
        below (one shared `before_action`).\n\nDraft content (`status != \"published\"`)
        403s on EVERY action here (`ensure_published!` is an\nunconditional `before_action`),
        not just this one.\n\nResponse shape is the SHARED `Content::Responses::Writer.as_json`
        used by BOTH this controller\nand `Api::V3::Admin::ContentResponsesController`
        — deliberately camelCase\n(`stepId`/`userId`/`fieldName`/`createdAt`/`updatedAt`),
        breaking this API's snake_case\nconvention. NOT renamed here: the shape is
        shared code, and renaming only the member side\nwould desync the two surfaces
        the service exists to keep symmetrical.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - responses
                additionalProperties: false
                properties:
                  responses:
                    type: array
                    items:
                      "$ref": "#/components/schemas/SparkContentResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            Two distinct causes render the SAME bare shape: `"You are not a member of this program"`
            (`programs/member/base_controller.rb:21`) or `"Content is not yet published"`
            (`ensure_published!`, applies to every action on this controller).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            Two distinct causes render the SAME bare shape: `"Program not found"` or
            `"Content not found"` (the row #150-fixed lookup above).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/contents/{content_id}/steps/{step_id}/responses":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.contents.responses.create
      summary: Upsert the caller's answer for one step.
      description: |
        Body: `{"field_name": string (required), "value": any}`. Upsert keyed on
        `(step_id, user_id, field_name)` — a second call with the same three overwrites `value` rather
        than creating a duplicate row (`Content::ContentResponse.upsert_response!`).

        Shares the routing-landmine note and the row #150 program-scoping fix documented on `index`
        above — both apply identically here (`find_content` runs as a `before_action` on every
        action). Additionally 404s via `find_step` when `step_id` does not belong to this content.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - field_name
              additionalProperties: false
              properties:
                field_name:
                  type: string
                value:
                  description: Free-form — the step's own block type determines the
                    expected shape.
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - response
                additionalProperties: false
                properties:
                  response:
                    "$ref": "#/components/schemas/SparkContentResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census — see `index` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            Base census (see `index`) plus `"Step not found"` (`find_step` — the `step_id` does not
            belong to this content).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: "\U0001F534 NOT VERBATIM (fixed, #27 — this description was
            WRONG before the fix landed: it\ndocumented a 422 the CODE could never
            reach, since a missing `field_name` raised an\nunhandled 500 via source's
            unrescued `params.require`, an idiom that appears nowhere else\nin this
            engine). TWO causes now share the bare `{\"error\": \"<message>\"}` shape:\n
            \ • `\"field_name is required\"` — the new explicit presence check, the
            house pattern every\n    other controller in this engine already uses.\n
            \ • `ActiveRecord::RecordInvalid` from the upsert — any OTHER validation
            failure once\n    `field_name` is present.\n"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/ai_matches":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.ai_matches.index
      summary: List ai matches.
  "/programs/{program_id}/member/ai_matches/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.ai_matches.show
      summary: Retrieve an ai matche.
  "/programs/{program_id}/member/community":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.community.index
      summary: Community content sections for one program — forums, Q&A, articles,
        events, resources, and the caller's learning progress.
      description: "Always a `200`; the body is an object with UP TO SIX keys, each
        independently present or\nabsent. `{}` is a legal body.\n\n⚠️ OMISSION IS
        NOT ONE UNIFORM RULE — three different mechanisms across six keys, verified\nagainst
        the code rather than assumed uniform:\n  - `forums` / `articles` / `events`:
        omitted ONLY when their feature check fails. When the\n    check passes they
        are ALWAYS present, even as `[]` — an empty result is NOT an omission.\n  -
        `qa`: omitted when the check fails, OR when the program has NO forum at all
        (`qa_forum`\n    resolves nil) — a SECOND, data-driven omission path independent
        of the feature check.\n  - `resources` / `learning`: omitted purely on empty
        content (`resource_ids.any?` /\n    `learning.any?`) — their feature checks
        below are structurally incapable of returning\n    false on this tree (see
        next paragraph), so content is the only gate that ever fires.\n\n\U0001F534
        TWO OF THE SIX FEATURE CHECKS ARE PERMANENTLY INERT ON THIS TREE, verified
        against\nconfig/initializers/define_features.rb rather than assumed — inherited
        from source, not\nport-introduced:\n  - `resources`: `program_feature_enabled?(:resources)`
        calls `try(:resources_enabled?)`. No\n    `resources` feature exists (only
        the unrelated, connection-scoped\n    `mentoring_connection_learning_resources_enabled?`)
        — `try` returns nil, `nil != false` is\n    `true`, so this check can never
        be false.\n  - `certificates`: calls `try(:certificate_enabled?)` — SINGULAR.
        The real feature is\n    `FeatureName::CERTIFICATES = \"certificates\"` (PLURAL),
        which would derive\n    `certificates_enabled?`. The singular form never resolves,
        so this is ALSO permanently\n    true — moot in practice, because the branch
        is independently dead (next paragraph).\n`forums` / `qa` / `articles` / `events`
        / `training` DO ride real, genuinely-toggleable\nfeatures (`forums_enabled?`
        / `qa_enabled?` / `articles_enabled?` / `program_events_enabled?` /\n`mentoring_connections_v2_enabled?`
        — each confirmed as a real call elsewhere in the codebase).\n\n\U0001F534
        THE CERTIFICATES HALF OF `learning` IS DEAD ON BOTH TREES, independent of
        the gate above:\n`Organization`/`Program` has no `connection_certificates`
        association anywhere on this tree, so\n`try(:connection_certificates)` is
        always nil and `|| []` always empties the branch. No\ncertificate-typed row
        can ever appear in `learning` today.\n\n⚠️ `qa`'s fallback: with no forum
        literally named \"Q&A\", `qa_forum` falls back to the program's\nFIRST program-forum
        (`@program.forums.program_forums.first`) — so a topic posted to an ordinary\nforum
        can appear in BOTH `forums` and `qa` simultaneously if the program has no
        dedicated\nQ&A forum. Inherited source behaviour, not a port defect.\n\n#139
        (ruled) applied to five keys: `last_activity`/`read_time`/`added_by`/`due_date`/\n`earned_date`,
        diverging from source's `lastActivity`/`readTime`/`addedBy`/`dueDate`/\n`earnedDate`.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramCommunityResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            The caller is an active org member but not an active user of THIS program
            (`programs/member/base_controller.rb:20`). Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: No such program in the caller's organization. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.index
      summary: The caller's own connections in this program, paginated.
      description: |
        Query params: `status` (`active`/`inactive`/`closed`/`drafted`/`pending`/`proposed`/
        `rejected`/`withdrawn`), `search` (name substring), `page`, `per_page` (default 25, max 100).

        Always scoped to the caller's OWN connections — exact parity with the landed V3
        `GroupPolicy::Engage` (`(@user || @member).groups`). There is no program-wide listing on this
        surface; an admin needing one already has the landed Manage listing elsewhere. `scope` is not
        an accepted param (#146(3), history in the controller comment) — a request sending it is
        rejected by request validation, not silently accepted and dropped.

        #139 (ruled) applied: `member_count`/`created_at`/`expiry_time`, not source's `memberCount`/
        `createdAt`/`expiryTime`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connections
                - meta
                additionalProperties: false
                properties:
                  connections:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ConnectionSummary"
                  meta:
                    type: object
                    required:
                    - page
                    - per_page
                    - total
                    - total_pages
                    additionalProperties: false
                    properties:
                      page:
                        type: integer
                      per_page:
                        type: integer
                      total:
                        type: integer
                      total_pages:
                        type: integer
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          description: '`"Program not found"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    post:
      tags:
      - programs
      operationId: programs.member.connections.create
      summary: Propose a new PENDING connection (project-based programs only).
      description: "Body: `{\"name\": string, \"description\"?: string, \"start_date\"?:
        ISO8601 date}`. The proposer\nis added as a member with their own program
        role.\n\n\U0001F534 S3 (fixed, not verbatim): an unparseable `start_date`
        raised a raw unrescued\n`Time.zone.parse` `ArgumentError` (a 500), not a 422.
        `start_date` is documented optional, so\na bad value is reported the same
        way the model's own validation failures are.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              additionalProperties: false
              properties:
                name:
                  type: string
                description:
                  type: string
                start_date:
                  type: string
                  description: ISO8601. Rejected (422) if unparseable.
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            Base census (program/membership — `index`'s `create` has no connection to be forbidden
            FROM yet, so only the program-level pair applies) plus
            `"Connection creation is admin-only for this program type"` for a non-project-based
            program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: '`"Program not found"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            Rails' native `errors.as_json` hash shape, or a bare `{"errors": {"start_date": [...]}}`
            for S3's parse failure.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{connection_id}/activities":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.activities.index
      summary: Paginated activity feed for one connection, newest first.
      description: "Always a `200` with `activities` (possibly empty) and a `meta`
        pagination envelope.\n\n#139 (ruled) applied: `action_type`/`created_at`/`user_id`,
        not source's\n`actionType`/`createdAt`/`userId`.\n\nS2 (fixed, not verbatim):
        source's shared `page` helper is unclamped, so `?page=0` (or any\nnon-positive
        value) computes a negative SQL `OFFSET` — a raw 500, not a 400. Fixed centrally\nin
        `Programs::Member::BaseController#page`/`#per_page` (both clamped to a minimum
        of 1; `per_page`\nadditionally capped at 100, as source already did) rather
        than copied per-controller the way\nsource's own `expenses_controller.rb`
        patches it locally — every current and future paginated\nsibling under this
        base gets the fix for free.\n\n\U0001F534 FIVE 4xx SOURCES ACROSS TWO BASE
        CONTROLLERS, evaluated in this order — count them before\nassuming \"the\"
        403 or \"the\" 404:\n  1. Envelope `401` (auth resolver, before any action
        runs).\n  2. Bare `404` \"Program not found\" (`programs/member/base_controller.rb:14`)
        — no such program\n     in the caller's org.\n  3. Bare `403` \"You are not
        a member of this program\" (`:21`) — active org member, not an\n     active
        user of THIS program.\n  4. Bare `404` \"Connection not found\" (`connections/base_controller.rb:24`)
        — no such group in\n     this program.\n  5. Bare `403` \"Forbidden\" (`:37`)
        — the caller is a user of the program but not a member of\n     THIS connection
        (and not a program admin, who bypasses via `is_admin?`).\nAll five are BARE
        shapes, not this API's envelope — `PortedBareError` for every one.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionActivitiesResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            Two distinct causes render the SAME bare shape with DIFFERENT messages — see the
            top-level description for the full 4xx census and evaluation order.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            Two distinct causes ("Program not found" / "Connection not found") render the SAME bare
            shape — see the top-level description.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/activity_graph":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.activity_graph
      summary: This connection's status-change history, for a timeline view.
      description: |
        Verbatim from source (routes.rb:349): declared as a BARE `get` directly inside the
        `resources :connections` block, outside any `member`/`collection` wrapper. Measured, not
        assumed: Rails resolves this via the NESTED (`:connection_id`) convention here, not
        `member`'s `:id` — the ONLY two operations in this controller keyed that way (this and
        `progress`). The base's dual read (`params[:connection_id] || params[:id]`) is what makes
        both param names work throughout this file.

        #139 (ruled) applied: `from_state`/`to_state`/`date_id`/`created_at`, not source's
        `fromState`/`toState`/`dateId`/`createdAt`. `from_state`/`to_state` are rendered as their raw
        STRING column values (`group_state_changes.to_state :string`) — source never translates them
        to labels here, so neither does this port.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - activity_graph
                additionalProperties: false
                properties:
                  activity_graph:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - from_state
                      - to_state
                      - date_id
                      - created_at
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                        from_state:
                          type:
                          - string
                          - 'null'
                        to_state:
                          type:
                          - string
                          - 'null'
                        date_id:
                          type:
                          - integer
                          - 'null'
                        created_at:
                          type: string
                          description: ISO8601.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/ai_plan":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.ai_plan.index
      summary: List ai plan.
    post:
      tags:
      - programs
      operationId: programs.member.connections.ai_plan
      summary: Ai plan a connection.
  "/programs/{program_id}/member/connections/{connection_id}/budget":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.budget.show
      summary: This connection's current budget, or null if none exists.
      description: "#22 (ruled — MISSED ROW, landed after the fact): this row exists
        on source\n(`budget_controller.rb`, routed singular `resource :budget, only:
        [:show, :update]`) but was\nnever carried over in the original connections/*
        pass. No shape/behavior change from source\nbeyond the fixes noted on `update`
        below.\n\nSame 5-source 4xx census as `activities`/`expenses`/`surveys`/`meetings`/`members`
        (identical\nbase controllers) — see `activities.meta.yaml`.\n\n\U0001F534
        NOT VERBATIM (fixed, #154 ruled): source had no gate beyond the base census
        — any member of\nthis connection could read its budget. Full-stack's real
        authority is `can_view_expenses?`\n(`ActsAsBudgetable`; the same predicate
        `ExpenseObject::UserActor#basic` uses on an expense's\nown `direct_budget`).
        Called on the CONNECTION itself, not the (possibly nil) current budget,\nso
        the gate applies to the \"no budget yet\" empty state too, not only populated
        budgets.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - budget
                additionalProperties: false
                properties:
                  budget:
                    anyOf:
                    - "$ref": "#/components/schemas/ConnectionBudget"
                    - type: 'null'
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            Base census (see `activities.meta.yaml`) plus this action's own `"Forbidden"`
            (`can_view_expenses?` — the caller's role has no expense-visibility permission and they
            are not an admin/hierarchical manager). All bare shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Base census — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    put:
      tags:
      - programs
      operationId: programs.member.connections.budget.update_put
      summary: Update the current budget amount (PUT alias of the PATCH above).
      description: |
        Not a second behaviour — source's singular `resource ... only: [:update]` draws PATCH and PUT
        at the same path, both dispatching to the same action. Documented rather than left implicit
        because a routed verb with no operation publishes an empty contract for a live endpoint (the
        account_settings precedent). Carries the identical dual-403 shape as the PATCH form.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - amount
              additionalProperties: false
              properties:
                amount:
                  type: number
                start_time:
                  type: string
                  format: date-time
                end_time:
                  type: string
                  format: date-time
      responses:
        '200':
          description: An existing budget's amount was updated.
          content:
            application/json:
              schema:
                type: object
                required:
                - budget
                additionalProperties: false
                properties:
                  budget:
                    "$ref": "#/components/schemas/ConnectionBudget"
        '201':
          description: No budget existed for this connection; a new one was created.
          content:
            application/json:
              schema:
                type: object
                required:
                - budget
                additionalProperties: false
                properties:
                  budget:
                    "$ref": "#/components/schemas/ConnectionBudget"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Same dual shape as the PATCH form above.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/Error"
        '404':
          description: Base census — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Same wrapper as the PATCH form above.
          content:
            application/json:
              schema:
                type: object
                required:
                - errors
                additionalProperties: false
                properties:
                  errors:
                    "$ref": "#/components/schemas/PortedValidationErrorsHash"
    patch:
      tags:
      - programs
      operationId: programs.member.connections.budget.update
      summary: Update the current budget amount, creating one if none exists. Admin
        only.
      description: "Body: `{\"amount\": decimal (required), \"start_time\"?: ISO8601,
        \"end_time\"?: ISO8601}`.\n`start_time`/`end_time` only apply when NO budget
        exists yet (a fresh one is built spanning\nthe current calendar year by default);
        updating an EXISTING budget only ever touches `amount`.\n\n\U0001F534 #154
        ruled (SUPERSEDES the #153-derived interim landed at #22): `group/actions.rb`
        has NO\nverb for budget — checked, not assumed, its USER/STATUS/PROGRAM restriction
        maps contain zero\nBUDGET entries. #22 matched bare `is_admin?` to this subtree's
        `allow_manage_on?` norm as the\nbest-available interim. But budget mutation
        is really an EXPENSE-family operation, and\nfull-stack's actual authority
        is `can_manage_expenses?` (`ActsAsBudgetable` —\n`can_have_active_budget?
        && can_view_expenses?`; the identical predicate\n`ExpenseObject::UserActor#special_create`
        uses on an expense's own `direct_budget`). Called on\nthe CONNECTION, not
        the (possibly nil) current budget — the latter is nil on the\ncreate-a-new-budget
        path, where a nil receiver would make budget creation impossible for\nanyone.\n\nNOT
        VERBATIM (fixed): source assigns the bare ivar `@current_member`, which is
        nil on dest\n(no equivalent memoization runs earlier in the request — same
        defect\n`expenses_controller.rb#create` already had and was fixed the same
        way) — verbatim would have\nsilently written `updated_by_member: nil`, failing
        `Budget`'s own `validates\n:updated_by_member, presence: true, if: proc {
        root? && changed? }` on every edit. Uses the\n`current_member` method instead.\n\nS3
        (fixed, not verbatim): source's `Time.zone.parse(params[:start_time])`/`params[:end_time])`\nis
        unrescued when building a NEW budget — an unparseable value raised `ArgumentError`,
        a raw\n500. Now returns a `422` with the same `{\"errors\": {field: [message]}}`
        wrapper Rails' own\nmodel-validation failures use.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - amount
              additionalProperties: false
              properties:
                amount:
                  type: number
                start_time:
                  type: string
                  format: date-time
                end_time:
                  type: string
                  format: date-time
      responses:
        '200':
          description: An existing budget's amount was updated.
          content:
            application/json:
              schema:
                type: object
                required:
                - budget
                additionalProperties: false
                properties:
                  budget:
                    "$ref": "#/components/schemas/ConnectionBudget"
        '201':
          description: No budget existed for this connection; a new one was created.
          content:
            application/json:
              schema:
                type: object
                required:
                - budget
                additionalProperties: false
                properties:
                  budget:
                    "$ref": "#/components/schemas/ConnectionBudget"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: "\U0001F534 TWO DIFFERENT BODY SHAPES share this status (the
            account_settings lesson, recurring\nhere — #145's S4 class):\n  • The
            base census (see `activities.meta.yaml`) plus this action's own\n    `\"Admin
            access required\"` — all render the BARE shape (`{\"error\": \"...\"}\"`).\n
            \ • The dest-only CSRF gate (`application_controller.rb:29`), which fires
            for a\n    SESSION-strategy caller missing `X-Requested-With` and precedes
            the controller\n    entirely, renders the ENVELOPE shape (`{\"error\":
            {\"code\": \"authorization_denied\",\n    ...}}`).\nDeclared as a union
            — collapsing to either one would fail response validation for the\nother.
            `show` above carries no such union: the CSRF gate exempts GET/HEAD.\n"
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/Error"
        '404':
          description: Base census — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: "\U0001F534 TWO DIFFERENT BODY SHAPES share this status (task
            #23):\n  • The status guard (`Group::Status::APPLICABLE_FOR_ACTIVE_BUDGET`
            — ACTIVE/INACTIVE/\n    PENDING; WIDER than `ACTIVE_CRITERIA`, includes
            PENDING) renders the BARE shape\n    (`{\"error\": \"This connection cannot
            have an active budget\"}`).\n  • THREE other causes share the `{\"errors\":
            {field: [message, ...]}}` wrapper: `Budget`\n    model-validation failure
            (native `errors.as_json` — e.g. amount out of bounds), or one\n    of
            the two S3 single-key shapes (`start_time`/`end_time` unparseable).\n"
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - type: object
                  required:
                  - errors
                  additionalProperties: false
                  properties:
                    errors:
                      "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{connection_id}/expenses":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.expenses.index
      summary: Expenses linked to this connection's current budget.
      description: "Always a `200` — `{\"expenses\": [], \"meta\": {...defaults}}`
        when the connection has no\ncurrent budget at all, otherwise the real paginated
        list.\n\n#139 (ruled) applied to five keys: `requested_amount`/`approved_amount`/`spent_amount`/\n`requested_at`/`requested_by`/`created_at`,
        diverging from source's `requestedAmount`/\n`approvedAmount`/`spentAmount`/`requestedAt`/`requestedBy`/`createdAt`.\n\nS2's
        fix (tracked, landed with `activities`) is inherited here too: source's own
        local\n`page`/`per_page` overrides — which pre-date the shared-base fix and
        duplicate it exactly —\nwere dropped as provably redundant rather than ported
        as dead duplication.\n\n\U0001F534 NOT VERBATIM (fixed, #154 ruled — THE gap
        the register surfaced): source had no gate beyond\nordinary connection membership,
        so any member could list every expense on the budget —\nincluding other members'
        `requested_amount`/`requested_by`/`approved_amount`. Full-stack never\nrenders
        an expense without `allow_read_on?` -> `ExpenseObject::UserActor#basic` ->\n`object.direct_budget&.can_view_expenses?(actor)`.
        Called on the CONNECTION, applying even\nbefore a budget exists (a program
        whose mentoring model scopes expense visibility to mentors\nonly now refuses
        a mentee here, where it previously listed everything).\n\nSame 5-source 4xx
        census as `activities`/`tasks` (identical base controllers) — see\n`activities.meta.yaml`.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionExpensesResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            Base census (see `activities.meta.yaml`) plus this action's own `"Forbidden"`
            (`can_view_expenses?`). All bare shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    post:
      tags:
      - programs
      operationId: programs.member.connections.expenses.create
      summary: Request a new expense against this connection's current budget.
      description: "Body: `{\"description\": string, \"requested_amount\": decimal}`.\n\n⛔
        DELIBERATE NON-GATE (#154 ruled — do not add one, unlike `index` above): full-stack's
        own\ncreate carries no actor predicate either, and the expense permission
        strategy is ROLE-SCOPED\nTO MENTOR AND EXPLICITLY EXCLUDES ADMINS (`mentoring_models/permission_strategy/expense.rb:5,\n11-13`).
        An admin-style gate here would admit the one actor the strategy denies and
        deny the\nmentor it grants.\n\n\U0001F534 NOT VERBATIM (fixed, not documented-and-shipped):
        source reads the bare ivar\n`@current_member`, memoized as a side effect of
        source's OWN `current_member` method having\nbeen called earlier in the request
        chain — which nothing in dest's request chain does. Dest's\nadapter defines
        `current_member` as a plain method with no such ivar side effect, and\n`authenticate_member!
        = nil` is a deliberate no-op keeper, so the ivar is genuinely nil here.\nMeasured
        against the model, not assumed: `Expense` validates both `created_by_member`
        and\n`requested_by_member` present ON CREATE, so a verbatim port would make
        EVERY create request\n422 — not a silent nil write, a complete feature break
        on the first real request. Uses the\n`current_member` METHOD instead, the
        same substitution every other controller in this port\nalready makes.\n\n`422`
        with `{errors: {...}}` (a HASH shape, not this API's field-list `details`
        array) when\nthe model rejects the write; a distinct bare-string `422` when
        there is no current budget at\nall to attach the expense to — S4's \"no single
        error contract in this subtree\" finding, two\nmore instances of it in this
        one action.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConnectionExpenseCreateRequest"
      responses:
        '201':
          description: The created expense.
          content:
            application/json:
              schema:
                type: object
                required:
                - expense
                additionalProperties: false
                properties:
                  expense:
                    "$ref": "#/components/schemas/ConnectionExpense"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            TWO distinct shapes on the same status: a bare `{"error": "No active budget for this
            connection"}` when the connection has no current budget, or `{"errors": {<field>:
            [<message>]}}` — Rails' native `errors.as_json` HASH shape — when the model rejects the
            write (e.g. a missing/non-numeric `requested_amount`).
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{connection_id}/meetings":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.meetings.index
      summary: Paginated meeting list for one connection, newest first.
      description: |
        Always a `200` with `meetings` (possibly empty) and a `meta` pagination envelope.

        `?upcoming=true` (or `=1`) switches BOTH the filter (`start_time >= now`) AND the sort
        direction (ascending instead of descending) — a caller asking for "upcoming" gets the
        soonest meeting first, not the newest-created.

        NOT verbatim (fixed): source chained a second `.order(start_time: :asc)` onto a relation
        that already carried `.order(start_time: :desc)` from above. Rails `ORDER BY` clauses
        accumulate rather than replace, so the DESC always won and `upcoming=true` silently never
        sorted soonest-first on source — a real, if quiet, defect (the filter half worked; only the
        sort half was dead). Fixed with `reorder` for the ascending branch.

        #139 (ruled) applied: `start_time`/`owner_id`/`program_id`/`group_id`, not source's
        `startTime`/`ownerId`/`programId`/`groupId`.

        S1/S2 inherited for free from the shared bases (no local patch needed in this row).

        Same 5-source 4xx census as `activities`/`expenses`/`surveys` (identical base controllers) —
        see `activities.meta.yaml`.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionMeetingsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    post:
      tags:
      - programs
      operationId: programs.member.connections.meetings.create
      summary: Schedule a new meeting on this connection.
      description: "Body: `{\"topic\": string, \"start_time\": ISO8601, \"end_time\"?:
        ISO8601 (accepted, unused)}`.\n\n`end_time` is READ by nothing — the create
        body only ever writes `topic`/`start_time`/\n`owner`/`program`/`mentee_id`.
        Documented as accepted-but-ignored rather than silently\ndropped from the
        schema, since a caller sending it gets no error and no effect.\n\n`mentee_id`
        is stamped from `@connection.students.first` — the FIRST result of an\nunordered
        association, not a designated primary mentee. For a group connection (>1 mentee)\nthis
        is arbitrary among students; harmless here only because `Meeting#mentee_id`
        isn't\nvalidated present for a group meeting (`group_meeting?` is true whenever
        `group_id` is\npresent, which it always is when built off `@connection.meetings`).\n\n\U0001F534
        NOT VERBATIM (fixed): `Meeting` requires `end_time` present\n(`IceCubeSchedulable`'s
        `validates :start_time, :end_time, presence: true`) — source never\nsets it,
        so a verbatim port would 422 EVERY create request with `{\"end_time\": [\"can't
        be\nblank\"], \"occurrences\": [...]}`, never reaching the model's OWN error
        shape at all.\n`end_time` stays accepted-but-unread on the wire (see the request
        schema below) — this only\nsupplies the value the model itself demands, defaulting
        to `start_time + 60 minutes`, the\nsame figure `suggested_slots` already assumes.\n\nTwo
        validation failures short-circuit before the model is even built (invalid/past\n`start_time`);
        anything else funnels through `Meeting`'s own validations. All three render\nthe
        SAME `{\"errors\": {field: [message]}}` hash shape — no S4 split in this action,
        unlike\n`expenses#create`.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConnectionMeetingCreateRequest"
      responses:
        '201':
          description: The created meeting.
          content:
            application/json:
              schema:
                type: object
                required:
                - meeting
                additionalProperties: false
                properties:
                  meeting:
                    "$ref": "#/components/schemas/ConnectionMeeting"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{connection_id}/meetings/suggested_slots":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.meetings.suggested_slots
      summary: Three static candidate meeting slots — not computed from either party's
        calendar.
      description: "\U0001F534 HARDCODED, NOT COMPUTED: always exactly 3 slots, one
        week apart, starting tomorrow at\n10:00 UTC — irrespective of the connection,
        its members' timezones, or any real availability.\nNever reads `@connection`
        at all. Ported as-is (a stub placeholder, matching `people.meta.yaml`'s\n`match_score`
        precedent) rather than dropped, since it is a real routed, documented endpoint
        on\nsource, not dead code.\n\n#139 (ruled) applied: `suggested_slots`/`start_time`,
        not source's `suggestedSlots`/`startTime`.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - suggested_slots
                additionalProperties: false
                properties:
                  suggested_slots:
                    type: array
                    items:
                      type: object
                      required:
                      - start_time
                      - duration
                      additionalProperties: false
                      properties:
                        start_time:
                          type: string
                          description: ISO8601.
                        duration:
                          type: integer
                          description: Minutes. Always `60`.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/meetings/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.meetings.show
      summary: A single meeting on this connection.
      description: |
        A THIRD `404` source layers on top of the 5-source census (see `activities.meta.yaml`):
        `{"error": "Meeting not found"}` when `:id` doesn't resolve within `@connection.meetings` —
        evaluated LAST, after program/connection/membership all check out.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - meeting
                additionalProperties: false
                properties:
                  meeting:
                    "$ref": "#/components/schemas/ConnectionMeeting"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            THREE distinct causes render the SAME bare shape — "Program not found",
            "Connection not found", or (uniquely to this operation) "Meeting not found". See the
            top-level `index` description.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/members":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.members.index
      summary: This connection's members with role and ownership info.
      description: |
        #139 (ruled) applied: `user_id`/`member_id`/`role_id`/`avatar_url`, not source's `userId`/
        `memberId`/`roleId`/`avatarUrl`.

        Same 5-source 4xx census as `activities`/`expenses`/`surveys`/`meetings` (identical base
        controllers) — see `activities.meta.yaml`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - members
                additionalProperties: false
                properties:
                  members:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ConnectionMember"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    post:
      tags:
      - programs
      operationId: programs.member.connections.members.create
      summary: Add a user to this connection.
      description: "Body: `{\"user_id\": integer, \"role_id\"?: integer}` — `role_id`
        defaults to the program's first\nmentoring role.\n\n\U0001F534 #146(1) (ruled
        — operator escalation, not document-and-ship): source gated every write in\nthis
        controller on ordinary connection membership ONLY — no admin check, no PBE
        check. Any\nconnection member could add arbitrary program users, remove the
        mentor, flip ownership, or\nreplace the whole participant set. Legacy dest
        treats membership management as ADMIN work, so\nrouting this verbatim would
        have changed WHO may restructure a group. FIXED by applying the\nSAME `project_based?`
        gate `connections_controller.rb#create` already enforces (line 56) — not\nan
        admin gate: `members_controller.rb` genuinely carried no gate at all where
        its sibling did,\nso the surface is PBE-shaped by design and simply missed
        the gate its sibling has. An\nadmin-only gate would have BROKEN intended PBE
        self-management; this one closes the hole for\nevery non-PBE program without
        touching it.\n\n\U0001F534 FIXED, NOT VERBATIM (independent of the ruling
        — a real correctness bug found while\nporting): source built the membership
        with `role:` set directly, then called the generic\n`.save`. That NEVER runs
        `update_role!` — the ONLY place in this model that derives the STI\n`type`
        column (`Connection::MentorMembership` / `MenteeMembership`) from the role\n(`MEMBERSHIP_TYPE_HASH`).
        A verbatim port would persist `type: nil` forever: the member is\nadded (`has_member?`
        is type-agnostic) but invisible to every type-keyed role check\n(`has_mentor?`/`has_mentee?`),
        breaking any downstream feature gated on role. Fixed by using\nthe SAME `build`
        + `update_role!` two-step `connections_controller.rb`'s own membership\ncreation
        (its `create`, line 76-77) already establishes — porting the sibling's answer
        rather\nthan inventing a new one, the same principle behind ①.\n\n\U0001F534
        ③ FIXED — a follow-up operator escalation on ①'s original fix, landed AFTER
        ① shipped: the\n`project_based?` gate alone checks only the PROGRAM's type,
        not whether THIS actor has\nauthority over THIS role bucket. On the real full-stack
        surface, PBE self-management is\nOWNER-based per role bucket, never any-member-based\n(`user.can_manage_role_in_group?(group,
        role)` — the same predicate a real membership write\nis gated on, `group_members_updater_membership_actions_util.rb:49`),
        and it is\n`allow_manage_on?(group) || (is_owner_of?(group) && role.can_be_added_by_owners?)`
        —\n`Role#can_be_added_by_owners` is a real, per-role column (default `true`)
        letting a program\nprotect a role (e.g. mentor) from owner self-service while
        leaving others open. Every write\nbelow now checks this, on the role(s) it
        actually mutates, before touching a record.\n\n⚠️ TWO ACCEPTED DIVERGENCES
        FROM FULL-STACK (operator-ruled: accepted and documented, not\nguarded): full-stack
        restricts `MANAGE_MEMBERS` to ACTIVE/INACTIVE/DRAFTED/PENDING connections\n(`group/actions.rb:93`),
        so it forbids membership writes (`create`/`update`/`destroy`/\n`replace`)
        on a PROPOSED connection, while this API's read-only gate blocks only `closed?`
        —\nV3 permits what full-stack forbids there. Separately, full-stack restricts\n`ADD_REMOVE_OWNERS`
        to ACTIVE/INACTIVE/PENDING/PROPOSED (`:94`), so it forbids an owner flip\non
        a DRAFTED connection, which this API's gate also does not block. Both are
        narrow (PBE-only,\nthe actor is already owner-or-admin either way) and intentionally
        left as scope divergences\nrather than two new status guards.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - user_id
              additionalProperties: false
              properties:
                user_id:
                  type: integer
                role_id:
                  type: integer
      responses:
        '201':
          description: The added member.
          content:
            application/json:
              schema:
                type: object
                required:
                - member
                additionalProperties: false
                properties:
                  member:
                    "$ref": "#/components/schemas/ConnectionMember"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            FOUR distinct causes render the SAME bare shape: the shared base census (see
            `activities.meta.yaml`) plus `"Membership management is admin-only for this program
            type"` (①'s gate) plus `"Forbidden"` (③'s per-role actor check — the caller may not
            manage the role being assigned).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            Base census (see `activities.meta.yaml`) plus two more unique to this action:
            `"User not found"` and `"Role not found"`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: '`"User is already a member"` — a bare shape, not this API''s
            hash/array errors.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/members/replace":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.members.replace
      summary: Swap one connection member for another, in one transaction.
      description: |
        Body: `{"remove_user_id": integer, "add_user_id": integer, "role_id"?: integer}` — `role_id`
        defaults to the removed member's own role.

        Shares ①'s #146(1) gate and ②'s STI fix: source's `Connection::Membership.create!(group:,
        user:, role:)` has the SAME `type`-desync defect as `create`/`update`, here even more silent
        — `create!` never raises for a missing `type`; it just persists `type: nil`. Fixed the same
        way, building through the connection's own association and finishing with `update_role!`.

        ③'s actor check applies to BOTH the outgoing member's role AND the incoming role, not once
        for the whole batch — this is the row the escalation's own reproduction targeted. A
        batch-level "can manage this group's members" check would let an owner who may only manage
        students strip a PROTECTED mentor (`can_be_added_by_owners: false`) as long as the INCOMING
        role happened to be one they may manage — the removal itself needs its own authority,
        independent of what replaces it.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - remove_user_id
              - add_user_id
              additionalProperties: false
              properties:
                remove_user_id:
                  type: integer
                add_user_id:
                  type: integer
                role_id:
                  type: integer
      responses:
        '200':
          description: The newly added member (replacing the removed one).
          content:
            application/json:
              schema:
                type: object
                required:
                - member
                additionalProperties: false
                properties:
                  member:
                    "$ref": "#/components/schemas/ConnectionMember"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census plus ①'s and ③'s gates — see `create` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            Base census plus THREE unique to this action: `"User to remove not found"`,
            `"User to add not found"`, `"User to remove is not a member"`, and `"Role not found"`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            TWO shapes on this status: a bare `"Incoming user is already a member"`, or Rails' native
            `errors.as_json` hash shape from the transaction's `RecordInvalid` rescue.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{connection_id}/members/{user_id}":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    put:
      tags:
      - programs
      operationId: programs.member.connections.members.replace_member
      summary: Replace a specific member in a connection.
    patch:
      tags:
      - programs
      operationId: programs.member.connections.members.update
      summary: Change a member's role and/or ownership flag.
      description: |
        Body: `{"role_id"?: integer, "owner"?: boolean}`.

        Shares ①'s #146(1) gate and ②'s STI `update_role!` fix — a role reassignment through source's
        plain `.update(role: role)` has the identical `type`-desync defect (a mentor demoted to
        student would still read as a MentorMembership everywhere else in the app). One consequence
        of routing through `update_role!` worth naming: the in-memory Ruby object keeps its
        PRE-change STI class after `update_role!` saves a new `type` to the row (Ruby doesn't re-cast
        an object when a column changes under it), so reloading it would scope by the stale class and
        404. Re-fetched through the base `Connection::Membership` class instead, whose `find` resolves
        the correct subclass from the row's CURRENT `type`.

        ③'s actor check applies TWICE for a role change: BOTH the member's CURRENT role and the role
        they're moving TO must be ones this actor may manage — checking only the destination would
        let an owner who may manage students but not mentors demote a mentor INTO a student they're
        allowed to manage. An OWNER FLIP (`owner` present at all, `true` or `false`) is a SEPARATE
        authority (`user.can_approve_project_requests?(group)` — admin, or already an owner of this
        group) checked independently of any role change in the same request; without it, two
        non-owner members could collude (A flips B, B flips A) and both end up owners, since neither
        flip is technically a SELF-flip.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                role_id:
                  type: integer
                owner:
                  type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - member
                additionalProperties: false
                properties:
                  member:
                    "$ref": "#/components/schemas/ConnectionMember"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census plus ①'s and ③'s gates — see `create` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            Base census plus two unique to this action: `"Member not found"` (no membership for this
            `user_id` on this connection) and `"Role not found"`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Rails' native `errors.as_json` hash shape — model validation
            failures (e.g. an ineligible owner).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedValidationErrorsHash"
    delete:
      tags:
      - programs
      operationId: programs.member.connections.members.destroy
      summary: Remove a member from this connection.
      description: |
        Shares ①'s #146(1) gate. `204` with an empty body on success.

        Removing a 1:1 connection's only mentee (or only mentor) destroys the now-memberless
        connection as an existing, unrelated app-level cascade — not something this action triggers
        itself.

        ③'s actor check is gated on the REMOVED member's role bucket — an owner who may manage
        students but not mentors must not be able to remove the mentor.
      responses:
        '204':
          description: Member removed.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census plus ①'s and ③'s gates — see `create` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Base census plus `"Member not found"` — see `update` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/progress":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.progress
      summary: Certificate-qualification progress for each member of this connection.
      description: |
        Same bare-route, `:connection_id`-keyed shape as `activity_graph` above — see its
        description.

        #139 (ruled) applied: `user_id`/`certificate_status`/`qualified_at`, not source's `userId`/
        `certificateStatus`/`qualifiedAt`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - progress
                additionalProperties: false
                properties:
                  progress:
                    type: array
                    items:
                      type: object
                      required:
                      - user_id
                      - role
                      - certificate_status
                      - qualified_at
                      - criteria
                      additionalProperties: false
                      properties:
                        user_id:
                          type:
                          - integer
                          - 'null'
                        role:
                          type:
                          - string
                          - 'null'
                        certificate_status:
                          type:
                          - string
                          - 'null'
                        qualified_at:
                          type:
                          - string
                          - 'null'
                          description: ISO8601.
                        criteria:
                          type: object
                          additionalProperties: true
                          description: Free-form — passed through from `CertificateCriteria::RewardingProgress#rewarding_criteria`.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/reflections":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.reflections.index
      summary: A mentee's mentor-visible or program-visible reflections on this connection.
      description: "Always a `200` with `reflections` (possibly empty, newest first,
        capped at 20). Excludes any\nreflection whose `visibility` is neither `mentor_visible`
        nor `program_visible` (i.e. a fully\nprivate one) — for EVERY reader, not
        just a mentor.\n\n#139 (ruled) applied: `trigger_type`/`occurred_at`, not
        source's `triggerType`/`occurredAt`.\n\n\U0001F534 #146(2) (ruled — operator
        escalation, not a documented-and-shipped item): source resolved\nthe TARGET
        mentee as \"the connection member who isn't me\" — correct for a strict 1:1
        (there is\nonly ever one other party), but WRONG in a group: a mentee reading
        this endpoint could receive\na DIFFERENT, PEER mentee's `mentor_visible` reflections,
        since \"not me\" matches any other\nmember indiscriminately. This project's
        own domain rules make one-to-many mentoring produce\nGROUP connections — the
        common shape, not an edge case — so the defect was live on the common\npath,
        not a corner case. FIXED by scoping to the reader's own role: a mentee always
        sees their\nOWN reflections; only the connection's actual mentor (or an admin,
        who bypasses connection\nmembership checks entirely) may view a different
        member's. This also incidentally fixes a\nsmaller pre-existing bug: a mentee
        reading their OWN 1:1 connection under the old code got\n`404 \"Mentee not
        found\"` (the \"isn't me\" filter matched nothing), rather than their own
        data.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - reflections
                additionalProperties: false
                properties:
                  reflections:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ConnectionReflection"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            THREE distinct causes render the SAME bare shape: "Program not found" ·
            "Connection not found" · (unique to this row) `"Mentee not found"` — when the reader is
            neither the mentor, an admin, nor a mentee themselves, or the connection genuinely has no
            mentee at all.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/reflections/coaching_hints":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.reflections.coaching_hints
      summary: Recurring reflection themes and AI-authored coaching hints for the
        target mentee.
      description: |
        #139 (ruled) applied: `recurring_themes`/`theme_suggestions`/`mentee_name`, not source's
        `recurringThemes`/`themeSuggestions`/`menteeName`. Shares `index`'s reader-role scoping fix
        and its 404 census — see the `index` description.

        `recurring_themes` requires a theme to appear in 3+ of the last 10 reflections. `hints` are up
        to 3 raw `ai_insights.coaching_hint` strings already stored on the reflection's own
        `properties` — not generated by this endpoint. `theme_suggestions` are a fixed-template
        sentence per recurring theme, not an LLM call.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - recurring_themes
                - hints
                - theme_suggestions
                - mentee_name
                additionalProperties: false
                properties:
                  recurring_themes:
                    type: array
                    items:
                      type: string
                  hints:
                    type: array
                    items:
                      type: string
                  theme_suggestions:
                    type: array
                    items:
                      type: string
                  mentee_name:
                    type: string
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Same three-source census as `index` — see its description.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/reflections/mood_trend":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.reflections.mood_trend
      summary: A 3-point rolling comparison of the target mentee's reported mood.
      description: |
        `trend` is one of `improving` / `declining` / `stable` / `insufficient_data` (fewer than 3
        mood-bearing reflections). Shares `index`'s reader-role scoping fix and its 404 census — see
        the `index` description.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - moods
                - trend
                additionalProperties: false
                properties:
                  moods:
                    type: array
                    items:
                      type: object
                      required:
                      - mood
                      - date
                      additionalProperties: false
                      properties:
                        mood:
                          type: number
                        date:
                          type:
                          - string
                          - 'null'
                          description: ISO8601 date (no time component).
                  trend:
                    type: string
                    enum:
                    - improving
                    - declining
                    - stable
                    - insufficient_data
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Same three-source census as `index` — see its description.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/resources":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.resources.index
      summary: Learning resources shared in this connection.
      description: "\U0001F534 ALWAYS `{\"resources\": [], \"meta\": {\"page\": 1,
        \"per_page\": 25, \"total\": 0, \"total_pages\": 0}}`\non both trees — verbatim
        from source, whose own comment names this \"Wave 1: returns empty\nlist (full
        implementation in Wave 3).\" Not a port defect and not something this port
        can\ncomplete: Wave 3 is a source-side milestone with no corresponding work
        item here. Documented\nat the contract so a client (or a future implementer)
        does not mistake the permanently-empty\nresponse for \"no resources have been
        shared yet.\"\n\n4xx census identical to `activities` (same two base controllers):
        envelope 401 · bare 404\n\"Program not found\" · bare 403 \"You are not a
        member of this program\" · bare 404 \"Connection\nnot found\" · bare 403 \"Forbidden\".\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionResourcesResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            Two distinct causes render the SAME bare shape with DIFFERENT messages — see the
            top-level description's linked census (`activities.meta.yaml`).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes ("Program not found" / "Connection not
            found") render the SAME bare shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/rumi_nudges":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.rumi_nudges.index
      summary: List rumi nudges.
  "/programs/{program_id}/member/connections/{connection_id}/surveys":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.surveys.index
      summary: Active engagement surveys for this program, with this connection's
        response status.
      description: "Always a `200` with `surveys` (possibly empty) — no pagination
        envelope (source never\npaginates this list).\n\n#139 (ruled) applied: `completed_at`/`is_draft`,
        not source's `completedAt`/`isDraft`.\n\nNOT verbatim (fixed): `Survey#translates
        :name` (Globalize) touches `translations` once per\nrow on `survey.name` —
        unpreloaded on source, an N+1 caught by dest's test suite (source's own\ntest
        never lists more than one survey at a time). Fixed with `.includes(:translations)`,
        same\nshape as the `community`/`activities` N+1 fixes elsewhere in this port.\n\n\U0001F534
        #146 (ruled): source also has a `POST .../surveys/{id}/remind` action. Dropped\nentirely,
        not ported — it never sends anything; it counts connection members who haven't\nresponded
        and returns `{\"reminded\": <count>}` as if a reminder had gone out. A fake-success\nno-op
        is worse than a missing feature: a caller believes a reminder was sent.\n\n\U0001F534
        `is_draft` DOCUMENTED, NOT FIXED — dead-but-harmless: `@connection.survey_responses`
        reads\nthrough `SurveyResponse`'s own `default_scope { where(is_draft: false)
        }`, so a draft response\nnever reaches this lookup at all. `is_draft` can
        therefore only ever render `false` (a\ncompleted response exists) or `null`
        (none exists) — never `true`. Kept on the wire rather\nthan dropped, matching
        this port's standing precedent for a field verified inert rather than\nbroken
        (see `people.meta.yaml`'s `match_score`).\n\nSame 5-source 4xx census as `activities`/`expenses`
        (identical base controllers) — see\n`activities.meta.yaml`.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionSurveysResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/surveys/{id}/remind":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.surveys.remind
      summary: Remind a survey.
  "/programs/{program_id}/member/connections/{connection_id}/tasks/reorder":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - programs
      operationId: programs.member.connections.tasks.reorder
      summary: Reorder this connection's mentoring-model tasks.
      description: "Body: `{\"positions\": [{\"id\": <task_id>, \"position\": <int>},
        ...]}`. Renders the connection's\nFULL task list in its new order on success
        — not just the tasks that were repositioned.\n\n#139 (ruled) applied to six
        keys: `due_date`/`milestone_id`/`goal_id`/`assignee_id`/\n`completed_date`/`completed_by`,
        diverging from source's `dueDate`/`milestoneId`/`goalId`/\n`assigneeId`/`completedDate`/`completedBy`.\n\nS1
        (fixed, not verbatim): source's read-only guard was keyed on an action-name
        whitelist that\nnever named `reorder`, so this write was UNGUARDED against
        a closed connection. Now blocked by\nthe base's verb-keyed guard (`connections/base_controller.rb`)
        like every other write under\nthis base — see `activities.meta.yaml` for the
        shared 4xx census this adds one entry to.\n\n\U0001F534 NOT VERBATIM (fixed,
        #27 — SUPERSEDES the S4-tracked interim, which accepted this as an\nunrescued
        raw exception): source's bare `@connection.mentoring_model_tasks.find(entry[:id])`\nraised
        `ActiveRecord::RecordNotFound` with no rescue anywhere in the action — this
        engine has\nZERO `rescue_from` (verified engine-wide), so an `id` inside `positions`
        not belonging to this\nconnection was an unhandled 500. The SCOPE itself was
        already correct (no other connection's\ntask is reachable); only the response
        code was wrong. Now `find_by` + an explicit render, the\nsame house pattern
        the rest of this subtree already uses, yielding a clean 404 with this\ncontroller's
        own bare `{\"error\": \"Task not found\"}` shape instead of a raw exception.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ConnectionTasksReorderResponse"
        '400':
          description: "`positions` was not an array. Bare error shape."
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            THREE distinct causes render the SAME bare shape with DIFFERENT messages: "You are not a
            member of this program" · "Forbidden" (not a member of this connection) · "Connection is
            closed (read-only)" (S1's guard, now reachable here). See `activities.meta.yaml` for the
            shared base census.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            THREE distinct causes render the SAME bare shape: "Program not found" · "Connection not
            found" · "Task not found" (#27's fix — an `id` inside `positions` not belonging to this
            connection).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/topics":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.forum.index
      summary: This connection's forum topics, newest first.
      description: "`[]` when the connection has never had a topic created (no forum
        row exists yet — see the\nwrite-on-read fix below).\n\n\U0001F534 FIXED, NOT
        VERBATIM (found while porting, not one of #146's four escalated/ruled items,\nbut
        named alongside them in the same TODOS entry as a smaller instance of the
        same family):\nsource's `resolve_forum` ran on EVERY action — `index`/`show`
        included — and\nauto-vivified a `Forum` row (`@connection.forum || @connection.create_forum(...)`)
        the FIRST\ntime anyone so much as viewed the topic list, including on a CLOSED
        (read-only) connection.\nA plain GET should not have a persistent side effect.
        Reads now only LOOK UP an existing\nforum and never create one; only `create`
        (the first genuine write) auto-vivifies.\n\n#139 (ruled) applied: `user_id`/`posts_count`/`created_at`/`updated_at`,
        not source's\n`userId`/`postsCount`/`createdAt`/`updatedAt`.\n\nSame 5-source
        4xx census as `activities`/`expenses`/`surveys`/`meetings`/`members` (identical\nbase
        controllers) — see `activities.meta.yaml`.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - topics
                additionalProperties: false
                properties:
                  topics:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ConnectionTopic"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    post:
      tags:
      - programs
      operationId: programs.member.connections.forum.create
      summary: Start a new forum topic on this connection (creating the forum lazily,
        if needed).
      description: |
        Body: `{"title": string, "body": string}`. Creates the connection's forum on first use — see
        the `index` description for why that lazy-create moved here from every read.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - title
              - body
              additionalProperties: false
              properties:
                title:
                  type: string
                body:
                  type: string
      responses:
        '201':
          description: The created topic.
          content:
            application/json:
              schema:
                type: object
                required:
                - topic
                additionalProperties: false
                properties:
                  topic:
                    "$ref": "#/components/schemas/ConnectionTopic"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            THREE distinct causes render the SAME bare shape: the base census (see
            `activities.meta.yaml`) plus S1's write-on-a-closed-connection guard, now reachable here
            like every other write under this base.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Rails' native `errors.as_json` hash shape — `title`/`body`
            blank.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{connection_id}/topics/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.forum.show
      summary: A topic with its posts, oldest first.
      description: |
        A THIRD `404` source on top of the base census (see `activities.meta.yaml`):
        `{"error": "Topic not found"}` — including when the connection has no forum at all yet
        (`@forum` is nil, so no topic id can ever resolve under it).
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - topic
                - posts
                additionalProperties: false
                properties:
                  topic:
                    "$ref": "#/components/schemas/ConnectionTopic"
                  posts:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ConnectionPost"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            THREE distinct causes render the SAME bare shape: base census plus
            `"Topic not found"` — see the top-level description.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{connection_id}/topics/{id}/posts":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.forum.create_post
      summary: Reply to a topic.
      description: "Body: `{\"body\": string}`.\n\n\U0001F534 FIXED, NOT VERBATIM:
        source called a LOCAL `enforce_read_only_for_write!` here — this\naction was
        one of the MEASURED examples motivating S1's central fix (a custom, non-CRUD
        write\naction dodging source's action-name whitelist). Dropped as provably
        redundant: the base's\nverb-keyed guard (`connections/base_controller.rb`)
        now blocks every non-GET action,\nincluding this one, before the action body
        even runs.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - body
              additionalProperties: false
              properties:
                body:
                  type: string
      responses:
        '201':
          description: The created post.
          content:
            application/json:
              schema:
                type: object
                required:
                - post
                additionalProperties: false
                properties:
                  post:
                    "$ref": "#/components/schemas/ConnectionPost"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            THREE distinct causes render the SAME bare shape: base census plus S1's write-on-a-closed
            -connection guard — see `create` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: |
            THREE distinct causes render the SAME bare shape: base census plus `"Topic not found"`
            — see `show` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Rails' native `errors.as_json` hash shape — `body` blank.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.show
      summary: Full detail for one connection — members, task stats, next meeting.
      description: |
        #139 (ruled) applied: `read_only`/`created_at`/`expiry_time`/`task_stats`/`next_meeting`
        (and their nested fields), not source's `readOnly`/`createdAt`/`expiryTime`/`taskStats`/
        `nextMeeting`.

        NOT verbatim (fixed): `membership_json` reads `user.member` and `member.picture_url` (->
        `profile_picture`) — two N+1s unpreloaded by source, caught by dest's test suite.

        Same 5-source 4xx census as the connections/* subtree's other rows (identical base
        controllers) — see `activities.meta.yaml`. This one keyed on `:id` rather than
        `:connection_id` (this IS the connection resource, not a child of it) — the base's dual read
        (`params[:connection_id] || params[:id]`) covers both.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
    put:
      tags:
      - programs
      operationId: programs.member.connections.replace
      summary: Replace a connection.
    patch:
      tags:
      - programs
      operationId: programs.member.connections.update
      summary: Rename or redescribe a connection.
      description: |
        Body: `{"name"?: string, "description"?: string}`. Any connection member may call this — no
        additional gate beyond ordinary membership.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                name:
                  type: string
                description:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            THREE distinct causes render the SAME bare shape: the base census plus S1's write-guard,
            now reachable here (`"Connection is closed (read-only)"`) like every other write in this
            subtree.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: Rails' native `errors.as_json` hash shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{id}/answers":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - programs
      operationId: programs.member.connections.update_answers
      summary: Save answers to this program's connection questions for this connection.
      description: 'Body: `{"answers": {"<question_id>": "<answer text>", ...}}`.
        Always `{"success": true}`.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - answers
              additionalProperties: false
              properties:
                answers:
                  type: object
                  description: Question id (string) to answer text.
                  additionalProperties:
                    type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - success
                additionalProperties: false
                properties:
                  success:
                    type: boolean
                    const: true
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            THREE distinct causes render the SAME bare shape: base census plus S1's write-guard — see
            `update` above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{id}/close":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.close
      summary: Close an active connection. Admin only.
      description: |
        Body: `{"reason"?: string, "closure_reason_id"?: integer}` — defaults to the program's
        default+completed `GroupClosureReason` if omitted.

        NOT verbatim: skips S1's write-guard (`enforce_read_only!`) — verbatim from source
        (routes.rb:9). This action operates on an ACTIVE connection, so the skip is harmless
        redundancy for it specifically; see the class-wide derivation on the `skip_before_action`
        line in the controller.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                reason:
                  type: string
                closure_reason_id:
                  type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census plus `"Admin access required"` — see `publish`
            above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            TWO shapes: a bare `"No closure reason available for this program"`, or Rails' native
            `errors.as_json` hash shape from the transaction's `RecordInvalid` rescue.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{id}/discard":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.discard
      summary: Discard (destroy) a DRAFTED connection.
      description: "\U0001F534 FIXED, NOT VERBATIM — advisor-confirmed against full-stack,
        not derived from a\nneighboring action in this file: source destroyed UNCONDITIONALLY,
        gated on bare `is_admin?`\nalone. Its OWN doc comment said \"discards a drafted
        connection\", but the code never checked\nstatus before calling `.destroy`
        — unlike its sibling `withdraw` (below), which DOES gate on\nstatus first.
        Calling this on an ACTIVE or CLOSED connection with real history would have\nirreversibly
        destroyed it. Two fixes, both matching full-stack's own authority for this
        exact\nverb (`group/actions.rb:78,127`): (1) requires `drafted?` before destroying
        — same status\ncheck `withdraw` already had; (2) gated on `allow_manage_on?(group)`
        rather than bare\n`is_admin?` — the metaprogrammed ActionPermission predicate,
        chosen by VERB against\nfull-stack directly rather than copied from a sibling.
        The one actor newly denied here is a\nsegment-restricted sub-admin whom full-stack
        already 403s today — conformance, not\nregression.\n\nNOT verbatim: skips
        S1's write-guard — verbatim from source; this action operates on DRAFTED\n(never
        closed), so the skip is harmless redundancy here specifically.\n"
      responses:
        '204':
          description: Connection destroyed.
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census plus `"Admin access required"` (now `allow_manage_on?`-gated,
            see above).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: '`"Only drafted connections can be discarded"` — a bare shape,
            the fix above.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{id}/expiry":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - programs
      operationId: programs.member.connections.update_expiry
      summary: Update the expiry date of an active connection. Admin only.
      description: "Body: `{\"expiry_time\": ISO8601 (required), \"reason\"?: string}`.\n\n\U0001F534
        S3 (fixed, not verbatim): same unrescued `Time.zone.parse` shape as `create`'s\n`start_date`
        and `reactivate`'s `expiry_time` — a required field here, so the parse failure\nand
        the presence failure are two distinct 422s.\n\nNOT verbatim: skips S1's write-guard
        — verbatim from source; this action operates on an\nACTIVE connection, so
        the skip is harmless redundancy here specifically.\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - expiry_time
              additionalProperties: false
              properties:
                expiry_time:
                  type: string
                  description: ISO8601.
                reason:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census plus `"Admin access required"` — see `publish`
            above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            THREE shapes on this status: a bare `"expiry_time is required"`, a bare
            `{"errors": {"expiry_time": [...]}}` for S3's parse failure, or Rails' native
            `errors.as_json` hash shape from `change_expiry_date`'s own validation failure.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{id}/join_settings":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.join_settings
      summary: Per-role join settings for this connection.
      description: |
        #139 (ruled) applied: `role_name`/`allow_join`/`min_limit`/`max_limit`, not source's
        `roleName`/`allowJoin`/`minLimit`/`maxLimit`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - join_settings
                additionalProperties: false
                properties:
                  join_settings:
                    type: array
                    items:
                      type: object
                      required:
                      - role_id
                      - role_name
                      - allow_join
                      - min_limit
                      - max_limit
                      additionalProperties: false
                      properties:
                        role_id:
                          type: integer
                        role_name:
                          type:
                          - string
                          - 'null'
                        allow_join:
                          type:
                          - boolean
                          - 'null'
                        min_limit:
                          type:
                          - integer
                          - 'null'
                        max_limit:
                          type:
                          - integer
                          - 'null'
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{id}/publish":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.publish
      summary: Publish a drafted or pending connection, making it active. Admin only.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            THREE distinct causes render the SAME bare shape: base census plus
            `"Admin access required"` (bare `is_admin?`, verbatim from source — not advisor-reviewed
            for this action; see `discard` for the one action in this file that WAS).
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: '`{"error": "<message>"}` — whatever `Group#publish` itself
            raises, rescued and stringified. A bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{id}/questions":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.connections.questions
      summary: This program's connection questions, with this connection's own answers
        where present.
      description: |
        NOT verbatim (fixed): `Connection::Question#translates :question_text` (Globalize) touches
        `translations` once per row — an N+1 unpreloaded by source, caught by dest's test suite. Same
        shape as the `surveys`/`community` N+1 fixes elsewhere in this port.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - questions
                additionalProperties: false
                properties:
                  questions:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - text
                      - answer_text
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                        text:
                          type:
                          - string
                          - 'null'
                        answer_text:
                          type:
                          - string
                          - 'null'
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/connections/{id}/reactivate":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.reactivate
      summary: Reopen a closed connection. Admin only.
      description: "Body: `{\"expiry_time\"?: ISO8601, \"reason\"?: string}`.\n\n\U0001F534
        S3 (fixed, not verbatim): unrescued `Time.zone.parse` on an unparseable `expiry_time`\nraised
        a raw 500. Same fix shape as `create`'s `start_date`.\n\nNOT verbatim: skips
        S1's write-guard — verbatim from source, and load-bearing HERE\nspecifically:
        reactivating a CLOSED connection is this action's entire purpose, so without\nthe
        skip every call would 403 against the base's verb-keyed guard before the action
        body\never ran.\n"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                expiry_time:
                  type: string
                  description: ISO8601. Rejected (422) if present but unparseable.
                reason:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Base census plus `"Admin access required"` — see `publish`
            above.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            Rails' native `errors.as_json` hash shape, or a bare `{"errors": {"expiry_time": [...]}}`
            for S3's parse failure.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/connections/{id}/withdraw":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - programs
      operationId: programs.member.connections.withdraw
      summary: Withdraw a pending or proposed connection.
      description: |
        Body: `{"reason"?: string}`. Any connection member may call this — no admin gate, unlike its
        sibling `discard`.

        NOT verbatim: skips S1's write-guard — verbatim from source; this action's own status check
        (`pending?`/`proposed?`) already excludes `closed?`, so the skip is harmless redundancy here
        specifically.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                reason:
                  type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - connection
                additionalProperties: false
                properties:
                  connection:
                    "$ref": "#/components/schemas/ConnectionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: Two distinct causes render the SAME bare shape — see `activities.meta.yaml`.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '422':
          description: |
            TWO shapes: a bare `"Only pending or proposed connections can be withdrawn"`, or Rails'
            native `errors.as_json` hash shape from the `RecordInvalid` rescue.
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/PortedBareError"
                - "$ref": "#/components/schemas/PortedValidationErrorsHash"
  "/programs/{program_id}/member/overview":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.overview.show
      summary: The calling member's dashboard for one program — stats, announcements,
        and next actions.
      description: |
        Always a `200` with all six top-level keys once the caller resolves as an active user of the
        program — there is no branch that omits one.

        ⚠️ EVERY KEY BELOW IS SNAKE_CASE, DIVERGING FROM SOURCE. Source renders 7 camelCase keys
        (`startDate`, `endDate`, `userRole`, `sessionsCompleted`, `hoursLogged`, `tasksPending`,
        `nextActions`) mixed with snake_case in the same payload, against `openapi/CLAUDE.md`'s
        documented rule ("All keys are snake_case in responses and request bodies"). Ruled once for the
        whole camelCase-wire-key class (#139) rather than per endpoint, because a per-endpoint answer
        makes the idiom site-dependent — the same failure shape as `nullable` (#123). MEASURED, not
        assumed, before ruling: source's own SPA never observes wire casing at all — `deepCamel` was
        executed (not read from a comment) against source's locked dependency pin on all 7 keys
        including the nested `next_actions[]` array, and camel-in / snake-in converge on byte-identical
        output; dest's `camelize-schema.mjs` was executed the other direction and produces exactly the
        camel names source's SPA types expect. The one caveat that does NOT clear: an unported
        api-e2e suite asserts one of the 7 keys directly off the wire with no seam in between — a cost
        for whenever that suite lands, not today.

        #140 (ruled, FIXED, not verbatim): source's `total_tasks` counted every task in the caller's
        groups regardless of whose it is, while `tasks_pending` counted only the caller's own — so in
        a group connection (the common shape for one-to-many programs) a member who had done nothing
        could read a near-100% dashboard, crediting other members' completed work. `total_tasks` is
        now scoped identically to `tasks_pending`, one line, one query added.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramOverviewResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            The caller is an active org member but not an active user of THIS program
            (`programs/member/base_controller.rb:20`). Bare error shape, not this API's envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: No such program in the caller's organization. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/people":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.people.index
      summary: Every other active or pending user in the program, from the calling
        member's perspective.
      description: "Always a `200` with a `people` array — an empty array is the normal
        answer for a program with\nno other members, not an error. The caller is excluded
        from their own list.\n\n#139 (ruled) applied: `match_score`, not source's
        `matchScore`.\n\n\U0001F534 `match_score` IS A TEMPORARY PLACEHOLDER, NOT
        A COMPUTED MATCH (#141, ruled by the operator).\nThe controller computes it
        as `rand(60..95)` — a random number, not a real compatibility\nsignal. Ruled
        (C): land verbatim with disclosure rather than drop the field, because a real\nmatching
        service (`MatchingClient::MatchingComputerApi`) already exists to wire in
        later, and\nremoving the key now would mean re-adding it then. Any consumer
        of this field must treat it as\nnon-functional until a future change computes
        it for real.\n\nTwo fields are STRUCTURALLY OMITTED, not null, and only for
        a SUBSET of rows:\n  - `availability`: present only when the row's `role`
        is `mentor`.\n  - `match_score`: present only when the row is a mentor AND
        the calling member is a mentee\n    (mentors never see a match score for another
        mentor, and mentees never see their own).\n\n⚠️ PERFORMANCE, recorded rather
        than fixed, and MEASURED rather than estimated: this endpoint\nhas NO pagination
        (the shared base controller's `page`/`per_page` helpers exist and are unused\nhere)
        and issues several queries PER ROW — a profile-questions lookup, an `answer_for`
        per\nquestion, and (for mentor rows) a `groups.count` for availability. On
        a real-scale program\n(45 users) this measures **345 queries for a single
        request**. Reachable and real on every\nrequest with any program membership,
        unlike some other inherited findings in this port. Not\nfixed here: batching
        it (bulk `answer_for`/preloaded profile answers across all rows) is a\nmaterially
        larger rewrite than a preload hint, and no ruling has asked for it. Pinned
        with an\nexplicit `assert_queries` against a small, stable fixture roster
        (not the 45-user one, so the\npin isn't coupled to unrelated org-wide fixture
        drift) so a future change to this method's\nquery shape is caught, not silently
        absorbed.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramPeopleResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            The caller is an active org member but not an active user of THIS program
            (`programs/member/base_controller.rb:20`). Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: No such program in the caller's organization. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/reflections":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.reflections.index
      summary: The caller's own top-level reflections across reflectable contexts,
        paginated.
      description: |
        Reflectable contexts: `Meeting`, `Content::Spark`, `Content::RoleplayAttempt`, `Member`,
        `Agent`. Query params: `page`, `per_page` (default 20, max 50 — narrower than this API's
        usual 25/100, kept as source's own local bounds rather than the shared base's).

        DISTINCT FROM `connections/reflections.meta.yaml` (landed 9ae06911380): that row is
        CONNECTION-scoped (a mentor reading one mentee's reflections on one connection); this one is
        PROGRAM-scoped (a member reading their OWN reflections across every reflectable context in
        the program). Two different controllers, two different authorities, coincidentally similar
        names.

        NOT AN LLM CALL, and not fenced by #60: this controller is pure read (three GETs over
        `Interaction.of_reflection_type`), touches no `Reflection::Analyzer`/ChronusAI/embedding
        code, and every `reflection_*` accessor it renders is a stored-JSON `dig`
        (`interaction.rb:257-262`). `Reflection::Analyzer` is the SOLE writer of `themes`/`mood`/
        `summary` (fenced separately, tracked #60) — until that seam lands, every reflection here
        renders those fields empty/null. Live-but-empty, the same shape already documented at
        `connections/reflections`'s `is_draft` finding — not a silent failure.

        #139 (ruled) applied: `per_page`, not source's `perPage`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - reflections
                - meta
                additionalProperties: false
                properties:
                  reflections:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ReflectionSummary"
                  meta:
                    type: object
                    required:
                    - total
                    - page
                    - per_page
                    additionalProperties: false
                    properties:
                      total:
                        type: integer
                      page:
                        type: integer
                      per_page:
                        type: integer
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: '`"You are not a member of this program"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: '`"Program not found"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/reflections/journal":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.reflections.journal
      summary: Up to 50 recent reflections, plus in-controller insight arithmetic.
      description: |
        `growth_observation` is NOT generated: it is a mood-average comparison (first half vs second
        half of up to 10 recent reflections) and a theme-frequency tally, both plain Ruby over
        already-stored `themes`/`mood` values. `null` whenever fewer than 5 reflections exist, or
        when the available data is empty (`themes`/`mood` both blank — see `index`'s
        live-but-empty note, since that's the common case until #60 lands).

        #139 (ruled) applied: `top_themes`/`mood_trend`/`streak`/`total_reflections`/
        `growth_observation`, not source's `topThemes`/`moodTrend`/`streak`/`totalReflections`/
        `growthObservation`.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - reflections
                - insights
                additionalProperties: false
                properties:
                  reflections:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ReflectionSummary"
                  insights:
                    type: object
                    required:
                    - top_themes
                    - mood_trend
                    - streak
                    - total_reflections
                    - growth_observation
                    additionalProperties: false
                    properties:
                      top_themes:
                        type: array
                        items:
                          type: object
                          required:
                          - theme
                          - count
                          additionalProperties: false
                          properties:
                            theme:
                              type: string
                            count:
                              type: integer
                      mood_trend:
                        type: array
                        items:
                          type: number
                        description: Up to the 12 most recent non-null moods, newest
                          first.
                      streak:
                        type: integer
                        description: Consecutive days (including today) with at least
                          one active/completed reflection, within the last 30 days.
                      total_reflections:
                        type: integer
                        description: Count of the up-to-50 reflections this response
                          considered, not the caller's all-time total.
                      growth_observation:
                        type:
                        - string
                        - 'null'
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: '`"You are not a member of this program"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: '`"Program not found"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/reflections/pending":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.reflections.pending
      summary: Up to 5 reflections awaiting the caller's action.
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required:
                - reflections
                additionalProperties: false
                properties:
                  reflections:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ReflectionSummary"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: '`"You are not a member of this program"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: '`"Program not found"` — a bare shape.'
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/requests":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.requests.index
      summary: Pending requests targeting the calling member — mentoring requests
        for a mentor, offers for a mentee.
      description: |
        Always a `200` with a `requests` array — an empty array is the normal answer for a caller with
        nothing pending, not an error.

        The two branches are mutually exclusive and produce the SAME item shape with a different
        `type`: a mentor's incoming `NOT_ANSWERED` mentor requests, or a mentee's `PENDING` mentor
        offers. `is_mentor?` decides which query runs — never both, so a member who is both roles in
        different connections sees only their mentor-side requests here.

        ⚠️ `from.title` is a DEAD FIELD: the controller hardcodes it to `""` unconditionally — never
        the sender's actual title/role. Declared `type: string` (not nullable — it is always the empty
        string, never null) rather than dropped, because dropping it would change the wire shape for a
        field a future change might populate; ported and documented, not fixed.
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramRequestsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            The caller is an active org member but not an active user of THIS program
            (`programs/member/base_controller.rb:20`). Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: No such program in the caller's organization. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/programs/{program_id}/member/semantic-search":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.semantic_search.index
      summary: List semantic search.
  "/programs/{program_id}/member/sessions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - programs
      operationId: programs.member.sessions.index
      summary: The calling member's meetings for one program, split into upcoming
        and past (capped at 20).
      description: "Always a `200` with both keys — an empty array on either side
        is the normal answer for a\ncaller with no meetings in that bucket, not an
        error.\n\n⚠️ `meeting_url` is `meeting_url`, not source's `meetingUrl` (#139,
        ruled once for the whole\ncamelCase-wire-key class).\n\n\U0001F534 THREE FIELDS
        ARE PERMANENTLY NULL/FIXED ON THIS TREE, INHERITED ON BOTH TREES — documented
        at\nthe contract rather than fixed, following the same disposition as `rumi_nudges`'s
        never-fired\naction and `overview`'s `try`-guarded fallbacks:\n  - `meeting_url`
        (upcoming): `Meeting#meeting_url` does not exist on either tree — verified,\n
        \   not assumed (no method, no column). `meeting.try(:meeting_url)` is always
        nil.\n  - `type` (upcoming): derives from the same absent method (`.present?`
        on always-nil), so it\n    is ALWAYS the literal `\"in-person\"` — never `\"virtual\"`
        on this tree.\n  - `rating` (past): `MemberMeeting#rating` does not exist
        either — dest carries a\n    `has_many :meeting_ratings` collection instead
        (`member_meeting.rb:118`), a different\n    shape entirely, not a drop-in
        replacement. `member_meeting&.try(:rating)` is always nil.\nContrast with
        `notes` (past): `Meeting#description` IS a real, nullable text column, so\n`notes`
        genuinely varies — null only for a meeting with no description, not always.\n\n`avatar`
        (upcoming only — the past shape has no such field) and `partner` share one
        nullability\nsource: `find_partner` returns `group.students.first` / `group.mentors.first`,
        which is nil for\na group with none on that side. When `partner` is nil, `partner`
        renders `\"Unknown\"` (the `||`\ncatches it) but `avatar` has no such fallback
        and renders null. When `partner` resolves,\n`avatar` is NEVER null — `picture_url`
        returns a default placeholder asset for a member with no\nuploaded photo (member.rb:826),
        the same measured fact as `requests`' `from.avatar`.\n"
      responses:
        '200':
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramSessionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          description: |
            The caller is an active org member but not an active user of THIS program
            (`programs/member/base_controller.rb:20`). Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
        '404':
          description: No such program in the caller's organization. Bare error shape.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PortedBareError"
  "/quiz_questions":
    "$ref": v3/resources/quiz_questions.yaml#/paths/~1quiz_questions
  "/quiz_questions/{id}":
    "$ref": v3/resources/quiz_questions.yaml#/paths/~1quiz_questions~1{id}
  "/reflections":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - reflections
      operationId: reflections.index
      summary: List reflections.
    post:
      tags:
      - reflections
      operationId: reflections.create
      summary: Create a reflection.
  "/reflections/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - reflections
      operationId: reflections.show
      summary: Retrieve a reflection.
    put:
      tags:
      - reflections
      operationId: reflections.replace
      summary: Replace a reflection.
    patch:
      tags:
      - reflections
      operationId: reflections.update
      summary: Update a reflection.
  "/reflections/{id}/complete":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - reflections
      operationId: reflections.complete
      summary: Complete a reflection.
  "/reflections/{id}/messages":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - reflections
      operationId: reflections.messages.create
      summary: Create a message.
  "/resource_stats":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - resource_stats
      operationId: resource_stats.destroy
      summary: Reset the Views and Marked-as-Helpful counts of the program's resources.
      description: |
        Zeroes `view_count` on every resource the program owns and deletes their Marked-as-Helpful
        ratings, then queues an ES reindex of the affected documents. There is no per-resource variant
        and no request body: the whole library is reset, exactly as the legacy "Reset Resource Stats"
        action did.

        On a **standalone** (single-program) organization there is no separate organization level, so the
        organization-owned ("global") resources are part of the same library and are reset too. On a
        multi-program organization they are not — those are managed at the organization level.

        Chronus-internal maintenance: permitted only for a super-console session that also holds
        `manage_resources` in this program, matching the legacy affordance's visibility. Everyone else —
        including a customer program administrator — gets `403`. Program context is required; an
        organization-context request is denied rather than widened to every program.
      responses:
        '200':
          description: The counters were reset.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ResourceStatsResetResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/resources":
    "$ref": v3/resources/resource_publications.yaml#/paths/~1resources
  "/resources/aggregates":
    "$ref": v3/resources/resource_publications.yaml#/paths/~1resources~1aggregates
  "/resources/reorder":
    "$ref": v3/resources/resource_publications.yaml#/paths/~1resources~1reorder
  "/resources/{id}":
    "$ref": v3/resources/resource_publications.yaml#/paths/~1resources~1{id}
  "/rich_text_editor/attachments":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - rich_text_editor
      operationId: rich_text_editor.attachments.index
      summary: List the organization's uploaded images or files (asset browser).
      description: |
        Lists the caller organization's rich-text-editor assets of the requested `kind`, newest-first,
        paginated — the SPA analogue of the legacy Froala image/file managers. **Admin-only**: assets
        are org-global with no owner, so this returns every admin's uploads across the organization.
      responses:
        '200':
          description: A page of the org's assets of the requested kind.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                - pagination
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    type: array
                    items:
                      "$ref": "#/components/schemas/RichTextEditorAttachment"
                  pagination:
                    type: object
                    required:
                    - total
                    - page
                    - limit
                    - has_more
                    additionalProperties: false
                    properties:
                      total:
                        type: integer
                      page:
                        type: integer
                      limit:
                        type: integer
                      has_more:
                        type: boolean
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: kind
        in: query
        required: true
        description: Which asset library to browse.
        schema:
          type: string
          enum:
          - image
          - file
      - name: page
        in: query
        required: false
        description: 1-based page number (default 1).
        schema:
          type: integer
          minimum: 1
      - name: limit
        in: query
        required: false
        description: Page size (1–100; default 25; out-of-range falls back to 25).
        schema:
          type: integer
          minimum: 1
          maximum: 100
    post:
      tags:
      - rich_text_editor
      operationId: rich_text_editor.attachments.create
      summary: Upload an image or file attachment for the rich-text editor.
      description: |
        Stores an uploaded image (`kind=image`) or file (`kind=file`) to S3 via the shared
        RichTextEditor asset models, tenant-stamped to the caller's organization, and returns the
        serving URL to embed as an `<img>`/`<a>`. Enforces the same size/content-type/extension limits
        as the legacy Froala upload path.

        A retried POST with the same `Idempotency-Key` and body replays the stored `201` exactly once
        (a mismatched body returns `422 idempotency_key_reuse`); omit the header for at-least-once.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              - kind
              additionalProperties: false
              properties:
                file:
                  type: string
                  format: binary
                  description: The image or file to upload.
                kind:
                  type: string
                  enum:
                  - image
                  - file
                  description: "`image` → an inline picture; `file` → a downloadable
                    attachment."
      responses:
        '201':
          description: The asset was stored; the serving URL is returned.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    "$ref": "#/components/schemas/RichTextEditorAttachment"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - "$ref": "#/components/parameters/IdempotencyKey"
  "/rich_text_editor/attachments/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    delete:
      tags:
      - rich_text_editor
      operationId: rich_text_editor.attachments.destroy
      summary: Delete one uploaded asset (asset browser).
      description: |
        Deletes one of the caller organization's rich-text-editor assets by id. **Admin-only**,
        org-scoped. Note: no referential-integrity check — deleting an asset still referenced by other
        content breaks that reference (same behavior as the legacy file/image manager).
      responses:
        '200':
          description: The asset was deleted.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    type: object
                    required:
                    - id
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: id
        in: path
        required: true
        description: The asset id.
        schema:
          type: integer
  "/role_goal_exemplars":
    "$ref": v3/resources/role_goal_exemplars.yaml#/paths/~1role_goal_exemplars
  "/role_goal_exemplars/{id}":
    "$ref": v3/resources/role_goal_exemplars.yaml#/paths/~1role_goal_exemplars~1{id}
  "/roleplay_categories":
    "$ref": v3/resources/roleplay_categories.yaml#/paths/~1roleplay_categories
  "/roleplay_categories/{id}":
    "$ref": v3/resources/roleplay_categories.yaml#/paths/~1roleplay_categories~1{id}
  "/roleplay_shares":
    "$ref": v3/resources/roleplay_shares.yaml#/paths/~1roleplay_shares
  "/roleplay_shares/{id}":
    "$ref": v3/resources/roleplay_shares.yaml#/paths/~1roleplay_shares~1{id}
  "/rumi/action":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - retired
      operationId: rumi.action.redirect
      summary: Legacy 307 to /bot_apps/actions.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - retired
      operationId: rumi.action.redirect_post
      summary: Legacy 307 to /bot_apps/actions.
      description: |
        Same grace-window redirect as GET. POST must stay documented so the
        validator does not 405 the Bolt edge.
      responses:
        '200':
          description: Deferred — see the GET operation description above.
  "/rumi/chat":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - retired
      operationId: rumi.chat.redirect
      summary: Legacy 307 to /bot_apps/events.
      description: |
        Contract deferred per row #159 so G6 admits a routed dest endpoint. Drop
        `not_implemented` and replace with the real request/response when that
        unit is taken, not before.
      responses:
        '200':
          description: Deferred — see the operation description above.
    post:
      tags:
      - retired
      operationId: rumi.chat.redirect_post
      summary: Legacy 307 to /bot_apps/events.
      description: |
        Same grace-window redirect as GET. POST must stay documented so the
        validator does not 405 the Bolt edge.
      responses:
        '200':
          description: Deferred — see the GET operation description above.
  "/session":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - sessions
      operationId: session.index
      summary: List session.
    post:
      tags:
      - sessions
      operationId: session.create
      summary: Session.
    delete:
      tags:
      - sessions
      operationId: session.destroy
      summary: Delete session.
  "/session/validate":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - session_validate
      operationId: session.validate
      summary: Validate the caller's session and return a minimal identity payload.
      description: |
        A suspended or dormant member gets the envelope **401**, not a 403: `AuthContextResolver`
        scopes its lookup to `organization.members.active` (`auth_context_resolver.rb:21`) and
        `Member.active` is `state: ACTIVE` only, so their session never resolves and
        `resolve_auth_context` renders 401 before the action runs. The controller's own
        suspended/dormant 403 branch (`session_validate_controller.rb:10-12`) is kept verbatim from
        source — where the base had no state filter — but is unreachable through the router, so it is
        deliberately NOT documented here.
      responses:
        '200':
          description: The session is usable and the member is neither suspended nor
            dormant.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/SessionValidateResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/sessions/current":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - resources
      operationId: resources.sessions.current
      summary: Session-scoped HTTP metadata for the SPA (logout URL + CSRF token).
      responses:
        '200':
          description: The caller's session metadata.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ResourceEnvelopeSessionDetail"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
  "/skill_scales":
    "$ref": v3/resources/skill_scales.yaml#/paths/~1skill_scales
  "/skill_scales/{id}":
    "$ref": v3/resources/skill_scales.yaml#/paths/~1skill_scales~1{id}
  "/skill_scales/{id}/activate":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: skill_scales.activate
      summary: Activate a draft scale. Rejected unless the scale has at least one
        level.
      responses:
        '200':
          description: The activated scale.
          content:
            application/json:
              schema:
                "$ref": v3/resources/skill_scales.yaml#/components/schemas/ResourceEnvelopeSkillScaleDetail
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/skill_scales/{id}/levels":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    patch:
      tags:
      - commands
      operationId: skill_scales.update_levels
      summary: |
        Replace the scale's level collection wholesale (delete-all then recreate, atomic). At least
        one level is required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - levels
              additionalProperties: false
              properties:
                levels:
                  type: array
                  minItems: 1
                  description: The complete replacement set of levels, in the order
                    given.
                  items:
                    type: object
                    required:
                    - value
                    - label
                    additionalProperties: false
                    properties:
                      value:
                        type: integer
                        description: Ordinal proficiency value.
                      label:
                        type: string
                      description:
                        type:
                        - string
                        - 'null'
                      position:
                        type: integer
                        description: Render order; defaults to the array index when
                          omitted.
                      color:
                        type:
                        - string
                        - 'null'
      responses:
        '200':
          description: The scale with its replacement levels.
          content:
            application/json:
              schema:
                "$ref": v3/resources/skill_scales.yaml#/components/schemas/ResourceEnvelopeSkillScaleDetail
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/skill_scales/{id}/set_primary":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - commands
      operationId: skill_scales.set_primary
      summary: |
        Make this scale the organization's primary one, unsetting any other primary scale in the same
        organization. Only an active scale qualifies.
      responses:
        '200':
          description: The scale, now primary.
          content:
            application/json:
              schema:
                "$ref": v3/resources/skill_scales.yaml#/components/schemas/ResourceEnvelopeSkillScaleDetail
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/smart_rubrics":
    "$ref": v3/resources/smart_rubrics.yaml#/paths/~1smart_rubrics
  "/smart_rubrics/{id}":
    "$ref": v3/resources/smart_rubrics.yaml#/paths/~1smart_rubrics~1{id}
  "/survey_campaign_messages":
    "$ref": v3/resources/survey_campaign_messages.yaml#/paths/~1survey_campaign_messages
  "/survey_campaign_messages/test_email":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - test_emails
      operationId: survey_campaign_messages.test_email
      summary: Send a preview of a survey reminder to one or more program administrators.
      description: |
        Sends the reminder currently being composed, as a preview, to the given addresses. Creates no
        record, and takes the unsaved `subject`/`body` rather than a reminder id — so the composer can
        send a test before the reminder exists.

        `campaign_id` is optional: with it, merge tags are validated against that survey's campaign and
        the preview is rendered by that survey type's own notification class; without it, tags are
        checked against the program's set.

        Recipients must be administrators of this program, at most 10 at a time. A malformed or
        non-admin address is reported as a `422` naming it — never silently dropped. Both a subject and
        a body are required, and an unknown merge tag or widget is rejected before anything is sent.

        Requires `manage_survey_campaigns`; choosing a `sender_id` other than yourself additionally
        requires `choose_sender_for_campaign_email`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - subject
              - body
              - emails
              additionalProperties: false
              properties:
                subject:
                  type: string
                  description: The unsaved subject line, merge tags included.
                body:
                  type: string
                  description: The unsaved HTML body, merge tags included.
                emails:
                  type: array
                  description: Recipient addresses. Each must belong to an administrator
                    of this program.
                  items:
                    type: string
                sender_id:
                  type:
                  - integer
                  - 'null'
                  description: 'Who the mail appears to come from. A null id (or omitting
                    it) sends as the program.

                    '
                campaign_id:
                  type:
                  - integer
                  - 'null'
                  description: |
                    The survey campaign whose tag set to validate against, as returned by the survey's
                    `reminder_campaign_id`. Omit while composing against no particular survey.
      responses:
        '200':
          description: The test email was sent to each listed recipient.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/SurveyReminderTestEmailResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/survey_campaign_messages/{id}":
    "$ref": v3/resources/survey_campaign_messages.yaml#/paths/~1survey_campaign_messages~1{id}
  "/surveys":
    "$ref": v3/resources/surveys.yaml#/paths/~1surveys
  "/surveys/aggregates":
    "$ref": v3/resources/surveys.yaml#/paths/~1surveys~1aggregates
  "/surveys/{id}":
    "$ref": v3/resources/surveys.yaml#/paths/~1surveys~1{id}
  "/taxonomy/skill_categories":
    "$ref": v3/resources/taxonomy/skill_categories.yaml#/paths/~1taxonomy~1skill_categories
  "/taxonomy/skill_categories/{id}":
    "$ref": v3/resources/taxonomy/skill_categories.yaml#/paths/~1taxonomy~1skill_categories~1{id}
  "/themes/current":
    "$ref": v3/resources/themes.yaml#/paths/~1themes~1current
  "/training_plans":
    "$ref": v3/resources/training_plans.yaml#/paths/~1training_plans
  "/training_plans/aggregates":
    "$ref": v3/resources/training_plans.yaml#/paths/~1training_plans~1aggregates
  "/training_plans/reorder":
    "$ref": v3/resources/training_plans.yaml#/paths/~1training_plans~1reorder
  "/training_plans/{id}":
    "$ref": v3/resources/training_plans.yaml#/paths/~1training_plans~1{id}
  "/training_resource_links":
    "$ref": v3/resources/training_resource_links.yaml#/paths/~1training_resource_links
  "/training_resource_links/{id}":
    "$ref": v3/resources/training_resource_links.yaml#/paths/~1training_resource_links~1{id}
  "/training_resources":
    "$ref": v3/resources/training_resources.yaml#/paths/~1training_resources
  "/training_resources/aggregates":
    "$ref": v3/resources/training_resources.yaml#/paths/~1training_resources~1aggregates
  "/training_resources/{id}":
    "$ref": v3/resources/training_resources.yaml#/paths/~1training_resources~1{id}
  "/training_sections":
    "$ref": v3/resources/training_sections.yaml#/paths/~1training_sections
  "/training_sections/{id}":
    "$ref": v3/resources/training_sections.yaml#/paths/~1training_sections~1{id}
  "/up_next":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.up_next
      summary: The member's Up Next view — time-ordered blocks of what to do next.
      responses:
        '200':
          description: The home/up_next view envelope.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/UpNextViewResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/uploads":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - uploads
      operationId: uploads.create
      summary: Stage an uploaded file and return a claim ticket.
      description: |
        Validates and stores one file to a temporary location, then returns the `code` that identifies
        it. The file is not attached to anything yet: the owning resource redeems the ticket on its own
        create/update by passing `{ file_name, code }` through to `assign_attachment`, which is also how
        the legacy dropzone works — so both stacks share one set of size, content-type, virus-scan and
        per-format validation rules.

        `target` selects which of those rules apply and where the file is staged. A validation failure
        returns `422`, with the `code` naming the specific check that failed so a client can branch on
        it; anything a target's validator derives from the file that the owning record needs at save
        time comes back in `metadata`.

        Per target — `scorm`: validates a SCORM 1.2 package and returns `title`, `launch_data` and
        `launch_resource_identifier`. Failure codes: `manifest_missing`, `scorm_version_mismatch`,
        `invalid_file_types`, `sco_type_resource_missing`. `course_image`: an image for a course, held
        to the same size and content-type validations `TrainingPlan#image` declares, and redeemed as
        that record's `image`. `announcement_attachment`: the admin announcement's file attachment — no
        deep validator beyond the model's own size and content-type rules, so it returns no `metadata`;
        redeemed via the announcement's `attachment` write field.

        `admin_message_attachment`: an admin-composer message attachment — size and content type only,
        no deep validation, so `metadata` comes back empty.

        Staging is at-least-once, not idempotent: a retried POST stages a fresh copy under a new
        `code`, and a duplicate that is never redeemed is inert. (Idempotency would fingerprint the
        whole multipart body in memory, and the random MIME boundary defeats body-based replay anyway.)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              - target
              additionalProperties: false
              properties:
                file:
                  type: string
                  format: binary
                  description: The file to stage.
                target:
                  type: string
                  enum:
                  - scorm
                  - admin_message_attachment
                  - course_image
                  - announcement_attachment
                  description: Where the file is destined; selects the validation
                    and storage rules.
      responses:
        '201':
          description: The file was staged; the claim ticket is returned.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    "$ref": "#/components/schemas/StagedUpload"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '422':
          "$ref": "#/components/responses/ErrorResponse"
  "/uploads/targets/{name}":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - uploads
      operationId: uploads.target
      summary: The upload rules for one target.
      description: |
        Reports the constraints a form needs before it uploads: the size ceiling and the accepted content
        types, both read from the owning model's own validations so they cannot drift from what
        `POST /api/v3/uploads` enforces. The legacy analogue is `get_data_hash_for_dropzone`, which
        stamped the same values onto the dropzone element from the server-rendered view.

        Returns the size as a byte count, not a sentence — the client formats the message in its own
        locale. Scoped to a single target: a form only needs its own.

        Answers `403` when the caller may not upload for the target, or when the tenant cannot use it,
        so a form can omit the field entirely rather than offer something that will be refused.
      responses:
        '200':
          description: The target's rules.
          content:
            application/json:
              schema:
                type: object
                required:
                - app_mode
                - data
                additionalProperties: false
                properties:
                  app_mode:
                    "$ref": "#/components/schemas/AppMode"
                  data:
                    "$ref": "#/components/schemas/UploadTarget"
        '400':
          "$ref": "#/components/responses/ErrorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: name
        in: path
        required: true
        description: The upload target.
        schema:
          type: string
          enum:
          - scorm
          - admin_message_attachment
          - course_image
          - announcement_attachment
  "/users":
    "$ref": v3/resources/users.yaml#/paths/~1users
  "/users/{id}":
    "$ref": v3/resources/users.yaml#/paths/~1users~1{id}
  "/views/announcements/audience_details":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.announcements.audience_details
      summary: What one chosen announcement audience resolves to — its size and the
        filters defining it.
      description: |
        Per-view and on demand, for the audience card beside the announcement form's audience picker.

        Deliberately NOT a field on the audience options list: an AdminView's count falls through to a full
        view generation whenever its cache is cold, so carrying a count on every option would run one
        generation per option — dozens on a program with a large view library, on every form open.

        `filters` are the view's applied filters as label/value pairs, each value already flattened to one
        readable line (the same
        information the legacy view-preview panel renders). `view_id` is scoped through the same narrowing
        the options list applies, so a segment-restricted admin cannot read the count of a view they were
        never offered — an id outside that set is a `404`.

        Read-only, admin-gated; denied in engage.
      responses:
        '200':
          description: The chosen audience's size and filters.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AnnouncementAudienceDetailsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: view_id
        in: query
        required: true
        description: The user view (AdminView) to describe.
        schema:
          type: integer
  "/views/announcements/compose_options":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.announcements.compose_options
      summary: Delivery and audience constraints for the announcement composer.
      description: |
        What the announcement form needs before it can render, in one round trip. Program-scoped rather
        than read off an announcement, because create has no record yet.

        `email_delivery` reports, per mode, whether the notification mail would actually be delivered.
        Legacy sends a different mailer on create (`AnnouncementNotification`) than on edit
        (`AnnouncementUpdateNotification`), matching the observer's `:creation` / `:updation` split, and
        the two carry independent enable/disable state — so one flag could not answer for both. When
        `disabled` is true the mail is suppressed for this program and the form should say so, rather
        than promising a send that silently never happens. `template_url` is the ready-made deep link to
        the mailer-template editor, and is null unless the caller may reach that editor at all.

        `audience_options.segment_restricted` is true for an admin who may only act within the user
        views they manage. Such an admin must target a user view; offering All users or Specific roles
        only lets them fill the whole form in before the policy answers 403.

        Read-only, admin-gated; denied without the manage-announcements permission.
      responses:
        '200':
          description: The program's announcement delivery and audience constraints.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AnnouncementComposeOptionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/email_campaigns/analytics":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.email_campaigns.analytics
      summary: Delivery and engagement analytics for one email campaign's detail page.
      description: |
        Four summary tiles (sent / delivered / open rate / click rate) plus a monthly delivery trend,
        for the campaign named by `campaign_id`. Rates are fractions 0..1, or null when nothing has
        been sent. Read-only, admin-gated; denied in engage. 404 when the campaign is outside the
        caller's program scope.
      responses:
        '200':
          description: The campaign's delivery summary and monthly trend.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EmailCampaignAnalyticsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: campaign_id
        in: query
        required: true
        description: The campaign to report on. Must be in the caller's program.
        schema:
          type: integer
  "/views/email_campaigns/audience_details":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.email_campaigns.audience_details
      summary: What one chosen campaign audience resolves to — its size and the filters
        defining it.
      description: |
        Per-view and on demand, for the audience card beside the campaign form's picker.

        Deliberately NOT a field on the audience options list: an AdminView's count falls through to a full
        view generation whenever its cache is cold, so carrying a count on every option would run one
        generation per option — dozens on a program with a large view library, on every form open.

        `filters` are the view's applied filters as label/value pairs, each value already flattened to one
        readable line (the same
        information the legacy view-preview panel renders). `view_id` is scoped through the same narrowing
        the options list applies, so a segment-restricted admin cannot read the count of a view they were
        never offered — an id outside that set is a `404`.

        Read-only, admin-gated; denied in engage.
      responses:
        '200':
          description: The chosen audience's size and filters.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EmailCampaignAudienceDetailsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: view_id
        in: query
        required: true
        description: The audience (AbstractView) to describe.
        schema:
          type: integer
  "/views/email_campaigns/compose_options":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.email_campaigns.compose_options
      summary: Sender options and merge tags for the email composer.
      description: |
        Everything the composer needs before it can render an email, in one round trip.

        `sender_options` mirrors what the legacy picker offers: the program first — a `null` id means
        "send as the program" — then the program's admins, or only the acting user when the caller lacks
        permission to choose a sender.

        `merge_tags` is computed from this program's own configuration, never a fixed list: the supported
        set branches on matching style, calendar, project-based and career-mentoring, so two programs in
        one organization legitimately offer different tags. Organization tag aliases are applied and
        tags hidden from the UI are omitted. `example` carries HTML, for rendering a preview.

        Read-only, admin-gated; denied in engage.
      responses:
        '200':
          description: The program's sender options and merge tags.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EmailCampaignComposeOptionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/membership_requests/application_questions":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.membership_requests.application_questions
      summary: The program's application questions for the Enroll › Applications page.
      description: |
        The application-question vocabulary the Applications list needs but a rendered answer cannot
        carry: each question's kind (so the answer editor picks the right control) and, for a choice
        question, its selectable choices. It also names the answer columns the list can show. Read-only,
        admin-gated (the approve-membership-request permission); denied in engage.

        Ordered as the program orders its profile questions. The set mirrors the legacy filter's:
        the non-administrative roles' non-default questions, excluding file questions (a file answer has
        no value to match on).

        A question's `id` matches `answers[].question_id` on a membership request.
      responses:
        '200':
          description: The program's application questions, each with its kind and
            choices.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MembershipRequestApplicationQuestionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/membership_requests/notification_emails":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.membership_requests.notification_emails
      summary: The applicant-facing notification emails for the Enroll › Applications
        page.
      description: |
        The three mails an applicant receives as their application moves through review — received on
        submission, accepted, rejected — each with its effective subject and whether it is live for
        this program. Read-only, admin-gated (the approve-membership-request permission); denied in
        engage.

        The record set is the mailer registry, not the `mailer_templates` table: a uid with no row is
        enabled with its class-default subject, so all three always appear.
      responses:
        '200':
          description: The three applicant notification emails and their effective
            state.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MembershipRequestNotificationEmailsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/mentoring_models/engagement_surveys":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.mentoring_models.engagement_surveys
      summary: Engagement surveys each connection plan template assigns to a role.
      description: |
        The option pool a certificate rule's "specific surveys" criterion draws from. Engagement
        surveys are assigned per connection plan template AND per role (`MentoringModel::TaskTemplate`),
        so a rule scoped to several templates may only require surveys common to all of them — the
        caller intersects the rows for the templates it has selected.
      responses:
        '200':
          description: One row per (connection plan template, role) pair that assigns
            engagement surveys.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/EngagementSurveyOptionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/navigation/global":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.navigation.global
      summary: Global (organization-level) navigation tree for the SPA shell.
      responses:
        '200':
          description: Navigation tree, pruned to the caller's visibility.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/NavTreeResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/navigation/local/{scope}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/NavScopeParam"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.navigation.local
      summary: Local navigation tree for a scope that needs no record id.
      responses:
        '200':
          description: Navigation tree, pruned to the caller's visibility.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/NavTreeResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/navigation/local/{scope}/{id}":
    x-not-implemented: true
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/NavScopeParam"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.navigation.local_item
      summary: Local navigation tree scoped to a specific record (e.g. a program).
      responses:
        '200':
          description: Navigation tree, pruned to the caller's visibility.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/NavTreeResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/onboarding/flow_editor/{id}":
    parameters:
    - "$ref": "#/components/parameters/Id"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.onboarding.flow_editor
      summary: Editor view for one onboarding flow — the editable document + role
        options.
      description: |
        Returns the onboarding-flow editor screen as a single `onboarding_flow_editor` block whose `data`
        is the flow document (`layout_flow` with its pages/components, `testimonials`, `layout_images`)
        plus the `applicable_roles` choices for the audience selector. The document sub-objects are
        free-form (they mirror the legacy DataBuilder serializers), so they are typed as open objects.
      responses:
        '200':
          description: The editor document for the requested flow.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OnboardingFlowEditorResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/program_invitations/analytics":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.program_invitations.analytics
      summary: Program invitation email-activity funnel for the Enroll › Invitations
        dashboard.
      description: |
        Program-wide sent / delivered / opened / clicked counts from the campaign-analytics
        rollup. Read-only, admin-gated (the send-invitation permission); denied in engage.
      responses:
        '200':
          description: The invitation email-activity funnel.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProgramInvitationAnalyticsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/roleplay_share_popover":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.roleplay_share_popover.show
      summary: Share-popover candidate list for a roleplay — owner row plus eligible
        mentors (or org-wide search) with grant overlay.
      responses:
        '200':
          description: Owner row and ranked candidate members with grant overlay.
          content:
            application/json:
              schema:
                type: object
                required:
                - group
                - name
                - blocks
                additionalProperties: false
                properties:
                  group:
                    const: roleplay
                  name:
                    const: share_popover
                  blocks:
                    type: array
                    items:
                      type: object
                      required:
                      - type
                      - data
                      additionalProperties: false
                      properties:
                        type:
                          const: share_candidates
                        data:
                          type: object
                          required:
                          - owner
                          - candidates
                          additionalProperties: false
                          properties:
                            owner:
                              "$ref": "#/components/schemas/RoleplayShareCandidateMember"
                            candidates:
                              type: array
                              items:
                                type: object
                                required:
                                - member
                                - role
                                - share_id
                                - granted
                                additionalProperties: false
                                properties:
                                  member:
                                    "$ref": "#/components/schemas/RoleplayShareCandidateMember"
                                  role:
                                    type: string
                                    enum:
                                    - mentor
                                    - member
                                  share_id:
                                    type:
                                    - integer
                                    - 'null'
                                  granted:
                                    type: boolean
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: content_id
        in: query
        required: true
        description: Id of the Content::Roleplay whose share popover is being opened.
        schema:
          type: integer
      - name: q
        in: query
        description: |
          Optional org-wide member search (prefix-matched on first/last name or
          email). When present, replaces the default eligible-mentor list.
        schema:
          type: string
  "/views/roles/options":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.roles.options
      summary: Program role options for an SPA picker/filter, curated per view context.
      responses:
        '200':
          description: Curated role options for the screen.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RoleOptionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: context
        in: query
        description: |
          Which curated role set to return. Mirrors Role::Contexts::ALIASES —
          `all`, `for_view` (displayable roles), or `for_single_role_sensitive_view`
          (displayable, but empty unless more than one viewable mentoring role
          exists). Defaults to `for_view`.
        schema:
          type: string
          enum:
          - all
          - for_view
          - for_single_role_sensitive_view
      - name: type
        in: query
        description: |
          Narrow the returned set. `mentoring` and `administrative` select a role
          classification; `without_admin` drops the `admin` role only — sub-admins
          and other administrative roles are kept. An unrecognized value returns
          no roles.
        schema:
          type: string
          enum:
          - mentoring
          - administrative
          - without_admin
          - non_administrative
  "/views/shared_roleplay/{share_id}":
    parameters:
    - name: share_id
      in: path
      required: true
      description: Id of the Content::RoleplayShare grant the caller is opening.
      schema:
        type: integer
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.shared_roleplay.show
      summary: Grantee read-only view of every forward attempt on a shared roleplay.
      responses:
        '200':
          description: Owner, content summary, and every forward attempt for the share.
          content:
            application/json:
              schema:
                type: object
                required:
                - group
                - name
                - blocks
                additionalProperties: false
                properties:
                  group:
                    const: roleplay
                  name:
                    const: shared_roleplay
                  blocks:
                    type: array
                    items:
                      type: object
                      required:
                      - type
                      - data
                      additionalProperties: false
                      properties:
                        type:
                          const: shared_roleplay
                        data:
                          type: object
                          additionalProperties: true
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
  "/views/survey_campaign_messages/compose_options":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.survey_campaign_messages.compose_options
      summary: Sender options and merge tags for the survey-reminder composer.
      description: |
        Everything the reminder composer needs before it can render an email, in one round trip.

        `sender_options` mirrors what the legacy picker offers: the program first — a `null` id means
        "send as the program" — then the program's admins, or only the acting user when the caller lacks
        permission to choose a sender.

        `merge_tags` comes from the SURVEY, not a fixed list: the supported set is chosen per survey
        type, so a connection survey and a meeting-feedback survey legitimately offer different tokens.
        Resolve it either way — pass `campaign_id` (the survey's `reminder_campaign_id`) for a saved
        survey, or `type` while one is still being CREATED and has no campaign yet, which answers the
        tags that survey type will offer. `campaign_id` wins when both are given, and a request carrying
        neither (or a `type` this route cannot create) gets an empty tag list. Organization tag aliases
        are applied and tags hidden from the UI are omitted. `example` carries HTML, for a preview.

        Read-only, admin-gated; denied in engage.
      responses:
        '200':
          description: The program's sender options and this survey's merge tags.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/SurveyCampaignMessageComposeOptionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: campaign_id
        in: query
        required: false
        description: The survey's reminder campaign, as returned by `Survey.reminder_campaign_id`.
        schema:
          type: integer
      - name: type
        in: query
        required: false
        description: |
          The survey flavor to resolve tags for, used while composing a reminder for a survey that does
          not exist yet. Ignored when `campaign_id` resolves.
        schema:
          type: string
          enum:
          - connection
          - program
  "/views/surveys/delivery_options":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.surveys.delivery_options
      summary: Which delivery settings a survey of a given type could carry, before
        it exists.
      description: |
        The survey builder's Delivery step is driven by the `can_*` capability flags. On a saved survey
        those ride the detail render (`GET /surveys/{id}`); while creating there is no record to render,
        so this answers the same question for a type that has not been saved yet.

        The gates are a function of the survey's STI type AND the program's feature flags, so they
        cannot be derived from the type alone on the client: a program with v2 connections forbids a due
        date on a survey whose type otherwise allows one, and the non-logged-in and overdue-mandate
        settings are program features. The builder answers by constructing the same unsaved record
        `SurveyWriter` would and asking it the same predicates `SurveySchema` asks.

        `type` is clamped to the types this route can create (`connection`, `program`); anything else is
        denied rather than probed. `one_response_locked` and `can_have_reminders` are always false — a
        survey that does not exist has neither responses nor a campaign.

        Read-only, admin-gated; denied in engage.
      responses:
        '200':
          description: The delivery capability gates for that type in this program.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/SurveyDeliveryOptionsResponse"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: type
        in: query
        required: true
        description: The survey type about to be created.
        schema:
          type: string
          enum:
          - connection
          - program
  "/views/{group}/{name}":
    x-not-implemented: true
    parameters:
    - name: group
      in: path
      required: true
      description: View group (e.g. `homepage`, `dashboard`).
      schema:
        type: string
        pattern: "^[a-z][a-z0-9_]*$"
    - name: name
      in: path
      required: true
      description: View name within the group (e.g. `home`, `program-admin`).
      schema:
        type: string
        pattern: "^[a-z][a-z0-9_-]*$"
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    get:
      tags:
      - views
      operationId: views.show
      summary: Fetch the block list for a page.
      responses:
        '200':
          description: Block list, in render order.
          content:
            application/json:
              schema:
                type: object
                required:
                - group
                - name
                - blocks
                additionalProperties: false
                properties:
                  group:
                    type: string
                  name:
                    type: string
                  blocks:
                    type: array
                    items:
                      "$ref": "#/components/schemas/ViewBlock"
        '401':
          "$ref": "#/components/responses/ErrorResponse"
        '403':
          "$ref": "#/components/responses/ErrorResponse"
        '404':
          "$ref": "#/components/responses/ErrorResponse"
      parameters:
      - name: include
        in: query
        description: Comma-separated list of block types to include (partial refresh).
        required: false
        schema:
          type: string
      - name: preview_limit
        in: query
        description: Cap items per block (for skeleton loads).
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 25
  "/webhooks/workflow_engine":
    parameters:
    - "$ref": "#/components/parameters/AppMode"
    - "$ref": "#/components/parameters/AppModeHeader"
    post:
      tags:
      - workflow_engine_events
      operationId: webhooks.workflow_engine
      summary: Workflow engine a webhook.
  "/writeback_divergences":
    "$ref": v3/resources/writeback_divergences.yaml#/paths/~1writeback_divergences
  "/writeback_divergences/aggregates":
    "$ref": v3/resources/writeback_divergences.yaml#/paths/~1writeback_divergences~1aggregates
  "/writeback_divergences/{id}":
    "$ref": v3/resources/writeback_divergences.yaml#/paths/~1writeback_divergences~1{id}
  "/writeback_entries":
    "$ref": v3/resources/writeback_entries.yaml#/paths/~1writeback_entries
  "/writeback_entries/aggregates":
    "$ref": v3/resources/writeback_entries.yaml#/paths/~1writeback_entries~1aggregates
  "/writeback_entries/{id}":
    "$ref": v3/resources/writeback_entries.yaml#/paths/~1writeback_entries~1{id}
components:
  schemas:
    Pagination:
      type: object
      required:
      - total
      - page
      - limit
      - has_more
      additionalProperties: false
      properties:
        total:
          type: integer
          minimum: 0
        page:
          type: integer
          minimum: 1
        limit:
          type: integer
          minimum: 1
        has_more:
          type: boolean
    AggregatesEnvelope:
      type: object
      description: Counts keyed by bucket value (e.g. a status), returned by an aggregates
        query. Records that have no value for the selected bucket field are grouped
        under the reserved key "null".
      required:
      - app_mode
      - data
      additionalProperties: false
      properties:
        app_mode:
          type: string
          enum:
          - manage
          - engage
        data:
          type: object
          additionalProperties:
            type: integer
    ImageSingle:
      type: object
      description: A single-rendition image — direct URL plus optional intrinsic dimensions.
      required:
      - url
      additionalProperties: false
      properties:
        url:
          type: string
        width:
          type: integer
        height:
          type: integer
        alt:
          type: string
    ImageRenditions:
      type: object
      description: A multi-rendition image — named styles keyed by name.
      required:
      - renditions
      additionalProperties: false
      properties:
        alt:
          type: string
        renditions:
          type: object
          additionalProperties:
            type: object
            required:
            - url
            additionalProperties: false
            properties:
              url:
                type: string
              width:
                type: integer
              height:
                type: integer
    PortedOkAck:
      type: object
      description: |
        The bare `{ok: true}` acknowledgement the ported proj-rumi hand-written controllers render
        from a successful write. Shared here rather than redeclared per meta file. Interface-stack
        writes return the record envelope instead — never this.
      required:
      - ok
      additionalProperties: false
      properties:
        ok:
          type: boolean
    Image:
      description: An image rendering — either a single URL or a set of named renditions.
      oneOf:
      - "$ref": "#/components/schemas/ImageSingle"
      - "$ref": "#/components/schemas/ImageRenditions"
    Error:
      type: object
      description: |
        Uniform error envelope used across every namespace. The `code` is a
        stable string for programmatic handling; `message` is human-readable;
        `details` carries field-level errors for validation failures.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: object
          required:
          - code
          - message
          additionalProperties: false
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: array
              items:
                type: object
                required:
                - field
                - message
                additionalProperties: false
                properties:
                  field:
                    type: string
                  message:
                    type: string
                  code:
                    type: string
    PortedBareError:
      type: object
      description: |
        The pre-envelope error shape the ported proj-rumi hand-written controllers render
        directly (`render json: {error: "Forbidden"}`). Declared rather than normalised onto
        `Error` because packages/rumi reads the bare string, so changing it is a client-breaking
        change, not a tidy-up. Interface-stack endpoints must use `Error` — never this.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    OAuthError:
      type: object
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
          enum:
          - invalid_request
          - unsupported_grant_type
          - invalid_target
          - invalid_client
          - unauthorized_client
        error_description:
          type: string
    UploadClaim:
      type: object
      description: |
        A claim ticket for a file already staged by `POST /api/v3/uploads`, redeemed when the owning
        record is created or updated. The file itself never rides the resource's own body — the two
        stacks share one set of size, content-type and virus-scan rules by sharing the staging step.

        Send `file_name: null` to remove the current attachment; omit the whole field to leave it
        alone.
      required:
      - file_name
      additionalProperties: false
      properties:
        file_name:
          type:
          - string
          - 'null'
          description: The staged file's name, as returned by the upload. Null removes
            the attachment.
        code:
          type:
          - string
          - 'null'
          description: The claim ticket. Null (or absent) alongside a name keeps the
            current file.
    ReorderRequest:
      type: object
      description: A new order for a whole scoped collection, applied in one transaction.
      required:
      - ordered_ids
      additionalProperties: false
      properties:
        ordered_ids:
          type: array
          description: Every id in the caller's scope, exactly once, in the order
            to persist. A list that repeats an id, omits one, or names one outside
            the scope is rejected rather than reinterpreted, so a stale client list
            can never persist an order nobody asked for.
          minItems: 1
          maxItems: 500
          uniqueItems: true
          items:
            type: integer
    RouteName:
      type: string
      description: |
        Canonical name of a frontend destination — the single source of truth
        for intent-based linking between Rails and the SPA. The backend emits a
        *name*, never a URL; the SPA resolves it to a TanStack path via
        `packages/web/src/lib/routes.ts#urlFor`. Renaming a path is a one-line
        SPA change with zero backend impact.

        Enum members map 1:1 to real SPA routes (a route file + a `urlFor`
        builder must exist), so it grows as pages are migrated. Page-style names
        are top-level destinations; resource-style names carry an id (and optional
        view) in `params`.

        Some names are SPA-internal (in-app param navigation the backend does not
        emit today, e.g. `program`); they live here so the SPA has one URL source
        of truth and `/go` deep-linking comes for free.
      enum:
      - catalog
      - programs
      - communities
      - flagged_users
      - program
      - skills
      - skillDetail
      - skillEdit
      - roleplays
      - personas
      - personaDetail
      - personaEdit
      - goals
      - goalEdit
      - overview
      - optimize
      - insights
      - rumiSettings
      - mentoringAdoption
      - agents
      - agentDetail
      - agentEdit
      - capabilities
      - skillCategoryDetail
      - users
      - userDetail
      - userEdit
      - groups
      - groupDetail
      - groupEdit
      - connections
      - webhooks
      - apiKeys
      - settings
      - content
      - contentDetail
      - botApps
      - communityDetail
      - orgUnitEdit
      - userHome
      - userConnections
      - userConnectionDetail
      - userInbox
      - userProfile
      - userDiscover
      - userPrograms
      - userProgramDetail
      - userEvents
      - userEventDetail
      - userCommunities
      - userCommunityDetail
      - userSkills
      - userSkillDetail
      - userCareer
      - userContent
      - userContentDetail
      - userIntro
      - userChat
      - userRoleplay
      - userRoleplayTab
      - userSharedRoleplay
      - userContentPage
      - userSearch
      - userConversations
      - userConversation
      - organization
      - certificate_design
      - skills
      - skillDetail
      - skillEdit
      - roleplays
      - roleplayDetail
      - roleplayCreate
      - roleplayEdit
      - playbooks
      - playbookContentGaps
      - optimizeContentGaps
      - adminConfig
      - playbookCoach
      - playbookDetail
      - personas
      - personaDetail
      - personaEdit
      - personaCreate
      - practiceObjectives
      - practiceObjectiveEdit
      - practiceObjectiveCreate
      - overview
      - optimize
      - insights
      - goalAnalytics
      - mentoringAdoption
      - coachingQuality
      - dashboardDetail
      - agents
      - agentDetail
      - agentEdit
      - connectorDetail
      - dataConnector
      - capabilities
      - capabilitiesSkills
      - capabilitiesCategories
      - capabilitiesScales
      - capabilitiesGoals
      - organizationGoals
      - organizationGoalDetail
      - goalsStandards
      - goalsStandardsCoverage
      - capabilitiesImport
      - skillCategoryDetail
      - users
      - userDetail
      - userEdit
      - groups
      - groupDetail
      - groupEdit
      - connectors
      - connections
      - connectionsConnected
      - connectionsProviders
      - webhooks
      - apiKeys
      - settings
      - settingsWorkspace
      - settingsOrgUnits
      - settingsPersonas
      - settingsSecurityRoles
      - settingsSecurityRoleDetail
      - settingsA2a
      - settingsRumi
      - settingsCycles
      - settingsConfig
      - settingsAiAudit
      - settingsAccessLog
      - settingsAppeals
      - settingsModelProvenance
      - settingsAttestations
      - settingsEvidencePackage
      - content
      - contentDetail
      - botApps
      - communityDetail
      - orgUnitEdit
      - userHome
      - userMe
      - userMeGoalDetail
      - userCircle
      - userConnections
      - userConnectionDetail
      - userConnectionReports
      - userConnectionReportDetail
      - userConnectionMentees
      - userCheckIns
      - userCheckInDetail
      - userInbox
      - userProfile
      - userDiscover
      - userPrograms
      - userProgramDetail
      - userEvents
      - userEventDetail
      - userCommunities
      - userCommunityDetail
      - userSkills
      - userSkillDetail
      - userPeopleDetail
      - userAiMentors
      - userAiMentorDetail
      - userAiCoaches
      - userAiCoachDetail
      - userCareer
      - userContent
      - userContentDetail
      - userIntro
      - userIntroShowcase
      - userIntroTmobile
      - userChat
      - userRoleplay
      - userRoleplayTab
      - userRoleplaySession
      - userSharedRoleplay
      - userContentPage
      - userSearch
      - userConversations
      - userConversation
      - analytics
      - analyticsDashboards
      - analyticsManage
      - analyticsManageDashboards
      - analyticsInsights
      - analyticsInsightNew
      - analyticsInsightEdit
      - analyticsDashboardNew
      - analyticsDashboardEdit
    RouteRef:
      type: object
      description: |
        A frontend destination expressed as intent, not a URL. The backend
        never encodes a FE path; the SPA resolves `name` (+ optional
        params/search/hash) to a concrete URL. Mirrors exactly what a URL can
        express — nothing more (no composite/conditional intents).
      required:
      - name
      additionalProperties: false
      properties:
        name:
          "$ref": "#/components/schemas/RouteName"
        params:
          type: object
          additionalProperties: true
          description: Path segments — e.g. id, view variant, nested ids.
        search:
          type: object
          additionalProperties: true
          description: Query-string params — filters, tabs, sort.
        hash:
          type: string
          description: URL anchor (rare).
    AccountSettings:
      type: object
      description: |
        ⚠️ camelCase throughout, breaking this API's snake_case rule. The keys are spelled the way
        the controller renders them — the spec describes the wire, so do NOT "fix" them here;
        renaming is a controller change and both must move together.
      required:
      - email
      - timezone
      - canChangePassword
      - canRequestAccountDeletion
      - lastDeletionRequestAt
      - authConfigs
      additionalProperties: false
      properties:
        email:
          type: string
        timezone:
          type:
          - string
          - 'null'
          description: The member's `time_zone`, which is nullable — a member need
            never have set one.
        canChangePassword:
          type: boolean
          description: |
            Whether this member may change their password at all — false for SSO-only members. The
            `change_password` endpoint re-checks it and 403s, so this is a UI hint, not the gate.
        canRequestAccountDeletion:
          type: boolean
          description: |
            Organization-level, not member-level: the org's security setting. Coerced with `!!`, so
            an organization with NO security setting reports false rather than null.
        lastDeletionRequestAt:
          type:
          - string
          - 'null'
          format: date-time
          description: |
            Null both when no request was ever made AND when the organization does not allow
            deletion requests — the controller skips the lookup entirely in that case, so this
            field cannot be used to infer history while the setting is off.
        authConfigs:
          type: array
          description: |
            The organization's NON-INDIGENOUS auth configs only, each flagged with whether this
            member is linked to it. Excludes the built-in Chronus credential config, so a
            password-only member sees an empty array.
          items:
            "$ref": "#/components/schemas/AccountSettingsAuthConfig"
    AccountSettingsAuthConfig:
      type: object
      required:
      - id
      - title
      - logoUrl
      - linked
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        logoUrl:
          type:
          - string
          - 'null'
          description: Rendered through `presence`, so a blank logo URL arrives as
            null, never "".
        linked:
          type: boolean
    AccountSettingsUpdateRequest:
      type: object
      description: "\U0001F534 `timezone` is deliberately NOT `required`, and that
        is a correctness choice rather than\nlaxity: the controller itself rejects
        a missing or blank timezone with a 422 carrying\n\"Invalid timezone\". Declaring
        it required here would make the always-on request validator\n(`engine.rb:16`)
        answer the same case with a middleware 400 instead, changing the status a\nclient
        sees for input the controller already handles.\n"
      additionalProperties: false
      properties:
        timezone:
          type: string
          description: An identifier from `TimezoneConstants::VALID_TIMEZONE_IDENTIFIERS`.
          example: America/New_York
    AccountSettingsChangePasswordRequest:
      type: object
      description: "\U0001F534 No property is `required`, for the same reason as the
        timezone body: every absent-field\ncase is already answered by the controller
        — a missing `currentPassword` fails\nauthentication (422 \"Current password
        is incorrect\"), and a missing new password fails model\nvalidation (422).
        `required` here would convert those into middleware 400s.\n"
      additionalProperties: false
      properties:
        currentPassword:
          type: string
        newPassword:
          type: string
        passwordConfirmation:
          type: string
    AccountSettingsUpdateResult:
      type: object
      required:
      - success
      - timezone
      additionalProperties: false
      properties:
        success:
          type: boolean
          description: Always true — a failure is carried by the status code and the
            bare error shape.
        timezone:
          type: string
          description: Echoes the accepted identifier.
    AccountSettingsSuccess:
      type: object
      required:
      - success
      additionalProperties: false
      properties:
        success:
          type: boolean
          description: Always true — a failure is carried by the status code and the
            bare error shape.
    AuthConfig:
      type: object
      required:
      - id
      - authType
      - title
      - description
      - enabled
      - logoUrl
      - isIndigenous
      - isDefault
      - isCustom
      - isSaml
      - isRemoteLogin
      - canBeDisabled
      - canBeDeleted
      - canPreventEnable
      - passwordMessage
      additionalProperties: false
      properties:
        id:
          type: integer
          description: Auth config ID.
        authType:
          type: string
          description: Authentication type.
        title:
          type: string
          description: Display title.
        description:
          type:
          - string
          - 'null'
        enabled:
          type: boolean
        logoUrl:
          type:
          - string
          - 'null'
        isIndigenous:
          type: boolean
          description: Is native email/password auth.
        isDefault:
          type: boolean
          description: Is a default auth type.
        isCustom:
          type: boolean
        isSaml:
          type: boolean
        isRemoteLogin:
          type: boolean
          description: Supports remote login.
        canBeDisabled:
          type: boolean
        canBeDeleted:
          type: boolean
        canPreventEnable:
          type: boolean
          description: Cannot be enabled right now (e.g. SAML without an IDP configured).
        passwordMessage:
          type:
          - string
          - 'null'
          description: Password policy message. Only ever set for the indigenous auth
            config.
    AuthConfigErrors:
      type: object
      required:
      - errors
      additionalProperties: false
      properties:
        errors:
          type: array
          items:
            type: string
    CoachingQualityCell:
      description: A count or rate, or a suppressed marker when its cohort is below
        the org's minimum.
      oneOf:
      - type: number
      - type: 'null'
      - type: object
        required:
        - suppressed
        - reason
        - minimum_cohort_size
        additionalProperties: false
        properties:
          suppressed:
            const: true
          reason:
            type: string
          minimum_cohort_size:
            type: integer
    CoachingQualityMetric:
      type: object
      required:
      - numerator
      - denominator
      - rate
      additionalProperties: false
      properties:
        numerator:
          "$ref": "#/components/schemas/CoachingQualityCell"
        denominator:
          "$ref": "#/components/schemas/CoachingQualityCell"
        rate:
          "$ref": "#/components/schemas/CoachingQualityCell"
    AvailabilitySimpleError:
      type: object
      description: 'The bespoke `{error: "..."}` shape this controller renders directly.'
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    AvailabilityEnvelope:
      type: object
      required:
      - availability
      additionalProperties: false
      properties:
        availability:
          type: object
          required:
          - programIds
          - memberViewIds
          - availableToAll
          additionalProperties: false
          properties:
            programIds:
              type: array
              items:
                type: integer
            memberViewIds:
              type: array
              items:
                type: integer
            availableToAll:
              type: boolean
              description: True iff both `program_availabilities` and `member_view_availabilities`
                are empty (`Content::Roleplay#available_to_all?`).
    AvailabilityWriteBody:
      type: object
      description: Snake_case on the wire (`params[:program_ids]`/`params[:member_view_ids]`
        read directly). Both properties optional — omitting a key leaves that availability
        set unchanged; sending an empty array clears it.
      additionalProperties: false
      properties:
        program_ids:
          type: array
          items:
            type: integer
        member_view_ids:
          type: array
          items:
            type: integer
    MaterialSimpleError:
      type: object
      description: 'The bespoke `{error: "..."}` shape this controller renders directly.'
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    MaterialMessageResponse:
      type: object
      required:
      - message
      additionalProperties: false
      properties:
        message:
          type: string
    Material:
      type: object
      description: Mirrors `Content::Roleplay::MaterialSerializer`.
      required:
      - id
      - materialType
      - materialId
      - materialUrl
      - title
      - url
      - estimatedMinutes
      - contextNote
      - sortOrder
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        materialType:
          type: string
          enum:
          - link
          - spark
          - course
          - upload
        materialId:
          type:
          - integer
          - 'null'
        materialUrl:
          type:
          - string
          - 'null'
        title:
          type: string
          description: Never blank (`validates :title
          presence: true`).
        url:
          type:
          - string
          - 'null'
          description: Resolved link target (`resolved_url`); null for "upload" or
            an unresolved spark/course reference.
        estimatedMinutes:
          type:
          - integer
          - 'null'
        contextNote:
          type:
          - string
          - 'null'
        sortOrder:
          type: integer
          description: "\U0001F534 Always 0 in practice today — see the `create` operation's
            registered-defect note. Not modeled as a fixed `const: 0` since that is
            a bug, not a guarantee."
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    MaterialsIndexEnvelope:
      type: object
      required:
      - materials
      additionalProperties: false
      properties:
        materials:
          type: array
          items:
            "$ref": "#/components/schemas/Material"
    MaterialShowEnvelope:
      type: object
      required:
      - material
      additionalProperties: false
      properties:
        material:
          "$ref": "#/components/schemas/Material"
    MaterialWriteBody:
      type: object
      description: Snake_case on the wire (`material_params` reads snake_case directly).
      required:
      - title
      additionalProperties: false
      properties:
        material_type:
          type: string
          enum:
          - link
          - spark
          - course
          - upload
          description: Defaults to "link" when omitted.
        material_id:
          type: integer
          description: Required (by model validation) when `material_type` is "spark"
            or "course".
        material_url:
          type: string
          description: Required (by model validation) when `material_type` is "link".
        title:
          type: string
        estimated_minutes:
          type: integer
        context_note:
          type: string
        sort_order:
          type: integer
          description: Accepted, but see the registered `sort_order` defect above
            — the server-computed default never applies.
    MaterialUpdateBody:
      type: object
      description: Snake_case on the wire — see `MaterialWriteBody`. Every property
        optional (partial update).
      additionalProperties: false
      properties:
        material_type:
          type: string
          enum:
          - link
          - spark
          - course
          - upload
        material_id:
          type:
          - integer
          - 'null'
        material_url:
          type:
          - string
          - 'null'
        title:
          type: string
        estimated_minutes:
          type:
          - integer
          - 'null'
        context_note:
          type:
          - string
          - 'null'
        sort_order:
          type: integer
    RoleplayCriterionAssignmentCriterionSummary:
      type: object
      required:
      - id
      - name
      - objectiveType
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        objectiveType:
          type: string
          enum:
          - skill
          - behavior
          - framework
    RoleplayCriterionAssignment:
      type: object
      required:
      - id
      - contentId
      - practiceObjectiveId
      - isRequired
      additionalProperties: false
      properties:
        id:
          type: integer
        contentId:
          type: integer
        practiceObjectiveId:
          type: integer
        isRequired:
          type: boolean
        weight:
          type: integer
        practiceObjective:
          "$ref": "#/components/schemas/RoleplayCriterionAssignmentCriterionSummary"
    RoleplayCriterionAssignmentIndexResult:
      type: object
      required:
      - practiceObjectiveAssignments
      additionalProperties: false
      properties:
        practiceObjectiveAssignments:
          type: array
          items:
            "$ref": "#/components/schemas/RoleplayCriterionAssignment"
    RoleplayCriterionAssignmentResult:
      type: object
      required:
      - practiceObjectiveAssignment
      additionalProperties: false
      properties:
        practiceObjectiveAssignment:
          "$ref": "#/components/schemas/RoleplayCriterionAssignment"
    RoleplayCriterionAssignmentError:
      type: object
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    RoleplayCriterion:
      type: object
      required:
      - id
      - name
      - description
      - objectiveType
      - config
      - proficiencyExpectations
      - parentObjectiveId
      - usageCount
      - usedInRoleplays
      - tagList
      - createdById
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        objectiveType:
          type: string
          enum:
          - skill
          - behavior
          - framework
        config:
          type:
          - object
          - 'null'
          additionalProperties: true
        proficiencyExpectations:
          type:
          - object
          - 'null'
          additionalProperties: true
        parentObjectiveId:
          type:
          - integer
          - 'null'
        usageCount:
          type:
          - integer
          - 'null'
        usedInRoleplays:
          type: array
          items:
            type: object
            required:
            - id
            - title
            additionalProperties: false
            properties:
              id:
                type: integer
              title:
                type: string
        tagList:
          type: array
          items:
            type: string
        createdById:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    RoleplayCriteriaListMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    RoleplayCriteriaIndexResult:
      type: object
      required:
      - objectives
      - meta
      additionalProperties: false
      properties:
        objectives:
          type: array
          items:
            "$ref": "#/components/schemas/RoleplayCriterion"
        meta:
          "$ref": "#/components/schemas/RoleplayCriteriaListMeta"
    RoleplayCriterionResult:
      type: object
      required:
      - objective
      additionalProperties: false
      properties:
        objective:
          "$ref": "#/components/schemas/RoleplayCriterion"
    RoleplayCriterionAiGenerateResult:
      type: object
      required:
      - type
      - message
      additionalProperties: false
      properties:
        type:
          type: string
          enum:
          - question
          - draft
          - refinement
        message:
          type: string
        draft:
          type:
          - object
          - 'null'
          additionalProperties: true
    RoleplayCriterionError:
      type: object
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    PersonaSimpleError:
      type: object
      description: 'The bespoke `{error: "..."}` shape this controller renders directly.'
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    PersonaMessageResponse:
      type: object
      required:
      - message
      additionalProperties: false
      properties:
        message:
          type: string
    Persona:
      type: object
      description: Mirrors `Content::Roleplay::PersonaSerializer`.
      required:
      - id
      - name
      - role
      - avatarUrl
      - demeanor
      - voiceId
      - background
      - usageCount
      - tagList
      - createdById
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
          description: Never blank (`validates :name
          presence: true`).
        role:
          type:
          - string
          - 'null'
        avatarUrl:
          type:
          - string
          - 'null'
        demeanor:
          type: string
          enum:
          - friendly
          - neutral
          - challenging
          - hostile
        voiceId:
          type:
          - string
          - 'null'
        background:
          type:
          - string
          - 'null'
        usageCount:
          type: integer
        tagList:
          type: array
          items:
            type: string
        createdById:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    PersonasIndexEnvelope:
      type: object
      required:
      - personas
      - meta
      additionalProperties: false
      properties:
        personas:
          type: array
          items:
            "$ref": "#/components/schemas/Persona"
        meta:
          type: object
          required:
          - total
          - page
          - perPage
          - totalPages
          additionalProperties: false
          properties:
            total:
              type: integer
            page:
              type: integer
            perPage:
              type: integer
            totalPages:
              type: integer
    PersonaShowEnvelope:
      type: object
      required:
      - persona
      additionalProperties: false
      properties:
        persona:
          "$ref": "#/components/schemas/Persona"
    PersonaCreateBody:
      type: object
      description: Snake_case on the wire — see the `create` operation description.
      required:
      - name
      additionalProperties: false
      properties:
        name:
          type: string
        role:
          type: string
        avatar_url:
          type: string
        background:
          type: string
        demeanor:
          type: string
          enum:
          - friendly
          - neutral
          - challenging
          - hostile
        voice_id:
          type:
          - string
          - 'null'
        tag_list:
          type: array
          items:
            type: string
    PersonaUpdateBody:
      type: object
      description: Snake_case on the wire — see the `update` operation description.
      additionalProperties: false
      properties:
        name:
          type: string
        role:
          type:
          - string
          - 'null'
        avatar_url:
          type:
          - string
          - 'null'
        background:
          type:
          - string
          - 'null'
        demeanor:
          type: string
          enum:
          - friendly
          - neutral
          - challenging
          - hostile
        voice_id:
          type:
          - string
          - 'null'
        tag_list:
          type: array
          items:
            type: string
    PersonaAiGenerateBody:
      type: object
      description: Snake_case on the wire (`params[:chat_history]` read directly).
      additionalProperties: false
      properties:
        message:
          type: string
          description: Current user turn. Preferred over `prompt`.
        chat_history:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Prior turns, `[{role, content}, ...]`.
        prompt:
          type: string
          description: Legacy single-shot input; used as `message` only when `message`
            is blank.
    PersonaAiGenerateResult:
      type: object
      description: 'Rendered via `render json: result` straight from `PersonaAIGenerateService`''s
        plain Ruby hash (symbol keys) — NOT through `PersonaSerializer`, so this response
        is snake_case, unlike every other operation in this file.'
      required:
      - type
      - message
      - draft
      additionalProperties: false
      properties:
        type:
          type: string
          enum:
          - question
          - draft
          - refinement
        message:
          type: string
        draft:
          type:
          - object
          - 'null'
          description: Present (non-null) only when `type` is "draft" or "refinement".
          required:
          - name
          - role
          - demeanor
          - voice_id
          - background
          - avatar_url
          - tag_list
          additionalProperties: false
          properties:
            name:
              type: string
            role:
              type: string
            demeanor:
              type: string
              enum:
              - friendly
              - neutral
              - challenging
              - hostile
            voice_id:
              type: string
              description: Never null on output — falls back through a demeanor→voice
                map, then a hardcoded default.
            background:
              type: string
            avatar_url:
              type:
              - string
              - 'null'
            tag_list:
              type: array
              items:
                type: string
    ContentSimpleError:
      type: object
      description: 'The bespoke `{error: "..."}` shape every hand-written `Admin::BaseController`
        action in this family renders directly (translated i18n key or joined AR error
        messages) — NOT the Interface-stack shared `Error`/`ErrorResponse` (`{error:
        {code, message, details}}`).'
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    ContentPublishError:
      type: object
      description: "`publish`'s 422 body — `{error:, reasons:}.compact`, so `reasons`
        is present only when `PublishService` returned reasons (Spark validation failures)."
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
        reasons:
          type: array
          items:
            type: string
    RoleplayMetricSlice:
      type: object
      description: |
        One readiness level or conversation mode. `count` and `pct` travel together
        deliberately — a bare percentage hides how thin the sample is. `pct` is null
        when the denominator is zero, which is not the same statement as 0%.
      required:
      - count
      - pct
      additionalProperties: false
      properties:
        count:
          type: integer
        pct:
          type: number
          nullable: true
    ContentMessageResponse:
      type: object
      required:
      - message
      additionalProperties: false
      properties:
        message:
          type: string
    ContentsIndexEnvelope:
      type: object
      required:
      - contents
      - meta
      additionalProperties: false
      properties:
        contents:
          type: array
          items:
            "$ref": "#/components/schemas/ContentSummary"
        meta:
          type: object
          required:
          - total
          - page
          - perPage
          - totalPages
          additionalProperties: false
          properties:
            total:
              type: integer
            page:
              type: integer
            perPage:
              type: integer
            totalPages:
              type: integer
    ContentShowEnvelope:
      type: object
      required:
      - content
      additionalProperties: false
      properties:
        content:
          "$ref": "#/components/schemas/ContentDetail"
    ContentSummary:
      type: object
      description: Mirrors `content_summary_json` — the row shape for `index`.
      required:
      - id
      - type
      - title
      - description
      - status
      - authorId
      - organizationId
      - programId
      - version
      - typeData
      - publishedAt
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        type:
          type: string
          enum:
          - roleplay
          - spark
          - page
          - video
          - podcast
          - document
          - book
          - article
          - content
          description: Demodulized+underscored STI subtype (`content.type.demodulize.underscore`).
            "content" is the base-class fallback an unrecognized `create` `type` resolves
            to.
        title:
          type: string
          description: 'Never blank. `Content::Roleplay#title` overrides the reader
            to read `type_data&.dig("title")` (verified: `app/models/content/roleplay.rb:20`),
            but `validates :title, presence: true` calls that SAME overridden accessor
            for validation purposes, so a roleplay whose `type_data` lacks a title
            fails to save — it can never reach a persisted, API-visible state with
            a blank title. Same guarantee holds for every other subtype via the plain
            column.'
        description:
          type: string
          description: Always a string, never null — rendered as `content.description.to_s.truncate(300)`.
            For `Content::Roleplay` (whose `description` reader is also `type_data`-backed,
            unguarded by any presence validation), a missing key becomes `nil.to_s`
            → `""`, not `null`.
        status:
          type: string
          enum:
          - draft
          - published
          - archived
          - under_review
        authorId:
          type: integer
        organizationId:
          type: integer
        programId:
          type:
          - integer
          - 'null'
        version:
          type: integer
          description: AASM-unrelated row version counter; observed always 1 (no code
            path bumps it).
        typeData:
          type:
          - object
          - 'null'
          additionalProperties: true
          description: Free-form per-subtype JSON blob (scenario data, spark config,
            page settings, …).
        publishedAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        authorName:
          type:
          - string
          - 'null'
        categoryId:
          type:
          - integer
          - 'null'
        category:
          type:
          - object
          - 'null'
          additionalProperties: true
          description: Roleplay category summary (id, name, colour) or null.
        personas:
          type: array
          description: Roleplay counterparty personas shown on the row.
          items:
            type: object
            additionalProperties: true
        readyRate:
          type:
          - number
          - 'null'
          description: Share (0–1) of scored attempts that reached readiness "ready".
            Null — never 0 — when there is no countable attempt, and null in full
            for an unpublished roleplay.
        metrics:
          type:
          - object
          - 'null'
          additionalProperties: false
          description: 'Adoption + readiness figures for the library''s performance
            columns. Null IN FULL for a draft or archived roleplay: the table renders
            "N/A", which is a different statement from 0.'
          required:
          - learners
          - attempts
          - avgAttemptsToReady
          - totalPracticeSeconds
          - readyRate
          properties:
            learners:
              type: integer
            attempts:
              type: integer
            avgAttemptsToReady:
              type:
              - number
              - 'null'
              description: Mean number of attempts a learner took on this roleplay
                before first reaching ready. Null when nobody has — learners who never
                got there are excluded, not counted as zero.
            totalPracticeSeconds:
              type: integer
            readyRate:
              type:
              - number
              - 'null'
    ContentDetail:
      type: object
      description: Mirrors `content_detail_json` — `ContentSummary` plus fields always
        present on the detail endpoints, plus subtype-specific extensions (optional;
        present only for that STI subtype).
      required:
      - id
      - type
      - title
      - description
      - status
      - authorId
      - organizationId
      - programId
      - version
      - typeData
      - publishedAt
      - createdAt
      - updatedAt
      - visibility
      - featuredImage
      - analytics
      - userMetadata
      - adminMetadata
      - archivedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        type:
          type: string
          enum:
          - roleplay
          - spark
          - page
          - video
          - podcast
          - document
          - book
          - article
          - content
        title:
          type: string
          description: Never blank — see `ContentSummary.title`.
        description:
          type: string
          description: Always a string, never null — see `ContentSummary.description`.
        status:
          type: string
          enum:
          - draft
          - published
          - archived
          - under_review
        authorId:
          type: integer
        organizationId:
          type: integer
        programId:
          type:
          - integer
          - 'null'
        version:
          type:
          - integer
          - string
          description: Integer for every subtype EXCEPT `Content::Document`, where
            `content_detail_json` unconditionally overwrites this key with `content.doc_version`
            (a string, e.g. "1.0") — a real key-collision in the controller, not modeled
            by the DSL. Verified directly in `contents_controller.rb`'s `content_detail_json`
            (the document block runs after the base merge and reassigns `result[:version]`).
        typeData:
          type:
          - object
          - 'null'
          additionalProperties: true
        publishedAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        visibility:
          type:
          - object
          - 'null'
          additionalProperties: true
          description: Freeform access-control blob (`{type, allowedUsers?, allowedGroups?}`
            by convention; not enforced).
        featuredImage:
          type:
          - string
          - 'null'
        analytics:
          type:
          - object
          - 'null'
          additionalProperties: true
        userMetadata:
          type:
          - object
          - 'null'
          additionalProperties: true
        adminMetadata:
          type:
          - object
          - 'null'
          additionalProperties: true
        archivedAt:
          type:
          - string
          - 'null'
          format: date-time
        personas:
          type: array
          description: Present only when `type == "roleplay"`.
          items:
            type: object
            required:
            - id
            - name
            - role
            - avatarUrl
            - demeanor
            - background
            additionalProperties: false
            properties:
              id:
                type: integer
              name:
                type: string
              role:
                type:
                - string
                - 'null'
              avatarUrl:
                type:
                - string
                - 'null'
              demeanor:
                type:
                - string
                - 'null'
              background:
                type:
                - string
                - 'null'
        goals:
          type: array
          description: Present only when `type == "roleplay"`.
          items:
            type: object
            required:
            - id
            - name
            - description
            - goalType
            - isRequired
            additionalProperties: false
            properties:
              id:
                type: integer
              name:
                type: string
              description:
                type:
                - string
                - 'null'
              goalType:
                type: string
                enum:
                - competency
                - binary
                - compound
                - knowledge_based
                - timed
                - unscored
              isRequired:
                type: boolean
              weight:
                type: integer
                description: Present only when the assignment's `weight` is set (`ga.weight.present?`)
                  — conditionally emitted, not always in the payload.
        materials:
          type: array
          description: Present only when `type == "roleplay"`. Mirrors `Content::Roleplay::MaterialSerializer`.
          items:
            type: object
            required:
            - id
            - materialType
            - materialId
            - materialUrl
            - title
            - url
            - estimatedMinutes
            - contextNote
            - sortOrder
            - createdAt
            - updatedAt
            additionalProperties: false
            properties:
              id:
                type: integer
              materialType:
                type: string
                enum:
                - link
                - spark
                - course
                - upload
              materialId:
                type:
                - integer
                - 'null'
              materialUrl:
                type:
                - string
                - 'null'
              title:
                type: string
              url:
                type:
                - string
                - 'null'
                description: Resolved link target; `null` for "upload" or an unresolved
                  spark/course reference.
              estimatedMinutes:
                type:
                - integer
                - 'null'
              contextNote:
                type:
                - string
                - 'null'
              sortOrder:
                type: integer
              createdAt:
                type: string
                format: date-time
              updatedAt:
                type: string
                format: date-time
        sparkType:
          type:
          - string
          - 'null'
          description: Present only when `type == "spark"`. One of `Content::Spark::SPARK_TYPES`
            (survey/assessment/micro_content).
        scoringConfig:
          type: object
          additionalProperties: true
          description: Present only when `type == "spark"`.
        displaySettings:
          type: object
          additionalProperties: true
          description: Present only when `type == "spark"`.
        completionConfig:
          type: object
          additionalProperties: true
          description: Present only when `type == "spark"`.
        documentType:
          type:
          - string
          - 'null'
          description: Present only when `type == "document"`.
        format:
          type:
          - string
          - 'null'
          description: Present only when `type == "document"` (document format) or
            `type == "book"` (book format).
        fileUrl:
          type:
          - string
          - 'null'
          description: Present only when `type == "document"` or `type == "book"`.
        content:
          type:
          - string
          - 'null'
          description: Present only when `type == "document"` — the document body
            (`content.doc_content`).
        isTemplate:
          type: boolean
          description: Present only when `type == "document"`.
        settings:
          type: object
          additionalProperties: true
          description: Present only when `type` is "document", "book", or "article".
        authors:
          type: array
          items:
            type: string
          description: Present only when `type == "book"`.
        isbn:
          type:
          - string
          - 'null'
          description: Present only when `type == "book"`.
        publisher:
          type:
          - string
          - 'null'
          description: Present only when `type == "book"`.
        pageCount:
          type:
          - integer
          - 'null'
          description: Present only when `type == "book"`.
        genres:
          type: array
          items:
            type: string
          description: Present only when `type == "book"`.
        edition:
          type:
          - string
          - 'null'
          description: Present only when `type == "book"`.
        readingTime:
          type:
          - integer
          - 'null'
          description: Present only when `type` is "book" or "article".
        summary:
          type:
          - string
          - 'null'
          description: Present only when `type` is "book" or "article".
        language:
          type: string
          description: Present only when `type` is "document", "book", or "article".
            Defaults to "en".
        sourceUrl:
          type:
          - string
          - 'null'
          description: Present only when `type == "article"`.
        sourceName:
          type:
          - string
          - 'null'
          description: Present only when `type == "article"`.
        author:
          type:
          - string
          - 'null'
          description: Present only when `type == "article"` — the article's byline
            (`content.author_name`), distinct from the top-level `authorId` (the admin
            who created the record).
        publishedDate:
          type:
          - string
          - 'null'
          description: Present only when `type == "article"`.
        articleType:
          type:
          - string
          - 'null'
          description: Present only when `type == "article"`.
        imageUrl:
          type:
          - string
          - 'null'
          description: Present only when `type == "article"`.
        pageType:
          type:
          - string
          - 'null'
          description: Present only when `type == "page"`.
        coverConfig:
          type: object
          additionalProperties: true
          description: Present only when `type == "page"`.
        seoConfig:
          type: object
          additionalProperties: true
          description: Present only when `type == "page"`.
        blocks:
          type: array
          description: Present only when `type == "page"`. Mirrors `Content::Page::PageBlockSerializer`.
          items:
            type: object
            required:
            - id
            - contentId
            - blockType
            - position
            - content
            - settings
            - parentBlockId
            - schemaVersion
            - createdAt
            - updatedAt
            additionalProperties: false
            properties:
              id:
                type: string
                description: Client-generated UUID string primary key.
              contentId:
                type: integer
              blockType:
                type: string
              position:
                type: integer
              content:
                type:
                - object
                - 'null'
                additionalProperties: true
              settings:
                type:
                - object
                - 'null'
                additionalProperties: true
              parentBlockId:
                type:
                - string
                - 'null'
              schemaVersion:
                type: integer
              createdAt:
                type: string
                format: date-time
              updatedAt:
                type: string
                format: date-time
        videoUrl:
          type:
          - string
          - 'null'
          description: Present only when `type == "video"`.
        provider:
          type: string
          description: Present only when `type == "video"`. Defaults to "custom".
        thumbnailUrl:
          type:
          - string
          - 'null'
          description: Present only when `type == "video"`.
        chapters:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Present only when `type == "video"`.
        subtitles:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Present only when `type == "video"`.
        videoSettings:
          type: object
          additionalProperties: true
          description: Present only when `type == "video"`.
        duration:
          type: integer
          description: Present only when `type` is "video" or "podcast" (seconds).
            Defaults to 0.
        transcript:
          type:
          - string
          - 'null'
          description: Present only when `type` is "video" or "podcast".
        audioUrl:
          type:
          - string
          - 'null'
          description: Present only when `type == "podcast"`.
        series:
          type:
          - string
          - 'null'
          description: Present only when `type == "podcast"`.
        episode:
          type:
          - integer
          - 'null'
          description: Present only when `type == "podcast"`. Type inferred from field
            name — unconfirmed against real data (opaque `type_data`).
        season:
          type:
          - integer
          - 'null'
          description: Present only when `type == "podcast"`. Type inferred from field
            name — unconfirmed against real data (opaque `type_data`).
        showNotes:
          type: string
          description: Present only when `type == "podcast"`. Defaults to "".
        guests:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Present only when `type == "podcast"`.
        topics:
          type: array
          items:
            type: string
          description: Present only when `type == "podcast"`.
        podcastSettings:
          type: object
          additionalProperties: true
          description: Present only when `type == "podcast"`.
    ContentWriteBody:
      type: object
      description: "`POST /admin/contents` request. Snake_case on the wire (verified:
        `content_params = params.permit(:title, :description, :program_id, :featured_image,
        ...)` reads snake_case directly, and the SPA's `updateContent` explicitly
        sets `payload.type_data = ...`). Verified against the live caller (`content/mutations.ts#createContent`),
        which sends only `type`/`title`/`description` — the remaining `content_params`-permitted
        keys (`program_id`, `featured_image`, `visibility`, `user_metadata`, `admin_metadata`)
        are accepted by the controller but unexercised by any current SPA caller."
      required:
      - type
      - title
      additionalProperties: false
      properties:
        type:
          type: string
          description: Short (`roleplay`) or full STI (`Content::Roleplay`) form.
            Omitted defaults to "roleplay"; unrecognized falls back to base `Content`
            — both silent, not 400s.
        title:
          type: string
        description:
          type: string
        program_id:
          type:
          - integer
          - 'null'
          description: Must belong to the current organization
          or 422.: 
        featured_image:
          type: string
        type_data:
          type: object
          additionalProperties: true
          description: Per-subtype builder blob, persisted verbatim to `content.type_data`.
        visibility:
          type: object
          additionalProperties: true
        user_metadata:
          type: object
          additionalProperties: true
        admin_metadata:
          type: object
          additionalProperties: true
    ContentUpdateBody:
      type: object
      description: "`PATCH`/`PUT /admin/contents/{id}` request. Snake_case on the
        wire (same as `ContentWriteBody`). Every property optional (partial update)
        — verified against the live caller (`content/mutations.ts#updateContent`),
        which sends only the slice the calling screen owns (`title`+`description`,
        or `type_data`)."
      additionalProperties: false
      properties:
        title:
          type: string
        description:
          type: string
        program_id:
          type:
          - integer
          - 'null'
          description: Must belong to the current organization
          or 422.: 
        featured_image:
          type: string
        type_data:
          type: object
          additionalProperties: true
        visibility:
          type: object
          additionalProperties: true
        user_metadata:
          type: object
          additionalProperties: true
        admin_metadata:
          type: object
          additionalProperties: true
    EmailLogsPaginationMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    EmailLogMessage:
      type: object
      required:
      - senderName
      - senderEmail
      - recipientName
      - recipientEmail
      - emailTitle
      - programName
      - programId
      - sentAt
      - events
      - senderRemoved
      - recipientRemoved
      additionalProperties: false
      properties:
        senderName:
          type:
          - string
          - 'null'
        senderEmail:
          type:
          - string
          - 'null'
        recipientName:
          type:
          - string
          - 'null'
        recipientEmail:
          type:
          - string
          - 'null'
        emailTitle:
          type:
          - string
          - 'null'
          description: Resolved from the mail template or, for a campaign-backed email,
            the campaign/survey title.
        programName:
          type:
          - string
          - 'null'
        programId:
          type:
          - string
          - 'null'
          description: Passed through from the microservice row as-is; its exact JSON
            type is not this port's to define.
        sentAt:
          type:
          - string
          - 'null'
          format: date-time
          description: Timestamp of the ACCEPTED event, if any.
        events:
          type: array
          items:
            type: object
            required:
            - type
            - timestamp
            - details
            additionalProperties: false
            properties:
              type:
                type: string
              timestamp:
                type:
                - string
                - 'null'
              details:
                type:
                - object
                - 'null'
                additionalProperties: true
        senderRemoved:
          type: boolean
          description: The sender id was present but no longer resolves to a local
            Member.
        recipientRemoved:
          type: boolean
          description: The recipient id was present but no longer resolves to a local
            Member.
    MemberEventsEnvelope:
      type: object
      required:
      - events
      - meta
      additionalProperties: false
      properties:
        events:
          type: array
          description: Event records, the current member's own view (RSVP status,
            medium detail).
          items:
            "$ref": "#/components/schemas/MemberEvent"
        meta:
          type: object
          required:
          - total
          - page
          - perPage
          - totalPages
          additionalProperties: false
          description: Pagination metadata.
          properties:
            total:
              type: integer
            page:
              type: integer
            perPage:
              type: integer
            totalPages:
              type: integer
    MemberEvent:
      type: object
      required:
      - id
      - title
      - description
      - startTime
      - endTime
      - timeZone
      - location
      - status
      - attendeeCount
      - maxSlots
      - programName
      - programId
      - rsvpStatus
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
          description: HTML tags stripped, truncated to 300 chars. Empty string, never
            null.
        startTime:
          type:
          - string
          - 'null'
          format: date-time
        endTime:
          type:
          - string
          - 'null'
          format: date-time
          description: Never null in practice — the `.upcoming` scope requires `end_time`
            present.
        timeZone:
          type:
          - string
          - 'null'
        location:
          type:
          - string
          - 'null'
          description: The mode's medium detail (address / meeting URL / phone number),
            not necessarily a physical address. `null` for a provider mode with no
            medium detail, or an event with no modes.
        status:
          type: string
          enum:
          - upcoming
          - ongoing
          - completed
        attendeeCount:
          type: integer
          description: Count of invites with an RSVP status of "yes".
        maxSlots:
          type:
          - integer
          - 'null'
        programName:
          type: string
          description: Empty string when the event has no host program.
        programId:
          type:
          - integer
          - 'null'
        rsvpStatus:
          type: string
          enum:
          - true
          - false
          - maybe
          - not_responded
          - not_invited
    GlobalConnectionsReportIndex:
      type: object
      required:
      - summary
      - connections
      - programs
      - meta
      additionalProperties: false
      properties:
        summary:
          "$ref": "#/components/schemas/GlobalConnectionsReportSummary"
        connections:
          type: array
          items:
            "$ref": "#/components/schemas/GlobalConnectionsReportRow"
        programs:
          type: array
          items:
            type: object
            required:
            - id
            - name
            additionalProperties: false
            properties:
              id:
                type: integer
              name:
                type: string
        meta:
          "$ref": "#/components/schemas/GlobalConnectionsReportMeta"
    GlobalConnectionsReportSummary:
      type: object
      required:
      - totalConnections
      - totalGroups
      - activeGroups
      - totalFlashMeetings
      - programsWithConnections
      - groupsByStatus
      additionalProperties: false
      properties:
        totalConnections:
          type: integer
        totalGroups:
          type: integer
        activeGroups:
          type: integer
        totalFlashMeetings:
          type: integer
        programsWithConnections:
          type: integer
        groupsByStatus:
          type: array
          items:
            type: object
            required:
            - status
            - count
            additionalProperties: false
            properties:
              status:
                type: string
                enum:
                - active
                - inactive
                - closed
                - drafted
                - pending
              count:
                type: integer
    GlobalConnectionsReportRow:
      type: object
      required:
      - id
      - objectType
      - name
      - programId
      - programName
      - connectionType
      - status
      - mentors
      - mentees
      - startedOn
      - closedAt
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        objectType:
          type: string
          enum:
          - group
          - meeting
        name:
          type: string
        programId:
          type: integer
        programName:
          type: string
        connectionType:
          type: string
          enum:
          - ongoing
          - circle
          - flash_meeting
        status:
          type: string
          description: |
            Group statuses (active/inactive/closed/drafted/pending/unknown) for a group row;
            meeting statuses (upcoming/completed/cancelled/past) for a meeting row.
          enum:
          - active
          - inactive
          - closed
          - drafted
          - pending
          - unknown
          - upcoming
          - completed
          - cancelled
          - past
        mentors:
          type: array
          items:
            "$ref": "#/components/schemas/GlobalConnectionsReportPerson"
        mentees:
          type: array
          items:
            "$ref": "#/components/schemas/GlobalConnectionsReportPerson"
        startedOn:
          type:
          - string
          - 'null'
          format: date-time
        closedAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type: string
          format: date-time
    GlobalConnectionsReportPerson:
      type: object
      required:
      - id
      - name
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
    GlobalConnectionsReportMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    GoalAnalyticsCell:
      description: A count or rate, or a suppressed marker when its cohort is below
        the org's minimum.
      oneOf:
      - type: number
      - type: object
        required:
        - suppressed
        - reason
        - minimum_cohort_size
        additionalProperties: false
        properties:
          suppressed:
            const: true
          reason:
            type: string
          minimum_cohort_size:
            type: integer
    GoalAnalyticsFunnelBreakdown:
      description: Current-goal counts by lifecycle state, or a suppressed marker
        for the whole breakdown.
      oneOf:
      - type: object
        required:
        - suppressed
        - reason
        - minimum_cohort_size
        additionalProperties: false
        properties:
          suppressed:
            const: true
          reason:
            type: string
          minimum_cohort_size:
            type: integer
      - type: object
        required:
        - suppressed
        - minimum_cohort_size
        - cells
        - total
        additionalProperties: false
        properties:
          suppressed:
            const: false
          minimum_cohort_size:
            type: integer
          cells:
            type: object
            required:
            - draft
            - scored
            - finalized
            additionalProperties: false
            properties:
              draft:
                type: integer
              scored:
                type: integer
              finalized:
                type: integer
          total:
            type: integer
    InsightsShellError:
      type: object
      description: |
        This tier's 4xx shape — source renders a BARE `{error: "<string>"}`, not this API's
        `{error: {code, message}}` envelope. Declared as-is because the SPA reads the bare form.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    InsightsDashboard:
      type: object
      required:
      - id
      - name
      - description
      - kind
      - goodDataRef
      - componentKey
      - createdAt
      - updatedAt
      - createdById
      - updatedById
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        kind:
          type: string
          description: gooddata / widget_grid / react_spa — only the first ever required
            an embed token.
        goodDataRef:
          type: string
          nullable: true
        componentKey:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
        createdById:
          type: integer
          nullable: true
        updatedById:
          type: integer
          nullable: true
    GoodDataEmbedToken:
      type: object
      required:
      - embed_url
      - token
      - expires_at
      - workspace_id
      - ref
      - entity
      - mode
      additionalProperties: false
      properties:
        embed_url:
          type: string
          description: The GoodData URL to iframe, already resolved for the entity+mode
            pair.
        token:
          type: string
          description: 'The API token the embed authenticates with. Short-lived by
            policy (see `expires_at`), but treat it as a credential — never log or
            persist it client-side.

            '
        expires_at:
          type: string
          format: date-time
          description: ISO8601 UTC, minted 5 minutes ahead (`EmbedToken::DEFAULT_TTL`).
        workspace_id:
          type: string
        ref:
          type: string
          description: The dashboard's or insight's `gooddata_ref`, echoed back.
        entity:
          type: string
          enum:
          - dashboard
          - insight
        mode:
          type: string
          enum:
          - player
          - editor
    InsightsInsight:
      type: object
      required:
      - id
      - name
      - description
      - goodDataRef
      - createdAt
      - updatedAt
      - createdById
      - updatedById
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        goodDataRef:
          type: string
          description: Required by the model — an insight cannot exist without a GoodData
            reference.
        createdAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
        createdById:
          type: integer
          nullable: true
        updatedById:
          type: integer
          nullable: true
    LongitudinalGrowthInflection:
      type: object
      nullable: true
      required:
      - kind
      - pattern_id
      - observed_periods
      - first_period_start
      - last_period_start
      additionalProperties: false
      properties:
        kind:
          type: string
          enum:
          - stalled
          - breakthrough
        pattern_id:
          type: integer
        observed_periods:
          type: integer
        first_period_start:
          type: string
          format: date
        last_period_start:
          type: string
          format: date
    LongitudinalGrowthPoint:
      type: object
      required:
      - period_start
      - period_end
      - value
      - inflection
      additionalProperties: false
      properties:
        period_start:
          type: string
          format: date
        period_end:
          type: string
          format: date
        value:
          type: number
        inflection:
          "$ref": "#/components/schemas/LongitudinalGrowthInflection"
    LongitudinalGrowthObservation:
      type: object
      required:
      - observed_on
      - label
      - note
      additionalProperties: false
      properties:
        observed_on:
          type: string
          format: date
        label:
          type: string
        note:
          type: string
          nullable: true
    MentoringAdoptionMetric:
      type: object
      required:
      - rate
      - threshold
      - status
      additionalProperties: false
      properties:
        rate:
          description: Percentage rate, or a suppressed marker when cohort < k.
          oneOf:
          - type: number
          - type: object
            required:
            - suppressed
            - reason
            - minimum_cohort_size
            additionalProperties: false
            properties:
              suppressed:
                const: true
              reason:
                type: string
              minimum_cohort_size:
                type: integer
        numerator:
          type:
          - integer
          - 'null'
        denominator:
          type:
          - integer
          - 'null'
        threshold:
          type: integer
        status:
          type: string
          enum:
          - met
          - not_met
          - suppressed
    ArticleSummary:
      type: object
      description: Camel-cased — see the index operation's description.
      required:
      - id
      - title
      - authorName
      - authorId
      - type
      - status
      - viewCount
      - helpfulCount
      - labels
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        authorName:
          type:
          - string
          - 'null'
        authorId:
          type:
          - integer
          - 'null'
        type:
          type:
          - string
          - 'null'
          enum:
          - text
          - media
          - list
          - upload
          - null
        status:
          type: string
          enum:
          - draft
          - published
          - unknown
        viewCount:
          type: integer
        helpfulCount:
          type: integer
        labels:
          type: array
          items:
            type: string
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ArticleDetail:
      type: object
      required:
      - id
      - title
      - authorName
      - authorId
      - type
      - status
      - viewCount
      - helpfulCount
      - labels
      - createdAt
      - body
      - embedCode
      - publishedAt
      - attachmentFileName
      - attachmentContentType
      - attachmentFileSize
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        authorName:
          type:
          - string
          - 'null'
        authorId:
          type:
          - integer
          - 'null'
        type:
          type:
          - string
          - 'null'
          enum:
          - text
          - media
          - list
          - upload
          - null
        status:
          type: string
          enum:
          - draft
          - published
          - unknown
        viewCount:
          type: integer
        helpfulCount:
          type: integer
        labels:
          type: array
          items:
            type: string
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        body:
          type:
          - string
          - 'null'
        embedCode:
          type:
          - string
          - 'null'
        publishedAt:
          type:
          - string
          - 'null'
          format: date-time
        attachmentFileName:
          type:
          - string
          - 'null'
        attachmentContentType:
          type:
          - string
          - 'null'
        attachmentFileSize:
          type:
          - integer
          - 'null'
    ArticlesPagination:
      type: object
      description: |
        This controller's base class (`Admin::Programs::BaseController#pagination_meta`) emits
        camelCase keys, not this spec's usual snake_case `{total, page, limit, has_more}`.
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    BulkMatchConfig:
      type: object
      additionalProperties: false
      required:
      - id
      - orientationType
      - maxPickableSlots
      - minMenteesPerGroup
      - mentorsPerGroup
      - sortValue
      - sortOrder
      - requestNotes
      - showDrafted
      - showPublished
      - primaryViewId
      - secondaryViewId
      - persisted
      properties:
        id:
          type:
          - integer
          - 'null'
        orientationType:
          type: integer
          description: 0-4, see AbstractBulkMatch::OrientationType.
        maxPickableSlots:
          type:
          - integer
          - 'null'
        minMenteesPerGroup:
          type:
          - integer
          - 'null'
        mentorsPerGroup:
          type:
          - integer
          - 'null'
        sortValue:
          type:
          - string
          - 'null'
        sortOrder:
          type: string
          enum:
          - asc
          - desc
        requestNotes:
          type:
          - boolean
          - 'null'
        showDrafted:
          type:
          - boolean
          - 'null'
        showPublished:
          type:
          - boolean
          - 'null'
        primaryViewId:
          type:
          - integer
          - 'null'
        secondaryViewId:
          type:
          - integer
          - 'null'
        persisted:
          type: boolean
    BulkMatchProgramCapabilities:
      type: object
      additionalProperties: false
      required:
      - matchingConfigured
      - projectBased
      - allowMultipleGroups
      - diversityEnabled
      - ongoingApplicable
      properties:
        matchingConfigured:
          type: boolean
        projectBased:
          type: boolean
        allowMultipleGroups:
          type: boolean
        diversityEnabled:
          type: boolean
        ongoingApplicable:
          type: boolean
    BulkMatchOrientationType:
      type: object
      additionalProperties: false
      required:
      - value
      - label
      properties:
        value:
          type: integer
        label:
          type: string
    BulkMatchShowPayload:
      type: object
      additionalProperties: false
      required:
      - bulkMatch
      - program
      - orientationTypes
      properties:
        bulkMatch:
          "$ref": "#/components/schemas/BulkMatchConfig"
        program:
          "$ref": "#/components/schemas/BulkMatchProgramCapabilities"
        orientationTypes:
          type: array
          items:
            "$ref": "#/components/schemas/BulkMatchOrientationType"
    BulkMatchUpdatePayload:
      type: object
      additionalProperties: false
      required:
      - bulkMatch
      properties:
        bulkMatch:
          "$ref": "#/components/schemas/BulkMatchConfig"
    BulkMatchAdminView:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      - userCount
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        userCount:
          type: integer
    BulkMatchAdminViewsPayload:
      type: object
      additionalProperties: false
      required:
      - mentorViews
      - menteeViews
      - connectionViews
      properties:
        mentorViews:
          type: array
          items:
            "$ref": "#/components/schemas/BulkMatchAdminView"
        menteeViews:
          type: array
          items:
            "$ref": "#/components/schemas/BulkMatchAdminView"
        connectionViews:
          type: array
          items:
            "$ref": "#/components/schemas/BulkMatchAdminView"
    BulkMatchUpdateBody:
      type: object
      additionalProperties: false
      properties:
        orientationType:
          type: integer
          description: Orientation type (0-4).
        maxPickableSlots:
          type: integer
          description: Max slots per entity.
        minMenteesPerGroup:
          type: integer
          description: "\U0001F534 Read by the controller (min_mentees_per_group)
            but absent from the source openapi DSL block — a real DSL/controller disagreement,
            controller followed."
        mentorsPerGroup:
          type: integer
          description: "\U0001F534 Same disagreement as minMenteesPerGroup above (mentors_per_group)."
        sortValue:
          type: string
          description: Sort column.
        sortOrder:
          type: string
          description: Only an exact "desc" flips to descending; any other value (including
            case variants) is silently treated as ascending — the controller never
            rejects it, so this is intentionally not an enum.
        requestNotes:
          type: boolean
          description: Request notes on draft.
        showDrafted:
          type: boolean
          description: Show drafted pairs.
        showPublished:
          type: boolean
          description: Show published pairs.
    BulkMatchErrorResponse:
      type: object
      additionalProperties: false
      required:
      - error
      properties:
        error:
          type: string
    BulkMatchComputeBody:
      type: object
      additionalProperties: false
      properties:
        orientationType:
          type: integer
          description: Orientation type (0-4); defaults to 0 when absent.
        primaryViewId:
          type: integer
          description: Admin view ID for primary entities.
        secondaryViewId:
          type: integer
          description: Admin view ID for secondary entities (defaulted to primaryViewId
            when the config is primary-view-only).
        maxPickableSlots:
          type: integer
        minMenteesPerGroup:
          type: integer
          description: "\U0001F534 Read by the controller but absent from the source
            DSL block, same disagreement as `update`'s body."
        mentorsPerGroup:
          type: integer
          description: "\U0001F534 Same disagreement as minMenteesPerGroup above."
    BulkMatchComputedEntity:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - slotsAvailable
      - recommendedCount
      - connectionsCount
      properties:
        id:
          type: integer
        name:
          type: string
          description: 'Falls back to "User #<id>" if `User#name` raises.'
        slotsAvailable:
          type: integer
        recommendedCount:
          type: integer
        connectionsCount:
          type: integer
    BulkMatchSuggestion:
      type: object
      additionalProperties: false
      required:
      - id
      - score
      properties:
        id:
          type: integer
        score:
          type: number
    BulkMatchSecondaryComputedEntity:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - suggestedPrimaryEntities
      - groupStatus
      - groupId
      properties:
        id:
          type: integer
        name:
          type: string
          description: 'Falls back to "User #<id>" if `User#name` raises.'
        suggestedPrimaryEntities:
          type: array
          maxItems: 10
          items:
            "$ref": "#/components/schemas/BulkMatchSuggestion"
        groupStatus:
          type: string
          description: Defaults to "unmatched" when no group/recommendation info is
            present.
        groupId:
          type:
          - integer
          - 'null'
    BulkMatchComputationResults:
      type: object
      additionalProperties: false
      required:
      - status
      - bulkMatchId
      - isLargeSet
      - primaryEntities
      - secondaryEntities
      - meta
      properties:
        status:
          const: completed
        bulkMatchId:
          type: integer
        isLargeSet:
          type: boolean
        primaryEntities:
          type: array
          items:
            "$ref": "#/components/schemas/BulkMatchComputedEntity"
        secondaryEntities:
          type: array
          items:
            "$ref": "#/components/schemas/BulkMatchSecondaryComputedEntity"
        meta:
          type: object
          additionalProperties: false
          required:
          - totalPrimary
          - totalSecondary
          properties:
            totalPrimary:
              type: integer
            totalSecondary:
              type: integer
    BulkMatchUpdatePairBody:
      type: object
      additionalProperties: false
      required:
      - updateType
      - bulkMatchId
      - primaryEntityId
      - secondaryEntityId
      properties:
        updateType:
          type: string
          enum:
          - draft
          - publish
          - discard
        bulkMatchId:
          type: integer
          description: "\U0001F534 Read unconditionally (`BulkMatch.find_by!(program_id:,
            id: params[:bulkMatchId])`) before dispatching on `updateType` — absent
            from the source `openapi` DSL block, a real DSL/controller disagreement,
            controller followed."
        primaryEntityId:
          type: integer
          description: Mentor (or group-role) user ID.
        secondaryEntityId:
          type: integer
          description: Mentee user ID.
        groupId:
          type: integer
          description: Existing group ID — required for publish (of an existing draft)
            and discard, ignored for a fresh draft/publish.
        notes:
          type: string
          description: Notes for the connection.
        message:
          type: string
          description: "Message for publish. \U0001F534 Source-side defect (escalated):
            publishing a BRAND-NEW group (no prior draft) silently drops this message
            — only publishing an EXISTING drafted group sends it."
    BulkMatchUpdatePairResult:
      type: object
      additionalProperties: false
      required:
      - status
      properties:
        groupId:
          type: integer
          description: Present for draft/publish, absent for discard.
        status:
          type: string
          enum:
          - drafted
          - published
          - discarded
    CheckinReportBareError:
      type: object
      description: |
        `Programs::BaseController#resolve_program`'s "not found" branch renders a bare
        `{error: "<string>"}`, not this API's `{error: {code, message, details}}` envelope.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    CheckinReportIndex:
      type: object
      required:
      - checkins
      - summary
      - meta
      - roles
      additionalProperties: false
      properties:
        checkins:
          type: array
          items:
            "$ref": "#/components/schemas/CheckinReportRow"
        summary:
          "$ref": "#/components/schemas/CheckinReportSummary"
        meta:
          "$ref": "#/components/schemas/CheckinReportMeta"
        roles:
          type: array
          items:
            "$ref": "#/components/schemas/CheckinReportRole"
    CheckinReportRow:
      type: object
      required:
      - id
      - member
      - memberEmail
      - group
      - role
      - date
      - duration
      - durationMinutes
      - type
      - comment
      - title
      additionalProperties: false
      properties:
        id:
          type: integer
        member:
          type: string
          description: '"first_name last_name", or an empty string if the checkin
            has no member.'
        memberEmail:
          type:
          - string
          - 'null'
        group:
          type:
          - string
          - 'null'
        role:
          type: string
          description: The role's customized term, or its raw name if no term is configured.
            Empty if no role.
        date:
          type:
          - string
          - 'null'
          format: date
        duration:
          type: number
          description: Hours, rounded to 2 decimals.
        durationMinutes:
          type: integer
        type:
          type: string
          enum:
          - Task
          - Meeting
        comment:
          type:
          - string
          - 'null'
        title:
          type:
          - string
          - 'null'
    CheckinReportSummary:
      type: object
      required:
      - totalHours
      - meetingHours
      - taskHours
      additionalProperties: false
      properties:
        totalHours:
          type: number
        meetingHours:
          type: number
        taskHours:
          type: number
    CheckinReportMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    CheckinReportRole:
      type: object
      required:
      - id
      - name
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
    ConnectionActivityReportBareError:
      type: object
      description: |
        `Programs::BaseController#resolve_program`'s "not found" branch renders a bare
        `{error: "<string>"}`, not this API's `{error: {code, message, details}}` envelope.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    ConnectionActivityReportIndex:
      type: object
      required:
      - summary
      - connections
      - meta
      - dateRange
      additionalProperties: false
      properties:
        summary:
          "$ref": "#/components/schemas/ConnectionActivityReportSummary"
        connections:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionActivityReportRow"
        meta:
          "$ref": "#/components/schemas/ConnectionActivityReportMeta"
        dateRange:
          "$ref": "#/components/schemas/ConnectionActivityReportDateRange"
    ConnectionActivityReportSummary:
      type: object
      required:
      - totalConnections
      - activeConnections
      - totalMessages
      - totalPosts
      - totalTasks
      - totalMeetings
      - totalSurveyResponses
      - totalActivities
      - connectionsByStatus
      additionalProperties: false
      properties:
        totalConnections:
          type: integer
        activeConnections:
          type: integer
        totalMessages:
          type: integer
        totalPosts:
          type: integer
        totalTasks:
          type: integer
        totalMeetings:
          type: integer
        totalSurveyResponses:
          type: integer
        totalActivities:
          type: integer
        connectionsByStatus:
          type: array
          items:
            type: object
            required:
            - status
            - count
            additionalProperties: false
            properties:
              status:
                type: string
                enum:
                - active
                - inactive
                - closed
                - drafted
                - pending
              count:
                type: integer
    ConnectionActivityReportRow:
      type: object
      required:
      - id
      - name
      - mentors
      - mentees
      - startedOn
      - closeDate
      - currentStatus
      - messagesCount
      - postsCount
      - tasksCount
      - meetingsCount
      - surveyResponsesCount
      - totalActivities
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        mentors:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionActivityReportPerson"
        mentees:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionActivityReportPerson"
        startedOn:
          type:
          - string
          - 'null'
          format: date-time
        closeDate:
          type:
          - string
          - 'null'
          format: date-time
        currentStatus:
          type: string
          enum:
          - active
          - inactive
          - closed
          - drafted
          - pending
          - unknown
        messagesCount:
          type: integer
        postsCount:
          type: integer
        tasksCount:
          type: integer
        meetingsCount:
          type: integer
        surveyResponsesCount:
          type: integer
        totalActivities:
          type: integer
    ConnectionActivityReportPerson:
      type: object
      required:
      - id
      - name
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
    ConnectionActivityReportMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    ConnectionActivityReportDateRange:
      type: object
      required:
      - startDate
      - endDate
      additionalProperties: false
      properties:
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
    DemographicReportBareError:
      type: object
      description: |
        `Programs::BaseController#resolve_program`'s "not found" branch renders a bare
        `{error: "<string>"}`, not this API's `{error: {code, message, details}}` envelope.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    DemographicReportUnavailable:
      type: object
      description: |
        No location profile question configured for the organization, or no role in this program
        links one.
      required:
      - available
      - message
      additionalProperties: false
      properties:
        available:
          const: false
        message:
          type: string
    DemographicReportAvailable:
      type: object
      required:
      - available
      - roles
      - showForAllRoles
      additionalProperties: false
      properties:
        available:
          const: true
        roles:
          type: array
          items:
            "$ref": "#/components/schemas/DemographicReportRole"
        showForAllRoles:
          type: boolean
        mapData:
          type: array
          items:
            "$ref": "#/components/schemas/DemographicReportMapEntry"
        columns:
          type: array
          items:
            "$ref": "#/components/schemas/DemographicReportColumn"
        tableData:
          type: array
          items:
            "$ref": "#/components/schemas/DemographicReportTableRow"
        meta:
          "$ref": "#/components/schemas/DemographicReportMeta"
        mostUsersStats:
          type: object
          description: column_key → the country name with the highest count for that
            column.
          additionalProperties:
            type: string
    DemographicReportRole:
      type: object
      required:
      - name
      - label
      additionalProperties: false
      properties:
        name:
          type: string
        label:
          type:
          - string
          - 'null'
    DemographicReportMapEntry:
      type: object
      required:
      - address
      - lat
      - lng
      - count
      additionalProperties: false
      properties:
        address:
          type:
          - string
          - 'null'
        lat:
          type:
          - number
          - 'null'
        lng:
          type:
          - number
          - 'null'
        count:
          type: integer
    DemographicReportColumn:
      type: object
      required:
      - key
      - title
      additionalProperties: false
      properties:
        key:
          type: string
        title:
          type: string
    DemographicReportTableRow:
      type: object
      required:
      - country
      - counts
      - cities
      additionalProperties: false
      properties:
        country:
          type: string
        counts:
          type: object
          description: column_key (e.g. all_users_count, <role_name>_count) → count.
          additionalProperties:
            type: integer
        cities:
          type: array
          items:
            type: object
            required:
            - name
            - counts
            additionalProperties: false
            properties:
              name:
                type: string
              counts:
                type: object
                additionalProperties:
                  type: integer
    DemographicReportMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    EngagementPaginationMeta:
      type: object
      additionalProperties: false
      required:
      - total
      - page
      - perPage
      - totalPages
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    EngagementSectionCount:
      type: object
      additionalProperties: false
      required:
      - total
      properties:
        total:
          type: integer
          description: Blanket-rescued to 0 on any error (`count_for`) — a zero here
            can mean zero or broken.
    EngagementSummary:
      type: object
      additionalProperties: false
      required:
      - surveys
      - announcements
      - campaigns
      - resources
      - events
      - forums
      - articles
      - meetingGuides
      - certificates
      - qa
      - moderateContent
      - integrations
      - threeSixtySurveys
      properties:
        surveys:
          "$ref": "#/components/schemas/EngagementSectionCount"
        announcements:
          "$ref": "#/components/schemas/EngagementSectionCount"
        campaigns:
          "$ref": "#/components/schemas/EngagementSectionCount"
        resources:
          "$ref": "#/components/schemas/EngagementSectionCount"
        events:
          "$ref": "#/components/schemas/EngagementSectionCount"
        forums:
          "$ref": "#/components/schemas/EngagementSectionCount"
        articles:
          "$ref": "#/components/schemas/EngagementSectionCount"
        meetingGuides:
          "$ref": "#/components/schemas/EngagementSectionCount"
        certificates:
          "$ref": "#/components/schemas/EngagementSectionCount"
        qa:
          "$ref": "#/components/schemas/EngagementSectionCount"
        moderateContent:
          "$ref": "#/components/schemas/EngagementSectionCount"
        integrations:
          "$ref": "#/components/schemas/EngagementSectionCount"
        threeSixtySurveys:
          "$ref": "#/components/schemas/EngagementSectionCount"
    SurveyListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - type
      - status
      - responseCount
      - totalResponses
      - dueDate
      - createdAt
      properties:
        id:
          type: integer
        name:
          type:
          - string
          - 'null'
        type:
          type:
          - string
          - 'null'
        status:
          type:
          - string
          - 'null'
        responseCount:
          type: integer
        totalResponses:
          type: integer
        dueDate:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    SurveysPayload:
      type: object
      additionalProperties: false
      required:
      - surveys
      - meta
      properties:
        surveys:
          type: array
          items:
            "$ref": "#/components/schemas/SurveyListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    SurveyQuestion:
      type: object
      additionalProperties: false
      required:
      - id
      - text
      - type
      - required
      - position
      properties:
        id:
          type: integer
        text:
          type:
          - string
          - 'null'
        type:
          type:
          - string
          - 'null'
        required:
          type: boolean
        position:
          type:
          - integer
          - 'null'
    EngagementSurveyDetail:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - type
      - status
      - responseCount
      - questions
      - createdAt
      properties:
        id:
          type: integer
        name:
          type:
          - string
          - 'null'
        type:
          type:
          - string
          - 'null'
        status:
          type:
          - string
          - 'null'
        responseCount:
          type: integer
        questions:
          type: array
          items:
            "$ref": "#/components/schemas/SurveyQuestion"
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    SurveyDetailPayload:
      type: object
      additionalProperties: false
      required:
      - survey
      properties:
        survey:
          "$ref": "#/components/schemas/EngagementSurveyDetail"
    AnnouncementListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - body
      - status
      - recipientRoleNames
      - viewCount
      - createdAt
      - updatedAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        body:
          type:
          - string
          - 'null'
        status:
          type:
          - string
          - 'null'
        recipientRoleNames:
          type: array
          items:
            type: string
        viewCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        updatedAt:
          type:
          - string
          - 'null'
          format: date-time
    AnnouncementsPayload:
      type: object
      additionalProperties: false
      required:
      - announcements
      - meta
      properties:
        announcements:
          type: array
          items:
            "$ref": "#/components/schemas/AnnouncementListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    AnnouncementDetailPayload:
      type: object
      additionalProperties: false
      required:
      - announcement
      properties:
        announcement:
          "$ref": "#/components/schemas/AnnouncementListItem"
    CampaignListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - type
      - status
      - enabledAt
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        type:
          type:
          - string
          - 'null'
        status:
          type:
          - string
          - 'null'
        enabledAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    CampaignsPayload:
      type: object
      additionalProperties: false
      required:
      - campaigns
      - meta
      properties:
        campaigns:
          type: array
          items:
            "$ref": "#/components/schemas/CampaignListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    ResourceListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - content
      - viewCount
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        content:
          type:
          - string
          - 'null'
        viewCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ResourcesPayload:
      type: object
      additionalProperties: false
      required:
      - resources
      - meta
      properties:
        resources:
          type: array
          items:
            "$ref": "#/components/schemas/ResourceListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    EventListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - startDate
      - endDate
      - location
      - status
      - attendeeCount
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        startDate:
          type:
          - string
          - 'null'
          format: date-time
        endDate:
          type:
          - string
          - 'null'
          format: date-time
        location:
          type:
          - string
          - 'null'
          description: Populated from `event.description`, not a real location field
            — the wire key name doesn't match its source field's semantics.
        status:
          type:
          - string
          - 'null'
        attendeeCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    EventsPayload:
      type: object
      additionalProperties: false
      required:
      - events
      - meta
      properties:
        events:
          type: array
          items:
            "$ref": "#/components/schemas/EventListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    ForumListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - description
      - topicCount
      - createdAt
      properties:
        id:
          type: integer
        name:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        topicCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ForumsPayload:
      type: object
      additionalProperties: false
      required:
      - forums
      - meta
      properties:
        forums:
          type: array
          items:
            "$ref": "#/components/schemas/ForumListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    ArticleListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - authorName
      - status
      - viewCount
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        authorName:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - draft
          - published
        viewCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ArticlesPayload:
      type: object
      additionalProperties: false
      required:
      - articles
      - meta
      properties:
        articles:
          type: array
          items:
            "$ref": "#/components/schemas/ArticleListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    MeetingGuideListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    MeetingGuidesPayload:
      type: object
      additionalProperties: false
      required:
      - meetingGuides
      - meta
      properties:
        meetingGuides:
          type: array
          items:
            "$ref": "#/components/schemas/MeetingGuideListItem"
          maxItems: 0
          description: Always empty — both of source's fallback associations are absent
            on this tree. The real feature lives in the separately-landed `meeting_guides`
            row.
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    CertificateListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    CertificatesPayload:
      type: object
      additionalProperties: false
      required:
      - certificates
      - meta
      properties:
        certificates:
          type: array
          items:
            "$ref": "#/components/schemas/CertificateListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    QaQuestionListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - summary
      - authorName
      - answerCount
      - views
      - createdAt
      properties:
        id:
          type: integer
        summary:
          type:
          - string
          - 'null'
        authorName:
          type:
          - string
          - 'null'
        answerCount:
          type: integer
        views:
          type:
          - integer
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    QaPayload:
      type: object
      additionalProperties: false
      required:
      - qaQuestions
      - meta
      properties:
        qaQuestions:
          type: array
          items:
            "$ref": "#/components/schemas/QaQuestionListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    ModeratablePostListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - body
      - authorName
      - topicTitle
      - forumName
      - createdAt
      properties:
        id:
          type: integer
        body:
          type:
          - string
          - 'null'
        authorName:
          type:
          - string
          - 'null'
        topicTitle:
          type:
          - string
          - 'null'
        forumName:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ModerateContentPayload:
      type: object
      additionalProperties: false
      required:
      - posts
      - meta
      properties:
        posts:
          type: array
          items:
            "$ref": "#/components/schemas/ModeratablePostListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    IntegrationListItem:
      type: object
      additionalProperties: false
      required:
      - code
      - name
      - description
      - enabled
      properties:
        code:
          type: string
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        enabled:
          type: boolean
          description: A blanket rescue around the org feature-flag lookup silently
            reports every integration as disabled on any error, rather than surfacing
            the failure.
    IntegrationsPayload:
      type: object
      additionalProperties: false
      required:
      - integrations
      properties:
        integrations:
          type: array
          items:
            "$ref": "#/components/schemas/IntegrationListItem"
    ThreeSixtySurveyListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - status
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - draft
          - active
          - closed
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ThreeSixtySurveysPayload:
      type: object
      additionalProperties: false
      required:
      - threeSixtySurveys
      - meta
      properties:
        threeSixtySurveys:
          type: array
          items:
            "$ref": "#/components/schemas/ThreeSixtySurveyListItem"
        meta:
          "$ref": "#/components/schemas/EngagementPaginationMeta"
    EngagementSectionReadPayload:
      oneOf:
      - "$ref": "#/components/schemas/SurveysPayload"
      - "$ref": "#/components/schemas/AnnouncementsPayload"
      - "$ref": "#/components/schemas/CampaignsPayload"
      - "$ref": "#/components/schemas/ResourcesPayload"
      - "$ref": "#/components/schemas/EventsPayload"
      - "$ref": "#/components/schemas/ForumsPayload"
      - "$ref": "#/components/schemas/ArticlesPayload"
      - "$ref": "#/components/schemas/MeetingGuidesPayload"
      - "$ref": "#/components/schemas/CertificatesPayload"
      - "$ref": "#/components/schemas/QaPayload"
      - "$ref": "#/components/schemas/ModerateContentPayload"
      - "$ref": "#/components/schemas/IntegrationsPayload"
      - "$ref": "#/components/schemas/ThreeSixtySurveysPayload"
    EngagementSectionItemReadPayload:
      oneOf:
      - "$ref": "#/components/schemas/SurveyDetailPayload"
      - "$ref": "#/components/schemas/AnnouncementDetailPayload"
      - "$ref": "#/components/schemas/SurveysPayload"
      - "$ref": "#/components/schemas/AnnouncementsPayload"
      - "$ref": "#/components/schemas/CampaignsPayload"
      - "$ref": "#/components/schemas/ResourcesPayload"
      - "$ref": "#/components/schemas/EventsPayload"
      - "$ref": "#/components/schemas/ForumsPayload"
      - "$ref": "#/components/schemas/ArticlesPayload"
      - "$ref": "#/components/schemas/MeetingGuidesPayload"
      - "$ref": "#/components/schemas/CertificatesPayload"
      - "$ref": "#/components/schemas/QaPayload"
      - "$ref": "#/components/schemas/ModerateContentPayload"
      - "$ref": "#/components/schemas/IntegrationsPayload"
      - "$ref": "#/components/schemas/ThreeSixtySurveysPayload"
    EngagementErrorResponse:
      type: object
      additionalProperties: false
      required:
      - error
      properties:
        error:
          type: string
    EngagementValidationErrorResponse:
      type: object
      additionalProperties: false
      required:
      - error
      - details
      properties:
        error:
          type: string
        details:
          type: object
          additionalProperties: true
    GoalSettingIntentQuestion:
      type: object
      description: The role question currently attached as the mentee goal-setting
        intent question.
      required:
      - id
      - question_text
      - question_type
      - question_choices
      - question_choices_count
      - profile_question_id
      additionalProperties: false
      properties:
        id:
          type: integer
        question_text:
          type: string
        question_type:
          type: integer
          description: Raw AbstractQuestion::Type code — this codebase defines no
            wire enum for it.
        question_choices:
          type: array
          items:
            type: string
        question_choices_count:
          type: integer
        profile_question_id:
          type: integer
    GoalSettingAdditionalQuestion:
      type: object
      description: A secondary or additional question attached to the mentee goal-setting
        configuration.
      required:
      - id
      - question_text
      - question_type
      - question_choices
      - profile_question_id
      - position
      - is_secondary
      - can_be_secondary
      additionalProperties: false
      properties:
        id:
          type: integer
        question_text:
          type: string
        question_type:
          type: integer
          description: Raw AbstractQuestion::Type code — this codebase defines no
            wire enum for it.
        question_choices:
          type: array
          items:
            type: string
        profile_question_id:
          type: integer
        position:
          type: integer
        is_secondary:
          type: boolean
        can_be_secondary:
          type: boolean
    GoalSettingAttachableQuestion:
      type: object
      description: |
        A role question available to attach as an intent or additional question.
        `section_name`, `parent_question_text`, and `is_restricted_for_ai` are omitted (not
        null) when the source data has nothing to report — `GoalConfiguration::ListingService`
        builds this hash with `.compact`.
      required:
      - id
      - profile_question_id
      - question_text
      - question_type
      - is_conditional
      - is_parent_available
      additionalProperties: false
      properties:
        id:
          type: integer
        profile_question_id:
          type: integer
        section_name:
          type: string
        question_text:
          type: string
        question_type:
          type: integer
          description: Raw AbstractQuestion::Type code — this codebase defines no
            wire enum for it.
        is_conditional:
          type: boolean
        is_parent_available:
          type: boolean
        parent_question_text:
          type: string
        is_restricted_for_ai:
          type: boolean
          description: Present only when AI gating is active for the program.
    GoalSettingConfiguration:
      type: object
      required:
      - id
      - status
      - is_editable
      - allow_update
      - ai_for_summary_enabled
      - intent_question
      - additional_questions
      additionalProperties: false
      properties:
        id:
          type: integer
        status:
          type: string
          enum:
          - draft
          - active
          - archived
          - update_in_progress
        is_editable:
          type: boolean
        allow_update:
          type: boolean
        ai_for_summary_enabled:
          type: boolean
        intent_question:
          anyOf:
          - "$ref": "#/components/schemas/GoalSettingIntentQuestion"
          - type: 'null'
        additional_questions:
          type: array
          items:
            "$ref": "#/components/schemas/GoalSettingAdditionalQuestion"
    GoalSettingConfigurationResponse:
      type: object
      required:
      - configuration
      additionalProperties: false
      properties:
        configuration:
          "$ref": "#/components/schemas/GoalSettingConfiguration"
    MatchReportBareError:
      type: object
      description: 'This controller''s own guards render a bare `{error: "<string>"}`,
        not the standard envelope.'
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    MatchReportShow:
      type: object
      required:
      - currentStatus
      - menteeActions
      - mentorDistribution
      additionalProperties: false
      properties:
        currentStatus:
          "$ref": "#/components/schemas/MatchReportCurrentStatus"
        menteeActions:
          "$ref": "#/components/schemas/MatchReportMenteeActions"
        mentorDistribution:
          "$ref": "#/components/schemas/MatchReportMentorDistribution"
    MatchReportCurrentStatus:
      type: object
      required:
      - programType
      - labels
      - metrics
      - dateRange
      additionalProperties: false
      properties:
        programType:
          type: string
          enum:
          - self_match
          - admin_match
          - flash
          - unknown
        labels:
          type: object
          required:
          - first
          - second
          - third
          additionalProperties: false
          properties:
            first:
              type: string
            second:
              type: string
            third:
              type: string
        metrics:
          type: object
          description: Empty when `programType` is `unknown`; otherwise always all
            three keys together.
          additionalProperties: false
          properties:
            first:
              "$ref": "#/components/schemas/MatchReportMetricEntry"
            second:
              "$ref": "#/components/schemas/MatchReportMetricEntry"
            third:
              "$ref": "#/components/schemas/MatchReportMetricEntry"
        dateRange:
          type: object
          required:
          - start
          - end
          - preset
          additionalProperties: false
          properties:
            start:
              type:
              - string
              - 'null'
              format: date-time
            end:
              type:
              - string
              - 'null'
              format: date-time
            preset:
              type:
              - string
              - 'null'
    MatchReportMetricEntry:
      type: object
      required:
      - percentage
      - positiveCount
      - negativeCount
      additionalProperties: false
      properties:
        percentage:
          type: number
        positiveCount:
          type: integer
        negativeCount:
          type: integer
    MatchReportMenteeActions:
      type: object
      required:
      - visible
      - appliedFilters
      - searchKeywords
      additionalProperties: false
      properties:
        visible:
          type: boolean
        appliedFilters:
          type: array
          items:
            type: object
            required:
            - question
            - count
            additionalProperties: false
            properties:
              question:
                type: string
              count:
                type: integer
        searchKeywords:
          type: array
          items:
            type: object
            required:
            - keyword
            - count
            additionalProperties: false
            properties:
              keyword:
                type: string
              count:
                type: integer
    MatchReportMentorDistribution:
      type: object
      required:
      - menteeCount
      - mentorCount
      - matchConfigs
      additionalProperties: false
      properties:
        menteeCount:
          type: integer
        mentorCount:
          type: integer
        matchConfigs:
          type: array
          items:
            type: object
            required:
            - id
            - mentorQuestion
            - studentQuestion
            - topDiscrepancies
            - isSetMatching
            - isIfThen
            additionalProperties: false
            properties:
              id:
                type: integer
              mentorQuestion:
                type:
                - string
                - 'null'
              studentQuestion:
                type:
                - string
                - 'null'
              topDiscrepancies:
                type: array
                items:
                  "$ref": "#/components/schemas/MatchReportCachedDiscrepancyRow"
              isSetMatching:
                type: boolean
              isIfThen:
                type: boolean
    MatchReportCachedDiscrepancyRow:
      type: object
      required:
      - studentAnswerChoice
      - studentNeedCount
      - mentorOfferCount
      - gap
      additionalProperties: false
      properties:
        studentAnswerChoice:
          type:
          - string
          - 'null'
        studentNeedCount:
          type:
          - integer
          - 'null'
        mentorOfferCount:
          type:
          - integer
          - 'null'
        gap:
          type:
          - integer
          - 'null'
    MatchReportDiscrepancy:
      type: object
      required:
      - matchConfigId
      - discrepancies
      - totalCategories
      additionalProperties: false
      properties:
        matchConfigId:
          type: integer
        discrepancies:
          type: array
          items:
            "$ref": "#/components/schemas/MatchReportDiscrepancyRow"
        totalCategories:
          type: integer
    MatchReportDiscrepancyRow:
      type: object
      required:
      - studentAnswerChoice
      - studentNeedCount
      - mentorOfferCount
      - gap
      - mentorAnswerChoices
      additionalProperties: false
      properties:
        studentAnswerChoice:
          type: string
        studentNeedCount:
          type: integer
        mentorOfferCount:
          type: integer
        gap:
          type: integer
        mentorAnswerChoices:
          description: Choice text → offer count. Present only for a set-matching-type
            match config.
          type:
          - object
          - 'null'
          additionalProperties:
            type: integer
    MatchingPaginationMeta:
      type: object
      additionalProperties: false
      required:
      - total
      - page
      - perPage
      - totalPages
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    MatchingUserRef:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      properties:
        id:
          type: integer
        name:
          type:
          - string
          - 'null'
    MatchingSectionCount:
      type: object
      additionalProperties: false
      required:
      - total
      properties:
        total:
          type: integer
          description: Independently rescue-wrapped to 0 on any error — a zero here
            can mean zero or broken.
    MatchingSummary:
      type: object
      additionalProperties: false
      required:
      - connections
      - mentorRequests
      - mentorOffers
      - meetingRequests
      - projectRequests
      - recommendations
      - bulkMatches
      - auditLogs
      properties:
        connections:
          "$ref": "#/components/schemas/MatchingSectionCount"
        mentorRequests:
          "$ref": "#/components/schemas/MatchingSectionCount"
        mentorOffers:
          "$ref": "#/components/schemas/MatchingSectionCount"
        meetingRequests:
          "$ref": "#/components/schemas/MatchingSectionCount"
        projectRequests:
          "$ref": "#/components/schemas/MatchingSectionCount"
        recommendations:
          "$ref": "#/components/schemas/MatchingSectionCount"
        bulkMatches:
          "$ref": "#/components/schemas/MatchingSectionCount"
        auditLogs:
          "$ref": "#/components/schemas/MatchingSectionCount"
    MatchingConnectionSummary:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - status
      - mentors
      - mentees
      - startDate
      - expiryDate
      - createdAt
      properties:
        id:
          type: integer
        name:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - active
          - inactive
          - closed
          - drafted
          - pending
          - proposed
          - rejected
          - withdrawn
          - unknown
        mentors:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingUserRef"
        mentees:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingUserRef"
        startDate:
          type:
          - string
          - 'null'
          format: date-time
        expiryDate:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ConnectionsPayload:
      type: object
      additionalProperties: false
      required:
      - connections
      - meta
      properties:
        connections:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingConnectionSummary"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    MatchingConnectionDetail:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - status
      - mentors
      - mentees
      - startDate
      - expiryDate
      - createdAt
      - notes
      - taskCount
      - meetingCount
      - messageCount
      properties:
        id:
          type: integer
        name:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - active
          - inactive
          - closed
          - drafted
          - pending
          - proposed
          - rejected
          - withdrawn
          - unknown
        mentors:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingUserRef"
        mentees:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingUserRef"
        startDate:
          type:
          - string
          - 'null'
          format: date-time
        expiryDate:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        notes:
          type:
          - string
          - 'null'
        taskCount:
          type: integer
        meetingCount:
          type: integer
        messageCount:
          type: integer
          description: Chat count for the connection; 0 on any lookup failure.
    ConnectionDetailPayload:
      type: object
      additionalProperties: false
      required:
      - connection
      properties:
        connection:
          "$ref": "#/components/schemas/MatchingConnectionDetail"
    MentorRequestItem:
      type: object
      additionalProperties: false
      required:
      - id
      - mentee
      - mentor
      - status
      - message
      - createdAt
      properties:
        id:
          type: integer
        mentee:
          "$ref": "#/components/schemas/MatchingUserRef"
        mentor:
          "$ref": "#/components/schemas/MatchingUserRef"
        status:
          type: string
          enum:
          - pending
          - accepted
          - rejected
          - withdrawn
          - closed
          - unknown
        message:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    MentorRequestsPayload:
      type: object
      additionalProperties: false
      required:
      - mentorRequests
      - meta
      properties:
        mentorRequests:
          type: array
          items:
            "$ref": "#/components/schemas/MentorRequestItem"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    MentorOfferItem:
      type: object
      additionalProperties: false
      required:
      - id
      - mentor
      - mentee
      - status
      - message
      - createdAt
      properties:
        id:
          type: integer
        mentor:
          "$ref": "#/components/schemas/MatchingUserRef"
        mentee:
          "$ref": "#/components/schemas/MatchingUserRef"
        status:
          type: string
        message:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    MentorOffersPayload:
      type: object
      additionalProperties: false
      required:
      - mentorOffers
      - meta
      properties:
        mentorOffers:
          type: array
          items:
            "$ref": "#/components/schemas/MentorOfferItem"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    MeetingRequestItem:
      type: object
      additionalProperties: false
      required:
      - id
      - senderName
      - receiverName
      - status
      - topic
      - message
      - responseText
      - closedAt
      - closedByName
      - createdAt
      properties:
        id:
          type: integer
        senderName:
          type:
          - string
          - 'null'
        receiverName:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - pending
          - accepted
          - rejected
          - withdrawn
          - closed
          - unknown
        topic:
          type:
          - string
          - 'null'
        message:
          type:
          - string
          - 'null'
        responseText:
          type:
          - string
          - 'null'
        closedAt:
          type:
          - string
          - 'null'
          format: date-time
        closedByName:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    MeetingRequestStatusCounts:
      type: object
      additionalProperties: false
      required:
      - pending
      - accepted
      - rejected
      - withdrawn
      - closed
      properties:
        pending:
          type: integer
        accepted:
          type: integer
        rejected:
          type: integer
        withdrawn:
          type: integer
        closed:
          type: integer
    MeetingRequestsMeta:
      type: object
      additionalProperties: false
      required:
      - total
      - page
      - perPage
      - totalPages
      - statusCounts
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
        statusCounts:
          "$ref": "#/components/schemas/MeetingRequestStatusCounts"
    MeetingRequestsPayload:
      type: object
      additionalProperties: false
      required:
      - meetingRequests
      - meta
      properties:
        meetingRequests:
          type: array
          items:
            "$ref": "#/components/schemas/MeetingRequestItem"
        meta:
          "$ref": "#/components/schemas/MeetingRequestsMeta"
    ProjectRequestItem:
      type: object
      additionalProperties: false
      required:
      - id
      - senderName
      - groupName
      - status
      - message
      - createdAt
      properties:
        id:
          type: integer
        senderName:
          type:
          - string
          - 'null'
        groupName:
          type:
          - string
          - 'null'
        status:
          type: string
        message:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    ProjectRequestsPayload:
      type: object
      additionalProperties: false
      required:
      - projectRequests
      - meta
      properties:
        projectRequests:
          type: array
          items:
            "$ref": "#/components/schemas/ProjectRequestItem"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    RecommendationItem:
      type: object
      additionalProperties: false
      required:
      - id
      - menteeName
      - mentorName
      - status
      - createdAt
      properties:
        id:
          type: integer
        menteeName:
          type:
          - string
          - 'null'
        mentorName:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - pending
          - accepted
          - rejected
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    RecommendationsPayload:
      type: object
      additionalProperties: false
      required:
      - recommendations
      - meta
      properties:
        recommendations:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendationItem"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    BulkMatchListItem:
      type: object
      additionalProperties: false
      required:
      - id
      - status
      - pairCount
      - createdAt
      properties:
        id:
          type: integer
        status:
          type: string
          enum:
          - drafted
          - in_progress
          - completed
          - failed
          - unknown
        pairCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    BulkMatchesPayload:
      type: object
      additionalProperties: false
      required:
      - bulkMatches
      - meta
      properties:
        bulkMatches:
          type: array
          items:
            "$ref": "#/components/schemas/BulkMatchListItem"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    AuditLogItem:
      type: object
      additionalProperties: false
      required:
      - id
      - userName
      - groupName
      - mentorNames
      - menteeNames
      - reason
      - createdAt
      properties:
        id:
          type: integer
        userName:
          type:
          - string
          - 'null'
        groupName:
          type:
          - string
          - 'null'
        mentorNames:
          type: array
          items:
            type: string
        menteeNames:
          type: array
          items:
            type: string
        reason:
          type:
          - string
          - 'null'
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    AuditLogsPayload:
      type: object
      additionalProperties: false
      required:
      - auditLogs
      - meta
      properties:
        auditLogs:
          type: array
          items:
            "$ref": "#/components/schemas/AuditLogItem"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    MatchingConnectionQuestion:
      type: object
      additionalProperties: false
      required:
      - id
      - text
      - questionType
      - required
      - position
      properties:
        id:
          type: integer
        text:
          type:
          - string
          - 'null'
        questionType:
          type:
          - string
          - 'null'
        required:
          type: boolean
        position:
          type:
          - integer
          - 'null'
    ConnectionQuestionsPayload:
      type: object
      additionalProperties: false
      required:
      - connectionQuestions
      properties:
        connectionQuestions:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingConnectionQuestion"
    RequestFormInstructions:
      type: object
      additionalProperties: false
      required:
      - content
      - updatedAt
      properties:
        content:
          type:
          - string
          - 'null'
        updatedAt:
          type:
          - string
          - 'null'
          format: date-time
    RequestFormPayload:
      type: object
      additionalProperties: false
      required:
      - instructions
      properties:
        instructions:
          "$ref": "#/components/schemas/RequestFormInstructions"
    MatchConfigRule:
      type: object
      additionalProperties: false
      required:
      - id
      - weight
      - threshold
      - operator
      - showMatchLabel
      - prefix
      - matchingType
      - mentorQuestionId
      - studentQuestionId
      properties:
        id:
          type: integer
        weight:
          type:
          - number
          - 'null'
        threshold:
          type:
          - number
          - 'null'
        operator:
          type:
          - string
          - 'null'
        showMatchLabel:
          type:
          - boolean
          - 'null'
        prefix:
          type:
          - string
          - 'null'
        matchingType:
          type:
          - string
          - 'null'
        mentorQuestionId:
          type:
          - integer
          - 'null'
        studentQuestionId:
          type:
          - integer
          - 'null'
    MatchConfig:
      type: object
      additionalProperties: false
      required:
      - matchingEnabled
      - matchingByAdminAlone
      - matchingByMenteeAlone
      - selfMatch
      - maxConnectionsForMentee
      - mentorOfferNeedsAcceptance
      - calendarEnabled
      - ongoingMentoringEnabled
      - matchConfigs
      properties:
        matchingEnabled:
          type: boolean
        matchingByAdminAlone:
          type: boolean
        matchingByMenteeAlone:
          type: boolean
        selfMatch:
          type: boolean
        maxConnectionsForMentee:
          type:
          - integer
          - 'null'
        mentorOfferNeedsAcceptance:
          type: boolean
        calendarEnabled:
          type: boolean
        ongoingMentoringEnabled:
          type: boolean
        matchConfigs:
          type: array
          items:
            "$ref": "#/components/schemas/MatchConfigRule"
    MatchConfigPayload:
      type: object
      additionalProperties: false
      required:
      - matchConfig
      properties:
        matchConfig:
          "$ref": "#/components/schemas/MatchConfig"
    MatchingMentoringModelSummary:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      - isDefault
      - milestoneCount
      - taskCount
      - goalCount
      - createdAt
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        isDefault:
          type: boolean
        milestoneCount:
          type: integer
        taskCount:
          type: integer
        goalCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    MentoringModelsPayload:
      type: object
      additionalProperties: false
      required:
      - mentoringModels
      - meta
      properties:
        mentoringModels:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingMentoringModelSummary"
        meta:
          "$ref": "#/components/schemas/MatchingPaginationMeta"
    MentoringModelMilestone:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      - position
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        position:
          type:
          - integer
          - 'null'
    MentoringModelTask:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      - duration
      - position
      - milestoneTemplateId
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        duration:
          type:
          - integer
          - 'null'
        position:
          type:
          - integer
          - 'null'
        milestoneTemplateId:
          type:
          - integer
          - 'null'
    MentoringModelGoal:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
    MatchingMentoringModelDetail:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      - isDefault
      - milestoneCount
      - taskCount
      - goalCount
      - createdAt
      - mentoringPeriod
      - goalProgressType
      - milestones
      - tasks
      - goals
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        isDefault:
          type: boolean
        milestoneCount:
          type: integer
        taskCount:
          type: integer
        goalCount:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        mentoringPeriod:
          type:
          - integer
          - 'null'
        goalProgressType:
          type: string
          enum:
          - auto
          - manual
        milestones:
          type: array
          items:
            "$ref": "#/components/schemas/MentoringModelMilestone"
        tasks:
          type: array
          items:
            "$ref": "#/components/schemas/MentoringModelTask"
        goals:
          type: array
          items:
            "$ref": "#/components/schemas/MentoringModelGoal"
    MentoringModelDetailPayload:
      type: object
      additionalProperties: false
      required:
      - mentoringModel
      properties:
        mentoringModel:
          "$ref": "#/components/schemas/MatchingMentoringModelDetail"
    MatchingClosureReason:
      type: object
      additionalProperties: false
      required:
      - id
      - reason
      - isDefault
      - isCompleted
      properties:
        id:
          type: integer
        reason:
          type:
          - string
          - 'null'
        isDefault:
          type: boolean
        isCompleted:
          type: boolean
    ConnectionSettingsOneTimeMentoring:
      type: object
      additionalProperties: false
      required:
      - feedbackSurveyDelayNotTimeBound
      - connectionExtensionPeriod
      properties:
        feedbackSurveyDelayNotTimeBound:
          type:
          - integer
          - 'null'
        connectionExtensionPeriod:
          type:
          - integer
          - 'null'
    ConnectionSettings:
      type: object
      additionalProperties: false
      required:
      - allowOneToManyMentoring
      - mentoringPeriod
      - mentoringPeriodUnit
      - allowUsersToLeaveConnection
      - allowToChangeConnectionExpiryDate
      - trackMeetingAttendance
      - feedbackEnabled
      - inactivityTrackingPeriod
      - autoTerminate
      - autoTerminateReasonId
      - allowUsersToMarkConnectionPublic
      - allowPrivateJournals
      - hybridTemplatesEnabled
      - nameTemplate
      - autoAddNewGroupMembersToMeeting
      - autoApproveMentoringRequests
      - recommendedMeetingFrequency
      - closureReasons
      - ongoingMentoringEnabled
      - calendarEnabled
      properties:
        allowOneToManyMentoring:
          type: boolean
        mentoringPeriod:
          type:
          - integer
          - 'null'
        mentoringPeriodUnit:
          type:
          - string
          - 'null'
        allowUsersToLeaveConnection:
          type: boolean
        allowToChangeConnectionExpiryDate:
          type: boolean
        trackMeetingAttendance:
          type: boolean
        feedbackEnabled:
          type: boolean
        inactivityTrackingPeriod:
          type:
          - integer
          - 'null'
        autoTerminate:
          type: boolean
        autoTerminateReasonId:
          type:
          - integer
          - 'null'
        allowUsersToMarkConnectionPublic:
          type: boolean
        allowPrivateJournals:
          type: boolean
        hybridTemplatesEnabled:
          type: boolean
        nameTemplate:
          type:
          - string
          - 'null'
        autoAddNewGroupMembersToMeeting:
          type: boolean
        autoApproveMentoringRequests:
          type: boolean
        recommendedMeetingFrequency:
          type:
          - integer
          - 'null'
        closureReasons:
          type: array
          items:
            "$ref": "#/components/schemas/MatchingClosureReason"
        oneTimeMentoring:
          "$ref": "#/components/schemas/ConnectionSettingsOneTimeMentoring"
        ongoingMentoringEnabled:
          type: boolean
        calendarEnabled:
          type: boolean
    ConnectionSettingsPayload:
      type: object
      additionalProperties: false
      required:
      - connectionSettings
      properties:
        connectionSettings:
          "$ref": "#/components/schemas/ConnectionSettings"
    MatchingSettings:
      type: object
      additionalProperties: false
      required:
      - ongoingMentoringEnabled
      - calendarEnabled
      - allowEndUsersToSeeMatchScores
      - anonymousMatching
      - allowMentoringModeChange
      - mentorRequestStyle
      - mentorOfferEnabled
      - maxPendingRequestsForMentee
      - needsMentoringRequestReminder
      - mentoringRequestReminderDuration
      - mentorRequestExpirationDays
      - defaultMaxConnectionsLimit
      - canIncreaseConnectionLimit
      - canDecreaseConnectionLimit
      - maxPendingRequestsForMentor
      - maxConnectionsForMentee
      - allowMentoringRequests
      - allowMentoringRequestsMessage
      - allowMenteeWithdrawMentorRequest
      - allowPreferenceMentorRequest
      - minPreferredMentors
      - mentorOfferNeedsAcceptance
      - allowNonMatchConnection
      - zeroMatchScoreMessage
      - needsMeetingRequestReminder
      - meetingRequestReminderDuration
      - meetingRequestAutoExpirationDays
      properties:
        ongoingMentoringEnabled:
          type: boolean
        calendarEnabled:
          type: boolean
        allowEndUsersToSeeMatchScores:
          type: boolean
        anonymousMatching:
          type: boolean
        allowMentoringModeChange:
          type: boolean
        mentorRequestStyle:
          type:
          - string
          - 'null'
          enum:
          - mentee_to_mentor
          - mentee_to_admin
          - none
          - null
        mentorOfferEnabled:
          type: boolean
        maxPendingRequestsForMentee:
          type:
          - integer
          - 'null'
        needsMentoringRequestReminder:
          type: boolean
        mentoringRequestReminderDuration:
          type:
          - integer
          - 'null'
        mentorRequestExpirationDays:
          type:
          - integer
          - 'null'
        defaultMaxConnectionsLimit:
          type:
          - integer
          - 'null'
        canIncreaseConnectionLimit:
          type: boolean
        canDecreaseConnectionLimit:
          type: boolean
        maxPendingRequestsForMentor:
          type:
          - integer
          - 'null'
        maxConnectionsForMentee:
          type:
          - integer
          - 'null'
        allowMentoringRequests:
          type: boolean
        allowMentoringRequestsMessage:
          type:
          - string
          - 'null'
        allowMenteeWithdrawMentorRequest:
          type: boolean
        allowPreferenceMentorRequest:
          type: boolean
        minPreferredMentors:
          type:
          - integer
          - 'null'
        mentorOfferNeedsAcceptance:
          type: boolean
        allowNonMatchConnection:
          type: boolean
        zeroMatchScoreMessage:
          type:
          - string
          - 'null'
        needsMeetingRequestReminder:
          type: boolean
        meetingRequestReminderDuration:
          type:
          - integer
          - 'null'
        meetingRequestAutoExpirationDays:
          type:
          - integer
          - 'null'
        allowMentorToConfigureAvailabilitySlots:
          type: boolean
        allowMentorToDescribeMeetingPreference:
          type: boolean
        slotTimeInMinutes:
          type:
          - integer
          - 'null'
        allowCreateMeetingForMentor:
          type: boolean
        flashToOngoingConnectionType:
          type:
          - string
          - 'null'
          enum:
          - create_ongoing_connection
          - send_mentoring_request
          - null
        advanceBookingTime:
          type:
          - integer
          - 'null'
        maxMeetingsForMentee:
          type:
          - integer
          - 'null'
        maxMeetingsForMenteeTimePeriod:
          type:
          - integer
          - 'null'
        maxPendingMeetingRequestsForMentee:
          type:
          - integer
          - 'null'
    MatchingSettingsPayload:
      type: object
      additionalProperties: false
      required:
      - matching
      properties:
        matching:
          "$ref": "#/components/schemas/MatchingSettings"
    MatchingSectionErrorResponse:
      type: object
      additionalProperties: false
      required:
      - error
      properties:
        error:
          type: string
    MatchingSectionValidationErrorResponse:
      type: object
      additionalProperties: false
      required:
      - error
      properties:
        error:
          type: string
        details:
          type: object
          additionalProperties: true
    MatchingSectionReadPayload:
      oneOf:
      - "$ref": "#/components/schemas/ConnectionsPayload"
      - "$ref": "#/components/schemas/MentorRequestsPayload"
      - "$ref": "#/components/schemas/MentorOffersPayload"
      - "$ref": "#/components/schemas/MeetingRequestsPayload"
      - "$ref": "#/components/schemas/ProjectRequestsPayload"
      - "$ref": "#/components/schemas/RecommendationsPayload"
      - "$ref": "#/components/schemas/BulkMatchesPayload"
      - "$ref": "#/components/schemas/AuditLogsPayload"
      - "$ref": "#/components/schemas/ConnectionQuestionsPayload"
      - "$ref": "#/components/schemas/RequestFormPayload"
      - "$ref": "#/components/schemas/MatchConfigPayload"
      - "$ref": "#/components/schemas/MentoringModelsPayload"
      - "$ref": "#/components/schemas/ConnectionSettingsPayload"
      - "$ref": "#/components/schemas/MatchingSettingsPayload"
    MatchingSectionItemReadPayload:
      oneOf:
      - "$ref": "#/components/schemas/ConnectionDetailPayload"
      - "$ref": "#/components/schemas/MentoringModelDetailPayload"
      - "$ref": "#/components/schemas/ConnectionsPayload"
      - "$ref": "#/components/schemas/MentorRequestsPayload"
      - "$ref": "#/components/schemas/MentorOffersPayload"
      - "$ref": "#/components/schemas/MeetingRequestsPayload"
      - "$ref": "#/components/schemas/ProjectRequestsPayload"
      - "$ref": "#/components/schemas/RecommendationsPayload"
      - "$ref": "#/components/schemas/BulkMatchesPayload"
      - "$ref": "#/components/schemas/AuditLogsPayload"
      - "$ref": "#/components/schemas/ConnectionQuestionsPayload"
      - "$ref": "#/components/schemas/RequestFormPayload"
      - "$ref": "#/components/schemas/MatchConfigPayload"
      - "$ref": "#/components/schemas/MentoringModelsPayload"
      - "$ref": "#/components/schemas/ConnectionSettingsPayload"
      - "$ref": "#/components/schemas/MatchingSettingsPayload"
    MatchingConfigUpdateBody:
      type: object
      additionalProperties: false
      properties:
        maxConnectionsForMentee:
          type: integer
        mentorOfferNeedsAcceptance:
          type: boolean
        content:
          type: string
        allowUsersToLeaveConnection:
          type: boolean
        allowToChangeConnectionExpiryDate:
          type: boolean
        feedbackEnabled:
          type: boolean
        trackMeetingAttendance:
          type: boolean
        allowUsersToMarkConnectionPublic:
          type: boolean
        allowPrivateJournals:
          type: boolean
        hybridTemplatesEnabled:
          type: boolean
        mentoringPeriod:
          type: integer
        inactivityTrackingPeriod:
          type: integer
        mentoringPeriodUnit:
          type: string
        autoTerminate:
          type: boolean
        autoTerminateReasonId:
          type: integer
        autoAddNewGroupMembersToMeeting:
          type: boolean
        autoApproveMentoringRequests:
          type: boolean
        recommendedMeetingFrequency:
          type: integer
        nameTemplate:
          type: string
        feedbackSurveyDelayNotTimeBound:
          type: integer
        connectionExtensionPeriod:
          type: integer
        allowMentoringRequestsMessage:
          type: string
        zeroMatchScoreMessage:
          type: string
        allowMentoringRequests:
          type: boolean
        allowEndUsersToSeeMatchScores:
          type: boolean
        anonymousMatching:
          type: boolean
        allowMentoringModeChange:
          type: boolean
        needsMentoringRequestReminder:
          type: boolean
        allowMenteeWithdrawMentorRequest:
          type: boolean
        allowPreferenceMentorRequest:
          type: boolean
        allowNonMatchConnection:
          type: boolean
        needsMeetingRequestReminder:
          type: boolean
        defaultMaxConnectionsLimit:
          type: integer
        maxPendingRequestsForMentee:
          type: integer
        mentoringRequestReminderDuration:
          type: integer
        mentorRequestExpirationDays:
          type: integer
        maxPendingRequestsForMentor:
          type: integer
        minPreferredMentors:
          type: integer
        meetingRequestReminderDuration:
          type: integer
        meetingRequestAutoExpirationDays:
          type: integer
        mentorRequestStyle:
          type: string
          enum:
          - mentee_to_mentor
          - mentee_to_admin
          - none
        canIncreaseConnectionLimit:
          type: boolean
        canDecreaseConnectionLimit:
          type: boolean
        slotTimeInMinutes:
          type: integer
        advanceBookingTime:
          type: integer
        maxMeetingsForMentee:
          type: integer
        maxMeetingsForMenteeTimePeriod:
          type: integer
        maxPendingMeetingRequestsForMentee:
          type: integer
        allowMentorToConfigureAvailabilitySlots:
          type: boolean
        allowMentorToDescribeMeetingPreference:
          type: boolean
        allowCreateMeetingForMentor:
          type: boolean
        flashToOngoingConnectionType:
          type: string
          enum:
          - create_ongoing_connection
          - send_mentoring_request
    MatchingUpdateSectionResponse:
      oneOf:
      - "$ref": "#/components/schemas/MatchConfigPayload"
      - "$ref": "#/components/schemas/RequestFormPayload"
      - "$ref": "#/components/schemas/ConnectionSettingsPayload"
      - "$ref": "#/components/schemas/MatchingSettingsPayload"
    MeetingGuidePackage:
      type: object
      required:
      - id
      - name
      - custom
      - categoriesCount
      - guidesCount
      - packageTypeLabel
      - categoryNames
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        custom:
          type: boolean
        categoriesCount:
          type: integer
        guidesCount:
          type: integer
        packageTypeLabel:
          type: string
          enum:
          - Ongoing
          - Flash
          - Unknown
        categoryNames:
          type: array
          items:
            type: string
          description: Names of assigned categories.
    MeetingGuidePackageManage:
      type: object
      description: The `index` package shape, plus `manage`'s own deletable/categories
        breakdown.
      required:
      - id
      - name
      - custom
      - categoriesCount
      - guidesCount
      - packageTypeLabel
      - categoryNames
      - deletable
      - categories
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        custom:
          type: boolean
        categoriesCount:
          type: integer
        guidesCount:
          type: integer
        packageTypeLabel:
          type: string
          enum:
          - Ongoing
          - Flash
          - Unknown
        categoryNames:
          type: array
          items:
            type: string
        deletable:
          type: boolean
        categories:
          type: array
          items:
            type: object
            required:
            - id
            - label
            - guidesCount
            - assigned
            additionalProperties: false
            properties:
              id:
                type: integer
              label:
                type: string
              guidesCount:
                type: integer
              assigned:
                type: boolean
    MeetingGuideFailure:
      type: object
      description: |
        `error` alone for a guard rejection; `success: false` alongside it when the same message
        came from a rescued exception instead.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
        success:
          type: boolean
    MentoringCalendarReportBareError:
      type: object
      description: |
        `Programs::BaseController#resolve_program`'s "not found" branch renders a bare
        `{error: "<string>"}`, not this API's `{error: {code, message, details}}` envelope.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    MentoringCalendarReportIndex:
      type: object
      required:
      - meetingOccurrences
      - totalMeetingTime
      - tabs
      - tabCounts
      - meta
      - dateRange
      additionalProperties: false
      properties:
        meetingOccurrences:
          type: array
          items:
            "$ref": "#/components/schemas/MentoringCalendarReportOccurrence"
        totalMeetingTime:
          type: number
          description: Hours, rounded to 1 decimal, across the tab-filtered occurrences.
        tabs:
          type: array
          items:
            type: object
            required:
            - key
            - label
            - active
            additionalProperties: false
            properties:
              key:
                type: string
                enum:
                - all
                - upcoming
                - overdue
                - completed
                - cancelled
              label:
                type: string
              active:
                type: boolean
        tabCounts:
          type: object
          required:
          - all
          - upcoming
          - overdue
          - completed
          - cancelled
          additionalProperties: false
          properties:
            all:
              type: integer
            upcoming:
              type: integer
            overdue:
              type: integer
            completed:
              type: integer
            cancelled:
              type: integer
        meta:
          "$ref": "#/components/schemas/MentoringCalendarReportMeta"
        dateRange:
          "$ref": "#/components/schemas/MentoringCalendarReportDateRange"
    MentoringCalendarReportOccurrence:
      type: object
      required:
      - id
      - meetingId
      - topic
      - startTime
      - endTime
      - durationHours
      - groupId
      - groupName
      - mentors
      - mentees
      - meetingMode
      - state
      - rsvpYes
      - rsvpNo
      - rsvpNoResponse
      additionalProperties: false
      properties:
        id:
          type: integer
        meetingId:
          type: integer
        topic:
          type:
          - string
          - 'null'
        startTime:
          type:
          - string
          - 'null'
          format: date-time
        endTime:
          type:
          - string
          - 'null'
          format: date-time
        durationHours:
          type: number
          description: Rounded to 2 decimals.
        groupId:
          type: integer
        groupName:
          type:
          - string
          - 'null'
        mentors:
          type: array
          items:
            "$ref": "#/components/schemas/MentoringCalendarReportPerson"
        mentees:
          type: array
          items:
            "$ref": "#/components/schemas/MentoringCalendarReportPerson"
        meetingMode:
          type:
          - string
          - 'null'
          description: Label from `MeetingModeCodeHelper::MEETING_MODE_NAME`; `null`
            for an uncovered mode code.
        state:
          type: string
          enum:
          - completed
          - cancelled
          - overdue
          - upcoming
        rsvpYes:
          type: array
          items:
            type: string
        rsvpNo:
          type: array
          items:
            type: string
        rsvpNoResponse:
          type: array
          items:
            type: string
    MentoringCalendarReportPerson:
      type: object
      required:
      - id
      - name
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
    MentoringCalendarReportMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    MentoringCalendarReportDateRange:
      type: object
      required:
      - startDate
      - endDate
      additionalProperties: false
      properties:
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
    MentoringTip:
      type: object
      description: Camel-cased — see the index operation's description.
      required:
      - id
      - message
      - enabled
      - roleNames
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        message:
          type: string
        enabled:
          type: boolean
        roleNames:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    MentoringTipRoleOption:
      type: object
      required:
      - name
      - label
      additionalProperties: false
      properties:
        name:
          type: string
        label:
          type: string
    OutcomesReportBareError:
      type: object
      description: |
        `Programs::BaseController#resolve_program`'s "not found" branch renders a bare
        `{error: "<string>"}`, not this API's `{error: {code, message, details}}` envelope.
        Declared as observed.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    OutcomesReportSeedData:
      type: object
      required:
      - presets
      - programCreatedAt
      - ongoingMentoringEnabled
      - mentoringRoles
      additionalProperties: false
      properties:
        presets:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportDatePreset"
        programCreatedAt:
          type: string
          format: date-time
        ongoingMentoringEnabled:
          type: boolean
        mentoringRoles:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportMentoringRole"
    OutcomesReportDatePreset:
      type: object
      required:
      - label
      - value
      - canEdit
      - selected
      - from
      - to
      additionalProperties: false
      properties:
        label:
          type: string
        value:
          type: string
          description: One of `DateRangePresets`' string constants, the fixed `OUTCOME_PRESETS`
            list.
          enum:
          - program_to_date
          - month_to_date
          - quarter_to_date
          - year_to_date
          - last_month
          - last_quarter
          - last_year
          - custom
        canEdit:
          type: boolean
          description: True only for the `custom` preset.
        selected:
          type: boolean
          description: True only for the `custom` preset (the default selection).
        from:
          type: string
          description: MM/DD/YYYY.
        to:
          type: string
          description: MM/DD/YYYY.
    OutcomesReportMentoringRole:
      type: object
      required:
      - id
      - name
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
    OutcomesReportRawRole:
      type: object
      additionalProperties: true
      description: |
        A raw `Role` ActiveRecord record, unfiltered. `Role` has no `as_json`/`serializable_hash`
        override, so `instance_values`-based serialization dumps every current `roles` table
        column verbatim (confirmed live: id, name, `description`/`eligibility_message`
        Globalize-translated text, program_id, timestamps, and ~18 admin-configuration
        booleans/settings unrelated to an outcomes report). Deliberately left open — see the
        `user_outcomes`/`connection_outcomes`/`meeting_outcomes` operation descriptions for why a
        closed enumeration here would be schema-coupled to an unrelated table and go stale on the
        next migration to `roles`.
    OutcomesReportRoleColorMapping:
      type: object
      additionalProperties:
        type: string
      description: |
        Hex color per mentoring role, keyed by the role's id AS A STRING (JSON object keys are
        always strings — `GraphColor.get_role_graph_color_mapping` builds this from integer role
        ids). Open rather than itemized since the key set is one entry per mentoring role in the
        program, not a fixed vocabulary.
    OutcomesReportEnabledStatusMapping:
      type: object
      additionalProperties:
        type: boolean
      description: |
        Graph-series visibility flags. Always carries a `users` key; one more key per mentoring
        role, keyed by the role's id as a string; `connection_outcomes`/`meeting_outcomes` (not
        `user_outcomes`) also carry a `total_connections_or_meetings` key. Open rather than
        itemized since the role-id keys aren't a fixed vocabulary.
    OutcomesReportGraphSeries:
      type: object
      required:
      - name
      - data
      - color
      - visibility
      additionalProperties: false
      properties:
        name:
          type: string
        data:
          type: array
          description: |
            Confirmed live in both shapes this endpoint can actually produce: a plain cumulative
            count per day (the ongoing/active-connections and every `user_outcomes` branch), OR a
            `[month_start_ms, count]` pair per month (the completed-connections and every
            `meeting_outcomes` branch — meetings are always reported month-by-month).
          items:
            oneOf:
            - type: integer
            - type: array
              items:
                type: integer
              minItems: 2
              maxItems: 2
        color:
          type: string
        visibility:
          type: boolean
    OutcomesReportRoleSummaryItem:
      type: object
      required:
      - id
      - name
      - count
      - change
      additionalProperties: false
      properties:
        id:
          type: string
          description: A section element id, e.g. `membership_mentor`/`matching_mentor`/`ongoing_mentor`/`positive_outcomes_mentor`/`flash_mentor`
            — prefix varies by report and branch.
        name:
          type: string
        count:
          type: integer
        change:
          type:
          - number
          - 'null'
          description: Percentage change vs. the prior comparable window; null when
            there's no prior window to compare against.
    OutcomesReportUserSummary:
      type: object
      required:
      - name
      - count
      - change
      additionalProperties: false
      properties:
        name:
          type: string
        count:
          type: integer
        change:
          type:
          - number
          - 'null'
    OutcomesReportUserOutcomes:
      type: object
      description: |
        `OutcomesReport::UserOutcomesReport#as_json` — see the `user_outcomes` operation's
        description for exactly which fields are conditional on what.
      required:
      - startDate
      - endDate
      - mentoringRolesForCharts
      - programId
      - nMentoringRoles
      additionalProperties: false
      properties:
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        mentoringRolesForCharts:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportRawRole"
        programId:
          type: integer
        nMentoringRoles:
          type: integer
        intervalInDays:
          type: integer
        totalCount:
          type: integer
        overallChange:
          type:
          - number
          - 'null'
        prevWindowValid:
          type: boolean
        rolewiseSummary:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportRoleSummaryItem"
        enabledStatusMapping:
          "$ref": "#/components/schemas/OutcomesReportEnabledStatusMapping"
        graphData:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportGraphSeries"
        roleGraphColorMapping:
          "$ref": "#/components/schemas/OutcomesReportRoleColorMapping"
        startDateForGraph:
          type: integer
          description: Milliseconds since epoch.
        startDayIndex:
          type: integer
    OutcomesReportConnectionOutcomes:
      type: object
      description: |
        `OutcomesReport::ConnectionOutcomesReport#as_json` — see the `connection_outcomes`
        operation's description for exactly which fields are conditional on what.
      required:
      - mentoring_roles
      - roleGraphColorMapping
      - enabledStatusMapping
      - startDate
      - endDate
      - startDayIndex
      - daysSpan
      - oldEndTime
      - oldStartTime
      - getOldData
      - startDateForGraph
      - forStatus
      additionalProperties: false
      properties:
        mentoring_roles:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportRawRole"
        roleGraphColorMapping:
          "$ref": "#/components/schemas/OutcomesReportRoleColorMapping"
        enabledStatusMapping:
          "$ref": "#/components/schemas/OutcomesReportEnabledStatusMapping"
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        startDayIndex:
          type: integer
        daysSpan:
          type: integer
        oldEndTime:
          type: string
          format: date-time
        oldStartTime:
          type: string
          format: date-time
        getOldData:
          type: boolean
        startDateForGraph:
          type: integer
          description: Milliseconds since epoch.
        forStatus:
          type:
          - integer
          - string
          description: The raw `Group::Status::ACTIVE` integer (`0`) when no `status`
            param was given; otherwise that param's string, echoed verbatim — confirmed
            live, not validated.
        totalCount:
          type: integer
        overallChange:
          type:
          - number
          - 'null'
        userSummary:
          "$ref": "#/components/schemas/OutcomesReportUserSummary"
        rolewiseSummary:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportRoleSummaryItem"
        graphData:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportGraphSeries"
        responseRate:
          type: number
        marginError:
          type: number
        responseRateInScopedConnections:
          type: number
    OutcomesReportMeetingOutcomes:
      type: object
      description: |
        `OutcomesReport::MeetingOutcomesReport#as_json` — see the `meeting_outcomes` operation's
        description. No `data_side` here: every field below is always present.
      required:
      - mentoring_roles
      - roleGraphColorMapping
      - enabledStatusMapping
      - startDate
      - endDate
      - startDayIndex
      - daysSpan
      - oldEndTime
      - oldStartTime
      - getOldData
      - startDateForGraph
      - memberIds
      - totalCount
      - overallChange
      - userSummary
      - rolewiseSummary
      - graphData
      additionalProperties: false
      properties:
        mentoring_roles:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportRawRole"
        roleGraphColorMapping:
          "$ref": "#/components/schemas/OutcomesReportRoleColorMapping"
        enabledStatusMapping:
          "$ref": "#/components/schemas/OutcomesReportEnabledStatusMapping"
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        startDayIndex:
          type: integer
        daysSpan:
          type: integer
        oldEndTime:
          type: string
          format: date-time
        oldStartTime:
          type: string
          format: date-time
        getOldData:
          type: boolean
        startDateForGraph:
          type: integer
          description: Milliseconds since epoch.
        memberIds:
          type:
          - array
          - 'null'
          items:
            type: integer
          description: Null when the request carries no cached/scoped member segment.
        totalCount:
          type: integer
        overallChange:
          type:
          - number
          - 'null'
        userSummary:
          "$ref": "#/components/schemas/OutcomesReportUserSummary"
        rolewiseSummary:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportRoleSummaryItem"
        graphData:
          type: array
          items:
            "$ref": "#/components/schemas/OutcomesReportGraphSeries"
        responseRate:
          type: number
        marginError:
          type: number
        responseRateInScopedConnections:
          type: number
    PeoplePaginationMeta:
      type: object
      additionalProperties: false
      required:
      - total
      - page
      - perPage
      - totalPages
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    PeopleSectionCount:
      type: object
      additionalProperties: false
      required:
      - total
      properties:
        total:
          type: integer
    PeopleSummary:
      type: object
      additionalProperties: false
      required:
      - members
      - admins
      - invitations
      - membershipRequests
      - blockedUsers
      - profileQuestions
      properties:
        members:
          "$ref": "#/components/schemas/PeopleSectionCount"
        admins:
          "$ref": "#/components/schemas/PeopleSectionCount"
        invitations:
          "$ref": "#/components/schemas/PeopleSectionCount"
        membershipRequests:
          "$ref": "#/components/schemas/PeopleSectionCount"
        blockedUsers:
          "$ref": "#/components/schemas/PeopleSectionCount"
        profileQuestions:
          "$ref": "#/components/schemas/PeopleSectionCount"
    PeopleMember:
      type: object
      additionalProperties: false
      required:
      - id
      - firstName
      - lastName
      - email
      - roles
      - status
      - joinedAt
      - lastActivityAt
      properties:
        id:
          type: integer
        firstName:
          type:
          - string
          - 'null'
        lastName:
          type:
          - string
          - 'null'
        email:
          type:
          - string
          - 'null'
        roles:
          type: array
          items:
            type: string
        status:
          type: string
          description: "`User#state`."
        joinedAt:
          type:
          - string
          - 'null'
          format: date-time
        lastActivityAt:
          type:
          - string
          - 'null'
          format: date-time
    PeopleMembersPayload:
      type: object
      additionalProperties: false
      required:
      - users
      - meta
      properties:
        users:
          type: array
          items:
            "$ref": "#/components/schemas/PeopleMember"
        meta:
          "$ref": "#/components/schemas/PeoplePaginationMeta"
    PeopleMemberDetail:
      type: object
      additionalProperties: false
      required:
      - id
      - firstName
      - lastName
      - email
      - roles
      - status
      - joinedAt
      - lastActivityAt
      - profilePicture
      properties:
        id:
          type: integer
        firstName:
          type:
          - string
          - 'null'
        lastName:
          type:
          - string
          - 'null'
        email:
          type:
          - string
          - 'null'
        roles:
          type: array
          items:
            type: string
        status:
          type: string
        joinedAt:
          type:
          - string
          - 'null'
          format: date-time
        lastActivityAt:
          type:
          - string
          - 'null'
          format: date-time
        profilePicture:
          type:
          - string
          - 'null'
    PeopleMemberUpdatePayload:
      type: object
      additionalProperties: false
      required:
      - user
      properties:
        user:
          "$ref": "#/components/schemas/PeopleMemberDetail"
    PeopleAdmin:
      type: object
      additionalProperties: false
      required:
      - id
      - firstName
      - lastName
      - email
      - joinedAt
      properties:
        id:
          type: integer
        firstName:
          type:
          - string
          - 'null'
        lastName:
          type:
          - string
          - 'null'
        email:
          type:
          - string
          - 'null'
        joinedAt:
          type:
          - string
          - 'null'
          format: date-time
    PeopleAdminsPayload:
      type: object
      additionalProperties: false
      required:
      - admins
      - meta
      properties:
        admins:
          type: array
          items:
            "$ref": "#/components/schemas/PeopleAdmin"
        meta:
          "$ref": "#/components/schemas/PeoplePaginationMeta"
    PeopleInvitation:
      type: object
      additionalProperties: false
      required:
      - id
      - email
      - roles
      - status
      - sentAt
      - expiresAt
      properties:
        id:
          type: integer
        email:
          type:
          - string
          - 'null'
        roles:
          type: array
          items:
            type: string
        status:
          type: string
          enum:
          - accepted
          - expired
          - pending
        sentAt:
          type:
          - string
          - 'null'
          format: date-time
        expiresAt:
          type:
          - string
          - 'null'
          format: date-time
    PeopleInvitationsPayload:
      type: object
      additionalProperties: false
      required:
      - invitations
      - meta
      properties:
        invitations:
          type: array
          items:
            "$ref": "#/components/schemas/PeopleInvitation"
        meta:
          "$ref": "#/components/schemas/PeoplePaginationMeta"
    PeopleInvitationsCreatePayload:
      type: object
      additionalProperties: false
      required:
      - invitations
      - sentCount
      properties:
        invitations:
          type: array
          items:
            "$ref": "#/components/schemas/PeopleInvitation"
        sentCount:
          type: integer
          description: Count of successfully-saved invitations only — a per-email
            save failure (e.g. duplicate invite) is silently dropped with no error
            surfaced for that email.
    PeopleMembershipRequest:
      type: object
      additionalProperties: false
      required:
      - id
      - firstName
      - lastName
      - email
      - roles
      - status
      - requestedAt
      properties:
        id:
          type: integer
        firstName:
          type:
          - string
          - 'null'
        lastName:
          type:
          - string
          - 'null'
        email:
          type:
          - string
          - 'null'
        roles:
          type: array
          items:
            type: string
        status:
          type: string
          enum:
          - pending
          - accepted
          - rejected
          - unknown
        requestedAt:
          type:
          - string
          - 'null'
          format: date-time
    PeopleMembershipRequestsPayload:
      type: object
      additionalProperties: false
      required:
      - membershipRequests
      - meta
      properties:
        membershipRequests:
          type: array
          items:
            "$ref": "#/components/schemas/PeopleMembershipRequest"
        meta:
          "$ref": "#/components/schemas/PeoplePaginationMeta"
    PeopleBlockedUser:
      type: object
      additionalProperties: false
      required:
      - id
      - firstName
      - lastName
      - email
      - blockedAt
      - reason
      properties:
        id:
          type: integer
        firstName:
          type:
          - string
          - 'null'
        lastName:
          type:
          - string
          - 'null'
        email:
          type:
          - string
          - 'null'
        blockedAt:
          type:
          - string
          - 'null'
          format: date-time
        reason:
          type:
          - string
          - 'null'
    PeopleBlockedUsersPayload:
      type: object
      additionalProperties: false
      required:
      - blockedUsers
      - meta
      properties:
        blockedUsers:
          type: array
          items:
            "$ref": "#/components/schemas/PeopleBlockedUser"
        meta:
          "$ref": "#/components/schemas/PeoplePaginationMeta"
    PeopleProfileQuestion:
      type: object
      additionalProperties: false
      required:
      - id
      - label
      - questionType
      - required
      - position
      - roles
      - options
      properties:
        id:
          type: integer
        label:
          type:
          - string
          - 'null'
        questionType:
          type:
          - string
          - 'null'
        required:
          type: boolean
        position:
          type:
          - integer
          - 'null'
        roles:
          type: array
          items:
            type: string
        options:
          type: array
          items:
            type: string
    PeopleProfileQuestionsPayload:
      type: object
      additionalProperties: false
      required:
      - profileQuestions
      properties:
        profileQuestions:
          type: array
          items:
            "$ref": "#/components/schemas/PeopleProfileQuestion"
    PeopleSectionReadPayload:
      oneOf:
      - "$ref": "#/components/schemas/PeopleMembersPayload"
      - "$ref": "#/components/schemas/PeopleAdminsPayload"
      - "$ref": "#/components/schemas/PeopleInvitationsPayload"
      - "$ref": "#/components/schemas/PeopleMembershipRequestsPayload"
      - "$ref": "#/components/schemas/PeopleBlockedUsersPayload"
      - "$ref": "#/components/schemas/PeopleProfileQuestionsPayload"
    PeopleErrorResponse:
      type: object
      additionalProperties: false
      required:
      - error
      properties:
        error:
          type: string
    QaQuestionSummary:
      type: object
      description: Camel-cased — see the index operation's description.
      required:
      - id
      - summary
      - description
      - authorName
      - answerCount
      - views
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        summary:
          type: string
        description:
          type:
          - string
          - 'null'
        authorName:
          type: string
          description: The author's display name, or "Unknown" if the user record
            is gone.
        answerCount:
          type: integer
        views:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    QaQuestionDetail:
      type: object
      required:
      - id
      - summary
      - description
      - authorName
      - answerCount
      - views
      - createdAt
      - followerCount
      additionalProperties: false
      properties:
        id:
          type: integer
        summary:
          type: string
        description:
          type:
          - string
          - 'null'
        authorName:
          type: string
        answerCount:
          type: integer
        views:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        followerCount:
          type: integer
    QaAnswer:
      type: object
      required:
      - id
      - content
      - authorName
      - score
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        content:
          type: string
        authorName:
          type: string
        score:
          type: integer
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    QaQuestionsPagination:
      type: object
      description: |
        This controller's base class (`Admin::Programs::BaseController#pagination_meta`) emits
        camelCase keys, not this spec's usual snake_case `{total, page, limit, has_more}`.
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    RecommendMentorsConfig:
      type: object
      additionalProperties: false
      required:
      - id
      - maxPickableSlots
      - maxSuggestionCount
      - sortValue
      - sortOrder
      - showDrafted
      - showPublished
      - showPastRecommendations
      - primaryViewId
      - secondaryViewId
      - persisted
      properties:
        id:
          type:
          - integer
          - 'null'
        maxPickableSlots:
          type:
          - integer
          - 'null'
        maxSuggestionCount:
          type: integer
        sortValue:
          type:
          - string
          - 'null'
        sortOrder:
          type: string
          enum:
          - asc
          - desc
        showDrafted:
          type:
          - boolean
          - 'null'
        showPublished:
          type:
          - boolean
          - 'null'
        showPastRecommendations:
          type: boolean
        primaryViewId:
          type:
          - integer
          - 'null'
        secondaryViewId:
          type:
          - integer
          - 'null'
        persisted:
          type: boolean
    RecommendMentorsProgramCapabilities:
      type: object
      additionalProperties: false
      required:
      - matchingConfigured
      - ongoingApplicable
      - chemistryCheckApplicable
      properties:
        matchingConfigured:
          type: boolean
        ongoingApplicable:
          type: boolean
        chemistryCheckApplicable:
          type: boolean
    RecommendMentorsSettings:
      type: object
      additionalProperties: false
      required:
      - maxLimit
      - defaultMaxRecommendationCount
      properties:
        maxLimit:
          type: integer
        defaultMaxRecommendationCount:
          type: integer
    RecommendMentorsShowPayload:
      type: object
      additionalProperties: false
      required:
      - bulkRecommendation
      - program
      - settings
      properties:
        bulkRecommendation:
          "$ref": "#/components/schemas/RecommendMentorsConfig"
        program:
          "$ref": "#/components/schemas/RecommendMentorsProgramCapabilities"
        settings:
          "$ref": "#/components/schemas/RecommendMentorsSettings"
    RecommendMentorsUpdatePayload:
      type: object
      additionalProperties: false
      required:
      - bulkRecommendation
      properties:
        bulkRecommendation:
          "$ref": "#/components/schemas/RecommendMentorsConfig"
    RecommendMentorsAdminView:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - description
      - userCount
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        userCount:
          type: integer
    RecommendMentorsAdminViewsPayload:
      type: object
      additionalProperties: false
      required:
      - mentorViews
      - menteeViews
      properties:
        mentorViews:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendMentorsAdminView"
        menteeViews:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendMentorsAdminView"
    RecommendMentorsUpdateBody:
      type: object
      additionalProperties: false
      properties:
        maxPickableSlots:
          type: integer
          description: Max recommendation slots per mentee.
        maxSuggestionCount:
          type: integer
          description: Max suggestions shown.
        sortValue:
          type: string
          description: Sort column.
        sortOrder:
          type: string
          description: Only an exact "desc" flips to descending; any other value (including
            case variants) is silently treated as ascending — the controller never
            rejects it, so this is intentionally not an enum.
        showDrafted:
          type: boolean
          description: Show drafted recommendations.
        showPublished:
          type: boolean
          description: Show published recommendations.
        showPastRecommendations:
          type: boolean
          description: Show past recommendations.
    RecommendMentorsErrorResponse:
      type: object
      additionalProperties: false
      required:
      - error
      properties:
        error:
          type: string
    RecommendMentorsComputeBody:
      type: object
      additionalProperties: false
      properties:
        primaryViewId:
          type: integer
          description: Admin view ID for mentors.
        secondaryViewId:
          type: integer
          description: Admin view ID for mentees.
        maxPickableSlots:
          type: integer
    RecommendMentorsComputedEntity:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - slotsAvailable
      - recommendedCount
      - connectionsCount
      properties:
        id:
          type: integer
        name:
          type: string
          description: 'Falls back to "User #<id>" if `User#name` raises.'
        slotsAvailable:
          type: integer
        recommendedCount:
          type: integer
        connectionsCount:
          type: integer
          description: "\U0001F534 DSL/controller disagreement candidate: `serialize_computation_results`
            here reads `result_hash[:mentor_ongoing_connections_count]` same as `bulk_match`'s
            copy — always present per the shared computation result hash, kept for
            parity."
    RecommendMentorsSuggestion:
      type: object
      additionalProperties: false
      required:
      - id
      - score
      properties:
        id:
          type: integer
        score:
          type: number
    RecommendMentorsSecondaryComputedEntity:
      type: object
      additionalProperties: false
      required:
      - id
      - name
      - suggestedMentors
      - recommendationStatus
      - recommendationId
      properties:
        id:
          type: integer
        name:
          type: string
          description: 'Falls back to "User #<id>" if `User#name` raises.'
        suggestedMentors:
          type: array
          maxItems: 10
          items:
            "$ref": "#/components/schemas/RecommendMentorsSuggestion"
        recommendationStatus:
          type: string
          description: Defaults to "unmatched" when no recommendation info is present.
        recommendationId:
          type:
          - integer
          - 'null'
    RecommendMentorsComputationResults:
      type: object
      additionalProperties: false
      required:
      - status
      - bulkRecommendationId
      - isLargeSet
      - primaryEntities
      - secondaryEntities
      - meta
      properties:
        status:
          const: completed
        bulkRecommendationId:
          type: integer
        isLargeSet:
          type: boolean
        primaryEntities:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendMentorsComputedEntity"
        secondaryEntities:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendMentorsSecondaryComputedEntity"
        meta:
          type: object
          additionalProperties: false
          required:
          - totalMentors
          - totalMentees
          properties:
            totalMentors:
              type: integer
            totalMentees:
              type: integer
    RecommendMentorsUpdatePairBody:
      type: object
      additionalProperties: false
      required:
      - updateType
      - secondaryEntityId
      properties:
        updateType:
          type: string
          enum:
          - draft
          - publish
          - discard
        secondaryEntityId:
          type: integer
          description: Mentee user ID.
        primaryEntityIdList:
          type: string
          description: Comma-separated mentor user IDs. Required for draft/publish
            (an empty list drafts a recommendation with zero preferences); unused
            for discard.
    RecommendMentorsUpdatePairResult:
      type: object
      additionalProperties: false
      required:
      - status
      properties:
        recommendationId:
          type: integer
          description: Present for draft/publish, absent for discard.
        status:
          type: string
          enum:
          - drafted
          - published
          - discarded
    RecommendMentorsBulkUpdatePairsBody:
      type: object
      additionalProperties: false
      required:
      - updateType
      - secondaryPrimaryEntityMap
      properties:
        updateType:
          type: string
          enum:
          - draft
          - publish
          - discard
        secondaryPrimaryEntityMap:
          type: object
          description: Map of mentee user ID (string key) => array of mentor user
            IDs.
          additionalProperties:
            type: array
            items:
              type: integer
    RecommendMentorsBulkUpdatePairsResult:
      type: object
      additionalProperties: false
      required:
      - status
      - count
      properties:
        status:
          const: ok
        count:
          type: integer
    RecommendMentorsPastRecommendationItem:
      type: object
      additionalProperties: false
      required:
      - mentorId
      - mentorName
      - publishedAt
      - ongoingConnectionsCount
      properties:
        mentorId:
          type: integer
        mentorName:
          type:
          - string
          - 'null'
          description: "\U0001F534 New `User#name` (bare, not `name_only:`) call site
            — a real `User`, not yet covered by an `ErrorAgent.expects` treatment;
            hits on the first real recommendation."
        publishedAt:
          type:
          - string
          - 'null'
          format: date-time
        ongoingConnectionsCount:
          type: integer
    RecommendMentorsPastRecommendationsPayload:
      type: object
      additionalProperties: false
      required:
      - pastRecommendations
      - meta
      properties:
        pastRecommendations:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendMentorsPastRecommendationItem"
        meta:
          type: object
          additionalProperties: false
          required:
          - total
          - page
          - perPage
          - totalPages
          properties:
            total:
              type: integer
            page:
              type: integer
            perPage:
              type: integer
              const: 10
            totalPages:
              type: integer
    ThreeSixtySurveyReviewerGroup:
      type: object
      required:
      - id
      - name
      - threshold
      - isSelf
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        threshold:
          type: integer
        isSelf:
          type: boolean
    ThreeSixtySurveySummary:
      type: object
      description: Camel-cased — see the index operation's description.
      required:
      - id
      - title
      - status
      - expiryDate
      - issuedAt
      - reviewersAdditionType
      - assesseeCount
      - competencyCount
      - questionCount
      - reviewerGroupCount
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        status:
          type: string
          enum:
          - drafted
          - published
        expiryDate:
          type:
          - string
          - 'null'
          format: date
        issuedAt:
          type:
          - string
          - 'null'
          format: date-time
        reviewersAdditionType:
          type: string
          enum:
          - admin_only
          - assessee_only
        assesseeCount:
          type: integer
        competencyCount:
          type: integer
        questionCount:
          type: integer
        reviewerGroupCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ThreeSixtySurveyDetail:
      type: object
      description: Camel-cased — see the index operation's description.
      required:
      - id
      - title
      - status
      - expiryDate
      - issuedAt
      - reviewersAdditionType
      - assesseeCount
      - competencyCount
      - questionCount
      - reviewerGroupCount
      - createdAt
      - updatedAt
      - description
      - assesseeAdditionMode
      - adminViewId
      - programId
      - reviewerGroups
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        status:
          type: string
          enum:
          - drafted
          - published
        expiryDate:
          type:
          - string
          - 'null'
          format: date
        issuedAt:
          type:
          - string
          - 'null'
          format: date-time
        reviewersAdditionType:
          type: string
          enum:
          - admin_only
          - assessee_only
        assesseeCount:
          type: integer
        competencyCount:
          type: integer
        questionCount:
          type: integer
        reviewerGroupCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        description:
          type:
          - string
          - 'null'
        assesseeAdditionMode:
          type: string
          enum:
          - manual
          - admin_view
        adminViewId:
          type:
          - integer
          - 'null'
        programId:
          type:
          - integer
          - 'null'
          description: Null for an org-wide survey.
        reviewerGroups:
          type: array
          items:
            "$ref": "#/components/schemas/ThreeSixtySurveyReviewerGroup"
    ThreeSixtySurveysPagination:
      type: object
      description: |
        This controller's base class (`Admin::Programs::BaseController#pagination_meta`) emits
        camelCase keys, not this spec's usual snake_case `{total, page, limit, has_more}` — see
        the index operation's description.
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    ThreeSixtySurveyWriteBody:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
        expiry_date:
          type: string
          format: date
        reviewers_addition_type:
          type: string
          enum:
          - admin_only
          - assessee_only
        reviewer_group_names:
          type: array
          items:
            type: string
          description: |
            Reviewer-group names to attach (typically from `ThreeSixty::ReviewerGroup::DefaultName`
            — Self, Line Manager, Peer, Direct Report, Other). Omit or send empty to leave
            reviewer groups unchanged.
    ThreeSixtySurveyError:
      type: object
      description: |
        This controller's own bare shape — `{error}` for a guard rejection (not drafted,
        incomplete), `{error, details}` when `details` carries the model's `full_messages` for
        an actual validation failure (`create`/`update`).
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: string
    VirtualMentoringDashboardBareError:
      type: object
      description: 'This controller''s own guards render a bare `{error: "<string>"}`,
        not the standard envelope.'
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    VirtualMentoringDashboardIndex:
      type: object
      required:
      - features
      additionalProperties: false
      properties:
        features:
          type: array
          items:
            "$ref": "#/components/schemas/VirtualMentoringDashboardFeature"
    VirtualMentoringDashboardFeature:
      type: object
      required:
      - id
      - title
      - category
      - icon
      - enabled
      - available
      - integrated
      - status
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
        category:
          type: string
          enum:
          - meeting_mode
          - bot_app
          - chat
          - calendar
        icon:
          type: string
        enabled:
          type: boolean
        available:
          type: boolean
        integrated:
          type: boolean
        status:
          type: string
          enum:
          - connected
          - not_connected
          - enabled
          - not_enabled
    VirtualMentoringDashboardShow:
      type: object
      required:
      - feature
      - title
      - category
      - enabled
      - startDate
      - endDate
      - metrics
      - showData
      additionalProperties: false
      properties:
        feature:
          type: string
        title:
          type: string
        category:
          type: string
          enum:
          - meeting_mode
          - bot_app
          - chat
          - calendar
        enabled:
          type: boolean
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        metrics:
          oneOf:
          - "$ref": "#/components/schemas/VirtualMentoringDashboardMeetingModeMetrics"
          - "$ref": "#/components/schemas/VirtualMentoringDashboardBotAppMetrics"
          - "$ref": "#/components/schemas/VirtualMentoringDashboardChatMetrics"
          - "$ref": "#/components/schemas/VirtualMentoringDashboardCalendarMetrics"
        showData:
          type: boolean
    VirtualMentoringDashboardMeetingModeMetrics:
      type: object
      required:
      - usersCount
      - meetingsCount
      - totalMeetingTime
      - averageMeetingTime
      additionalProperties: false
      properties:
        usersCount:
          type: integer
        meetingsCount:
          type: integer
        totalMeetingTime:
          type: number
        averageMeetingTime:
          type: number
    VirtualMentoringDashboardBotAppMetrics:
      type: object
      required:
      - usersCount
      - totalUsersCount
      - notificationsCount
      - activitiesCount
      additionalProperties: false
      properties:
        usersCount:
          type: integer
        totalUsersCount:
          type: integer
        notificationsCount:
          type: integer
        activitiesCount:
          type: integer
    VirtualMentoringDashboardChatMetrics:
      type: object
      required:
      - usersCount
      - messagesCount
      - messagesInsideConnectionCount
      - messagesOutsideConnectionCount
      additionalProperties: false
      properties:
        usersCount:
          type: integer
        messagesCount:
          type: integer
        messagesInsideConnectionCount:
          type: integer
        messagesOutsideConnectionCount:
          type: integer
    VirtualMentoringDashboardCalendarMetrics:
      type: object
      required:
      - usersCount
      - meetingsCount
      - totalMeetingTime
      - averageMeetingTime
      - orgLevel
      additionalProperties: false
      properties:
        usersCount:
          type: integer
        meetingsCount:
          type: integer
        totalMeetingTime:
          type: number
        averageMeetingTime:
          type: number
        orgLevel:
          type: boolean
    ProgramCreateError:
      type: object
      description: 'The bespoke error envelope `Api::V3::Admin::ProgramsController#create`
        renders directly — NOT the shared `Error` schema, because `detail` (singular)
        is an arbitrary hash whose shape depends on the failure (`{missing: [...]}`,
        `{expected: [...]}`, `{errors: [...]}`, or a Compiler/spec error''s `{step:,
        spec_path:, inner:}`), rather than the shared schema''s `details` (plural
        array of `{field, message}`).'
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: object
          required:
          - code
          - message
          additionalProperties: false
          properties:
            code:
              type: string
              description: missing_fields / invalid_engagement_type / feature_not_enabled
                / invalid_spec_defaults / compile_failed / invalid_spec / invalid_program.
            message:
              type: string
            detail:
              type: object
              additionalProperties: true
              description: Structured detail; shape varies by `code` (see above).
                Absent for some codes.
    RolloutStatusMembersCount:
      description: A site member count, or a suppressed marker when below minimum
        cohort.
      oneOf:
      - type: integer
      - type: object
        required:
        - suppressed
        - reason
        - minimum_cohort_size
        additionalProperties: false
        properties:
          suppressed:
            const: true
          reason:
            type: string
          minimum_cohort_size:
            type: integer
    RolloutStatusSite:
      type: object
      required:
      - org_unit_id
      - name
      - path
      - status
      - members_count
      additionalProperties: false
      properties:
        org_unit_id:
          type: integer
        name:
          type: string
        path:
          type: string
        status:
          type: string
          enum:
          - live
          - pending
        members_count:
          "$ref": "#/components/schemas/RolloutStatusMembersCount"
    TrainingResourcesPaginationMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    OrganizationLanguageEntry:
      type: object
      required:
      - id
      - locale
      - name
      - displayTitle
      - enabled
      - isDefault
      - enabledProgramIds
      - enabledProgramNames
      additionalProperties: false
      properties:
        id:
          type:
          - integer
          - 'null'
          description: OrganizationLanguage ID. Null for the synthetic English entry.
        locale:
          type: string
          description: Language code (e.g. en, es, fr).
        name:
          type: string
        displayTitle:
          type:
          - string
          - 'null'
        enabled:
          type: string
          enum:
          - enabled
          - drafted
          - disabled
        isDefault:
          type: boolean
          description: Whether this is the default (English) language.
        enabledProgramIds:
          type: array
          items:
            type: integer
        enabledProgramNames:
          type: array
          items:
            type: string
    OrganizationLanguageErrors:
      type: object
      required:
      - errors
      additionalProperties: false
      properties:
        errors:
          type: array
          items:
            type: string
    LegacyAgentResponse:
      type: object
      required:
      - agent
      additionalProperties: false
      properties:
        agent:
          "$ref": "#/components/schemas/LegacyAgent"
    LegacyAgent:
      type: object
      description: "The member-facing agent projection served by the legacy `Api::V3::AgentsController`,
        kept\ndistinct from the DSL resource's `AgentSummary`/`AgentDetail` because
        it is a different,\nhand-rendered shape over the same model.\n\U0001F534 Source's
        `agent_json` also emits `skillDomains`; it is OMITTED here on purpose.\n`skill_domains`
        is one of decision J's eight admin-only virtuals, and this is a\nmember-facing
        surface with no admin gate — so rendering it would re-open through a bespoke\ncontroller
        exactly what J closed field-level on `resources/agents`. Do not add it back.\n`status`,
        `primary`, `published_at` and `created_at` ARE present by contrast: they are\nbroader
        than my_mentor's six-field projection but are not among J's eight.\n⚠️ `publishedAt`
        / `createdAt` are camelCase, breaking this API's snake_case rule. They are\nspelled
        the way the controller renders them — the spec describes the wire, so do NOT
        \"fix\"\nthem here; renaming is a controller change and both must move together.\n"
      required:
      - id
      - name
      - description
      - tagline
      - status
      - category
      - persona
      - primary
      - publishedAt
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        tagline:
          type:
          - string
          - 'null'
        status:
          type: string
          description: |
            Free-form on the wire on purpose — no enum. The column is a plain string with a
            "draft" default and AASM owns the transitions, so enumerating the states here would
            turn any state this contract lagged behind into a response-validation failure. Only
            `active` agents are ever returned by these two actions.
        category:
          type:
          - string
          - 'null'
        persona:
          type:
          - object
          - 'null'
          additionalProperties: true
        primary:
          type: boolean
        publishedAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type: string
          format: date-time
    AgentConversationSummary:
      type: object
      required:
      - id
      - agentId
      - title
      - status
      - messageCount
      - lastMessageAt
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        agentId:
          type: integer
        title:
          type:
          - string
          - 'null'
        status:
          type:
          - string
          - 'null'
        messageCount:
          type: integer
        lastMessageAt:
          type:
          - string
          - 'null'
          format: date-time
          description: |
            Read from stored JSON (`properties["last_message_at"]`) — always null on this landing,
            since the message-append path that would populate it (F38 Row D, `conversations`) is
            not yet ported. Live-but-empty data-supply rider, not a blocker.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TokenResponse:
      type: object
      required:
      - access_token
      - token_type
      - expires_in
      additionalProperties: false
      properties:
        access_token:
          type: string
        issued_token_type:
          const: urn:ietf:params:oauth:token-type:access_token
        token_type:
          const: Bearer
        expires_in:
          type: integer
    InboxBareError:
      type: object
      description: |
        This tier's 4xx shape. Source renders a BARE `{error: "<string>"}`, NOT this API's
        `{error: {code, message}}` envelope. Declared as-is because the SPA reads the bare form —
        normalising it would break the live client.
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: string
    InboxPaginationMeta:
      type: object
      description: Emitted by `PaginationConcern#pagination_meta`.
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    InboxChatRoom:
      type: object
      required:
      - id
      - name
      - roomType
      - participants
      - lastMessage
      - unreadCount
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
          description: The group's name, or the other participant's name for a direct
            room ("Unknown" if unresolvable).
        roomType:
          type: string
          enum:
          - direct
          - group
        participants:
          type: array
          description: EMPTY unless the caller preloaded `members` — populated on
            `show`, absent on `index`.
          items:
            type: object
            required:
            - id
            - name
            - imageUrl
            additionalProperties: false
            properties:
              id:
                type: integer
              name:
                type: string
              imageUrl:
                type: string
                nullable: true
        lastMessage:
          nullable: true
          type: object
          required:
          - id
          - senderId
          - senderName
          - body
          - createdAt
          additionalProperties: false
          properties:
            id:
              type: integer
            senderId:
              type: integer
              nullable: true
            senderName:
              type: string
              nullable: true
            body:
              type: string
              nullable: true
              description: Truncated to 100 characters.
            createdAt:
              type: string
              format: date-time
        unreadCount:
          type: integer
        updatedAt:
          type: string
          format: date-time
    InboxChat:
      type: object
      required:
      - id
      - senderId
      - senderName
      - senderImageUrl
      - body
      - isOwn
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        senderId:
          type: integer
          nullable: true
        senderName:
          type: string
          nullable: true
        senderImageUrl:
          type: string
          nullable: true
        body:
          type: string
          nullable: true
        isOwn:
          type: boolean
          description: True when the caller is the sender.
        createdAt:
          type: string
          format: date-time
    CoachingConsentDecision:
      type: object
      required:
      - state
      - mode
      - disclosureVersion
      - blocksAccess
      additionalProperties: false
      properties:
        state:
          type: string
          enum:
          - pending
          - accepted
          - declined
        mode:
          type: string
          enum:
          - acknowledgement
          - precondition
        disclosureVersion:
          type: string
        blocksAccess:
          type: boolean
        decidedAt:
          type: string
          format: date-time
          nullable: true
    CoachingFeedback:
      type: object
      description: |
        One member rating of a coaching turn. Stored in `chronus_ai_feedbacks` with
        `rateable_type=Interaction`. Quality review only — never used for model training.
      required:
      - id
      - interaction_id
      - rating
      - reason_codes
      - rater_id
      - created_at
      - updated_at
      additionalProperties: false
      properties:
        id:
          type: integer
        interaction_id:
          type: integer
          description: The AGENT Interaction (message) that was rated.
        rating:
          type: string
          enum:
          - helpful
          - unhelpful
        reason_codes:
          type: array
          items:
            type: string
        comment:
          type:
          - string
          - 'null'
        rater_id:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CoachingFeedbackSummary:
      type: object
      required:
      - total
      - helpful
      - unhelpful
      additionalProperties: false
      properties:
        total:
          type: integer
        helpful:
          type: integer
        unhelpful:
          type: integer
    ComposeQuerySpec:
      type: object
      description: |
        One query in a compose bundle. The spec mirrors the query string of a
        direct GET on the matching /api/v3/<resource> endpoint — filter keys
        sit at the top level alongside `limit`/`page`/`fields`. The whole bundle
        resolves in one product surface, set by the `?app_mode=` query param /
        `App-Mode` header on the compose request.
      required:
      - resource
      additionalProperties: true
      properties:
        resource:
          type: string
          description: Resource path (e.g. `resources/programs`, `resources/members/current`).
        limit:
          type: integer
          minimum: 1
          maximum: 100
        page:
          type: integer
          minimum: 1
        fields:
          type: array
          items:
            type: string
    ComposeQueryResult:
      type: object
      description: |
        Per-query result. Carries the sub-request's HTTP status so the client
        can handle partial failures uniformly (the outer envelope is always
        200 unless the entire compose request was malformed). On 2xx the
        payload is mirrored as `data` (and `pagination` for collections);
        on non-2xx the payload is mirrored as `error`.
      required:
      - status
      additionalProperties: false
      properties:
        status:
          type: integer
        data: {}
        pagination:
          "$ref": "#/components/schemas/Pagination"
        error:
          type: object
          additionalProperties: true
    RumiNudgesResponse:
      type: object
      required:
      - nudges
      additionalProperties: false
      properties:
        nudges:
          type: array
          items:
            "$ref": "#/components/schemas/RumiNudge"
    RumiNudge:
      type: object
      description: |
        One CTA card. `label` and `prompt` are assembled server-side and embed the peer's name when
        one can be resolved — see the `label` note, because the two shapes are NOT distinguishable
        by any other field.
      required:
      - kind
      - label
      - prompt
      - meeting_id
      - trigger_id
      additionalProperties: false
      properties:
        kind:
          type: string
          enum:
          - pre_session_brief
          - post_meeting_debrief
        label:
          type: string
        prompt:
          type: string
        meeting_id:
          description: |
            Read straight from the trigger's free-form `payload` JSON, so untyped by construction —
            measured, not assumed: MySQL's `CAST(JSON_EXTRACT(...) AS UNSIGNED)` filter matches a
            JSON string `"42"` identically to the integer `42`, so a payload written with a string
            value is returned as a string. It is however GUARANTEED PRESENT and never null: a
            payload with no `meeting_id` casts to NULL and `NULL IN (...)` never matches, so such a
            trigger cannot be selected. ⚠️ The controller header comment, kept verbatim from
            source, marks this field `# nullable` — that comment is wrong for any produced row, and
            this declaration is authored from the query rather than from the comment.
        trigger_id:
          type: integer
    RoleplayAttemptSummary:
      type: object
      description: |
        `AttemptSummary` = `RoleplayAttemptDetail` (see `shared_session.meta.yaml`) WITHOUT the
        `detail: true`-only fields (strengths/growthAreas/reflection/pivotalMomentIndex/
        feedbackSummary/deliverySignals/highlights) — this index calls the shared renderer with
        `scores: true` only, not `detail: true`.
      required:
      - id
      - sessionId
      - sessionKind
      - attemptNumber
      - status
      - readinessLevel
      - coachingAssistCount
      - ruleXCapped
      - startedAt
      - endedAt
      - tokenCount
      - goalScores
      additionalProperties: false
      properties:
        id:
          type: integer
        sessionId:
          type: integer
        sessionKind:
          type:
          - string
          - 'null'
        attemptNumber:
          type: integer
        status:
          type: string
        readinessLevel:
          type:
          - string
          - 'null'
        coachingAssistCount:
          type: integer
        ruleXCapped:
          type: boolean
        startedAt:
          type:
          - string
          - 'null'
          format: date-time
        endedAt:
          type:
          - string
          - 'null'
          format: date-time
        tokenCount:
          type:
          - integer
          - 'null'
        goalScores:
          type: array
          items:
            "$ref": "#/components/schemas/RoleplayGoalScore"
    RoleplayReadinessTimelineEntry:
      type: object
      required:
      - attemptNumber
      - readinessLevel
      - coachingAssistCount
      - completedAt
      additionalProperties: false
      properties:
        attemptNumber:
          type: integer
        readinessLevel:
          type:
          - string
          - 'null'
        coachingAssistCount:
          type: integer
        completedAt:
          type:
          - string
          - 'null'
          format: date-time
    RoleplayStackedScorecard:
      type: object
      required:
      - attemptNumber
      - readinessLevel
      - goalScores
      additionalProperties: false
      properties:
        attemptNumber:
          type: integer
        readinessLevel:
          type:
          - string
          - 'null'
        goalScores:
          type: array
          items:
            "$ref": "#/components/schemas/RoleplayGoalScore"
    ReverseSessionResult:
      type: object
      required:
      - session
      - attemptId
      - sourceAttemptId
      - reused
      additionalProperties: false
      properties:
        session:
          "$ref": "#/components/schemas/RoleplaySessionDetail"
        attemptId:
          type: integer
        sourceAttemptId:
          type: integer
        reused:
          type: boolean
    RoleplayPaginationMeta:
      type: object
      required:
      - total
      - page
      - perPage
      - totalPages
      additionalProperties: false
      properties:
        total:
          type: integer
        page:
          type: integer
        perPage:
          type: integer
        totalPages:
          type: integer
    RoleplaySessionDetail:
      type: object
      description: |
        `RoleplaySharedSession` (see `shared_session.meta.yaml` — the base session fields, no
        detail branch; reused directly as the `index` item shape) plus the three fields source's
        `SessionSerializer` only renders when called with `detail: true`.
      required:
      - id
      - contentId
      - contentTitle
      - status
      - startedAt
      - endedAt
      - createdAt
      - modeUsed
      - sessionKind
      - parentSessionId
      - metadata
      - attemptCount
      - coachTurnCount
      additionalProperties: false
      properties:
        id:
          type: integer
        contentId:
          type: integer
        contentTitle:
          type:
          - string
          - 'null'
        status:
          type: string
        startedAt:
          type:
          - string
          - 'null'
          format: date-time
        endedAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        modeUsed:
          type:
          - string
          - 'null'
        sessionKind:
          type: string
        parentSessionId:
          type:
          - integer
          - 'null'
        metadata:
          description: Free-form JSON.
        attemptCount:
          type: integer
        coachTurnCount:
          type: integer
    RoleplayAttemptDetail:
      type: object
      description: |
        ⚠️ camelCase throughout — this endpoint's wire shape is source-verbatim from
        `Content::Roleplay::AttemptSerializer`, part of the `packages/rumi` roleplay surface's own
        established camelCase contract (its sibling serializers — Session/Message/GoalScore — are
        all camelCase too), NOT the `#139` snake_case convention applied elsewhere in this port.
      required:
      - id
      - sessionId
      - sessionKind
      - attemptNumber
      - status
      - readinessLevel
      - coachingAssistCount
      - ruleXCapped
      - startedAt
      - endedAt
      - tokenCount
      - goalScores
      - strengths
      - growthAreas
      - reflection
      - pivotalMomentIndex
      - feedbackSummary
      additionalProperties: false
      properties:
        id:
          type: integer
        sessionId:
          type: integer
        sessionKind:
          type:
          - string
          - 'null'
        attemptNumber:
          type: integer
        status:
          type: string
        readinessLevel:
          type:
          - string
          - 'null'
        coachingAssistCount:
          type: integer
        ruleXCapped:
          type: boolean
        startedAt:
          type:
          - string
          - 'null'
          format: date-time
        endedAt:
          type:
          - string
          - 'null'
          format: date-time
        tokenCount:
          type:
          - integer
          - 'null'
        goalScores:
          type: array
          items:
            "$ref": "#/components/schemas/RoleplayGoalScore"
        strengths:
          type: array
          items:
            type: string
        growthAreas:
          type: array
          items:
            type: string
        reflection:
          type:
          - string
          - 'null'
        pivotalMomentIndex:
          type:
          - integer
          - 'null'
        feedbackSummary:
          description: Free-form JSON — shape varies by scoring config; also the source
            of the two optional fields below.
        deliverySignals:
          description: Present only when `feedbackSummary["deliverySignals"]` is set.
        highlights:
          type: array
          description: Present only when `feedbackSummary["highlights"]` is a non-empty
            array (reverse-session "what to notice" bullets).
          items: {}
    RoleplayGoalScore:
      type: object
      description: |
        Shape branches on `goalType` (competency/compound/binary/default) — the optional fields
        below are populated per that branch, source-verbatim from `GoalScoreSerializer`.
      required:
      - id
      - goalId
      - goalName
      - goalType
      - feedback
      - evidence
      - evidenceText
      additionalProperties: false
      properties:
        id:
          type: integer
        goalId:
          type: integer
        goalName:
          type:
          - string
          - 'null'
        goalType:
          type:
          - string
          - 'null'
        feedback:
          type:
          - string
          - 'null'
        evidence:
          description: Free-form JSON.
        evidenceText:
          type:
          - string
          - 'null'
        weight:
          type: integer
          description: Present only when the roleplay's goal assignment declared a
            weight for this goal.
        proficiencyLevel:
          type:
          - integer
          - 'null'
          description: Present for competency/compound goal types.
        levelName:
          type:
          - string
          - 'null'
          description: Present for competency/compound goal types.
        subScores:
          description: Present for compound goal types when `evidence["subScores"]`
            is set.
        demonstrated:
          type:
          - boolean
          - 'null'
          description: Present for binary goal types.
        score:
          type:
          - number
          - 'null'
          description: Present for goal types other than competency/compound/binary.
        passed:
          type:
          - boolean
          - 'null'
          description: Present for goal types other than competency/compound/binary.
    RoleplaySharedSession:
      type: object
      required:
      - id
      - contentId
      - contentTitle
      - status
      - startedAt
      - endedAt
      - createdAt
      - modeUsed
      - sessionKind
      - parentSessionId
      additionalProperties: false
      properties:
        id:
          type: integer
        contentId:
          type: integer
        contentTitle:
          type:
          - string
          - 'null'
        status:
          type: string
        startedAt:
          type:
          - string
          - 'null'
          format: date-time
        endedAt:
          type:
          - string
          - 'null'
          format: date-time
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        modeUsed:
          type:
          - string
          - 'null'
        sessionKind:
          type: string
        parentSessionId:
          type:
          - integer
          - 'null'
    RoleplaySharedMessage:
      type: object
      required:
      - id
      - role
      - personaId
      - personaName
      - content
      - position
      - metadata
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        role:
          type: string
        personaId:
          type:
          - integer
          - 'null'
        personaName:
          type:
          - string
          - 'null'
        content:
          type:
          - string
          - 'null'
        position:
          type: integer
        metadata:
          description: Free-form JSON.
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
    DesignEditorAttachment:
      type: object
      description: A design-studio image — a stock default or a program upload — and
        its serving URL.
      required:
      - id
      - attachment_id
      - url
      - used_for
      - is_stock
      - is_default
      - linked_template_count
      additionalProperties: false
      properties:
        id:
          type:
          - integer
          - 'null'
          description: The attachment id (used to delete); null for a stock image.
        attachment_id:
          type:
          - integer
          - 'null'
          description: The id the design JSON references; null for a stock image.
        url:
          type: string
          description: The serving URL to render (an absolute stock URL or an env-relative
            upload path).
        used_for:
          type: string
          enum:
          - background_image
          - image
        is_stock:
          type: boolean
          description: True for a built-in image, false for a program upload.
        is_default:
          type: boolean
          description: True when the image is flagged a Chronus-only (global) default
            asset; the studio stars it. Stock images are always false.
        linked_template_count:
          type: integer
          description: Number of certificate templates currently using this image.
            Greater than zero blocks deletion — the studio shows an "in use by N templates"
            warning instead of a delete confirm. Always 0 for a stock image.
    DesignEditorConfig:
      type: object
      description: The certificate design studio editor configuration for one certificate
        type.
      required:
      - fonts
      - sizes
      - background_colors
      - background_images
      - clipart_images
      - merge_tags
      - default_content
      - super_console
      - max_upload_bytes
      - accepted_content_types
      - disallowed_file_name_pattern
      additionalProperties: false
      properties:
        fonts:
          type: array
          description: Selectable font families.
          items:
            type: string
        sizes:
          type: array
          description: Selectable font sizes (px, as strings).
          items:
            type: string
        background_colors:
          type: array
          description: The background colour palette (hex strings).
          items:
            type: string
        background_images:
          type: array
          description: Stock background image URLs.
          items:
            type: string
            format: uri
        clipart_images:
          type: array
          description: Stock clipart image URLs.
          items:
            type: string
            format: uri
        merge_tags:
          type: array
          description: The merge tags applicable to this certificate type.
          items:
            "$ref": "#/components/schemas/DesignEditorMergeTag"
        default_content:
          type: string
          description: The type's default Fabric design JSON, seeding a new template.
        super_console:
          type: boolean
          description: True when the caller is in the super console. Gates the studio's
            super-console-only affordances — the Chronus default-template toggle and
            the "make this a Chronus asset" upload option (both course-only). The
            server enforces both regardless.
        max_upload_bytes:
          type: integer
          description: Largest image the library accepts, in bytes. The client rejects
            an oversized file before uploading; the server enforces the same limit.
        accepted_content_types:
          type: array
          description: Content types the library accepts. The client rejects anything
            else before uploading; the server enforces the same list.
          items:
            type: string
        disallowed_file_name_pattern:
          type: string
          description: DISALLOWED_FILE_EXTENSIONS as a regular-expression source,
            matched case-insensitively against the file name. Served rather than duplicated
            so the client can't drift from the server's list; the server enforces
            it regardless.
    DesignEditorMergeTag:
      type: object
      description: A merge tag the editor can insert, with its label and a sample
        rendering.
      required:
      - tag
      - label
      - example
      additionalProperties: false
      properties:
        tag:
          type: string
          description: The tag name (inserted into the design as `{{tag}}`).
        label:
          type: string
          description: A human description of what the tag renders.
        example:
          type: string
          description: A program-contextual sample of the rendered value.
        custom_style:
          type: object
          description: Optional default style/placement overrides for the tag on the
            canvas.
          additionalProperties: true
    GoalExemplar:
      type: object
      required:
      - id
      - text
      - whyStrong
      - profileLabel
      additionalProperties: false
      properties:
        id:
          type: integer
        text:
          type: string
        whyStrong:
          type:
          - string
          - 'null'
        profileLabel:
          type:
          - string
          - 'null'
    MemberContextOnboardingStatus:
      type: object
      required:
      - onboarding_completed_at
      additionalProperties: false
      properties:
        onboarding_completed_at:
          type:
          - string
          - 'null'
          description: |
            Stored as a free-form string, NOT a validated timestamp —
            MemberContext::Schema declares it Types::String.optional and the endpoint writes
            whatever the client sends. Declared as `string` rather than `date-time` on purpose:
            claiming a format the write path does not enforce would be a false contract.
    MemberContextUpdateRequest:
      type: object
      description: |
        Only these two keys are permitted, and the object is closed. ⚠️ This DIVERGES from source,
        knowingly: source dropped an unknown key at `params.permit` and still returned 200 (a
        silent no-op), whereas here the always-on request validator (`engine.rb:16`) rejects the
        body with a 400 before the action runs. Closure is dest's house rule for request bodies —
        the spec is an allowlist — and the only known client sends exactly these two keys
        (SOURCE's `packages/rumi/…/MeetRumiOverlay.tsx:31-34` — not findable on dest, whose
        `packages/rumi` is 27 seed JSONs and no SPA code; it omits the optional key rather than
        nulling it).
      additionalProperties: false
      properties:
        onboarding_completed_at:
          type:
          - string
          - 'null'
        rumi_first_time_preference:
          type:
          - string
          - 'null'
    MentorBriefsListResponse:
      type: object
      required:
      - briefs
      additionalProperties: false
      properties:
        briefs:
          type: array
          items:
            "$ref": "#/components/schemas/MentorBriefSummary"
    MentorBriefResponse:
      type: object
      required:
      - brief
      additionalProperties: false
      properties:
        brief:
          "$ref": "#/components/schemas/MentorBriefDetail"
    MentorBriefSummary:
      type: object
      required:
      - id
      - member_id
      - member_name
      - mentor_id
      - scheduled_session_id
      - cycle_id
      - generated_at
      - delivered_at
      - aasm_state
      - created_at
      additionalProperties: false
      properties:
        id:
          type: integer
        member_id:
          type: integer
          description: The MENTEE the brief is about — not the caller, who is `mentor_id`.
        member_name:
          type:
          - string
          - 'null'
          description: |
            Joined first/last name, null when the mentee record is missing or both names are
            blank. Not localized and not a display-name helper — assembled in the controller.
        mentor_id:
          type: integer
        scheduled_session_id:
          type:
          - integer
          - 'null'
        cycle_id:
          type:
          - string
          - 'null'
          description: Denormalized cycle UUID for per-cycle aggregation without a
            join.
        generated_at:
          type:
          - string
          - 'null'
          format: date-time
        delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        aasm_state:
          type: string
          enum:
          - pending
          - generated
          - delivered
          - failed
        created_at:
          type: string
          format: date-time
    MentorBriefDetail:
      type: object
      required:
      - id
      - member_id
      - member_name
      - mentor_id
      - scheduled_session_id
      - cycle_id
      - generated_at
      - delivered_at
      - aasm_state
      - created_at
      - content
      additionalProperties: false
      properties:
        id:
          type: integer
        member_id:
          type: integer
        member_name:
          type:
          - string
          - 'null'
        mentor_id:
          type: integer
        scheduled_session_id:
          type:
          - integer
          - 'null'
        cycle_id:
          type:
          - string
          - 'null'
        generated_at:
          type:
          - string
          - 'null'
          format: date-time
        delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        aasm_state:
          type: string
          enum:
          - pending
          - generated
          - delivered
          - failed
        created_at:
          type: string
          format: date-time
        content:
          type:
          - object
          - 'null'
          description: |
            The generated brief body (a JSON column). Free-form: its shape is owned by
            MentorBriefService, which evolves independently of this contract.
          additionalProperties: true
    CycleSummary:
      type: object
      required:
      - cycle_id
      - conversation_id
      - title
      - started_at
      - has_brief
      - has_auto_update
      - commit_count
      additionalProperties: false
      properties:
        cycle_id:
          type:
          - string
          - 'null'
        conversation_id:
          type: integer
        title:
          type:
          - string
          - 'null'
        started_at:
          type:
          - string
          - 'null'
          format: date-time
        has_brief:
          type: boolean
        has_auto_update:
          type: boolean
        commit_count:
          type: integer
    DevelopmentRecord:
      type: object
      required:
      - cycle_id
      - mentee_member_id
      - conversation
      - commitments
      - briefs
      - notes_logged
      - auto_updates
      - roleplay_sessions
      additionalProperties: false
      properties:
        cycle_id:
          type: string
        mentee_member_id:
          type: integer
        conversation:
          oneOf:
          - "$ref": "#/components/schemas/ConversationFull"
          - "$ref": "#/components/schemas/DevelopmentRecordConversationSummary"
        commitments:
          type: array
          items:
            "$ref": "#/components/schemas/CommitmentView"
        briefs:
          type: array
          items:
            oneOf:
            - "$ref": "#/components/schemas/BriefFull"
            - "$ref": "#/components/schemas/BriefSummary"
        notes_logged:
          type:
          - string
          - 'null'
          enum:
          - logged
          - none
          - null
          description: "`null` in the `summary` view (notes are mentor-private, shown
            via a separate surface)."
        auto_updates:
          type: array
          items:
            oneOf:
            - "$ref": "#/components/schemas/AutoUpdateFull"
            - "$ref": "#/components/schemas/AutoUpdateSummary"
        roleplay_sessions:
          type: array
          items:
            "$ref": "#/components/schemas/RoleplaySessionCycleView"
    ConversationFull:
      type: object
      description: The `full` (mentee) view — includes the raw message transcript.
      required:
      - id
      - title
      - started_at
      - current_skill
      - messages
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        started_at:
          type:
          - string
          - 'null'
          format: date-time
        current_skill:
          description: Free-form JSON.
        messages:
          type: array
          items:
            "$ref": "#/components/schemas/MessageView"
    DevelopmentRecordConversationSummary:
      type: object
      description: The `summary` (mentor) view — NEVER includes raw message content,
        only a count.
      required:
      - id
      - title
      - started_at
      - message_count
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type:
          - string
          - 'null'
        started_at:
          type:
          - string
          - 'null'
          format: date-time
        message_count:
          type: integer
    MessageView:
      type: object
      required:
      - id
      - role
      - content
      - position
      - current_skill
      - occurred_at
      additionalProperties: false
      properties:
        id:
          type: integer
        role:
          type: string
          enum:
          - user
          - assistant
        content:
          type:
          - string
          - 'null'
        position:
          type:
          - integer
          - 'null'
        current_skill:
          description: Free-form JSON.
        occurred_at:
          type:
          - string
          - 'null'
          format: date-time
    CommitmentView:
      type: object
      required:
      - id
      - text
      - target_time
      - completed_at
      additionalProperties: false
      properties:
        id:
          type: integer
        text:
          type:
          - string
          - 'null'
        target_time:
          type:
          - string
          - 'null'
          format: date-time
        completed_at:
          type:
          - string
          - 'null'
          format: date-time
    BriefFull:
      type: object
      required:
      - id
      - generated_at
      - delivered_at
      - aasm_state
      - content
      additionalProperties: false
      properties:
        id:
          type: integer
        generated_at:
          type:
          - string
          - 'null'
          format: date-time
        delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        aasm_state:
          type: string
        content:
          description: Free-form JSON.
    BriefSummary:
      type: object
      required:
      - id
      - generated_at
      - delivered_at
      - aasm_state
      - content_summary
      additionalProperties: false
      properties:
        id:
          type: integer
        generated_at:
          type:
          - string
          - 'null'
          format: date-time
        delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        aasm_state:
          type: string
        content_summary:
          type:
          - string
          - 'null'
          description: Truncated to 180 chars — derived from `content["starting_state"]`.
    AutoUpdateFull:
      type: object
      required:
      - id
      - email_delivered_at
      - in_app_delivered_at
      - content
      additionalProperties: false
      properties:
        id:
          type: integer
        email_delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        in_app_delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        content:
          description: Free-form JSON.
    AutoUpdateSummary:
      type: object
      required:
      - id
      - email_delivered_at
      - in_app_delivered_at
      - content_preview
      additionalProperties: false
      properties:
        id:
          type: integer
        email_delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        in_app_delivered_at:
          type:
          - string
          - 'null'
          format: date-time
        content_preview:
          type:
          - string
          - 'null'
          description: Truncated to 180 chars — derived from `content["outcome"]`.
    RoleplaySessionCycleView:
      type: object
      required:
      - id
      - content_id
      - session_kind
      - status
      - started_at
      - ended_at
      additionalProperties: false
      properties:
        id:
          type: integer
        content_id:
          type: integer
        session_kind:
          type:
          - string
          - 'null'
        status:
          type: string
        started_at:
          type:
          - string
          - 'null'
          format: date-time
        ended_at:
          type:
          - string
          - 'null'
          format: date-time
    MentorNoteSummary:
      type: object
      required:
      - id
      - member_id
      - mentor_id
      - cycle_id
      - aasm_state
      - created_at
      additionalProperties: false
      properties:
        id:
          type: integer
        member_id:
          type: integer
        mentor_id:
          type: integer
        cycle_id:
          type:
          - string
          - 'null'
        aasm_state:
          type: string
        created_at:
          type:
          - string
          - 'null'
          format: date-time
    PortedBareErrors:
      type: object
      required:
      - errors
      additionalProperties: false
      properties:
        errors:
          type: array
          items:
            type: string
    InboxMessageThread:
      type: object
      required:
      - id
      - subject
      - senderId
      - senderName
      - senderImageUrl
      - snippet
      - replyCount
      - unread
      - messageType
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        subject:
          type: string
          nullable: true
        senderId:
          type: integer
          nullable: true
        senderName:
          type: string
          nullable: true
          description: The sender member's name, falling back to the stored `sender_name`
            for an unregistered sender.
        senderImageUrl:
          type: string
          nullable: true
        snippet:
          type: string
          nullable: true
          description: Body truncated to 100 characters.
        replyCount:
          type: integer
        unread:
          type: boolean
        messageType:
          "$ref": "#/components/schemas/InboxMessageType"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
          description: Latest activity in the thread; the root's createdAt when there
            are no replies.
    InboxMessage:
      type: object
      required:
      - id
      - senderId
      - senderName
      - senderImageUrl
      - subject
      - body
      - isOwn
      - messageType
      - createdAt
      additionalProperties: false
      properties:
        id:
          type: integer
        senderId:
          type: integer
          nullable: true
        senderName:
          type: string
          nullable: true
        senderImageUrl:
          type: string
          nullable: true
        subject:
          type: string
          nullable: true
        body:
          type: string
          nullable: true
        isOwn:
          type: boolean
        messageType:
          "$ref": "#/components/schemas/InboxMessageType"
        createdAt:
          type: string
          format: date-time
    InboxMessageType:
      type: string
      description: |
        Collapses `AdminMessage::MessageType`'s constants to three wire values.
        `ADMIN_TO_UNREGISTERED_USER` and any unmapped future type render as `admin_to_user`, so this
        enum is narrower than the underlying column.
      enum:
      - user_to_admin
      - admin_to_user
      - auto_email
    MyMentorResponse:
      type: object
      description: |
        `preferences` and `skills` are absent (not null) when `mentor` is null, because the
        controller returns a one-key body in that branch — hence only `mentor` is required.
      required:
      - mentor
      additionalProperties: false
      properties:
        mentor:
          anyOf:
          - "$ref": "#/components/schemas/MyMentorAgent"
          - type: 'null'
        preferences:
          "$ref": "#/components/schemas/MyMentorPreferences"
        skills:
          type: array
          items:
            "$ref": "#/components/schemas/MyMentorSkill"
    MyMentorAgent:
      type: object
      description: |
        A deliberately NARROW agent projection — six fields. The eight sensitive virtuals decision J
        restricted (config, access_control, mentor_agent_config, tool_config, admin_metadata,
        skill_domains, tool_allowlist, plus prompt data) are NOT here and must not be added: this is
        a member-facing surface with no admin gate.
      required:
      - id
      - name
      - description
      - tagline
      - persona
      - category
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        tagline:
          type:
          - string
          - 'null'
        persona:
          type:
          - object
          - 'null'
          additionalProperties: true
        category:
          type:
          - string
          - 'null'
    MyMentorPreferences:
      type: object
      required:
      - preferences
      - context
      - onboarded
      additionalProperties: false
      properties:
        preferences:
          type: object
          description: |
            AgentMemberPreference::DEFAULT_PREFERENCES merged with the stored row, so the three
            default keys are always present. Free-form beyond that — the set is not enumerated in
            the model, only defaulted.
          additionalProperties: true
        context:
          type: object
          description: Member-supplied context. `custom_instructions` is length-validated
            at 500.
          additionalProperties: true
        onboarded:
          type: boolean
    MyMentorSkill:
      type: object
      description: |
        One attached skill, flattened from the SkillAttachment and its Skill. `id` is the
        ATTACHMENT id and `package_id` the Skill id — a client keying off `id` to identify the
        skill itself will be wrong.
        `package_id` is DELIBERATELY NOT source's spelling: source rendered `packageId`, the lone
        camelCase key on a snake_case API. Renamed here while the endpoint had zero clients.
      required:
      - id
      - package_id
      - name
      - key
      - description
      - position
      - category
      - suggestions
      - details
      - goals
      - difficulty
      - duration
      additionalProperties: false
      properties:
        id:
          type: integer
        package_id:
          type: integer
        name:
          type: string
        key:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        position:
          type: integer
        category:
          description: Read from the Skill's free-form `metadata` JSON, so untyped
            by construction.
        suggestions:
          description: From `metadata`. Untyped.
        details:
          description: From `metadata`. Untyped.
        goals:
          description: From `metadata`. Untyped.
        difficulty:
          description: From `metadata`. Untyped.
        duration:
          description: From `metadata`. Untyped.
    MyMentorPreferencesUpdateRequest:
      type: object
      description: |
        The two VALUES stay open (`params.permit(preferences: {}, context: {})` permits arbitrary
        hashes, so closing them would 400 legitimate writes at the always-on request validator);
        the ENVELOPE is closed, which knowingly DIVERGES from source — source dropped an unknown
        top-level key at `permit` and still returned 200, whereas here it is rejected with a 400
        before the action runs. Closure is dest's house rule for request bodies. Neither action
        requires either key: both fall back (PUT merges nothing, onboarding assigns the defaults).
      additionalProperties: false
      properties:
        preferences:
          type: object
          additionalProperties: true
        context:
          type: object
          additionalProperties: true
    OnboardingFlowImage:
      type: object
      description: A stored onboarding `Layout::Image` and its serving URL.
      required:
      - id
      - image_type
      - image_file_name
      - image_content_type
      - image_file_size
      - abstract_program_id
      - url
      additionalProperties: false
      properties:
        id:
          type: integer
        image_type:
          type: integer
          enum:
          - 1
          - 2
          description: 1 = background, 2 = profile.
        image_file_name:
          type: string
        image_content_type:
          type: string
        image_file_size:
          type: integer
        abstract_program_id:
          type:
          - integer
          - 'null'
          description: The owning organization id; null for a global image.
        url:
          type:
          - string
          - 'null'
          description: The serving URL (webp-optimized when available).
    OnboardingTestimonialPhoto:
      type: object
      description: A temp-parked testimonial avatar, addressed by the file_name/code
        pair.
      required:
      - file_name
      - code
      - preview_url
      additionalProperties: false
      properties:
        file_name:
          type: string
          description: The original filename; echo it back on the flow update.
        code:
          type: string
          description: Opaque handle for the parked file; echo it back on the flow
            update.
        preview_url:
          type:
          - string
          - 'null'
          description: Short-lived URL for previewing the upload before it is persisted.
    OrgGoalLadder:
      type: object
      required:
      - orgGoals
      - visibleDepth
      - truncated
      - boundaryUnitName
      additionalProperties: false
      properties:
        orgGoals:
          type: array
          items:
            "$ref": "#/components/schemas/OrgGoalLadderItem"
        visibleDepth:
          type: integer
        truncated:
          type: boolean
        boundaryUnitName:
          type:
          - string
          - 'null'
    OrgGoalLadderItem:
      type: object
      required:
      - id
      - title
      - description
      - unitName
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type:
          - string
          - 'null'
        unitName:
          type:
          - string
          - 'null'
    ProgramCommunityResponse:
      type: object
      description: All six keys are OPTIONAL at the top level — see the per-key omission
        rules above.
      additionalProperties: false
      properties:
        forums:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramCommunityForumTopic"
        qa:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramCommunityQaTopic"
        articles:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramCommunityArticle"
        events:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramCommunityEvent"
        resources:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramCommunityResource"
        learning:
          type: array
          description: |
            Heterogeneous — a `training` row and a `certificate` row share no fields but `id`,
            `title` and `type`. Never both required at once, so declared as a `oneOf` per item
            rather than one object with optional fields (the port's all-required-fields convention).
          items:
            oneOf:
            - "$ref": "#/components/schemas/ProgramCommunityTrainingPlan"
            - "$ref": "#/components/schemas/ProgramCommunityCertificate"
    ProgramCommunityForumTopic:
      type: object
      required:
      - id
      - title
      - author
      - replies
      - last_activity
      - pinned
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
        author:
          type: string
          description: '"Unknown" when the topic''s user or their member association
            is absent.'
        replies:
          type: integer
        last_activity:
          type: string
          description: Relative ("5m ago", "3h ago", "2d ago") or an absolute "%b
            %d" past 7 days.
        pinned:
          type: boolean
    ProgramCommunityQaTopic:
      type: object
      required:
      - id
      - question
      - author
      - answers
      - accepted
      additionalProperties: false
      properties:
        id:
          type: string
        question:
          type: string
        author:
          type: string
        answers:
          type: integer
        accepted:
          type: boolean
          description: A hardcoded local, never actually computed from any answer's
            state. ALWAYS false.
    ProgramCommunityArticle:
      type: object
      required:
      - id
      - title
      - author
      - read_time
      - likes
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
          description: '"Untitled" when the article has no content row.'
        author:
          type: string
        read_time:
          type: string
          description: '"<n> min", n = ceil(word_count / 200), minimum 1.'
        likes:
          type: integer
    ProgramCommunityEvent:
      type: object
      required:
      - id
      - title
      - date
      - attendees
      - rsvpd
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
        date:
          type: string
          description: Formatted `"%b %d, %Y at %I:%M %p"`.
        attendees:
          type: integer
        rsvpd:
          type: boolean
          description: Whether the CALLER specifically has an accepted invite to this
            event.
    ProgramCommunityResource:
      type: object
      required:
      - id
      - title
      - type
      - added_by
      - date
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
        type:
          type: string
          description: |
            ALWAYS "doc" on this tree — verified: `Resource#content_type` does not exist (no such
            column or method), so `resource.try(:content_type)` is always nil and `.presence`
            falls through to the literal.
        added_by:
          type: string
          description: |
            ALWAYS "Admin" on this tree — verified: `Resource#member` does not exist either, same
            `try` shape as `type`.
        date:
          type: string
          description: Formatted `"%b %d, %Y"`.
    ProgramCommunityTrainingPlan:
      type: object
      description: One synthetic row summarizing ALL of the caller's mentoring-model
        tasks across their active-group connections in this program — not per-connection,
        one row total.
      required:
      - id
      - title
      - type
      - progress
      - due_date
      additionalProperties: false
      properties:
        id:
          type: string
          description: Constant literal `"training-plan"` — not a real record id.
        title:
          type: string
          description: Constant literal `"Mentoring Plan"`.
        type:
          type: string
          const: training
        progress:
          type: integer
          description: Percentage of the caller's tasks with status DONE, rounded.
        due_date:
          type:
          - string
          - 'null'
          description: |
            The latest due date among the caller's tasks that HAVE one, formatted `"%b %d, %Y"`.
            Null when every task is due-date-less (`maximum` over an empty/all-nil set).
    ProgramCommunityCertificate:
      type: object
      description: "\U0001F534 UNREACHABLE ON THIS TREE — see the top-level description.
        Declared from the code for\ncompleteness (so a future fix that reconnects
        `connection_certificates` doesn't need a new\nschema), not because any response
        can produce it today.\n"
      required:
      - id
      - title
      - type
      - earned_date
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
          description: '"Certificate" when the cert has no linked template.'
        type:
          type: string
          const: certificate
        earned_date:
          type: string
          description: Formatted `"%b %d, %Y"`.
    ConnectionActivitiesResponse:
      type: object
      required:
      - activities
      - meta
      additionalProperties: false
      properties:
        activities:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionActivity"
        meta:
          "$ref": "#/components/schemas/ConnectionActivitiesMeta"
    ConnectionActivity:
      type: object
      required:
      - id
      - action_type
      - created_at
      - user_id
      additionalProperties: false
      properties:
        id:
          type: integer
        action_type:
          type: integer
        created_at:
          type: string
          description: ISO8601.
        user_id:
          type: integer
    ConnectionActivitiesMeta:
      type: object
      required:
      - page
      - per_page
      - total
      - total_pages
      additionalProperties: false
      properties:
        page:
          type: integer
        per_page:
          type: integer
        total:
          type: integer
        total_pages:
          type: integer
    ConnectionBudget:
      type: object
      required:
      - id
      - amount
      - start_time
      - end_time
      - frozen
      - updated_at
      additionalProperties: false
      properties:
        id:
          type: integer
        amount:
          type:
          - number
          - 'null'
          description: |
            DB-nullable (`decimal`, no `NOT NULL`); a persisted direct budget is validated
            present on save (`Budget#root?` — every one of a connection's OWN `direct_budgets` is
            root, no `parent_id`), but declared nullable rather than assumed given no controller
            here handles a null case explicitly.
        start_time:
          type:
          - string
          - 'null'
          format: date-time
        end_time:
          type:
          - string
          - 'null'
          format: date-time
        frozen:
          type: boolean
          description: Whether `frozen_at` is present — a frozen budget's period has
            been closed out.
        updated_at:
          type: string
          format: date-time
    ConnectionSummary:
      type: object
      required:
      - id
      - name
      - status
      - member_count
      - created_at
      - expiry_time
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        status:
          type: string
          enum:
          - active
          - inactive
          - closed
          - drafted
          - pending
          - proposed
          - rejected
          - withdrawn
          - unknown
        member_count:
          type: integer
        created_at:
          type: string
          description: ISO8601.
        expiry_time:
          type:
          - string
          - 'null'
          description: ISO8601.
    ConnectionDetail:
      type: object
      required:
      - id
      - name
      - description
      - status
      - read_only
      - created_at
      - expiry_time
      - members
      - task_stats
      - next_meeting
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - active
          - inactive
          - closed
          - drafted
          - pending
          - proposed
          - rejected
          - withdrawn
          - unknown
        read_only:
          type: boolean
          description: "`true` iff `closed?`."
        created_at:
          type: string
          description: ISO8601.
        expiry_time:
          type:
          - string
          - 'null'
          description: ISO8601.
        members:
          type: array
          items:
            type: object
            required:
            - user_id
            - member_id
            - name
            - role
            - avatar_url
            additionalProperties: false
            properties:
              user_id:
                type:
                - integer
                - 'null'
              member_id:
                type:
                - integer
                - 'null'
              name:
                type:
                - string
                - 'null'
              role:
                type:
                - string
                - 'null'
              avatar_url:
                type:
                - string
                - 'null'
        task_stats:
          type: object
          required:
          - total
          - completed
          - progress
          additionalProperties: false
          properties:
            total:
              type: integer
            completed:
              type: integer
            progress:
              type: integer
              description: Percent, 0 when `total` is 0.
        next_meeting:
          type:
          - object
          - 'null'
          required:
          - id
          - start_time
          - topic
          additionalProperties: false
          properties:
            id:
              type: integer
            start_time:
              type: string
              description: ISO8601.
            topic:
              type: string
    ConnectionExpensesResponse:
      type: object
      required:
      - expenses
      - meta
      additionalProperties: false
      properties:
        expenses:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionExpense"
        meta:
          type: object
          required:
          - page
          - per_page
          - total
          - total_pages
          additionalProperties: false
          properties:
            page:
              type: integer
            per_page:
              type: integer
            total:
              type: integer
            total_pages:
              type: integer
    ConnectionExpense:
      type: object
      required:
      - id
      - description
      - status
      - requested_amount
      - approved_amount
      - spent_amount
      - requested_at
      - requested_by
      - created_at
      additionalProperties: false
      properties:
        id:
          type: integer
        description:
          type: string
        status:
          type: integer
        requested_amount:
          type: number
        approved_amount:
          type:
          - number
          - 'null'
        spent_amount:
          type:
          - number
          - 'null'
        requested_at:
          type: string
          description: ISO8601.
        requested_by:
          type:
          - string
          - 'null'
          description: "`requested_by_member&.name(name_only: true)` — null only if
            the association is absent."
        created_at:
          type: string
          description: ISO8601.
    ConnectionExpenseCreateRequest:
      type: object
      additionalProperties: false
      properties:
        description:
          type: string
        requested_amount:
          type: number
    PortedValidationErrorsHash:
      type: object
      description: |
        Rails' native `ActiveModel::Errors#as_json` shape — `{field: [message, ...]}` — NOT this
        API's `{error: {code, message, details: [{field, message}]}}` envelope. Declared as its
        own open shape (field names are dynamic) rather than normalized, since the wire is what it
        is regardless of house convention (S4's finding).
      additionalProperties:
        type: array
        items:
          type: string
    ConnectionTopic:
      type: object
      required:
      - id
      - title
      - body
      - user_id
      - posts_count
      - created_at
      - updated_at
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        body:
          type:
          - string
          - 'null'
        user_id:
          type:
          - integer
          - 'null'
        posts_count:
          type: integer
        created_at:
          type: string
          description: ISO8601.
        updated_at:
          type: string
          description: ISO8601.
    ConnectionPost:
      type: object
      required:
      - id
      - body
      - user_id
      - topic_id
      - created_at
      additionalProperties: false
      properties:
        id:
          type: integer
        body:
          type:
          - string
          - 'null'
        user_id:
          type:
          - integer
          - 'null'
        topic_id:
          type: integer
        created_at:
          type: string
          description: ISO8601.
    ConnectionMeetingsResponse:
      type: object
      required:
      - meetings
      - meta
      additionalProperties: false
      properties:
        meetings:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionMeeting"
        meta:
          type: object
          required:
          - page
          - per_page
          - total
          - total_pages
          additionalProperties: false
          properties:
            page:
              type: integer
            per_page:
              type: integer
            total:
              type: integer
            total_pages:
              type: integer
    ConnectionMeeting:
      type: object
      required:
      - id
      - topic
      - start_time
      - owner_id
      - program_id
      - group_id
      additionalProperties: false
      properties:
        id:
          type: integer
        topic:
          type: string
        start_time:
          type:
          - string
          - 'null'
          description: ISO8601.
        owner_id:
          type: integer
        program_id:
          type: integer
        group_id:
          type:
          - integer
          - 'null'
    ConnectionMeetingCreateRequest:
      type: object
      required:
      - topic
      - start_time
      additionalProperties: false
      properties:
        topic:
          type: string
        start_time:
          type: string
          description: ISO8601. Rejected (422) if unparseable or in the past.
        end_time:
          type: string
          description: ISO8601. Accepted but never read — see the operation description.
    ConnectionMember:
      type: object
      required:
      - user_id
      - member_id
      - name
      - role
      - role_id
      - owner
      - avatar_url
      additionalProperties: false
      properties:
        user_id:
          type:
          - integer
          - 'null'
        member_id:
          type:
          - integer
          - 'null'
        name:
          type:
          - string
          - 'null'
        role:
          type:
          - string
          - 'null'
        role_id:
          type:
          - integer
          - 'null'
        owner:
          type: boolean
        avatar_url:
          type:
          - string
          - 'null'
    ConnectionReflection:
      type: object
      required:
      - id
      - content
      - trigger_type
      - mood
      - themes
      - summary
      - occurred_at
      additionalProperties: false
      properties:
        id:
          type: integer
        content:
          type:
          - string
          - 'null'
          description: Truncated to 300 characters.
        trigger_type:
          type:
          - string
          - 'null'
        mood:
          type:
          - number
          - 'null'
        themes:
          type: array
          items:
            type: string
        summary:
          type:
          - string
          - 'null'
        occurred_at:
          type:
          - string
          - 'null'
          description: ISO8601.
    ConnectionResourcesResponse:
      type: object
      required:
      - resources
      - meta
      additionalProperties: false
      properties:
        resources:
          type: array
          description: ALWAYS empty on this tree — see the top-level description.
          items: {}
        meta:
          type: object
          description: ALWAYS the same literal values — see the top-level description.
          required:
          - page
          - per_page
          - total
          - total_pages
          additionalProperties: false
          properties:
            page:
              type: integer
              const: 1
            per_page:
              type: integer
              const: 25
            total:
              type: integer
              const: 0
            total_pages:
              type: integer
              const: 0
    ConnectionSurveysResponse:
      type: object
      required:
      - surveys
      additionalProperties: false
      properties:
        surveys:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionSurvey"
    ConnectionSurvey:
      type: object
      required:
      - id
      - name
      - completed_at
      - is_draft
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        completed_at:
          type:
          - string
          - 'null'
          description: ISO8601. Null when this connection has no response on file
            for this survey.
        is_draft:
          type:
          - boolean
          - 'null'
          description: |
            Always `false` or `null` in practice — see the top-level description. Kept on the
            wire rather than dropped.
    ConnectionTasksReorderResponse:
      type: object
      required:
      - tasks
      additionalProperties: false
      properties:
        tasks:
          type: array
          items:
            "$ref": "#/components/schemas/ConnectionTask"
    ConnectionTask:
      type: object
      required:
      - id
      - title
      - description
      - status
      - due_date
      - milestone_id
      - goal_id
      - assignee_id
      - position
      - completed_date
      - completed_by
      additionalProperties: false
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type:
          - string
          - 'null'
        status:
          type: string
          enum:
          - done
          - todo
        due_date:
          type:
          - string
          - 'null'
          description: ISO8601.
        milestone_id:
          type:
          - integer
          - 'null'
        goal_id:
          type:
          - integer
          - 'null'
        assignee_id:
          type:
          - integer
          - 'null'
          description: The task's `connection_membership_id`, renamed for the wire.
        position:
          type: integer
        completed_date:
          type:
          - string
          - 'null'
          description: ISO8601.
        completed_by:
          type:
          - integer
          - 'null'
    SparkContentResponse:
      type: object
      description: |
        ⚠️ camelCase, breaking this API's snake_case rule — the SHARED
        `Content::Responses::Writer.as_json` shape (see `index`'s description). Do NOT "fix" the
        casing here without also changing the admin surface this service backs.
      required:
      - id
      - stepId
      - userId
      - fieldName
      - value
      - createdAt
      - updatedAt
      additionalProperties: false
      properties:
        id:
          type: integer
        stepId:
          type: integer
        userId:
          type: integer
        fieldName:
          type: string
        value:
          description: Free-form — mirrors whatever was submitted (`create`'s request
            `value`).
        createdAt:
          type:
          - string
          - 'null'
          format: date-time
        updatedAt:
          type:
          - string
          - 'null'
          format: date-time
    SparkCompletionResult:
      type: object
      description: "\U0001F534 `score` is genuinely OMITTED from the wire response
        (not merely nullable) when the\ncontent's scoring mode is `\"none\"` or `show_score_to_user`
        is off — the controller only\nsets the key at all inside that conditional.
        Declared NOT required rather than nullable to\ndescribe the true wire shape,
        an intentional, narrow deviation from this file's usual\n\"shapes are all-required\"
        convention.\n"
      required:
      - already_completed
      - completed_at
      additionalProperties: false
      properties:
        already_completed:
          type: boolean
        completed_at:
          type:
          - string
          - 'null'
          format: date-time
        score:
          type: object
          required:
          - raw
          - max
          - percent
          - passed
          additionalProperties: false
          properties:
            raw:
              type:
              - number
              - 'null'
            max:
              type:
              - number
              - 'null'
            percent:
              type:
              - number
              - 'null'
            passed:
              type:
              - boolean
              - 'null'
    ProgramOverviewResponse:
      type: object
      required:
      - program
      - progress
      - user_role
      - stats
      - announcements
      - next_actions
      additionalProperties: false
      properties:
        program:
          "$ref": "#/components/schemas/ProgramOverviewSummary"
        progress:
          type: integer
          description: |
            Percentage, rounded, of the CALLER's own tasks with status DONE — see the top-level
            description for #140's fix.
        user_role:
          type: string
          enum:
          - mentor
          - mentee
        stats:
          "$ref": "#/components/schemas/ProgramOverviewStats"
        announcements:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramOverviewAnnouncement"
        next_actions:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramOverviewNextAction"
    ProgramOverviewSummary:
      type: object
      required:
      - id
      - name
      - description
      - start_date
      - end_date
      - type
      - color
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
        start_date:
          type: string
          description: Formatted `"%b %d, %Y"`, e.g. "Jan 05, 2026". Always the program's
            created_at.
        end_date:
          type: string
          description: |
            Formatted the same way. `expiry_time` does not resolve on dest's Program (verified:
            `try` returns nil), so this is ALWAYS one year from today on this tree, never a real
            program expiry — inherited from a `try`-guarded fallback, not a bug to fix here.
        type:
          type: string
          description: |
            `engagement_type_name` does not resolve on dest either (same `try` guard), so this is
            ALWAYS the literal `"mentoring"` on this tree.
        color:
          type: string
          description: Hardcoded literal `"blue"` in the controller — not derived
            from any field.
    ProgramOverviewStats:
      type: object
      required:
      - connections
      - sessions_completed
      - hours_logged
      - tasks_pending
      additionalProperties: false
      properties:
        connections:
          type: integer
        sessions_completed:
          type: integer
        hours_logged:
          type: number
          description: Rounded to one decimal place.
        tasks_pending:
          type: integer
    ProgramOverviewAnnouncement:
      type: object
      required:
      - id
      - title
      - type
      - date
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
        type:
          type: string
          enum:
          - urgent
          - info
        date:
          type: string
          description: Formatted `"%b %d, %Y"`.
    ProgramOverviewNextAction:
      type: object
      description: |
        One row from three DIFFERENT sources (upcoming meetings, pending tasks, upcoming events)
        merged and re-sorted by date — `type` is the only field that tells them apart.
      required:
      - id
      - title
      - type
      - date
      additionalProperties: false
      properties:
        id:
          type: string
        title:
          type: string
        type:
          type: string
          enum:
          - session
          - task
          - event
        date:
          type:
          - string
          - 'null'
    ProgramPeopleResponse:
      type: object
      required:
      - people
      additionalProperties: false
      properties:
        people:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramPerson"
    ProgramPerson:
      type: object
      required:
      - id
      - name
      - title
      - avatar
      - role
      - expertise
      additionalProperties: false
      properties:
        id:
          type: string
        name:
          type: string
        title:
          type: string
          description: '"" when the org has no TEXT-type profile question, or the
            member never answered it.'
        avatar:
          type: string
          description: |
            NEVER null — `picture_url` returns a default placeholder asset for a member with no
            uploaded photo (member.rb:826), the same measured fact as `requests`'/`sessions`' avatar
            fields. Nothing here reads through a safe-nav the way those endpoints' partner fields
            do, so unlike them this one has NO nullable case at all.
        role:
          type: string
          enum:
          - mentor
          - mentee
        expertise:
          type: array
          items:
            type: string
          description: Up to 5 free-text answer values, pulled from up to 2 profile
            questions.
        availability:
          type: string
          enum:
          - high
          - medium
          - low
          description: OMITTED for a mentee row — see the top-level description.
        match_score:
          type: integer
          description: "\U0001F534 TEMPORARY PLACEHOLDER — see the top-level description.
            OMITTED unless the row is a\nmentor AND the caller is a mentee. `rand(60..95)`,
            not a real computed value.\n"
    ReflectionSummary:
      type: object
      required:
      - id
      - status
      - content
      - trigger_type
      - mood
      - visibility
      - themes
      - summary
      - prompt
      - context_type
      - context_id
      - occurred_at
      - message_count
      additionalProperties: false
      properties:
        id:
          type: integer
        status:
          type: string
        content:
          type:
          - string
          - 'null'
          description: Truncated to 200 characters.
        trigger_type:
          type:
          - string
          - 'null'
        mood:
          type:
          - number
          - 'null'
        visibility:
          type: string
          description: Defaults to `"private"` when unset.
        themes:
          type: array
          items:
            type: string
        summary:
          type:
          - string
          - 'null'
        prompt:
          type:
          - string
          - 'null'
        context_type:
          type: string
          enum:
          - Meeting
          - Content::Spark
          - Content::RoleplayAttempt
          - Member
          - Agent
        context_id:
          type: integer
        occurred_at:
          type:
          - string
          - 'null'
          description: ISO8601.
        message_count:
          type: integer
          description: Counter-cache of child interactions (replies).
    ProgramRequestsResponse:
      type: object
      required:
      - requests
      additionalProperties: false
      properties:
        requests:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramMemberRequest"
    ProgramMemberRequest:
      type: object
      required:
      - id
      - from
      - type
      - message
      - date
      additionalProperties: false
      properties:
        id:
          type: string
        from:
          type: object
          required:
          - name
          - title
          - avatar
          additionalProperties: false
          properties:
            name:
              type: string
            title:
              type: string
              description: Dead field — see the top-level description. Always `""`.
            avatar:
              type:
              - string
              - 'null'
        type:
          type: string
          enum:
          - mentor_request
          - mentor_offer
        message:
          type: string
        date:
          type: string
          description: Formatted `"%b %d, %Y"`.
    ProgramSessionsResponse:
      type: object
      required:
      - upcoming
      - past
      additionalProperties: false
      properties:
        upcoming:
          type: array
          items:
            "$ref": "#/components/schemas/ProgramUpcomingSession"
        past:
          type: array
          description: Capped at 20, most recent first (`sort_by { -start_time.to_i
            }.first(20)`).
          items:
            "$ref": "#/components/schemas/ProgramPastSession"
    ProgramUpcomingSession:
      type: object
      required:
      - id
      - date
      - partner
      - avatar
      - topic
      - type
      - meeting_url
      additionalProperties: false
      properties:
        id:
          type: string
        date:
          type: string
          description: ISO8601 `start_time`.
        partner:
          type: string
          description: The caller's counterpart in the meeting's group — the first
            mentor (if the caller is a mentee) or first student (if a mentor). "Unknown"
            when the group has none.
        avatar:
          type:
          - string
          - 'null'
        topic:
          type: string
          description: '`meeting.topic`, falling back to the literal "Meeting" when
            blank.'
        type:
          type: string
          enum:
          - in-person
          - virtual
          description: ALWAYS "in-person" on this tree — see the top-level description.
        meeting_url:
          type: 'null'
          description: ALWAYS null on this tree — see the top-level description.
    ProgramPastSession:
      type: object
      description: A DIFFERENT shape from ProgramUpcomingSession, not a subset — no
        `avatar`, and `notes`/`rating` replace `type`/`meeting_url`.
      required:
      - id
      - date
      - partner
      - topic
      - notes
      - rating
      additionalProperties: false
      properties:
        id:
          type: string
        date:
          type: string
          description: ISO8601 `start_time`.
        partner:
          type: string
        topic:
          type: string
        notes:
          type:
          - string
          - 'null'
          description: "`meeting.description` — a real, nullable column. Genuinely
            varies; contrast `rating`."
        rating:
          type: 'null'
          description: ALWAYS null on this tree — see the top-level description.
    ResourceStatsResetResponse:
      type: object
      required:
      - data
      - app_mode
      additionalProperties: false
      properties:
        data:
          type: object
          required:
          - type
          - reset_count
          additionalProperties: false
          properties:
            type:
              type: string
              const: resource_stats
            reset_count:
              type: integer
              description: How many resources had their counters reset. Zero when
                the library is empty.
        app_mode:
          "$ref": "#/components/schemas/AppMode"
    SessionDetail:
      type: object
      description: The caller's HTTP-session metadata (logout URL + CSRF token + idle
        auto-logout policy).
      required:
      - type
      - logout_url
      - csrf_token
      - super_console
      - auto_logout
      additionalProperties: false
      properties:
        type:
          const: session
        logout_url:
          type: string
        csrf_token:
          type: string
        super_console:
          type: boolean
          description: Whether the caller is in a super console session.
        auto_logout:
          type: object
          description: |
            Idle auto-logout policy for the SPA's idle timer. server_epoch lets the
            client correct clock skew when reading the shared expiry cookie; cookie
            names are server-issued (single source of truth) and must not be hardcoded.
          required:
          - enabled
          - warning_seconds
          - server_epoch
          - session_cookie
          - chat_cookie
          - video_call_cookie
          additionalProperties: false
          properties:
            enabled:
              type: boolean
            warning_seconds:
              type: integer
            server_epoch:
              type: integer
            session_cookie:
              type: string
            chat_cookie:
              type: string
            video_call_cookie:
              type: string
    ResourceEnvelopeSessionDetail:
      type: object
      required:
      - data
      additionalProperties: false
      properties:
        data:
          "$ref": "#/components/schemas/SessionDetail"
    RichTextEditorAttachment:
      type: object
      description: A stored rich-text-editor asset (image or file) and its serving
        URL.
      required:
      - id
      - url
      - name
      - content_type
      - byte_size
      - created_at
      - kind
      additionalProperties: false
      properties:
        id:
          type: integer
          description: The asset id (used by the browser to delete).
        url:
          type: string
          format: uri
          description: The org-scoped serving URL (`/ck_pictures/:id` or `/ck_attachments/:id`).
        name:
          type: string
          description: The stored file name.
        content_type:
          type: string
          nullable: true
          description: The stored MIME type.
        byte_size:
          type: integer
          nullable: true
          description: The stored file size in bytes.
        created_at:
          type: string
          format: date-time
          nullable: true
          description: When the asset was uploaded.
        kind:
          type: string
          enum:
          - image
          - file
    RumiContextResponse:
      type: object
      required:
      - member_id
      - fetched_at
      - sections
      additionalProperties: false
      properties:
        member_id:
          type: integer
        fetched_at:
          type: string
          format: date-time
        sections:
          type: array
          items:
            "$ref": "#/components/schemas/RumiContextSection"
    RumiContextSection:
      type: object
      description: |
        One display block. `key` is narrower than the path parameter's enum: only seven of the
        nine forgettable keys ever produce a section (monitoring_flags and parked_themes are
        erasable but never rendered), so listing all nine here would describe responses the
        controller cannot emit.
      required:
      - key
      - label
      - items
      - forgettable
      additionalProperties: false
      properties:
        key:
          type: string
          enum:
          - emotional_state
          - active_situation
          - relationship_layer
          - active_commitments
          - self_insights
          - observed_patterns
          - stated_preferences
        label:
          type: string
          description: |
            English display text built in the controller, NOT an i18n key — the transparency UI
            renders it verbatim. Flagged because it is a hardcoded-copy debt, not a design.
        items:
          type: array
          description: |
            Rendered lines for the section. Item type is unconstrained on purpose: six of the
            seven sections build strings, but `relationship_layer` reads `goals` straight out of
            an untyped Types::Hash, so its entries are whatever was written there.
          items: {}
        forgettable:
          type: boolean
    RumiContextForgetKeyResponse:
      type: object
      required:
      - ok
      - key
      additionalProperties: false
      properties:
        ok:
          type: boolean
        key:
          type: string
    RumiDataExportResponse:
      type: object
      required:
      - export
      additionalProperties: false
      properties:
        export:
          type: object
          required:
          - member_id
          - exported_at
          - audit_events
          - member_context
          additionalProperties: false
          properties:
            member_id:
              type: integer
            exported_at:
              type: string
              format: date-time
            audit_events:
              type: array
              description: Newest-first Rumi audit events for this member, capped
                at 10000 rows.
              items:
                "$ref": "#/components/schemas/RumiAuditEventEntry"
            member_context:
              type: object
              description: |
                The member's MemberContext::Schema snapshot, serialised with `to_h`. Free-form on
                purpose: the schema is versioned in the model layer, and pinning its keys here would
                make every context migration a spec-breaking change.
              additionalProperties: true
    RumiAuditEventEntry:
      type: object
      required:
      - id
      - event_name
      - metadata
      - occurred_at
      additionalProperties: false
      properties:
        id:
          type: integer
        event_name:
          type: string
        metadata:
          description: |
            The event's `metadata_json` column, verbatim. Per-event shape, so unconstrained — no
            `type` at all, which already admits null; a nullability keyword here would only
            narrow it.
        occurred_at:
          type: string
          format: date-time
    SessionValidateResponse:
      type: object
      description: |
        Minimal identity for a validated session. `role` is a coarse two-value flag derived from
        `Member#admin?` (org-level), NOT the request's resolved App-Mode and NOT a program role.
      required:
      - memberId
      - orgId
      - role
      - email
      - firstName
      - lastName
      additionalProperties: false
      properties:
        memberId:
          type: integer
        orgId:
          type: integer
        role:
          type: string
          enum:
          - admin
          - member
        email:
          type: string
        firstName:
          type:
          - string
          - 'null'
        lastName:
          type:
          - string
          - 'null'
    AnnouncementTestEmailResponse:
      type: object
      required:
      - data
      - app_mode
      additionalProperties: false
      properties:
        data:
          type: object
          required:
          - emails
          additionalProperties: false
          properties:
            emails:
              type: array
              description: The addresses actually mailed, normalised to lowercase
                and de-duplicated.
              items:
                type: string
        app_mode:
          "$ref": "#/components/schemas/AppMode"
    CampaignMessageTestEmailResponse:
      type: object
      required:
      - data
      - app_mode
      additionalProperties: false
      properties:
        data:
          type: object
          required:
          - emails
          additionalProperties: false
          properties:
            emails:
              type: array
              description: The addresses actually mailed, normalised to lowercase
                and de-duplicated.
              items:
                type: string
        app_mode:
          "$ref": "#/components/schemas/AppMode"
    SurveyReminderTestEmailResponse:
      type: object
      required:
      - data
      - app_mode
      additionalProperties: false
      properties:
        data:
          type: object
          required:
          - emails
          additionalProperties: false
          properties:
            emails:
              type: array
              description: The addresses actually mailed, normalised to lowercase
                and de-duplicated.
              items:
                type: string
        app_mode:
          "$ref": "#/components/schemas/AppMode"
    UploadTarget:
      type: object
      description: The constraints for one upload target.
      required:
      - name
      - max_bytes
      - accepted_content_types
      additionalProperties: false
      properties:
        name:
          type: string
          description: The upload target.
        max_bytes:
          type: integer
          description: The largest file the target accepts, in bytes.
        accepted_content_types:
          type: array
          description: Accepted MIME types. Empty when the target accepts any.
          items:
            type: string
    StagedUpload:
      type: object
      description: A staged file awaiting an owning record.
      required:
      - code
      - file_name
      - temp_s3_link
      - metadata
      additionalProperties: false
      properties:
        code:
          type: string
          description: The claim ticket. Send it back with `file_name` when saving
            the owning record.
        file_name:
          type: string
          description: The stored file name.
        temp_s3_link:
          type:
          - string
          - 'null'
          description: |
            A short-lived download link to the staged file. Non-null only for targets whose form
            needs to read the file back before the owning record exists.
        metadata:
          type: object
          description: |
            Values the target's validator derived from the file that the owning record needs at save
            time. Empty unless the target produces any.
          additionalProperties: true
    AnnouncementAudienceDetailsResponse:
      type: object
      description: |
        Envelope for the audience-details view (`/views/announcements/audience_details`). The generic
        `{group, name, blocks}` shape with a single `audience_details` block.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: announcement
        name:
          const: audience_details
        blocks:
          type: array
          items:
            "$ref": "#/components/schemas/AnnouncementAudienceDetailsBlock"
    AnnouncementAudienceDetailsBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: audience_details
        data:
          type: object
          required:
          - user_count
          - filters
          additionalProperties: false
          properties:
            user_count:
              type: integer
              description: How many users the view currently resolves to.
            filters:
              type: array
              description: |
                One entry per applied filter, empty when the view applies none. `label` is the filter's
                name and `value` its already-flattened one-line rendering, kept apart so the card can
                render them as two columns.
              items:
                type: object
                required:
                - label
                - value
                additionalProperties: false
                properties:
                  label:
                    type: string
                  value:
                    type: string
    AnnouncementComposeOptionsResponse:
      type: object
      description: |
        Envelope for the compose-options view (`/views/announcements/compose_options`). The generic
        `{group, name, blocks}` shape carrying an `email_delivery` and an `audience_options` block.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: announcement
        name:
          const: compose_options
        blocks:
          type: array
          items:
            oneOf:
            - "$ref": "#/components/schemas/AnnouncementEmailDeliveryBlock"
            - "$ref": "#/components/schemas/AnnouncementAudienceOptionsBlock"
    AnnouncementEmailDeliveryBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: email_delivery
        data:
          type: object
          required:
          - creation
          - update
          additionalProperties: false
          properties:
            creation:
              "$ref": "#/components/schemas/AnnouncementMailerState"
            update:
              "$ref": "#/components/schemas/AnnouncementMailerState"
    AnnouncementMailerState:
      type: object
      required:
      - disabled
      - template_url
      additionalProperties: false
      properties:
        disabled:
          type: boolean
          description: True when this program has the mailer template turned off —
            nothing is sent.
        template_url:
          type:
          - string
          - 'null'
          description: |
            Deep link to the mailer-template editor, keyed by the mailer's UID and scoped to the
            program unless the mailer is organization-level. Null when the caller may not reach the
            editor. Built server-side from the same route helper the legacy form links with.
    AnnouncementAudienceOptionsBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: audience_options
        data:
          type: object
          required:
          - segment_restricted
          additionalProperties: false
          properties:
            segment_restricted:
              type: boolean
              description: |
                True when the caller may only publish to user views they manage, so the All users
                and Specific roles audience modes must not be offered.
    DiscoverMembersViewResponse:
      type: object
      description: |
        Envelope for the `discover/members` view. Same `{group, name, blocks}` shape as every
        view (ApplicationViewBuilder#render), with `group`/`name` as consts so a
        client can discriminate this view from its siblings.

        `blocks[].data` is deliberately an OPEN object: the SPA resolves blocks
        through a component registry keyed by `type` + `variant`, so layout lives
        in that registry, not the response. Enumerating each block payload here
        would be wrong by the next unit that adds a block.

        `ApplicationViewBuilder#extra` (default `{}`) is intentionally not modelled —
        no builder overrides it; one that does should extend this schema.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: discover
        name:
          const: members
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            additionalProperties: false
            properties:
              type:
                enum:
                - members
              variant:
                type: string
              title:
                type: string
              data:
                type: object
                additionalProperties: true
    DiscoverProgramsViewResponse:
      type: object
      description: |
        Envelope for the `discover/programs` view. Same `{group, name, blocks}` shape as every
        view (ApplicationViewBuilder#render), with `group`/`name` as consts so a
        client can discriminate this view from its siblings.

        `blocks[].data` is deliberately an OPEN object: the SPA resolves blocks
        through a component registry keyed by `type` + `variant`, so layout lives
        in that registry, not the response. Enumerating each block payload here
        would be wrong by the next unit that adds a block.

        `ApplicationViewBuilder#extra` (default `{}`) is intentionally not modelled —
        no builder overrides it; one that does should extend this schema.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: discover
        name:
          const: programs
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            additionalProperties: false
            properties:
              type:
                enum:
                - programs
              variant:
                type: string
              title:
                type: string
              data:
                type: object
                additionalProperties: true
    EmailCampaignAnalyticsResponse:
      type: object
      description: |
        Envelope for the campaign-analytics view (`/views/email_campaigns/analytics`). The generic
        `{group, name, blocks}` shape with two blocks: `delivery_summary` (the tiles) and
        `delivery_trend` (the chart series).
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: email_campaign
        name:
          const: analytics
        blocks:
          type: array
          items:
            oneOf:
            - "$ref": "#/components/schemas/EmailCampaignDeliverySummaryBlock"
            - "$ref": "#/components/schemas/EmailCampaignDeliveryTrendBlock"
    EmailCampaignDeliverySummaryBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: delivery_summary
        data:
          type: object
          required:
          - total_sent
          - delivered
          - open_rate
          - click_rate
          additionalProperties: false
          properties:
            total_sent:
              type: integer
            delivered:
              type: integer
            open_rate:
              type:
              - number
              - 'null'
              description: Fraction 0..1; null when nothing has been sent.
            click_rate:
              type:
              - number
              - 'null'
              description: Fraction 0..1; null when nothing has been sent.
    EmailCampaignDeliveryTrendBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: delivery_trend
        data:
          type: object
          required:
          - months
          additionalProperties: false
          properties:
            months:
              type: array
              items:
                type: object
                required:
                - month
                - sent
                - delivered
                - opened
                - clicked
                additionalProperties: false
                properties:
                  month:
                    type: integer
                    description: Calendar month number, 1-12.
                  sent:
                    type: integer
                  delivered:
                    type: integer
                  opened:
                    type: integer
                  clicked:
                    type: integer
    EmailCampaignAudienceDetailsResponse:
      type: object
      description: |
        Envelope for the audience-details view (`/views/email_campaigns/audience_details`). The generic
        `{group, name, blocks}` shape with a single `audience_details` block.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: email_campaign
        name:
          const: audience_details
        blocks:
          type: array
          items:
            "$ref": "#/components/schemas/EmailCampaignAudienceDetailsBlock"
    EmailCampaignAudienceDetailsBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: audience_details
        data:
          type: object
          required:
          - user_count
          - filters
          additionalProperties: false
          properties:
            user_count:
              type: integer
              description: How many users the view currently resolves to.
            filters:
              type: array
              description: |
                One entry per applied filter, empty when the view applies none. `label` is the filter's
                name and `value` its already-flattened one-line rendering, kept apart so the card can
                render them as two columns.
              items:
                type: object
                required:
                - label
                - value
                additionalProperties: false
                properties:
                  label:
                    type: string
                  value:
                    type: string
    EmailCampaignComposeOptionsResponse:
      type: object
      description: |
        Envelope for the compose-options view (`/views/email_campaigns/compose_options`). The generic
        `{group, name, blocks}` shape carrying a `sender_options` and a `merge_tags` block.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: email_campaign
        name:
          const: compose_options
        blocks:
          type: array
          items:
            oneOf:
            - "$ref": "#/components/schemas/EmailCampaignSenderOptionsBlock"
            - "$ref": "#/components/schemas/EmailCampaignMergeTagsBlock"
    EmailCampaignSenderOptionsBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: sender_options
        data:
          type: object
          required:
          - options
          additionalProperties: false
          properties:
            options:
              type: array
              items:
                type: object
                required:
                - id
                - label
                additionalProperties: false
                properties:
                  id:
                    type:
                    - integer
                    - 'null'
                    description: A null id means the mail is sent as the program itself.
                  label:
                    type: string
    EmailCampaignMergeTagsBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: merge_tags
        data:
          type: object
          required:
          - tags
          additionalProperties: false
          properties:
            tags:
              type: array
              items:
                type: object
                required:
                - token
                - name
                - description
                - example
                additionalProperties: false
                properties:
                  token:
                    type: string
                    description: The insertable token, braces included — e.g. `{{first_name}}`.
                    example: "{{first_name}}"
                  name:
                    type: string
                  description:
                    type: string
                  example:
                    type: string
                    description: A worked example value, as HTML. Empty when the tag
                      declares none.
    EngagementSurveyOptionsResponse:
      type: object
      description: |
        Envelope for the engagement-survey options view
        (`/views/mentoring_models/engagement_surveys`). Same `{group, name, blocks}` shape as the
        generic view envelope, with a single `engagement_survey_options` block.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: mentoring_models
        name:
          const: engagement_surveys
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                const: engagement_survey_options
              data:
                type: array
                items:
                  "$ref": "#/components/schemas/EngagementSurveyOption"
    EngagementSurveyOption:
      type: object
      description: The engagement surveys one connection plan template assigns to
        one role.
      required:
      - mentoring_model_id
      - role_key
      - surveys
      additionalProperties: false
      properties:
        mentoring_model_id:
          type: integer
        role_key:
          type: string
          description: Matches `RoleRef.key`.
        surveys:
          type: array
          items:
            type: object
            required:
            - id
            - name
            additionalProperties: false
            properties:
              id:
                type: integer
              name:
                type: string
    MembershipRequestApplicationQuestionsResponse:
      type: object
      description: |
        Envelope for the application-questions view
        (`/views/membership_requests/application_questions`). Same `{group, name, blocks}` shape as
        the generic view envelope, with a single `application_questions` block whose data is the
        ordered question list.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: membership_requests
        name:
          const: application_questions
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                const: application_questions
              data:
                type: array
                description: Ordered as the program orders its profile questions.
                items:
                  type: object
                  required:
                  - id
                  - label
                  - kind
                  - role_keys
                  - choices
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: 'The ProfileQuestion id — the `answers[].question_id`
                        on a membership request.

                        '
                    label:
                      type: string
                      description: The question text, with this program's custom terms
                        applied.
                    kind:
                      type: string
                      description: |
                        Which control the answer editor renders. There is no numeric kind — the
                        profile-question vocabulary has no numeric type (the rating scales are
                        choice-based). `composite` is an education/experience/publication/manager
                        question: its answer is a set of sub-fields, so it renders as a read-only
                        column and is NOT editable as a single value.
                      enum:
                      - text
                      - choice
                      - date
                      - composite
                    role_keys:
                      type: array
                      description: |
                        The keys of the roles that ask this question on the application form, sorted —
                        the same role-key vocabulary a membership request's `role_keys` carry, so the
                        two can be matched. Empty when the question appears elsewhere in the profile
                        but not on the application form.
                      items:
                        type: string
                    choices:
                      type: array
                      description: 'Selectable choices, in the program''s order. Empty
                        for a non-choice question.

                        '
                      items:
                        type: object
                        required:
                        - id
                        - label
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: The QuestionChoice id.
                          label:
                            type: string
                            description: The choice text, translated.
    MembershipRequestNotificationEmailsResponse:
      type: object
      description: |
        Envelope for the applicant notification-emails view
        (`/views/membership_requests/notification_emails`). Same `{group, name, blocks}` shape as
        the generic view envelope, with a single `notification_emails` block whose data is the
        ordered list of mails.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: membership_requests
        name:
          const: notification_emails
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                const: notification_emails
              data:
                type: array
                description: Ordered received → accepted → rejected.
                items:
                  type: object
                  required:
                  - uid
                  - title
                  - description
                  - subject
                  - enabled
                  additionalProperties: false
                  properties:
                    uid:
                      type: string
                      description: The mailer's stable uid — the key for deep-linking
                        the template editor.
                      example: 67pab4en
                    title:
                      type: string
                      description: The mail's admin-facing name, with this program's
                        custom terms applied.
                    description:
                      type: string
                      description: When the mail sends and to whom, with this program's
                        custom terms applied.
                    subject:
                      type: string
                      description: |
                        The effective subject line — the program override, else the organization
                        override, else the mailer's default.
                    enabled:
                      type: boolean
                      description: Whether the mail sends for this program (program
                        row → org row → enabled).
    MySpaceViewResponse:
      type: object
      description: |
        Envelope for the `home/my_space` view. Same `{group, name, blocks}` shape as every
        view (ApplicationViewBuilder#render), with `group`/`name` as consts so a client can
        discriminate this view from its siblings.

        `blocks` is fixed at exactly three types (working_on/learning/shared), each carrying
        `data.items` as a deliberately OPEN array: the SPA resolves items through a component
        registry keyed by `kind`, so enumerating every item shape here would be wrong by the
        next unit that adds one (`working_on` already mixes `goal`/`commitment` kinds; `shared`
        carries a different shape entirely).
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: home
        name:
          const: my_space
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                enum:
                - working_on
                - learning
                - shared
              data:
                type: object
                required:
                - items
                additionalProperties: false
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
    NavNode:
      type: object
      description: |
        One node of a navigation tree. Ids come from the server-side catalog
        (app/view_builders/navigation/); the SPA maps them to labels, icons,
        and routes via packages/web/src/pages/layout/navMeta.ts. `children`
        is omitted (never empty) on leaf nodes.
      required:
      - id
      - type
      additionalProperties: false
      properties:
        id:
          type: string
        type:
          type: string
          enum:
          - section
          - group
          - link
          - tab
        children:
          type: array
          items:
            "$ref": "#/components/schemas/NavNode"
    NavTreeResponse:
      type: object
      description: |
        Envelope for the navigation views (`/views/navigation/*`). Same
        `{group, name, blocks}` shape as the generic view envelope, with a
        single `nav_tree` block whose payload is the pruned navigation tree.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: navigation
        name:
          type: string
          description: "`global`, or `local/<scope>` for local navs."
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                const: nav_tree
              data:
                type: object
                required:
                - items
                additionalProperties: false
                properties:
                  items:
                    type: array
                    items:
                      "$ref": "#/components/schemas/NavNode"
    OnboardingFlowEditorResponse:
      type: object
      description: |
        Envelope for the onboarding-flow editor view (`/views/onboarding/flow_editor/{id}`). The
        generic `{group, name, blocks}` shape with a single `onboarding_flow_editor` block whose
        `data` is the editable flow document plus the audience role options.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: onboarding
        name:
          const: flow_editor
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                const: onboarding_flow_editor
              data:
                type: object
                required:
                - layout_flow
                - testimonials
                - layout_images
                - applicable_roles
                additionalProperties: false
                properties:
                  layout_flow:
                    type: object
                    additionalProperties: true
                  testimonials:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  layout_images:
                    type: array
                    items:
                      "$ref": "#/components/schemas/OnboardingFlowImage"
                  applicable_roles:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - name
                      - display_name
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                        name:
                          type: string
                        display_name:
                          type: string
    ProgramInvitationAnalyticsResponse:
      type: object
      description: |
        Envelope for the invitation-analytics view (`/views/program_invitations/analytics`).
        Same `{group, name, blocks}` shape as the generic view envelope, with a single
        `email_funnel` block whose data is the funnel counts.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: program
        name:
          const: invitation_analytics
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                const: email_funnel
              data:
                type: object
                required:
                - sent
                - delivered
                - opened
                - clicked
                additionalProperties: false
                properties:
                  sent:
                    type: integer
                  delivered:
                    type: integer
                  opened:
                    type: integer
                  clicked:
                    type: integer
    RoleOptionsResponse:
      type: object
      description: |
        Envelope for the role-options view (`/views/roles/options`). Same
        `{group, name, blocks}` shape as the generic view envelope, with a
        single `role_options` block whose data is the curated list of roles.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: roles
        name:
          const: options
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            - data
            additionalProperties: false
            properties:
              type:
                const: role_options
              data:
                type: array
                items:
                  "$ref": "#/components/schemas/RoleOption"
    RoleOption:
      type: object
      description: |
        A role as a picker option: the `RoleRef` fields plus `admin`, so a picker can
        exclude or lock the admin role without hardcoding a key that differs per program
        flavor (`administrator` in a track, `group_owner` in an ERG). The flag is
        view-only — `RoleRef` itself does not carry it.
      required:
      - key
      - term
      - admin
      additionalProperties: false
      properties:
        key:
          type: string
        term:
          type: string
        admin:
          type: boolean
          description: |
            True for the `admin` role only. Narrower than `?type=administrative`, which
            also selects `sub_admin` — this mirrors `Role#admin?`.
    RoleplayShareCandidateMember:
      type: object
      description: Minimal member identity rendered in the roleplay share popover.
      required:
      - id
      - name
      - photo_url
      additionalProperties: false
      properties:
        id:
          type: integer
        name:
          type: string
        photo_url:
          type:
          - string
          - 'null'
    SurveyCampaignMessageComposeOptionsResponse:
      type: object
      description: |
        Envelope for the reminder compose-options view (`/views/survey_campaign_messages/compose_options`).
        The generic `{group, name, blocks}` shape carrying a `sender_options` and a `merge_tags` block.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: survey_campaign_messages
        name:
          const: compose_options
        blocks:
          type: array
          items:
            oneOf:
            - "$ref": "#/components/schemas/SurveyCampaignMessageSenderOptionsBlock"
            - "$ref": "#/components/schemas/SurveyCampaignMessageMergeTagsBlock"
    SurveyCampaignMessageSenderOptionsBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: sender_options
        data:
          type: object
          required:
          - options
          additionalProperties: false
          properties:
            options:
              type: array
              items:
                type: object
                required:
                - id
                - label
                additionalProperties: false
                properties:
                  id:
                    type:
                    - integer
                    - 'null'
                    description: A null id means the mail is sent as the program itself.
                  label:
                    type: string
    SurveyCampaignMessageMergeTagsBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: merge_tags
        data:
          type: object
          required:
          - tags
          additionalProperties: false
          properties:
            tags:
              type: array
              items:
                type: object
                required:
                - token
                - name
                - description
                - example
                additionalProperties: false
                properties:
                  token:
                    type: string
                    description: The insertable token, braces included — e.g. `{{survey_name}}`.
                    example: "{{survey_name}}"
                  name:
                    type: string
                  description:
                    type: string
                  example:
                    type: string
                    description: A worked example value, as HTML. Empty when the tag
                      declares none.
    SurveyDeliveryOptionsResponse:
      type: object
      description: |
        Envelope for the survey delivery-options view (`/views/surveys/delivery_options`). The generic
        `{group, name, blocks}` shape carrying the capability gates for the type about to be created,
        plus the reminders the server will seed with it.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: surveys
        name:
          const: delivery_options
        blocks:
          type: array
          items:
            oneOf:
            - "$ref": "#/components/schemas/SurveyDeliveryCapabilitiesBlock"
            - "$ref": "#/components/schemas/SurveyDefaultRemindersBlock"
    SurveyDeliveryCapabilitiesBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: delivery_capabilities
        data:
          type: object
          description: |
            The same flag set the survey detail render carries, so the create and edit surfaces paint
            from one vocabulary.
          required:
          - can_set_recipients
          - can_set_due_date
          - can_share_progress_report
          - can_answer_without_login
          - can_mandate_overdue
          - can_set_one_response
          - one_response_locked
          - can_have_reminders
          additionalProperties: false
          properties:
            can_set_recipients:
              type: boolean
            can_set_due_date:
              type: boolean
            can_share_progress_report:
              type: boolean
            can_answer_without_login:
              type: boolean
            can_mandate_overdue:
              type: boolean
            can_set_one_response:
              type: boolean
            one_response_locked:
              type: boolean
              description: Always false here — an unsaved survey has no responses
                to lock against.
            can_have_reminders:
              type: boolean
              description: |
                Whether the TYPE can carry a reminder campaign. True before the record exists — the
                campaign itself is seeded on create.
    SurveyDefaultRemindersBlock:
      type: object
      required:
      - type
      - data
      additionalProperties: false
      properties:
        type:
          const: default_reminders
        data:
          type: object
          description: |
            The reminders `SurveyObserver#after_create` will seed for this type, read from the same
            YAML the seeding reads. Empty on a type that cannot have a campaign.
          required:
          - reminders
          additionalProperties: false
          properties:
            reminders:
              type: array
              items:
                type: object
                required:
                - subject
                - body
                - duration
                additionalProperties: false
                properties:
                  subject:
                    type: string
                  body:
                    type: string
                    description: The seeded HTML source, so the wizard can open one
                      for editing.
                  duration:
                    type: integer
                    description: Days after the survey is overdue.
    UpNextViewResponse:
      type: object
      description: |
        Envelope for the `home/up_next` view. Same `{group, name, blocks}` shape as every
        view (ApplicationViewBuilder#render), with `group`/`name` as consts so a
        client can discriminate this view from its siblings.

        `blocks[].data` is deliberately an OPEN object: the SPA resolves blocks
        through a component registry keyed by `type` + `variant`, so layout lives
        in that registry, not the response. Enumerating each block payload here
        would be wrong by the next unit that adds a block.

        `ApplicationViewBuilder#extra` (default `{}`) is intentionally not modelled —
        no builder overrides it; one that does should extend this schema.
      required:
      - group
      - name
      - blocks
      additionalProperties: false
      properties:
        group:
          const: home
        name:
          const: up_next
        blocks:
          type: array
          items:
            type: object
            required:
            - type
            additionalProperties: false
            properties:
              type:
                enum:
                - event
                - items
              variant:
                type: string
              title:
                type: string
              data:
                type: object
                additionalProperties: true
    ViewBlock:
      type: object
      required:
      - type
      - variant
      additionalProperties: false
      properties:
        type:
          type: string
          description: |
            Stable identifier for the block. Matches the widget constants in
            `app/services/home_page/components.rb` (e.g. `connections`,
            `mentoring_requests`, `program_events`, `nudges`, `out_of_office`).
        variant:
          type: string
          enum:
          - card
          - chart
          - list
          - table
          - banner
        title:
          type: string
        data:
          type: object
          additionalProperties: true
        actions:
          type: array
          items:
            type: object
            required:
            - rel
            - href
            additionalProperties: false
            properties:
              rel:
                type: string
              href:
                type: string
              method:
                type: string
                enum:
                - GET
                - POST
                - PATCH
                - DELETE
                default: GET
    AppMode:
      type: string
      enum:
      - manage
      - engage
    RoleRef:
      type: object
      properties:
        key:
          type: string
        term:
          type: string
      additionalProperties: false
      required:
      - key
      - term
  responses:
    ErrorResponse:
      description: Error envelope. The HTTP status code identifies the failure class.
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/Error"
    OAuthErrorResponse:
      description: OAuth 2.0 error. The HTTP status code identifies the failure class.
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/OAuthError"
  parameters:
    Page:
      name: page
      in: query
      description: 1-indexed page number.
      required: false
      schema:
        type: integer
        minimum: 1
        default: 1
    Limit:
      name: limit
      in: query
      description: Page size. Capped at 100.
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    Fields:
      name: fields
      in: query
      description: |
        Comma-separated list of fields to include in the response. Intersected
        with the policy allowlist. Cannot widen what the policy permits.
      required: false
      schema:
        type: string
        example: id,name,status
    Id:
      name: id
      in: path
      required: true
      schema:
        type: integer
        minimum: 1
    AgentId:
      name: agent_id
      in: path
      required: true
      description: Agent id.
      schema:
        type: integer
        minimum: 1
    MemberId:
      name: member_id
      in: path
      required: true
      description: Member id of the report or subject.
      schema:
        type: integer
        minimum: 1
    GoalId:
      name: goal_id
      in: path
      required: true
      description: Member-goal id.
      schema:
        type: integer
        minimum: 1
    CheckInId:
      name: check_in_id
      in: path
      required: true
      description: Formal check-in pair id.
      schema:
        type: integer
        minimum: 1
    InteractionId:
      name: interaction_id
      in: path
      required: true
      description: Interaction id (nudge row).
      schema:
        type: integer
        minimum: 1
    ConversationId:
      name: conversation_id
      in: path
      required: true
      description: Conversation (Interaction) id.
      schema:
        type: integer
        minimum: 1
    CycleId:
      name: cycle_id
      in: path
      required: true
      description: |
        Mentoring-intelligence cycle UUID on Interaction#cycle_id — not a numeric
        record id. PathBuilder would otherwise attach the integer `Id` component
        and openapi_first would 400 a UUID before the controller runs (TMO-T86).
      schema:
        type: string
        format: uuid
    PresetKey:
      name: preset_key
      in: path
      required: true
      description: Persona avatar preset key (`[\w-]+`).
      schema:
        type: string
        pattern: "^[A-Za-z0-9_-]+$"
    ThreadId:
      name: thread_id
      in: path
      required: true
      description: Bot-apps conversation thread id.
      schema:
        type: string
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      description: |
        Opaque client-generated key (recommended UUIDv4) used to deduplicate
        retried POSTs. When present, the server caches the first response for
        24 hours and replays it on subsequent requests with the same key.
        Same key with a *different* body returns 422 idempotency_key_reuse.
        Same key while the original is still executing returns 409.
        Honoured only by the POST operations that declare it; ignored elsewhere.
      required: false
      schema:
        type: string
        minLength: 1
        maxLength: 64
        pattern: "^[A-Za-z0-9_-]+$"
        example: 7c1e1bf0-2d4f-4b8f-9e9e-2f3a4b5c6d7e
    AppMode:
      name: app_mode
      in: query
      description: |
        Product surface to resolve this request against: `manage` (admin
        console) or `engage` (end-user experience). Selects the policy variant
        and the per-surface field set, and is echoed on every success envelope.
        Omit to inherit the strategy default (`manage` for credential auth,
        `engage` otherwise); `manage` requires an administrative role.
        Takes precedence over the `App-Mode` header when both are sent.
      required: false
      schema:
        "$ref": "#/components/schemas/AppMode"
    AppModeHeader:
      name: App-Mode
      in: header
      description: |
        Header form of the `app_mode` query parameter — identical
        `manage`/`engage` semantics. The `?app_mode=` query parameter takes
        precedence when both are present.
      required: false
      schema:
        "$ref": "#/components/schemas/AppMode"
    PerPage:
      name: per_page
      in: query
      required: false
      schema:
        type: integer
        description: Defaults to 25; clamped to [1, 500].
    RumiContextKeyParam:
      name: key
      in: path
      required: true
      description: |
        Which memory key to forget. The nine forgettable keys are `emotional_state`,
        `active_situation`, `relationship_layer`, `active_commitments`, `self_insights`,
        `monitoring_flags`, `observed_patterns`, `parked_themes`, `stated_preferences` — a SUBSET
        of MemberContext::Schema's attributes; the rest (skill_history, career_aspiration, consent
        and trigger bookkeeping) are not member-erasable.

        ⚠️ Listed as prose, deliberately NOT as an `enum`. The request validator is always on
        (`engine.rb:16`), so an enum here would 400 an invalid key at middleware and make the
        controller's 422 branch (`rumi_context_controller.rb:42-45`) unreachable — converting
        source's 422 contract into a 400 that source never returned. The membership check stays
        where source put it: in the action.
      schema:
        type: string
    NavScopeParam:
      name: scope
      in: path
      required: true
      description: |
        Local navigation scope. Enum mirrors Navigation::Local::Catalog::SCOPES
        — extend both together.
      schema:
        type: string
        enum:
        - program
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: _mentor_session
      description: |
        Opaque server-side session identifier. Set on web login; send it back on
        each request. Used by the Chronus web and mobile apps.
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Chronus-issued JWT. Used by partner integrations (on behalf of a user)
        and partner backends (client credentials). Obtain via
        `POST /api/v3/auth/token`.
